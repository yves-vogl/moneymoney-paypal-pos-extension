---
phase: 06.1-supply-chain-scorecard-hardening
plan: 08
type: execute
wave: 5
depends_on: ["06.1-01", "06.1-02", "06.1-03", "06.1-04", "06.1-05", "06.1-06", "06.1-07"]
files_modified:
  - docs/adr/0009-openssf-scorecard-stance.md
  - SECURITY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - mkdocs.yml
autonomous: true
requirements: [DOC-11, SEC-05, SEC-06, SEC-07, SEC-08, BUILD-07]
tags: [wave-5, docs, adr, security, requirements, doc-11, consolidating]

must_haves:
  truths:
    - "Final consolidating documentation batch — closes the documentation requirement DOC-11 (next-free DOC number; DOC-01..DOC-10 all taken — verified via grep on REQUIREMENTS.md). The plan ships ADR-0009, the SECURITY.md supply-chain section, the REQUIREMENTS.md additions, the ROADMAP.md traceability update, and the mkdocs.yml ADR-0009 nav un-comment."
    - "`docs/adr/0009-openssf-scorecard-stance.md` is NEW (per D-39 — ADR file number 0009 because 0001-0008 are taken). MADR template per PATTERNS §2.2: `# ADR-0009: OpenSSF Scorecard Stance` h1, then `## Status` (ACCEPTED), `## Date` (2026-06-23), `## Deciders` (Yves Vogl), `## Context`, `## Decision`, `## Consequences`, `## Alternatives Considered`. Date format ISO YYYY-MM-DD per ADR-0008:9."
    - "ADR-0009 mandatory content per D-35 + D-36 + RESEARCH §11: the five accepted-gap rationales — Fuzzing (0/10, Lua wrapper has no parser/codec; OSS-Fuzz overkill), Code-Review (0/10 — solo single-account maintainer; second-account or bot-reviewer Options B/C documented but Option A locked), Packaging (-1, structural N/A for single-file Lua distribution), Contributors (6/10 capped at adesso SE work-email autodetect; structural), Maintained (0/10 healing at ~90 days repo age, ~2026-09-15). PLUS: silver-CII-as-backlog pointer + compensating mitigations enumeration (SAST, reproducible build, TLS-only, redact-before-log, egress-allowlist, signed commits, signed tags, pinned actions, least-privilege workflow tokens, branch protection with signed-commit requirement)."
    - "ADR-0009 'Alternatives Considered' section per D-38 + PATTERNS §1 row 9: SBOM + Sigstore/cosign attestation + SLSA Provenance Level 3 are tracked as deferred-to-v1.1.x with explicit upgrade path `actions/attest-build-provenance@<SHA>` in release.yml. The two-options-rejected decision (Option B 1-reviewer + Option C bot-reviewer per RESEARCH §4) is recorded with rationale + when-to-revisit triggers. RESEARCH §11 G-09 (awk shell-interpolation) accepted-low-severity rationale also captured here."
    - "ADR-0009 'Acceptance bar revision' subsection (driven by RESEARCH §4 + §5): the original ROADMAP target of `Scorecard aggregate ≥ 8.5` is revised to `≥ 7.5 short-term / ≥ 8.5 once ossf/scorecard#5103 merges upstream AND Maintained heals at ~90 days`. Two upstream-timed events documented: (a) ossf/scorecard#5103 (Semgrep detection — open as of research date) → SAST.score 0→10 once merged; (b) repo age >90 days → Maintained 0→8+ automatic."
    - "`SECURITY.md` gains a new bilingual section per PATTERNS §1 row 10: title `## Lieferketten-Kontrollen / Supply-chain controls` AFTER the existing 'Out of Scope' / 'Out of scope' blocks. Content enumerates the active controls per CONTEXT §domain in-scope list: pinned actions (SEC-06), workflow least-privilege (SEC-07), Semgrep SAST (SEC-08), branch protection with signed commits + linear history (SEC-05), Scorecard nightly + introspection token (SEC-05 expansion), gitleaks secret scan (CI-05 + Plan 06.1-02 config), signed releases (D-72 + Phase 6 — `kommt mit erstem Release`), redact-before-log (SEC-01), egress allowlist (SEC-04 + D-12), SCORECARD_READ_TOKEN rotation policy (≤1 year, fine-grained PAT, `Administration:read` only). Mirror the bilingual DE-primary / EN-follows pattern of the existing SECURITY.md."
    - "`.planning/REQUIREMENTS.md` is appended with the new IDs per D-36: **SEC-05 modified in-place** (extended acceptance text per RESEARCH §4 Option A + the SCORECARD_READ_TOKEN provisioning + branch-protection invariants), **SEC-06** new (pinned dependencies — per RESEARCH §11 acceptance text downgrade to `Pinned-Dependencies.score == 10`), **SEC-07** new (token permissions — `Token-Permissions.score == 10`), **SEC-08** new (Semgrep SAST workflow live + ERROR-blocking + SARIF in code-scanning AND a backlog issue tracks ossf/scorecard#5103 — looser acceptance per RESEARCH §11), **BUILD-07** new (CII passing per D-35 + D-40 — badge in BOTH READMEs), **DOC-11** new (this ADR + SECURITY.md section + REQUIREMENTS.md update bundle)."
    - "`.planning/REQUIREMENTS.md` Traceability table appended: SEC-05 (Phase 6.1, Status: Complete by 06.1-04), SEC-06/SEC-07 (Phase 6.1, Status: Complete by 06.1-01), SEC-08 (Phase 6.1, Status: Complete by 06.1-03 — security control in place; score reporting deferred per RESEARCH §5), BUILD-07 (Phase 6.1, Status: Complete by 06.1-07), DOC-11 (Phase 6.1, Status: Complete by 06.1-08 — i.e. this plan itself when merged)."
    - "`.planning/ROADMAP.md` Phase 6.1 success-criteria block is amended to reflect the realistic targets (aggregate ≥ 7.5 short-term; Branch-Protection ≥ 3 not ≥ 8 per Option A; SAST acceptance per RESEARCH §11 #4); the eight original success-criteria entries are revised in-place. Phase 6.1 marked as `[x]` complete after this plan merges (CP for orchestrator)."
    - "`mkdocs.yml` Plan 06.1-06 forward-reference comment on ADR-0009 is REPLACED with the live nav entry `- OpenSSF-Scorecard: adr/0009-openssf-scorecard-stance.md`. The site rebuilds on the next push-to-main and surfaces ADR-0009 in the Architektur nav."
    - "Two backlog issues are OPENED via `gh issue create` (Yves task, surfaced in SUMMARY but not executed by the planner): (a) `CII Best Practices → Silver` (linked from ADR-0009), (b) `Track ossf/scorecard#5103 merge for SAST detection` (linked from ADR-0009 + linked from SEC-08 acceptance text)."
    - "NO `src/**`, `tools/**`, `spec/**` edits. Reproducible-build SHA on `dist/paypal-pos.lua` unchanged."
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no AI/Claude attribution. Conventional Commits: `docs(06.1): adr-0009 openssf scorecard stance` (ADR) + `docs(06.1): security.md supply-chain controls section` (SECURITY) + `docs(06.1): requirements + roadmap updates for SEC-05..08 BUILD-07 DOC-11` (planning artefacts) + `docs(06.1): un-comment adr-0009 in mkdocs nav` (mkdocs)."
  artifacts:
    - path: "docs/adr/0009-openssf-scorecard-stance.md"
      provides: "MADR-format ADR documenting accepted gaps + mitigations + acceptance bar revision + Alternatives Considered + backlog pointers per D-35 + D-36 + D-38 + D-39 + RESEARCH §4 + §5 + §11"
      contains: "Fuzzing"
      min_lines: 80
    - path: "SECURITY.md"
      provides: "New bilingual 'Lieferketten-Kontrollen / Supply-chain controls' section enumerating active controls per CONTEXT in-scope list; SCORECARD_READ_TOKEN rotation policy documented"
      contains: "Lieferketten-Kontrollen"
      min_lines: 100
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-05 modified in-place; SEC-06/SEC-07/SEC-08/BUILD-07/DOC-11 appended per D-36 with RESEARCH-revised acceptance texts; Traceability table updated"
      contains: "SEC-06"
      min_lines: 250
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6.1 success criteria amended to realistic targets (aggregate ≥7.5, Branch-Protection ≥3 per Option A, SAST per RESEARCH §11); Phase 6.1 marked complete"
      contains: "Phase 6.1"
    - path: "mkdocs.yml"
      provides: "ADR-0009 nav entry un-commented; site surfaces the new ADR on next deploy"
      contains: "0009-openssf-scorecard-stance"
  key_links:
    - from: "docs/adr/0009-openssf-scorecard-stance.md"
      to: "SECURITY.md + REQUIREMENTS.md + ROADMAP.md"
      via: "Cross-references in ADR body — ADR cites SEC-05/06/07/08 + BUILD-07 + DOC-11 by ID, SECURITY.md cites ADR-0009 for the supply-chain rationale, REQUIREMENTS.md DOC-11 acceptance cites ADR-0009 by filename"
      pattern: "ADR-0009"
    - from: ".planning/REQUIREMENTS.md SEC-05 (modified)"
      to: "tools/setup-branch-protection.sh + scorecard.yml (Plan 06.1-04 artefacts)"
      via: "Acceptance text references the SCORECARD_READ_TOKEN + post-condition assertions added by Plan 06.1-04"
      pattern: "SCORECARD_READ_TOKEN"
    - from: "mkdocs.yml ADR-0009 nav entry"
      to: "docs/adr/0009-openssf-scorecard-stance.md (this plan's new file)"
      via: "Forward-reference from Plan 06.1-06 was commented out; this plan un-comments after the ADR file exists"
      pattern: "0009-openssf-scorecard-stance"
---

<objective>
Ship the final consolidating documentation batch for Phase 6.1. Closes DOC-11 (the documentation requirement) and formally records every other Phase-6.1 requirement (SEC-05..SEC-08, BUILD-07) in REQUIREMENTS.md + ROADMAP.md. Creates `docs/adr/0009-openssf-scorecard-stance.md` per D-39 (ADR file number 0009 because 0001-0008 are taken). Extends SECURITY.md with a bilingual supply-chain-controls section per PATTERNS §1 row 10. Un-comments the ADR-0009 nav entry in mkdocs.yml (Plan 06.1-06 forward-reference). Documents the realistic Scorecard target (≥ 7.5 short-term, ≥ 8.5 once upstream-timed events resolve).

Purpose: Phase 6.1 is incomplete until the controls it ships are documented and the requirements that justify it are recorded. This plan is the closing PR — without it, an external auditor cannot read the supply-chain posture from the repo. Closes DOC-11 + formally records SEC-05/SEC-06/SEC-07/SEC-08/BUILD-07.

Output: Five modified files (1 new ADR, 4 modified planning + docs files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-CONTEXT.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-RESEARCH.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-PATTERNS.md
@docs/adr/0008-string-return-error-pattern.md
@SECURITY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@mkdocs.yml
</context>

<artifacts_this_phase_produces>
New files:
- `docs/adr/0009-openssf-scorecard-stance.md` (NEW)

Files modified:
- `SECURITY.md` — append new bilingual section
- `.planning/REQUIREMENTS.md` — modify SEC-05 in-place; append SEC-06/SEC-07/SEC-08/BUILD-07/DOC-11; update Traceability table
- `.planning/ROADMAP.md` — amend Phase 6.1 success criteria; mark phase complete (`[x]`)
- `mkdocs.yml` — un-comment ADR-0009 nav entry (Plan 06.1-06 forward-reference)

Files explicitly NOT touched (blacklist):
- `src/**`, `tools/**`, `spec/**`
- All other workflows
- All other docs (CHANGELOG.md, CONTRIBUTING.md, READMEs)
- `docs/adr/0001-*.md` through `docs/adr/0008-*.md` (immutable)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/adr/0009-openssf-scorecard-stance.md (MADR format)</name>
  <files>docs/adr/0009-openssf-scorecard-stance.md</files>
  <action>
    Create the new ADR following the MADR template observed in ADR-0008 (PATTERNS §2.2):

    Structure:
    1. `# ADR-0009: OpenSSF Scorecard Stance`
    2. `## Status` — `ACCEPTED`
    3. `## Date` — `2026-06-23` (ISO format per ADR-0008:9)
    4. `## Deciders` — `Yves Vogl`
    5. `## Context` — narrative of the 5.2 baseline + the five accepted gaps + the two upstream-timed events. Cite the OpenSSF Scorecard project and Phase 6 R1/R2 reviews. ~30 lines.
    6. `## Decision` — the locked stance: (a) ship the five hardening PRs in Phase 6.1; (b) accept Branch-Protection at Tier 1 = 3/10 per RESEARCH §4 Option A (solo single-account constraint); (c) accept SAST score 0 short-term until ossf/scorecard#5103 merges; (d) revise aggregate target from ≥ 8.5 to ≥ 7.5 short-term, ≥ 8.5 once upstream-timed events resolve. The five accepted gaps enumerated with rationale + compensating mitigations:
       - **Fuzzing (0/10)** — Lua wrapper around remote HTTPS API; no parser, codec, wireformat, or decoder. OSS-Fuzz / ClusterFuzzLite produce no findings on wrapper code. Compensating: busted unit tests + property-based tests planned for decoder edge cases.
       - **Code-Review (0/10)** — solo single-account maintainer; Scorecard cannot register approved-by-different-user reviews. Compensating: GPG-signed commits + branch protection + linear history + post-merge security re-reviews via loop-security-engineer agent.
       - **Packaging (-1)** — single-file Lua artifact; no LuaRocks / npm / PyPI publish path exists for MoneyMoney extensions. Structural N/A.
       - **Contributors (6/10)** — adesso SE org auto-detected via Yves' work email Git commits; structural cap until external contributors join.
       - **Maintained (0/10)** — repo age < 90 days. Self-heals ~2026-09-15.
       PLUS: silver-CII-as-backlog pointer (D-35); SBOM + cosign + SLSA Provenance Level 3 deferred to v1.1.x per D-38.
    7. `## Consequences` — what landing this ADR means: realistic Scorecard target communicated to external auditors; future Scorecard runs interpreted against this baseline; backlog issues opened (Yves task) for the two upstream-timed events + silver tier; SEC-05 expansion + SEC-06/SEC-07/SEC-08/BUILD-07 acceptance bars formally recorded in REQUIREMENTS.md.
    8. `## Alternatives Considered`:
       - **Branch-Protection Option B (1 reviewer, second-account self-approval)** — rejected per RESEARCH §4. Trade-off: lifts Branch-Protection 3 → 8, requires Yves to maintain a second authenticated GitHub account and approve via separate browser profile per PR. Operationally feasible but accepted-gap policy prevails.
       - **Branch-Protection Option C (bot reviewer — CodeRabbit / Greptile / Diamond)** — rejected per D-35 + ADR-0008 lineage. Trade-off: lifts Branch-Protection + Code-Review to ≥ 8 each, costs $20-50/month, adds external SaaS dependency that the no-telemetry constraint forbids.
       - **Snyk Code as second SAST tool** — rejected per RESEARCH §5 Option C. Trade-off: lifts SAST 0 → 10 immediately (Scorecard detects Snyk), adds external-service dependency, partial Lua coverage. Wait for ossf/scorecard#5103 instead.
       - **SBOM + cosign attestation + SLSA Provenance Level 3 in 6.1** — rejected per D-38. Trade-off: would land in 6.1 but the Scorecard Signed-Releases check only fires AFTER a release exists; Phase 6.1 does not produce a release. Defer to v1.1.x supply-chain follow-up phase. Upgrade path: `actions/attest-build-provenance@<SHA>` in release.yml's publish job (RESEARCH §7 has the concrete YAML diff).
       - **G-09 awk shell-interpolation fix as full hardening** — accepted-low-severity per RESEARCH §11 #9. The theoretical injection requires a malicious tag name; release.yml's `on: push: tags: ['v[0-9]+...']` pattern + verify-signed-tag gate block this path. Plan 06.1-01 still replaced the `awk | sed | cut` anti-pattern (PATTERNS §3 row 2) — the awk-injection-defence happens to land alongside.
       - **Lua-specific Semgrep community rules (`p/lua-community`)** — rejected per D-34. ~15 low-signal rules, most irrelevant for an API wrapper. Revisit if Phase-2/3 false-negative motivates.
       - **`p/ci` Semgrep ruleset for GitHub-Actions hardening** — rejected per D-34. Threat model already covered by SEC-06 (pinned actions) + SEC-07 (token permissions).

    The full ADR is ~100-150 lines. Read ADR-0008 first for tone, sentence shape, and the level of detail in `Consequences` and `Alternatives Considered`. Match that voice.

    Open backlog issues (Yves task — documented in this plan's SUMMARY, not executed by the planner):
    ```bash
    gh issue create --title "CII Best Practices → Silver" \
      --body "Track upgrade from Passing (≥5/10) to Silver (~7/10) post-v1.0.0. Linked from docs/adr/0009-openssf-scorecard-stance.md. ~3 h additional questionnaire effort."

    gh issue create --title "Track ossf/scorecard#5103 merge for Semgrep SAST detection" \
      --body "ossf/scorecard PR #5103 adds Semgrep to the SAST detection list. Once merged, the Scorecard SAST score auto-flips from 0 to 10 on next run. Linked from docs/adr/0009-openssf-scorecard-stance.md + SEC-08 acceptance text."
    ```
  </action>
  <verify>
    <automated>
      set -e
      test -f docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing"; exit 1; }

      # MADR sections
      grep -qE '^# ADR-0009: ' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing h1 'ADR-0009: ...'"; exit 1; }
      grep -qE '^## Status$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Status"; exit 1; }
      grep -q '^ACCEPTED$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 not ACCEPTED"; exit 1; }
      grep -qE '^## Date$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Date"; exit 1; }
      grep -qE '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 date not ISO YYYY-MM-DD"; exit 1; }
      grep -qE '^## Deciders$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Deciders"; exit 1; }
      grep -qE '^## Context$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Context"; exit 1; }
      grep -qE '^## Decision$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Decision"; exit 1; }
      grep -qE '^## Consequences$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Consequences"; exit 1; }
      grep -qE '^## Alternatives Considered$' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ## Alternatives Considered"; exit 1; }

      # Mandatory content — 5 accepted gaps + 4 alternatives rejected
      for gap in Fuzzing Code-Review Packaging Contributors Maintained; do
        grep -q "$gap" docs/adr/0009-openssf-scorecard-stance.md \
          || { echo "FAIL: ADR-0009 missing gap '$gap'"; exit 1; }
      done

      # SBOM + cosign + SLSA Provenance forward-pointer
      grep -q 'SBOM' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing SBOM forward-reference"; exit 1; }
      grep -q 'attest-build-provenance' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing actions/attest-build-provenance upgrade path"; exit 1; }

      # ossf/scorecard#5103
      grep -qE 'ossf/scorecard.{0,4}5103' docs/adr/0009-openssf-scorecard-stance.md \
        || { echo "FAIL: ADR-0009 missing ossf/scorecard#5103 reference"; exit 1; }

      LINES=$(wc -l < docs/adr/0009-openssf-scorecard-stance.md | tr -d ' ')
      if [ "$LINES" -lt 80 ]; then
        echo "FAIL: ADR-0009 only $LINES lines — too thin (expected ≥ 80)"
        exit 1
      fi

      echo "OK: ADR-0009 structurally complete"
    </automated>
  </verify>
  <done>
    `docs/adr/0009-openssf-scorecard-stance.md` exists with all MADR sections, ACCEPTED status, ISO date, all 5 accepted-gap rationales, all 4+ alternatives-rejected entries with rationale, SBOM/cosign/SLSA forward-pointer, ossf/scorecard#5103 reference, ≥ 80 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append bilingual supply-chain section to SECURITY.md</name>
  <files>SECURITY.md</files>
  <action>
    Append a new bilingual section AFTER the existing 'Out of Scope' (line 48-56 German) and 'Out of scope:' (line 71 English) blocks. Insert after the German section, before the existing `---` separator at line 57, OR after the English section at line 71. PATTERNS §1 row 10 specifies the position; the bilingual flow already in SECURITY.md (DE primary then `---` separator then EN) suggests inserting a parallel structure: new DE section before the separator, new EN section after EN's existing content.

    Recommended structure (DE primary, EN follows — mirrors existing SECURITY.md flow):

    ```markdown
    [... existing content unchanged through 'Out of Scope' (DE) ...]

    ## Lieferketten-Kontrollen

    Diese Extension folgt einer formalen Supply-chain-Härtungspolitik, die mit
    Phase 6.1 etabliert wurde. Die aktiven Kontrollen sind:

    | Kontrolle | Umsetzung | Referenz |
    |-----------|-----------|----------|
    | GitHub-Actions auf Commit-SHA gepinnt | Jede `uses:`-Referenz in `.github/workflows/*.yml` trägt eine 40-Zeichen-SHA + `# vX.Y.Z`-Kommentar; Dependabot bumpt SHA + Kommentar im Lockstep. | SEC-06 |
    | Workflow-Token least-privilege | Alle Workflows haben Top-Level `permissions: read-all`; Schreibrechte sind Job-lokal und minimal. | SEC-07 |
    | Semgrep SAST blockierend | `p/security-audit` + `p/secrets` laufen auf jedem Push und PR; ERROR-Findings brechen den Workflow; SARIF wird ins Code-Scanning hochgeladen. | SEC-08 |
    | Branch protection auf `main` | PR-Pflicht, signierte Commits, lineare History, force-push + delete blockiert, kein Admin-Bypass; required status checks: 5 (Lint+tests+reproducible build, gitleaks secret scan, Commit-message lint, Scorecard analysis, Semgrep SAST). | SEC-05 |
    | Scorecard nightly + Branch-Protection-Introspection | `ossf/scorecard-action` läuft wöchentlich; `SCORECARD_READ_TOKEN` (Fine-grained PAT, `Administration:read` only, Rotation ≤ 1 Jahr) ermöglicht die Branch-Protection-Lesung. | SEC-05 |
    | Gitleaks secret scan | Läuft auf jedem PR; `.gitleaks.toml` + `.gitleaksignore` definieren die Detection + per-Fingerprint-Allowlist auditierter Falschmeldungen. | CI-05 + G-14 |
    | Signed releases | GPG-signierter Git-Tag (`v[0-9]+.[0-9]+.[0-9]+`) löst die Release-Pipeline aus; `verify-signed-tag`-Job prüft die Signatur gegen den Maintainer-Fingerprint `FDE07046A6178E89ADB57FD3DE300C53D8E18642`. | BUILD-04 |
    | Reproducible build | `lua tools/build.lua --verify` baut deterministisch byte-identisch; CI failt bei Diff. | BUILD-02 |
    | Redact-before-log | Jede `print()`-Ausgabe läuft durch `M_log.*`-Redaktor, der JWT- und Bearer-Substrings entfernt (D-79-Gate). | SEC-01 |
    | Egress-Allowlist | Im gebauten Artefakt sind nur Hosts `oauth.zettle.com`, `purchase.izettle.com`, `finance.izettle.com` referenzierbar; CI-Grep blockiert sonstige Hostnamen. | SEC-04 + D-12 |

    Diese Posture ist gegenüber der OpenSSF Scorecard messbar; siehe
    [ADR-0009](docs/adr/0009-openssf-scorecard-stance.md) für den bewussten
    Trade-off-Diskurs (akzeptierte Lücken, alternative Optionen, upstream-timed
    Verbesserungen) und das CII Best Practices Badge (Passing-Tier) im README.

    ### `SCORECARD_READ_TOKEN`-Rotation

    Der Fine-grained PAT wird mit 1-Jahres-Ablauf erstellt. Vor Ablauf:

    1. Neuer PAT mit identischem Scope erzeugen (`Administration:read` only).
    2. `gh secret set SCORECARD_READ_TOKEN --repo yves-vogl/moneymoney-paypal-pos-extension`.
    3. Alten PAT in den GitHub-Settings revoken.
    4. `gh workflow run "OpenSSF Scorecard"` zur Verifikation triggern.

    [... existing `---` separator + English content unchanged through 'Out of scope:' ...]

    ## Supply-chain controls

    This extension follows a formal supply-chain hardening policy established
    in Phase 6.1. Active controls:

    | Control | Implementation | Reference |
    |---------|----------------|-----------|
    | Pinned GitHub Actions | Every `uses:` reference in `.github/workflows/*.yml` carries a 40-char SHA + `# vX.Y.Z` comment; Dependabot bumps SHA + comment in lockstep. | SEC-06 |
    | Least-privilege workflow tokens | All workflows declare top-level `permissions: read-all`; write scopes are job-local and minimal. | SEC-07 |
    | Blocking Semgrep SAST | `p/security-audit` + `p/secrets` run on every push and PR; ERROR findings fail the workflow; SARIF uploaded to code-scanning. | SEC-08 |
    | Branch protection on `main` | PR required, signed commits, linear history, force-push + delete blocked, no admin bypass; 5 required status checks. | SEC-05 |
    | Scorecard nightly + Branch-Protection introspection | `ossf/scorecard-action` runs weekly; `SCORECARD_READ_TOKEN` (fine-grained PAT, `Administration:read` only, rotation ≤ 1 year) enables Branch-Protection reads. | SEC-05 |
    | Gitleaks secret scan | Runs on every PR; `.gitleaks.toml` + `.gitleaksignore` define detection + per-fingerprint allowlist of audited false positives. | CI-05 + G-14 |
    | Signed releases | GPG-signed git tag (`v[0-9]+.[0-9]+.[0-9]+`) triggers the release pipeline; `verify-signed-tag` asserts the maintainer fingerprint `FDE07046A6178E89ADB57FD3DE300C53D8E18642`. | BUILD-04 |
    | Reproducible build | `lua tools/build.lua --verify` builds byte-identically; CI fails on diff. | BUILD-02 |
    | Redact-before-log | Every `print()` output flows through the `M_log.*` redactor stripping JWT and Bearer substrings (D-79 gate). | SEC-01 |
    | Egress allowlist | The built artifact may reference only the hosts `oauth.zettle.com`, `purchase.izettle.com`, `finance.izettle.com`; a CI grep blocks any other hostname. | SEC-04 + D-12 |

    The posture is measurable against the OpenSSF Scorecard; see
    [ADR-0009](docs/adr/0009-openssf-scorecard-stance.md) for the deliberate
    trade-off discussion (accepted gaps, alternative options, upstream-timed
    improvements) and the CII Best Practices Passing-tier badge in the README.

    ### `SCORECARD_READ_TOKEN` rotation

    The fine-grained PAT is created with a 1-year expiry. Before expiry:

    1. Generate new PAT with identical scope (`Administration:read` only).
    2. `gh secret set SCORECARD_READ_TOKEN --repo yves-vogl/moneymoney-paypal-pos-extension`.
    3. Revoke the old PAT in GitHub settings.
    4. Trigger `gh workflow run "OpenSSF Scorecard"` to verify.
    ```

    Insert position: append BOTH sections (DE before the `---` separator and EN at the very end of SECURITY.md). Do NOT touch any line above the insertion point.
  </action>
  <verify>
    <automated>
      set -e

      grep -q '^## Lieferketten-Kontrollen$' SECURITY.md \
        || { echo "FAIL: SECURITY.md missing DE Lieferketten-Kontrollen section"; exit 1; }
      grep -q '^## Supply-chain controls$' SECURITY.md \
        || { echo "FAIL: SECURITY.md missing EN Supply-chain controls section"; exit 1; }
      grep -q 'SEC-06' SECURITY.md || { echo "FAIL: SEC-06 reference missing"; exit 1; }
      grep -q 'SEC-07' SECURITY.md || { echo "FAIL: SEC-07 reference missing"; exit 1; }
      grep -q 'SEC-08' SECURITY.md || { echo "FAIL: SEC-08 reference missing"; exit 1; }
      grep -q 'SCORECARD_READ_TOKEN' SECURITY.md \
        || { echo "FAIL: SCORECARD_READ_TOKEN rotation not documented"; exit 1; }
      grep -q 'ADR-0009' SECURITY.md \
        || { echo "FAIL: SECURITY.md does not reference ADR-0009"; exit 1; }
      grep -q '0009-openssf-scorecard-stance' SECURITY.md \
        || { echo "FAIL: SECURITY.md ADR-0009 link target wrong"; exit 1; }

      LINES=$(wc -l < SECURITY.md | tr -d ' ')
      if [ "$LINES" -lt 100 ]; then
        echo "FAIL: SECURITY.md only $LINES lines — expected ≥ 100 after this plan"
        exit 1
      fi

      echo "OK: SECURITY.md bilingual supply-chain sections present"
    </automated>
  </verify>
  <done>
    `SECURITY.md` has both `## Lieferketten-Kontrollen` (German) and `## Supply-chain controls` (English) sections, each enumerating the 10 active controls with SEC-/CI-/BUILD-/D- IDs, plus the SCORECARD_READ_TOKEN rotation runbook in both languages. Cross-references to ADR-0009. No content above the insertion points changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md — modify SEC-05; append SEC-06/SEC-07/SEC-08/BUILD-07/DOC-11; update Traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Three sub-edits to `.planning/REQUIREMENTS.md`.

    Edit 3.1 — Modify SEC-05 in-place (line 82):
    - Current text: `[x] **SEC-05**: Branch protection on `main` requires GPG-signed commits and CI green`
    - New text:
      ```
      - [x] **SEC-05**: Branch protection on `main` requires GPG-signed commits, linear history, force-push and delete blocked, no admin bypass; a fine-grained PAT `SCORECARD_READ_TOKEN` with `Administration: read` scope is configured so the Scorecard Branch-Protection check can introspect; required status checks include Lint+tests+reproducible build, gitleaks secret scan, Commit-message lint, Scorecard analysis, and Semgrep SAST; Scorecard `Branch-Protection.score >= 3` post-merge (Tier 1; Option A locked per ADR-0009).
      ```

    Edit 3.2 — Append five new IDs after the existing Security & Trust block (after SEC-05 at line 82):
    - Insert immediately after SEC-05:
      ```
      - [ ] **SEC-06**: Every `uses:` reference in `.github/workflows/*.yml` is pinned to a 40-char commit SHA followed by a `# vX.Y.Z` comment; a CI grep gate fails any PR introducing a floating tag; Dependabot bumps SHA + comment in lockstep; Scorecard `Pinned-Dependencies.score == 10` post-merge.
      - [ ] **SEC-07**: Every workflow declares `permissions: read-all` at top level; write scopes (`contents: write`, `id-token: write`, `security-events: write`, `pages: write`) are job-local and minimal; every `actions/checkout` step that does not need git-stored credentials carries `persist-credentials: false`; Scorecard `Token-Permissions.score == 10` post-merge.
      - [ ] **SEC-08**: A Semgrep SAST workflow runs on every push and pull request with `p/security-audit` + `p/secrets`; ERROR-severity findings fail the workflow; SARIF results are uploaded to GitHub code-scanning; the workflow is a required status check on `main`; a backlog issue tracks ossf/scorecard#5103 merge for the Scorecard `SAST.score` to flip from 0 to 10.
      ```
    - And append to the Build & Release Engineering block (after BUILD-06 at line 91):
      ```
      - [ ] **BUILD-07**: The project has a CII Best Practices badge at "passing" level (≥5/10); the badge URL is rendered in both `README.md` and `README.de.md`; Scorecard `CII-Best-Practices.score >= 5` post-merge.
      ```
    - And append to the Distribution & Documentation block (after DOC-10 at line 120):
      ```
      - [ ] **DOC-11**: `docs/adr/0009-openssf-scorecard-stance.md` is ACCEPTED and documents the deliberately accepted gaps (Fuzzing 0/10, Code-Review 0/10, Packaging -1, Contributors structural cap, Maintained heals-itself) with rationale + compensating mitigations; `SECURITY.md` has bilingual "Lieferketten-Kontrollen / Supply-chain controls" sections enumerating active controls; backlog issues opened for CII Silver and ossf/scorecard#5103 tracking.
      ```

    Edit 3.3 — Update Traceability table at line 166-237:
    - Modify the SEC-05 row to `Phase 6.1` (was Phase 6) + Status `Complete by 06.1-04` (was Complete).
    - Append new rows:
      ```
      | SEC-06 | Phase 6.1 | Complete by 06.1-01 |
      | SEC-07 | Phase 6.1 | Complete by 06.1-01 |
      | SEC-08 | Phase 6.1 | Complete by 06.1-03 (security control in place; Scorecard score reporting deferred per ADR-0009) |
      | BUILD-07 | Phase 6.1 | Complete by 06.1-07 |
      | DOC-11 | Phase 6.1 | Complete by 06.1-08 |
      ```
    - Update Coverage summary at line 241-243:
      - `v1 requirements: **75** total` (was 70 — added 5 new IDs).
      - `Mapped to phases: **75 / 75** (... Phase 6.1: 5)`
      - Add new tail bullet: `Phase 6.1: 5 (SEC-05 modified, SEC-06, SEC-07, SEC-08, BUILD-07, DOC-11 — 6 IDs including the modified SEC-05; net new = 5)`.

    Preserve every other line byte-for-byte. The file is the canonical requirements register — only these edits.
  </action>
  <verify>
    <automated>
      set -e

      # SEC-05 modified
      grep -qE 'SEC-05.*SCORECARD_READ_TOKEN' .planning/REQUIREMENTS.md \
        || { echo "FAIL: SEC-05 not expanded with SCORECARD_READ_TOKEN"; exit 1; }
      grep -qE 'SEC-05.*Scorecard.*Branch-Protection' .planning/REQUIREMENTS.md \
        || { echo "FAIL: SEC-05 not expanded with Branch-Protection acceptance"; exit 1; }

      # New IDs present
      for id in SEC-06 SEC-07 SEC-08 BUILD-07 DOC-11; do
        grep -qE "^- \[ \] \*\*${id}\*\*" .planning/REQUIREMENTS.md \
          || { echo "FAIL: ${id} not appended"; exit 1; }
      done

      # Traceability table updated
      grep -qE '\| SEC-06 \| Phase 6\.1' .planning/REQUIREMENTS.md \
        || { echo "FAIL: SEC-06 not in Traceability table"; exit 1; }
      grep -qE '\| DOC-11 \| Phase 6\.1' .planning/REQUIREMENTS.md \
        || { echo "FAIL: DOC-11 not in Traceability table"; exit 1; }

      # Total count update
      grep -qE 'v1 requirements: \*\*75\*\*' .planning/REQUIREMENTS.md \
        || { echo "FAIL: total count not updated to 75"; exit 1; }

      # Existing IDs preserved (regression check on a random sample)
      grep -q 'AUTH-01' .planning/REQUIREMENTS.md \
        || { echo "FAIL: AUTH-01 regression — file corrupted"; exit 1; }
      grep -q 'BUILD-06' .planning/REQUIREMENTS.md \
        || { echo "FAIL: BUILD-06 regression — file corrupted"; exit 1; }
      grep -q 'DOC-10' .planning/REQUIREMENTS.md \
        || { echo "FAIL: DOC-10 regression — file corrupted"; exit 1; }

      echo "OK: REQUIREMENTS.md updates verified"
    </automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` has: SEC-05 modified in-place with expanded acceptance; SEC-06/SEC-07/SEC-08/BUILD-07/DOC-11 appended in their respective sections; Traceability table updated with 5 new Phase 6.1 rows; Coverage summary updated to 75/75. Every other line preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md — amend Phase 6.1 success criteria + mark complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Two sub-edits to `.planning/ROADMAP.md`.

    Edit 4.1 — Amend Phase 6.1 success criteria block (lines 310-319):
    - Replace success criterion #1: from `aggregate score >= 8.5` to:
      ```
      1. `https://api.securityscorecards.dev/projects/github.com/yves-vogl/moneymoney-paypal-pos-extension` returns aggregate `score >= 7.5` for the post-merge commit (short-term realistic target per ADR-0009 RESEARCH §10); aggregate `score >= 8.5` is reached once ossf/scorecard#5103 merges upstream (SAST detection enabled) AND Maintained heals at ~90 days repo age (~2026-09-15).
      ```
    - Replace success criterion #4 (Branch-Protection): from `score >= 8` to:
      ```
      4. `Branch-Protection` check returns `score >= 3` (Tier 1; Option A locked per ADR-0009 — solo single-account constraint): `SCORECARD_READ_TOKEN` (fine-grained PAT, `Administration:read`) is configured; `main` requires PR + 5 status checks (CI + Scorecard analysis + Semgrep SAST + gitleaks + commit-lint) + signed commits + linear history + no admin bypass; force-push and delete blocked.
      ```
    - Replace success criterion #5 (SAST): from `returns score 10` to:
      ```
      5. A Semgrep workflow runs on every push and PR with `p/security-audit` + `p/secrets`, ERROR-blocking, SARIF in code-scanning, and is a required status check on `main`; Scorecard `SAST.score` is deferred — flips from 0 to 10 once ossf/scorecard#5103 merges (tracked via backlog issue per ADR-0009).
      ```
    - Replace success criterion #6 (CII): from `rendered in README.md and README.de.md` to:
      ```
      6. `CII-Best-Practices` check returns `score >= 5` (Passing tier); the badge URL is rendered in both `README.md` and `README.de.md` (D-40); CII Silver is tracked as a backlog issue post-v1.0.0.
      ```
    - Replace success criterion #7 (ADR): from `docs/adr/0004-openssf-scorecard-stance.md` to:
      ```
      7. `docs/adr/0009-openssf-scorecard-stance.md` (ADR file number 0009 — 0001-0008 were already taken per D-39) documents the bewusst akzeptierten Lücken (Fuzzing 0/10, Solo Code-Review 0/10, Packaging -1, Contributors structural cap, Maintained heals-itself) with rationale, compensating mitigations, and Alternatives Considered (Option B 1-reviewer / Option C bot-reviewer / Snyk Code as second SAST tool / SBOM+cosign+SLSA deferred to v1.1.x).
      ```
    - Add success criterion #9 (NEW — MkDocs site per D-37):
      ```
      9. A MkDocs Material documentation site is deployed to GitHub Pages at `https://yves-vogl.github.io/moneymoney-paypal-pos-extension/` (D-37); bilingual (DE primary, EN secondary); a Documentation/Dokumentation badge in both READMEs links to it.
      ```

    Edit 4.2 — Mark Phase 6.1 complete:
    - Line 27 currently: `- [ ] **Phase 6.1 (INSERTED): Supply-chain & Scorecard hardening**`
    - Change to `- [x]`.

    Edit 4.3 — Update Progress Table (line 255-263):
    - Add new row: `| 6.1. Supply-chain & Scorecard hardening | 8/8 | Complete | <fill in date when this plan merges> |`

    Preserve every other line byte-for-byte.
  </action>
  <verify>
    <automated>
      set -e

      # Phase 6.1 marked complete
      grep -qE '^- \[x\] \*\*Phase 6\.1 \(INSERTED\):' .planning/ROADMAP.md \
        || { echo "FAIL: Phase 6.1 not marked [x]"; exit 1; }

      # Success criteria revised
      grep -qE 'score >= 7\.5' .planning/ROADMAP.md \
        || { echo "FAIL: aggregate target not revised to 7.5"; exit 1; }
      grep -qE 'Branch-Protection.*score >= 3' .planning/ROADMAP.md \
        || grep -qE 'Branch-Protection.*Tier 1' .planning/ROADMAP.md \
        || { echo "FAIL: Branch-Protection target not revised to Option A (Tier 1)"; exit 1; }
      grep -qE 'ossf/scorecard.{0,4}5103' .planning/ROADMAP.md \
        || { echo "FAIL: ROADMAP missing ossf/scorecard#5103 SAST deferral note"; exit 1; }
      grep -qE 'docs/adr/0009-openssf-scorecard-stance' .planning/ROADMAP.md \
        || { echo "FAIL: ROADMAP still references 0004 instead of 0009"; exit 1; }
      grep -qE 'README\.md.*README\.de\.md' .planning/ROADMAP.md \
        || grep -qE 'README\.de\.md.*README\.md' .planning/ROADMAP.md \
        || { echo "FAIL: ROADMAP CII success criterion does not mention both READMEs (D-40)"; exit 1; }
      grep -qE 'github\.io/moneymoney-paypal-pos-extension' .planning/ROADMAP.md \
        || { echo "FAIL: ROADMAP missing MkDocs site success criterion (D-37)"; exit 1; }

      # Progress Table updated
      grep -qE 'Phase 6\.1.*8/8' .planning/ROADMAP.md \
        || grep -qE '6\.1.*Supply-chain.*Complete' .planning/ROADMAP.md \
        || { echo "FAIL: Progress Table not updated for Phase 6.1"; exit 1; }

      echo "OK: ROADMAP.md updates verified"
    </automated>
  </verify>
  <done>
    `.planning/ROADMAP.md` Phase 6.1 success criteria amended (aggregate ≥ 7.5; Branch-Protection ≥ 3; SAST deferred-acceptance; ADR-0009 not 0004; both READMEs for CII per D-40; new criterion #9 for MkDocs site). Phase 6.1 marked `[x]`. Progress Table appended with Phase 6.1 row.
  </done>
</task>

<task type="auto">
  <name>Task 5: Un-comment ADR-0009 nav entry in mkdocs.yml</name>
  <files>mkdocs.yml</files>
  <action>
    Plan 06.1-06 shipped `mkdocs.yml` with the ADR-0009 nav entry commented out (forward-reference; the ADR file did not yet exist). This task un-comments it so the next push-to-main deploy surfaces the new ADR in the Architektur nav.

    Edit:
    - Find the commented line (Plan 06.1-06 wrote it like `# - OpenSSF-Scorecard: adr/0009-openssf-scorecard-stance.md` or similar — the exact form depends on Plan 06.1-06's executor implementation).
    - Remove the leading `# ` (and possibly the trailing comment about Plan 06.1-08).
    - Resulting entry: `- OpenSSF-Scorecard: adr/0009-openssf-scorecard-stance.md`

    Run `mkdocs build --strict` locally to confirm the new nav entry resolves (the ADR file now exists from Task 1).
  </action>
  <verify>
    <automated>
      set -e

      # ADR-0009 nav entry present AND uncommented
      grep -qE '^[[:space:]]+- (OpenSSF-Scorecard|ADR-0009|OpenSSF Scorecard):[[:space:]]+adr/0009-openssf-scorecard-stance\.md$' mkdocs.yml \
        || { echo "FAIL: mkdocs.yml ADR-0009 nav entry not un-commented or wrong format"; exit 1; }

      # No commented version remaining
      grep -E '^[[:space:]]+#[[:space:]]*-[[:space:]]+(OpenSSF-Scorecard|ADR-0009).*adr/0009' mkdocs.yml \
        && { echo "FAIL: a commented ADR-0009 nav line still present"; exit 1; } || true

      # mkdocs build green if installed
      if command -v mkdocs >/dev/null 2>&1; then
        mkdocs build --strict --quiet
        rm -rf site/
      fi

      echo "OK: mkdocs.yml ADR-0009 nav un-commented"
    </automated>
  </verify>
  <done>
    `mkdocs.yml` ADR-0009 nav entry is un-commented and surfaces in the Architektur nav. Local `mkdocs build --strict` succeeds (the ADR file now exists from Task 1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR ledger (docs/adr/) → external auditor reading the repo | Auditors trust ADRs to honestly enumerate accepted gaps and the rationale. A glossed-over accepted gap erodes trust. Mitigation: ADR-0009 explicitly names each gap, the score impact, and the alternative considered. |
| REQUIREMENTS.md acceptance text → CI gates | A requirement whose acceptance text drifts from the CI gate that enforces it produces a silent gap. Mitigation: Task 3's acceptance texts reference the specific gate commands (e.g. "the SEC-06 grep gate in ci.yml"). |
| SECURITY.md supply-chain section → external researcher | A researcher reading SECURITY.md for an audit needs to find the active controls without parsing CI YAML. Mitigation: Task 2's bilingual table lists every control with the SEC-/CI-/BUILD-ID + the workflow file that implements it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-08-01 | Information Disclosure | Honest accepted-gap disclosure | accept | ADR-0009 explicitly publishes the five accepted gaps. The honesty IS the mitigation — opaque scoring erodes auditor trust faster than transparent gaps. |
| T-06.1-08-02 | Tampering | REQUIREMENTS.md acceptance text drift from CI gate implementation | mitigate | Acceptance texts in Task 3 reference the specific gate command (e.g. `Scorecard analysis`/`Semgrep SAST` job names). Plan 06.1-04 enforces the gate strings byte-for-byte. |
| T-06.1-08-03 | Repudiation | Backlog issues not opened post-merge | accept | Yves task documented in SUMMARY; if missed, ADR-0009 still references the two upstream-timed events and the silver-CII intention. Backlog issues are tracking aids, not policy. |
</threat_model>

<verification>
1. All 5 file edits applied; all verify gates pass.
2. `mkdocs build --strict` succeeds (if mkdocs installed).
3. CI green on PR.
4. After merge: docs.yml deploys; ADR-0009 visible at `https://yves-vogl.github.io/moneymoney-paypal-pos-extension/architektur/openssf-scorecard-stance/` (or similar slug).
5. Yves opens the two backlog issues per task 1 SUMMARY note.
6. Phase 6.1 complete — STATE.md update is the orchestrator's task post-merge.
</verification>

<success_criteria>
- `docs/adr/0009-openssf-scorecard-stance.md` exists with MADR sections, ACCEPTED status, all 5 accepted-gap rationales, all 4+ alternatives-rejected entries, SBOM/cosign/SLSA forward-pointer, ossf/scorecard#5103 reference
- `SECURITY.md` has bilingual `## Lieferketten-Kontrollen` + `## Supply-chain controls` sections enumerating 10 active controls + SCORECARD_READ_TOKEN rotation runbook
- `.planning/REQUIREMENTS.md` SEC-05 modified; SEC-06/SEC-07/SEC-08/BUILD-07/DOC-11 appended; Traceability table updated; total count → 75
- `.planning/ROADMAP.md` Phase 6.1 success criteria amended (aggregate ≥ 7.5; Branch-Protection ≥ 3; SAST deferred-acceptance; ADR-0009; both READMEs for CII; MkDocs site criterion); Phase 6.1 marked `[x]`
- `mkdocs.yml` ADR-0009 nav entry un-commented
- CI green
- Reproducible-build SHA on `dist/paypal-pos.lua` unchanged
- Yves opens 2 backlog issues post-merge
</success_criteria>

<output>
Create `.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-08-SUMMARY.md` when done.
</output>

<pitfalls>
- **Pitfall 1 — ADR-0009 lands without dependent files yet (mkdocs nav un-comment in Task 5):** If Plan 06.1-06 implementation chose the placeholder-file path instead of the commented-nav path, Task 5 deletes the placeholder instead. Verify which pattern was used by reading the actual mkdocs.yml.
- **Pitfall 2 — REQUIREMENTS.md edit corrupts existing IDs:** The file has 70 existing requirements. Task 3 edits SEC-05 + appends 5 new + updates traceability. The verify gate regression-checks AUTH-01, BUILD-06, DOC-10 — three random sentinels. If any disappear, the file is corrupted; revert and re-apply by hand.
- **Pitfall 3 — DOC-11 number collision:** Verified at planning time that DOC-01..DOC-10 are taken; DOC-11 is next-free. If a parallel plan (e.g. a follow-up Phase 6.2 added DOC-11 first), recompute next-free. The verify gate does not catch this collision automatically — the planner trusts the manual verification.
- **Pitfall 4 — SECURITY.md insertion position:** The file has both DE and EN sections separated by `---`. Inserting in the wrong half corrupts the bilingual structure. Task 2's instruction explicitly: DE new section before `---`, EN new section after EN's existing content.
- **Pitfall 5 — ROADMAP.md Progress Table format:** The table has columns Phase / Plans Complete / Status / Completed. The new row must match the column structure or markdown rendering breaks.
- **Pitfall 6 — Backlog issues never opened:** SUMMARY task for Yves; if missed, the ADR-0009 references the two upstream-timed events but no GitHub issue tracks them. Not blocking but operationally important.
- **Pitfall 7 — `mkdocs build --strict` fails because ADR file path is wrong:** mkdocs nav paths are relative to `docs/` (or `docs_dir`). The entry should be `adr/0009-openssf-scorecard-stance.md`, NOT `docs/adr/0009-...`. Verify the leading-path convention against the other ADR nav entries Plan 06.1-06 shipped.
- **Pitfall 8 — Acceptance-text drift between REQUIREMENTS.md and ROADMAP.md:** Both files cite the same Scorecard targets (≥ 7.5 aggregate, Branch-Protection ≥ 3, etc.). If they drift, the requirements register loses authority. Cross-check both files before commit.
</pitfalls>

<closes>
- DOC-11 (ADR-0009 ACCEPTED + SECURITY.md supply-chain section + REQUIREMENTS.md updates + ROADMAP.md amendments + mkdocs.yml nav un-comment)
- SEC-05 (Phase 6.1 traceability — Complete by Plan 06.1-04, formally recorded here)
- SEC-06 (Phase 6.1 traceability — Complete by Plan 06.1-01)
- SEC-07 (Phase 6.1 traceability — Complete by Plan 06.1-01)
- SEC-08 (Phase 6.1 traceability — Complete by Plan 06.1-03; Scorecard score reporting deferred per ADR-0009)
- BUILD-07 (Phase 6.1 traceability — Complete by Plan 06.1-07)
</closes>
