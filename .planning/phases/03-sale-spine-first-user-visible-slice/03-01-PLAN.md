---
phase: 03-sale-spine-first-user-visible-slice
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spec/fixtures/purchases/purchase_simple_sale.json
  - spec/fixtures/purchases/purchase_with_vat_and_tip.json
  - spec/fixtures/purchases/purchase_refund.json
  - spec/fixtures/purchases/purchase_page1.json
  - spec/fixtures/purchases/purchase_page2.json
  - spec/fixtures/purchases/purchases_empty.json
  - spec/fixtures/purchases/purchase_non_eur.json
  - spec/fixtures/purchases/purchase_dst_boundary_summer.json
  - spec/fixtures/purchases/purchase_dst_boundary_winter.json
  - spec/fixtures/purchases/purchase_with_card_metadata.json
  - spec/dst_table_spec.lua
  - spec/mapping_spec.lua
  - spec/pagination_spec.lua
  - spec/purchases_spec.lua
autonomous: true
requirements: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-06, SALE-08, TEST-03, TEST-04]
requirements_addressed: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-06, SALE-08, TEST-03, TEST-04]
tags: [wave-0, scaffold, fixtures, dst, lua, mvp]

must_haves:
  truths:
    - "Ten hand-rolled JSON fixtures exist under spec/fixtures/purchases/ matching the Zettle purchase.adoc field shapes per D-44; every fixture wraps records in {\"purchases\":[...]} and carries a top-level _source citation"
    - "Card metadata fixture uses payments[0].attributes.cardType and payments[0].attributes.maskedPan (per RESEARCH §1 Payment Sub-Object correction over CONTEXT D-35 cardBrand/cardLastFour wording)"
    - "DST summer fixture timestamp = 2026-06-19T23:55:00Z (resolves to Berlin local 2026-06-20T01:55+02:00); DST winter fixture timestamp = 2026-01-31T23:55:00Z (resolves to Berlin local 2026-02-01T00:55+01:00); both gate D-36 / SALE-04"
    - "Non-EUR fixture has currency=\"USD\" so the D-37 silent-skip path can be exercised"
    - "Refund fixture has refund=true with refundsPurchaseUUID1 pointing at a sibling sale UUID (D-32)"
    - "Pagination fixtures: page1 has 1 record + lastPurchaseHash; page2 is terminal (empty array, no cursor)"
    - "Empty fixture has purchases=[] for the since-past-all path (SALE-06 empty refresh)"
    - "spec/dst_table_spec.lua is a RED-pending scaffold asserting _to_berlin_local_time correctness on 2026 summer + 2026 winter boundary timestamps (Wave 2 fills the function in src/mapping.lua)"
    - "spec/mapping_spec.lua is a pending scaffold listing every behavior test Wave 2 will green (SALE-01, SALE-02, SALE-04, SALE-08, I18N-01, D-32, D-37)"
    - "spec/pagination_spec.lua is a pending scaffold for M_pagination.iterate cursor termination (empty array, missing hash, MAX_PAGES guard)"
    - "spec/purchases_spec.lua is a pending scaffold for M_purchases.fetch URL shape (startDate query param) and Bearer-header use (SALE-06, D-33)"
    - "No fixture contains a real PayPal POS merchant API key, a real merchant UUID, or a real organization name (synthesis only — UUID-shaped strings and a German Beispiel-Café merchant name)"
    - "No fixture references any host outside {oauth.zettle.com, purchase.izettle.com} (the only hosts present in Phase 2 + Phase 3 egress); finance.izettle.com is Phase 4 territory and MUST NOT appear"
    - "src/webbanking_header.lua is NOT modified — M_purchases / M_pagination / M_mapping tables are already pre-declared in Phase 1"
    - "tools/manifest.txt is NOT modified — DST table is inlined into src/mapping.lua in Wave 2 (planner's discretion per D-40 / RESEARCH §2b)"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in commit message, code, or comments"
  artifacts:
    - path: "spec/fixtures/purchases/purchase_simple_sale.json"
      provides: "Single EUR sale, no VAT, no tip, no payments (SALE-01 / TEST-04 baseline)"
      contains: "purchaseUUID1"
    - path: "spec/fixtures/purchases/purchase_with_vat_and_tip.json"
      provides: "VAT-bearing sale (vatAmount > 0) with tip via payments[0].gratuityAmount (D-34 / I18N-01)"
      contains: "gratuityAmount"
    - path: "spec/fixtures/purchases/purchase_refund.json"
      provides: "Refund purchase with refund=true and refundsPurchaseUUID1 (D-32)"
      contains: "refundsPurchaseUUID1"
    - path: "spec/fixtures/purchases/purchase_page1.json"
      provides: "Cursor page 1: 1 record + lastPurchaseHash"
      contains: "lastPurchaseHash"
    - path: "spec/fixtures/purchases/purchase_page2.json"
      provides: "Cursor page 2: terminal (empty purchases[], no lastPurchaseHash)"
      contains: "purchases"
    - path: "spec/fixtures/purchases/purchases_empty.json"
      provides: "Empty response — since past all purchases (SALE-06 empty refresh)"
      contains: "purchases"
    - path: "spec/fixtures/purchases/purchase_non_eur.json"
      provides: "currency=USD purchase that must be skipped per D-37"
      contains: "USD"
    - path: "spec/fixtures/purchases/purchase_dst_boundary_summer.json"
      provides: "timestamp 2026-06-19T23:55:00Z to gate CEST conversion (SALE-04)"
      contains: "2026-06-19T23:55:00"
    - path: "spec/fixtures/purchases/purchase_dst_boundary_winter.json"
      provides: "timestamp 2026-01-31T23:55:00Z to gate CET conversion (SALE-04)"
      contains: "2026-01-31T23:55:00"
    - path: "spec/fixtures/purchases/purchase_with_card_metadata.json"
      provides: "payments[0].attributes.cardType=VISA + maskedPan ending 1111 (SALE-08 / D-35 corrected per RESEARCH §1)"
      contains: "attributes"
    - path: "spec/dst_table_spec.lua"
      provides: "Pending RED spec for _to_berlin_local_time DST boundary correctness"
      contains: "describe"
    - path: "spec/mapping_spec.lua"
      provides: "Pending scaffold for M_mapping.purchase_to_transaction / refund_to_transaction unit tests"
      contains: "describe"
    - path: "spec/pagination_spec.lua"
      provides: "Pending scaffold for M_pagination.iterate cursor termination"
      contains: "describe"
    - path: "spec/purchases_spec.lua"
      provides: "Pending scaffold for M_purchases.fetch URL + Bearer use"
      contains: "describe"
  key_links:
    - from: "spec/dst_table_spec.lua"
      to: "src/mapping.lua _to_berlin_local_time (Wave 2)"
      via: "asserts boundary correctness for 2026 summer + 2026 winter via DST_TABLE lookup"
      pattern: "_to_berlin_local_time"
    - from: "spec/mapping_spec.lua (Wave 2)"
      to: "spec/fixtures/purchases/purchase_simple_sale.json"
      via: "Fixtures.load(\"purchases/purchase_simple_sale\")"
      pattern: "Fixtures.load.*purchases/"
    - from: "spec/purchases_spec.lua (Wave 3)"
      to: "spec/fixtures/purchases/purchase_page1.json"
      via: "Mocks.push_response with raw fixture content"
      pattern: "Mocks.push_response"
    - from: "spec/pagination_spec.lua (Wave 3)"
      to: "spec/fixtures/purchases/purchases_empty.json"
      via: "Mocks.push_response for terminal-page test"
      pattern: "purchases_empty"
---

<objective>
Stand up Wave 0 test infrastructure for Phase 3 so every downstream wave (Waves 1-5) has hand-rolled JSON fixtures and pending spec scaffolds to populate. This plan unblocks the RED gating specs (Wave 1) and every subsequent implementation wave.

Purpose: Per 03-RESEARCH.md §"Wave 0 Gaps" and 03-CONTEXT.md D-44, ten fixtures under spec/fixtures/purchases/ must exist before any unit or integration test for src/purchases.lua, src/pagination.lua, or src/mapping.lua can run. Additionally, four pending spec scaffolds are created so the Phase-3 spec layout is fixed early and downstream waves only fill bodies (not file headers).

Output: Ten valid strict-JSON fixture files matching the Zettle purchase.adoc shapes (with the RESEARCH §1 correction that card metadata lives under payments[].attributes.cardType / maskedPan, not payments[].cardBrand / cardLastFour as CONTEXT D-35 phrasing suggested); a pending RED scaffold spec/dst_table_spec.lua asserting two 2026 boundary cases; pending scaffolds spec/mapping_spec.lua, spec/pagination_spec.lua, spec/purchases_spec.lua wired with before_each(Mocks.setup + dofile) plus one sanity test each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md
@.planning/phases/01-foundations-sandbox-probes/CONTEXT.md
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@spec/fixtures/auth/users_self_ok.json
@spec/http_spec.lua
@spec/log_redaction_spec.lua
@docs/adr/0001-amalgamator-design.md
@docs/adr/0003-sandbox-probe-results.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author ten hand-rolled JSON fixtures under spec/fixtures/purchases/</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-44 (fixture file list)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §1 "Zettle Purchase API Surface" — verbatim field shapes for purchase JSON, especially the Payment Sub-Object correction (card metadata under payments[].attributes.cardType + maskedPan)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2b for DST timestamps
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/fixtures/purchases/*.json" — Fixtures.load path convention, root object shape, fixture comparison to spec/fixtures/auth/users_self_ok.json
    - spec/helpers/fixtures.lua (current implementation: strict JSON via dkjson.decode; comments `//` will fail)
    - spec/fixtures/auth/users_self_ok.json (the canonical analog: top-level _source key, two-space indent, LF, single trailing newline)
  </read_first>
  <files>
    spec/fixtures/purchases/purchase_simple_sale.json,
    spec/fixtures/purchases/purchase_with_vat_and_tip.json,
    spec/fixtures/purchases/purchase_refund.json,
    spec/fixtures/purchases/purchase_page1.json,
    spec/fixtures/purchases/purchase_page2.json,
    spec/fixtures/purchases/purchases_empty.json,
    spec/fixtures/purchases/purchase_non_eur.json,
    spec/fixtures/purchases/purchase_dst_boundary_summer.json,
    spec/fixtures/purchases/purchase_dst_boundary_winter.json,
    spec/fixtures/purchases/purchase_with_card_metadata.json
  </files>
  <action>
    Create the directory `spec/fixtures/purchases/` and author ten strict-JSON fixtures. Each fixture's root object is `{"_source": "github.com/iZettle/api-documentation/purchase.adoc", "purchases": [ ...records... ], "lastPurchaseHash": <hash-or-null>}`. Strict JSON only — no `//` comments anywhere (dkjson rejects them). Two-space indent, LF line endings, single trailing newline. Place `_source` as the FIRST top-level key. NEVER include a real merchant API key, a real organization UUID captured from production, or a recorded production response — all values are synthesized. Hosts MUST NOT appear in any field value (these are response bodies, not URLs).

    Each record shape (subset of `purchase.adoc` documented fields used by Phase 3): `purchaseUUID1` (UUID-shaped string), `amount` (integer minor units, signed), `vatAmount` (integer minor units, ≥0), `currency` (3-letter ISO 4217 string), `timestamp` (ISO-8601 UTC with `Z` suffix or `+0000`), `purchaseNumber` (integer), `refund` (boolean), `refunded` (boolean), `refundsPurchaseUUID1` (UUID string, present only on refunds), `products` (array, may be empty), `payments` (array, may be empty), `groupedVatAmounts` (object, may be empty).

    Field values per fixture (do NOT inline JSON literals in this PLAN's action prose — author the JSON directly, matching this specification):

    1. `purchase_simple_sale.json` — one record: `purchaseUUID1` = `"11111111-1111-1111-1111-111111111111"`, `amount = 500`, `vatAmount = 0`, `currency = "EUR"`, `timestamp = "2026-05-15T10:30:00.000+0000"`, `purchaseNumber = 1001`, `refund = false`, `refunded = false`, `products = []`, `payments = []`, `groupedVatAmounts = {}`. Root: no `lastPurchaseHash` (single-page terminal). Covers SALE-01 baseline + TEST-04 schema gate (every required output field must materialise on this minimal input).

    2. `purchase_with_vat_and_tip.json` — one record: `purchaseUUID1` = `"22222222-2222-2222-2222-222222222222"`, `amount = 1995`, `vatAmount = 318`, `currency = "EUR"`, `timestamp = "2026-05-15T11:45:00.000+0000"`, `purchaseNumber = 1002`, `refund = false`, `refunded = false`, `products = []`, `payments = [{"uuid":"pay-1","type":"IZETTLE_CARD","amount":1995,"gratuityAmount":100,"attributes":{}}]`, `groupedVatAmounts = {"19":318}`. Covers tip aggregation (D-34 Trinkgeld line), VAT presence (MwSt line), and the I18N-01 German purpose-line set.

    3. `purchase_refund.json` — one record: `purchaseUUID1` = `"33333333-3333-3333-3333-333333333333"` (refund's own UUID), `amount = -995`, `vatAmount = -159`, `currency = "EUR"`, `timestamp = "2026-05-16T09:10:00.000+0000"`, `purchaseNumber = 1003`, `refund = true`, `refunded = false`, `refundsPurchaseUUID1 = "11111111-1111-1111-1111-111111111111"` (the simple-sale UUID), `products = []`, `payments = []`, `groupedVatAmounts = {}`. Covers D-32 refund mapping (negative amount, "zettle:refund:" transactionCode prefix, German "Rückerstattung zu Beleg #..." purpose line).

    4. `purchase_page1.json` — one record (any small synthesized purchase: `purchaseUUID1 = "44444444-4444-4444-4444-444444444444"`, `amount = 750`, `vatAmount = 0`, `currency = "EUR"`, `timestamp = "2026-05-17T08:00:00.000+0000"`, `purchaseNumber = 1004`, `refund = false`, `refunded = false`, `products = []`, `payments = []`, `groupedVatAmounts = {}`). Root: `lastPurchaseHash = "hash-page1-to-page2"`. Covers cursor handoff.

    5. `purchase_page2.json` — `purchases = []`, NO `lastPurchaseHash` field at all (the absent-cursor terminal signal). Covers the loop's belt-and-suspenders termination.

    6. `purchases_empty.json` — identical shape to `purchase_page2.json` (`purchases = []`, no `lastPurchaseHash`) but semantically distinct: this models a since-past-all-purchases first call. Use a DIFFERENT `_source` comment value (`"github.com/iZettle/api-documentation/purchase.adoc — empty window"`) so a future maintainer reading the fixture grasps the intent immediately.

    7. `purchase_non_eur.json` — one record: `purchaseUUID1 = "55555555-5555-5555-5555-555555555555"`, `amount = 2000`, `vatAmount = 0`, `currency = "USD"`, `timestamp = "2026-05-18T12:00:00.000+0000"`, `purchaseNumber = 1005`, `refund = false`, `refunded = false`, `products = []`, `payments = []`, `groupedVatAmounts = {}`. Covers D-37 silent-skip path.

    8. `purchase_dst_boundary_summer.json` — one record: `purchaseUUID1 = "66666666-6666-6666-6666-666666666666"`, `amount = 600`, `vatAmount = 0`, `currency = "EUR"`, `timestamp = "2026-06-19T23:55:00.000+0000"` (this is THE locked acceptance timestamp from RESEARCH §2b for the CEST/+02:00 case — DO NOT change), `purchaseNumber = 1006`, `refund = false`, `refunded = false`, `products = []`, `payments = []`, `groupedVatAmounts = {}`. Covers SALE-04 CEST conversion → Berlin local day 2026-06-20.

    9. `purchase_dst_boundary_winter.json` — one record: `purchaseUUID1 = "77777777-7777-7777-7777-777777777777"`, `amount = 700`, `vatAmount = 0`, `currency = "EUR"`, `timestamp = "2026-01-31T23:55:00.000+0000"` (locked from RESEARCH §2b for the CET/+01:00 case — DO NOT change), `purchaseNumber = 1007`, `refund = false`, `refunded = false`, `products = []`, `payments = []`, `groupedVatAmounts = {}`. Covers SALE-04 CET conversion → Berlin local day 2026-02-01.

    10. `purchase_with_card_metadata.json` — one record: `purchaseUUID1 = "88888888-8888-8888-8888-888888888888"`, `amount = 900`, `vatAmount = 0`, `currency = "EUR"`, `timestamp = "2026-05-19T14:00:00.000+0000"`, `purchaseNumber = 1008`, `refund = false`, `refunded = false`, `products = []`, `payments = [{"uuid":"pay-2","type":"IZETTLE_CARD","amount":900,"gratuityAmount":0,"attributes":{"cardType":"VISA","maskedPan":"411111******1111","cardPaymentEntryMode":"ICC"}}]`, `groupedVatAmounts = {}`. Covers SALE-08 / D-35 card-brand + last-four label upgrade — the maskedPan ends in `1111` so `:sub(-4)` yields `"1111"`. **CRITICAL:** uses `payments[].attributes.cardType` and `payments[].attributes.maskedPan` per RESEARCH §1 Payment Sub-Object correction over CONTEXT D-35 wording.

    For every fixture: the root object MUST be the four keys `_source`, `purchases`, `lastPurchaseHash` (when present), in that order. Do NOT include extra fields. The dkjson decode/encode round-trip is the legibility contract.
  </action>
  <behavior>
    - Each fixture is valid strict JSON parseable by dkjson with no errors
    - Each fixture has a top-level string-valued `_source` key as the first key
    - Each non-empty record has `purchaseUUID1`, `amount`, `currency`, `timestamp`, `purchaseNumber` set (Phase-3 mapping minimum)
    - `purchase_with_vat_and_tip.json` has `payments[0].gratuityAmount = 100` (the tip aggregator's input)
    - `purchase_refund.json` has `refund = true` and `refundsPurchaseUUID1` set
    - `purchase_with_card_metadata.json` uses `payments[0].attributes.cardType = "VISA"` and `maskedPan` ending in `1111` (NOT `cardBrand` / `cardLastFour`)
    - `purchase_non_eur.json` has `currency = "USD"` (so D-37 silent-skip path can fire)
    - `purchase_page1.json` has exactly one record and `lastPurchaseHash` set; `purchase_page2.json` has `purchases = []` and no `lastPurchaseHash`
    - DST summer fixture has `timestamp = "2026-06-19T23:55:00.000+0000"`; DST winter has `timestamp = "2026-01-31T23:55:00.000+0000"`
    - No fixture references hosts (only response-body content; no URLs)
    - No fixture contains a real merchant key, real Zettle org UUID, or real merchant name
  </behavior>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && lua -e 'local d=require("dkjson"); local files={"purchase_simple_sale","purchase_with_vat_and_tip","purchase_refund","purchase_page1","purchase_page2","purchases_empty","purchase_non_eur","purchase_dst_boundary_summer","purchase_dst_boundary_winter","purchase_with_card_metadata"}; for _,n in ipairs(files) do local f=io.open("spec/fixtures/purchases/"..n..".json","r"); assert(f, n..": file missing"); local raw=f:read("*a"); f:close(); local t,_,err=d.decode(raw); assert(not err, n..": "..tostring(err)); assert(type(t._source)=="string", n..": missing _source"); assert(type(t.purchases)=="table", n..": missing purchases array"); end; print("OK")' | grep -q '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/fixtures/purchases/*.json | wc -l` reports 10
    - Each file parses via `dkjson.decode` without error
    - Each file has a top-level string-valued `_source` key (first key in the document)
    - `grep -l '11111111-1111-1111-1111-111111111111' spec/fixtures/purchases/purchase_simple_sale.json` succeeds
    - `grep -l 'gratuityAmount' spec/fixtures/purchases/purchase_with_vat_and_tip.json` succeeds (tip path)
    - `grep -l 'refundsPurchaseUUID1' spec/fixtures/purchases/purchase_refund.json` succeeds
    - `grep -l '"refund": true' spec/fixtures/purchases/purchase_refund.json` succeeds
    - `grep -l 'lastPurchaseHash' spec/fixtures/purchases/purchase_page1.json` succeeds
    - `grep -L 'lastPurchaseHash' spec/fixtures/purchases/purchase_page2.json` succeeds (terminal page MUST NOT have the cursor)
    - `grep -L 'lastPurchaseHash' spec/fixtures/purchases/purchases_empty.json` succeeds (terminal page MUST NOT have the cursor)
    - `grep -l '"currency": "USD"' spec/fixtures/purchases/purchase_non_eur.json` succeeds
    - `grep -l '2026-06-19T23:55:00' spec/fixtures/purchases/purchase_dst_boundary_summer.json` succeeds
    - `grep -l '2026-01-31T23:55:00' spec/fixtures/purchases/purchase_dst_boundary_winter.json` succeeds
    - `grep -l 'attributes' spec/fixtures/purchases/purchase_with_card_metadata.json` succeeds
    - `grep -l 'cardType' spec/fixtures/purchases/purchase_with_card_metadata.json` succeeds
    - `grep -L 'cardBrand\|cardLastFour' spec/fixtures/purchases/purchase_with_card_metadata.json` succeeds (the deprecated D-35 names MUST NOT appear)
    - `grep -L -E '(zettle\.com|izettle\.com)' spec/fixtures/purchases/*.json` (none of the fixtures embed hostnames in field values — hostnames only allowed inside `_source` citation pointing at github.com)
  </acceptance_criteria>
  <done>
    Ten valid JSON fixtures under spec/fixtures/purchases/ matching the shapes documented in 03-RESEARCH.md §1; each carries a `_source` citation; the card-metadata fixture uses the corrected `payments[].attributes.cardType / maskedPan` path; DST boundary timestamps are exactly `2026-06-19T23:55:00.000+0000` and `2026-01-31T23:55:00.000+0000`; no real merchant data; commit message `test(03-01): add ten hand-rolled purchase fixtures`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create RED DST boundary spec scaffold (spec/dst_table_spec.lua)</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2b (DST_TABLE shape, _to_berlin_local_time signature, two acceptance fixtures, last_sunday_utc helper)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/mapping_spec.lua" (pure-logic spec pattern from spec/auth_spec.lua)
    - spec/helpers/fixtures.lua (Fixtures.load returns two values: raw, decoded)
    - spec/log_redaction_spec.lua L11-48 (before_each Mocks.setup + dofile pattern — copy verbatim)
    - spec/http_spec.lua L13-44 (build-once + dofile-per-test setup; preferred style for Phase-3 specs)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-36
  </read_first>
  <files>spec/dst_table_spec.lua</files>
  <action>
    Create `spec/dst_table_spec.lua` as a RED pending scaffold for the EU-DST boundary verification of `_to_berlin_local_time` (Wave 2 implementation in `src/mapping.lua`, planner discretion per D-40). The spec is REQUIRED to fail at this wave (Wave 0) because `_to_berlin_local_time` does not exist yet; Wave 2 makes it green.

    File preamble (copy verbatim from `spec/http_spec.lua` L11-44 with the file name substituted):
    - `local Mocks = require("spec.helpers.mm_mocks")`
    - `local Fixtures = require("spec.helpers.fixtures")`
    - Build-once `do … end` block calling `os.execute("lua tools/build.lua 2>/dev/null")` with error message `"dst_table_spec: failed to build dist/paypal-pos.lua before suite"`
    - `local function load_artifact() dofile("dist/paypal-pos.lua") end`
    - `describe("DST table (D-36 / SALE-04)", function() before_each(function() Mocks.setup(); load_artifact() end); after_each(function() Mocks.teardown() end); ...)`

    Sanity test (non-pending — confirms suite plumbing works even before `_to_berlin_local_time` exists):
    - `it("M_mapping module table is exposed", function() assert.is_table(M_mapping) end)`

    Pending tests (Wave 2 fills these — name them exactly as listed so `busted -t "<name>"` filters):
    - `pending("_to_berlin_local_time CEST summer offset 2026-06-19T23:55Z -> local 2026-06-20T01:55", function() ... end)`
    - `pending("_to_berlin_local_time CET winter offset 2026-01-31T23:55Z -> local 2026-02-01T00:55", function() ... end)`
    - `pending("_to_berlin_local_time DST table covers years 2020..2040 boundaries", function() ... end)`
    - `pending("_to_berlin_local_time exact start-boundary timestamp picks summer offset", function() ... end)`
    - `pending("_to_berlin_local_time exact end-boundary timestamp picks winter offset", function() ... end)`

    DO NOT implement assertions inside the pending blocks — leave them empty (busted's `pending(name, fn)` runs the body but allows failures to be reported as Pending). The body MAY contain a comment block citing the expected POSIX integers from RESEARCH §2b:

      `-- Wave 2 expectation:`
      `-- POSIX(2026-06-19T23:55:00Z) = 1781913300`
      `-- Berlin local POSIX (+7200) = 1781920500`
      `-- POSIX(2026-01-31T23:55:00Z) = 1769903700`
      `-- Berlin local POSIX (+3600) = 1769907300`
      `-- DST_TABLE[7] = {1774746000, 1792890000}  -- 2026 entry (summer_start_utc, summer_end_utc)`

    Apply `-- luacheck: ignore 431` at the top of the file if `describe` / `before_each` callback args shadow outer variables.

    Do NOT add `pcall` wrappers, do NOT touch `src/mapping.lua` (that is Wave 2).
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && ./.luarocks/bin/busted spec/dst_table_spec.lua 2>&1 | tee /tmp/wave0_dst.log && grep -q 'Pending' /tmp/wave0_dst.log && ! grep -E 'Failure|Error' /tmp/wave0_dst.log && grep -q 'M_mapping module table is exposed' /tmp/wave0_dst.log</automated>
  </verify>
  <acceptance_criteria>
    - `busted spec/dst_table_spec.lua` exits with no failures/errors
    - `busted spec/dst_table_spec.lua -t "M_mapping module table is exposed"` PASSES (sanity test confirms dofile worked)
    - `busted spec/dst_table_spec.lua -t "_to_berlin_local_time CEST summer offset 2026-06-19T23:55Z -> local 2026-06-20T01:55"` reports Pending
    - `busted spec/dst_table_spec.lua -t "_to_berlin_local_time CET winter offset 2026-01-31T23:55Z -> local 2026-02-01T00:55"` reports Pending
    - `grep -n 'local Mocks = require("spec.helpers.mm_mocks")' spec/dst_table_spec.lua` finds the import on a top line
    - `grep -n 'dofile("dist/paypal-pos.lua")' spec/dst_table_spec.lua` finds the dofile inside `before_each`
    - `grep -c '^  pending(' spec/dst_table_spec.lua` reports at least 5
    - `luacheck spec/dst_table_spec.lua 2>/dev/null || true` (env exception per Phase-2 02-07 deviation; CI Lua 5.4 luacheck is authoritative)
  </acceptance_criteria>
  <done>
    spec/dst_table_spec.lua exists with the standard Phase-2 spec preamble; one non-pending sanity test passes; at least five pending tests document the DST boundary contract Wave 2 will green; the file references the 2026 boundary integers from RESEARCH §2b as comments. Commit message `test(03-01): add pending DST boundary spec scaffold`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create pending mapping / pagination / purchases spec scaffolds + verify Fixtures.load nested path</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md (per-task verification map — pending test names per spec file must match exactly)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §"Phase Requirements → Test Map" L575-587 (which req gates which spec)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/mapping_spec.lua", §"spec/pagination_spec.lua", §"spec/purchases_spec.lua" (analog patterns)
    - spec/auth_spec.lua L13-22, L175-261 (pure-logic spec pattern: load fixture, call function directly)
    - spec/http_spec.lua L11-90 (URL/header capture pattern for purchases_spec)
    - spec/entry_spec.lua L122-128 (multi-push-response pattern for pagination_spec)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-33, D-34, D-35, D-37 (mapping spec content references), D-44 (fixture names)
  </read_first>
  <files>spec/mapping_spec.lua, spec/pagination_spec.lua, spec/purchases_spec.lua</files>
  <action>
    Create three new spec files following the standard Phase-2 preamble (`local Mocks = require ...`, `local Fixtures = require ...`, build-once `do … end`, `load_artifact`, `describe(... function() before_each(Mocks.setup + load_artifact) ... after_each(Mocks.teardown) ... end)`). Each file declares one outer `describe(...)` block and contains `pending("<name>", function() end)` entries for every Wave 2 / Wave 3 test command listed in 03-VALIDATION.md, plus ONE non-pending sanity test confirming the relevant module table is exposed.

    `spec/mapping_spec.lua` — Wave 2 will green these (one pending per behavior, named exactly):
    - sanity: `it("M_mapping module table is exposed", function() assert.is_table(M_mapping) end)`
    - `pending("purchase_to_transaction maps amount to EUR float (SALE-01)", function() end)`
    - `pending("purchase_to_transaction sets transactionCode = zettle:sale:<purchaseUUID1> (SALE-02 / D-38)", function() end)`
    - `pending("purchase_to_transaction sets booked = false and omits valueDate (D-31 / Phase 3 contract)", function() end)`
    - `pending("purchase_to_transaction sets bookingDate via Berlin local time (SALE-04 / D-36)", function() end)`
    - `pending("purchase_to_transaction defaults name to Kartenzahlung when payments empty (SALE-08 / D-35)", function() end)`
    - `pending("purchase_to_transaction upgrades name to <Brand> •••• <last4> when payments[0].attributes.cardType present (SALE-08 / D-35 corrected)", function() end)`
    - `pending("purchase_to_transaction purpose contains Brutto / MwSt / Trinkgeld / Netto / Beleg German lines (I18N-01 / D-34)", function() end)`
    - `pending("purchase_to_transaction omits MwSt line when vatAmount = 0 (D-34)", function() end)`
    - `pending("purchase_to_transaction omits Trinkgeld line when payments[].gratuityAmount sums to 0 (D-34)", function() end)`
    - `pending("purchase_to_transaction returns nil for non-EUR purchase (D-37)", function() end)`
    - `pending("refund_to_transaction returns negative amount (D-32)", function() end)`
    - `pending("refund_to_transaction sets transactionCode = zettle:refund:<purchaseUUID1> (D-38)", function() end)`
    - `pending("refund_to_transaction purpose references original purchaseNumber via refundsPurchaseUUID1 (D-32)", function() end)`
    - `pending("refund_to_transaction name appends Rückerstattung suffix (D-32 / D-35)", function() end)`
    - `pending("_format_amount renders 1995 minor units as 19,95 with German comma decimal (D-34)", function() end)`
    - `pending("_format_amount renders 500 minor units as 5,00 (D-34)", function() end)`

    `spec/pagination_spec.lua` — Wave 3 (Plan 03-04) will green these:
    - sanity: `it("M_pagination module table is exposed", function() assert.is_table(M_pagination) end)`
    - `pending("iterate accumulates purchases across two pages until empty (SALE-06)", function() end)`
    - `pending("iterate terminates immediately on empty purchases[] array (RESEARCH §2a Pitfall 1)", function() end)`
    - `pending("iterate terminates when lastPurchaseHash is absent on a non-empty page (defensive)", function() end)`
    - `pending("iterate updates params.lastPurchaseHash between pages (cursor handoff)", function() end)`
    - `pending("iterate aborts on MAX_PAGES guard and logs a warning", function() end)`
    - `pending("iterate routes HTTP error via M_errors.from_http_status", function() end)`
    - `pending("iterate returns error string when page shape is invalid", function() end)`

    `spec/purchases_spec.lua` — Wave 3 (Plan 03-05) will green these:
    - sanity: `it("M_purchases module table is exposed", function() assert.is_table(M_purchases) end)`
    - `pending("fetch GETs https://purchase.izettle.com/purchases/v2 (host allowlist)", function() end)`
    - `pending("fetch includes Authorization: Bearer <token> header (D-42)", function() end)`
    - `pending("fetch includes startDate query param formatted as UTC ISO-8601 (SALE-06 / D-33)", function() end)`
    - `pending("fetch includes limit=200 query param (RESEARCH §1 / A1)", function() end)`
    - `pending("fetch includes descending=false query param (RESEARCH §1)", function() end)`
    - `pending("fetch includes lastPurchaseHash query param when continuing pagination (RESEARCH §2a)", function() end)`
    - `pending("fetch routes error via M_errors.from_http_status (D-43)", function() end)`
    - `pending("fetch_all drives M_pagination.iterate with fetch as the fetch_page_fn", function() end)`

    Each file's top must include:
      - `local Mocks = require("spec.helpers.mm_mocks")`
      - `local Fixtures = require("spec.helpers.fixtures")` (mapping_spec and pagination_spec only — purchases_spec is allowed to skip Fixtures if it only uses inline strings; check 03-PATTERNS.md to confirm)
      - Build-once `do … end` block calling `os.execute("lua tools/build.lua 2>/dev/null")` with spec-specific error message
      - `local function load_artifact() dofile("dist/paypal-pos.lua") end`
      - `describe("...", function() before_each(function() Mocks.setup(); load_artifact() end); after_each(function() Mocks.teardown() end); ...end)`

    Add ONE Fixtures-loader sanity test inside `spec/mapping_spec.lua`:
    `it("Fixtures.load reads purchases/purchase_simple_sale", function() local raw, decoded = Fixtures.load("purchases/purchase_simple_sale"); assert.is_string(raw); assert.is_table(decoded); assert.is_table(decoded.purchases); assert.equals(1, #decoded.purchases) end)`. This empirically verifies the nested-path concat (PATTERNS.md §"Fixture path convention") works on macOS POSIX for the new `purchases/` subdirectory.

    No edits to `spec/helpers/fixtures.lua` are needed (path concat already supports nested directories — proven in Phase 2 for `auth/`). Do NOT add `pcall` wrappers, do NOT touch `src/`, do NOT add MoneyMoney global declarations (mm_mocks already supplies them).
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && ./.luarocks/bin/busted spec/mapping_spec.lua spec/pagination_spec.lua spec/purchases_spec.lua 2>&1 | tee /tmp/wave0_pending.log && ! grep -E 'Failure|Error' /tmp/wave0_pending.log && grep -q 'Pending' /tmp/wave0_pending.log && grep -q 'Fixtures.load reads purchases/purchase_simple_sale' /tmp/wave0_pending.log</automated>
  </verify>
  <acceptance_criteria>
    - `busted spec/mapping_spec.lua spec/pagination_spec.lua spec/purchases_spec.lua` exits with no failures/errors
    - `busted spec/mapping_spec.lua -t "M_mapping module table is exposed"` PASSES
    - `busted spec/pagination_spec.lua -t "M_pagination module table is exposed"` PASSES
    - `busted spec/purchases_spec.lua -t "M_purchases module table is exposed"` PASSES
    - `busted spec/mapping_spec.lua -t "Fixtures.load reads purchases/purchase_simple_sale"` PASSES (empirical proof that nested-path concat works)
    - `busted spec/mapping_spec.lua -t "purchase_to_transaction maps amount to EUR float (SALE-01)"` reports Pending
    - `busted spec/pagination_spec.lua -t "iterate terminates immediately on empty purchases[] array (RESEARCH §2a Pitfall 1)"` reports Pending
    - `busted spec/purchases_spec.lua -t "fetch includes startDate query param formatted as UTC ISO-8601 (SALE-06 / D-33)"` reports Pending
    - `grep -c '^  pending(' spec/mapping_spec.lua` reports at least 16
    - `grep -c '^  pending(' spec/pagination_spec.lua` reports at least 7
    - `grep -c '^  pending(' spec/purchases_spec.lua` reports at least 8
    - All three spec files contain `local Mocks = require("spec.helpers.mm_mocks")` near the top
    - All three spec files contain `dofile("dist/paypal-pos.lua")` inside `before_each`
    - `busted spec/` (full suite — pre-existing Phase-1/2 tests + new pending scaffolds) reports zero failures/errors
  </acceptance_criteria>
  <done>
    Three new spec files (`spec/mapping_spec.lua`, `spec/pagination_spec.lua`, `spec/purchases_spec.lua`) exist with the correct `before_each`/`after_each` plumbing, one sanity test each, one Fixtures.load nested-path verification inside `spec/mapping_spec.lua`, and `pending(...)` entries for every Wave 2 / Wave 3 test row in 03-VALIDATION.md. The full suite `busted spec/` is GREEN with the new pending tests visible as Pending (not Failure). Commit message `test(03-01): add pending mapping/pagination/purchases spec scaffolds`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec test code → mock surface | Spec files load the artifact via `dofile`; mocks substitute for MoneyMoney globals. No external network. |
| fixture JSON files → dkjson decoder | Static, hand-rolled JSON; no attacker-controlled input. Strict-JSON only (no comments). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W0-01 | Information Disclosure | spec/fixtures/purchases/*.json | mitigate | Fixtures contain only synthetic UUIDs (`11111111-...`, etc.), synthetic merchant data, and a German Beispiel-Café organization name; no real merchant API key, no real Zettle production org UUID. CI grep on `.planning/` and `spec/` confirms no real-key paste. (AUTH-05 defense-in-depth carryover from Phase 2.) |
| T-03-W0-02 | Tampering | spec/dst_table_spec.lua boundary fixtures | mitigate | DST boundary timestamps are LOCKED at `2026-06-19T23:55:00Z` (CEST) and `2026-01-31T23:55:00Z` (CET) — chosen so the +02h / +01h offset crosses midnight. Any future implementation that drifts these would fail the gate in Wave 2. |
| T-03-W0-03 | Tampering | spec/fixtures/purchases/*.json structure | mitigate | Each fixture wraps `purchases` in `{...}` with a top-level `_source` citation pointing at the authoritative `iZettle/api-documentation/purchase.adoc`. A future maintainer can re-derive the expected shape by re-reading the source. |
| T-03-W0-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs in this plan — all changes are pure JSON content + Lua source edits. No package-legitimacy gate applies. |
</threat_model>

<verification>
After all three tasks complete, run the full Phase-3 Wave-0 baseline:

```bash
cd /Users/yves/Development/paypal-pos-plugin
lua tools/build.lua --verify                                    # reproducible build still green (no src/ change)
./.luarocks/bin/busted spec/                                    # full suite green, new pending tests visible
ls spec/fixtures/purchases/*.json | wc -l                       # reports 10
grep -c '^  pending(' spec/mapping_spec.lua                     # >= 16
grep -c '^  pending(' spec/pagination_spec.lua                  # >= 7
grep -c '^  pending(' spec/purchases_spec.lua                   # >= 8
grep -c '^  pending(' spec/dst_table_spec.lua                   # >= 5
```

Manual sanity check (Claude reads, no human needed):
- `ls spec/fixtures/purchases/` → ten `.json` files
- `head -5 spec/fixtures/purchases/purchase_simple_sale.json` → starts with `{`, contains `_source` as first key
- `grep -n 'attributes' spec/fixtures/purchases/purchase_with_card_metadata.json` → card metadata under `attributes` sub-object (NOT direct fields)
- `grep -n 'cardBrand\|cardLastFour' spec/fixtures/purchases/` → empty (RESEARCH §1 correction over CONTEXT D-35 wording is honored)
</verification>

<success_criteria>
- Ten valid strict-JSON fixtures exist under `spec/fixtures/purchases/`
- Each fixture has a top-level `_source` key as the FIRST key
- Card-metadata fixture uses `payments[].attributes.cardType` + `maskedPan` (NOT the deprecated D-35 wording `cardBrand` / `cardLastFour`)
- DST summer fixture timestamp is exactly `2026-06-19T23:55:00.000+0000`
- DST winter fixture timestamp is exactly `2026-01-31T23:55:00.000+0000`
- `spec/dst_table_spec.lua` exists with sanity test + at least 5 pending tests (5+ DST boundary cases)
- `spec/mapping_spec.lua` exists with sanity test + at least 16 pending tests + Fixtures.load nested-path empirical proof
- `spec/pagination_spec.lua` exists with sanity test + at least 7 pending tests
- `spec/purchases_spec.lua` exists with sanity test + at least 8 pending tests
- `busted spec/` reports zero failures and zero errors (pending tests are reported as Pending)
- `lua tools/build.lua --verify` reports reproducible build still green (no src/ change in this plan)
- `src/webbanking_header.lua` is NOT modified (M_purchases / M_pagination / M_mapping already pre-declared in Phase 1)
- `tools/manifest.txt` is NOT modified (Wave 2 inlines DST table inside src/mapping.lua, no new module)
</success_criteria>

<artifacts_this_phase_produces>
New files (created by this plan):
- `spec/fixtures/purchases/purchase_simple_sale.json`
- `spec/fixtures/purchases/purchase_with_vat_and_tip.json`
- `spec/fixtures/purchases/purchase_refund.json`
- `spec/fixtures/purchases/purchase_page1.json`
- `spec/fixtures/purchases/purchase_page2.json`
- `spec/fixtures/purchases/purchases_empty.json`
- `spec/fixtures/purchases/purchase_non_eur.json`
- `spec/fixtures/purchases/purchase_dst_boundary_summer.json`
- `spec/fixtures/purchases/purchase_dst_boundary_winter.json`
- `spec/fixtures/purchases/purchase_with_card_metadata.json`
- `spec/dst_table_spec.lua` (RED pending scaffold)
- `spec/mapping_spec.lua` (pending scaffold)
- `spec/pagination_spec.lua` (pending scaffold)
- `spec/purchases_spec.lua` (pending scaffold)

Modified files: none in `src/`, none in `tools/`, none in `spec/helpers/`. This is a pure-scaffold wave.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-01-SUMMARY.md` when done, summarising:
- Fixture file list with key shape per file (gross/vat/tip presence, refund flag, DST timestamp, card metadata path)
- Confirmation that all ten fixtures parse via dkjson and carry `_source` citations
- Pending test count per spec file (target ≥ 16 / ≥ 7 / ≥ 8 / ≥ 5)
- Confirmation that `Fixtures.load("purchases/purchase_simple_sale")` works without changes to `spec/helpers/fixtures.lua`
- Confirmation that `busted spec/` is green with the new pending tests visible as Pending (zero Failure / zero Error)
- Confirmation that `lua tools/build.lua --verify` SHA is unchanged (no src/ touched)
- Any unexpected discoveries
</output>
