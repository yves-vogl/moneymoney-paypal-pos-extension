---
phase: 05-resilience-error-handling
plan: 05
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - spec/refresh_fail_whole_spec.lua
  - spec/refresh_log_redaction_spec.lua
  - spec/phase3_surface_preservation_spec.lua
  - docs/adr/0005-resilience-invariants.md
autonomous: true
requirements: [ERR-02, ERR-03, ERR-04, ERR-05, ERR-06]
tags: [wave-4, fail-whole, sec-03, adr, surface-preservation, tdd]

must_haves:
  truths:
    - "spec/refresh_fail_whole_spec.lua scaffold from Plan 05-02 has its 4 pending() blocks flipped to passing it() blocks. Each case queues a specific failure-mid-pipeline scenario and asserts: (a) RefreshAccount returns a German error string (not a table), (b) Mocks._captured_requests shows the pre-failure steps DID happen, (c) no transactions reach MoneyMoney, (d) the second RefreshAccount call with the SAME `since` re-runs the full pipeline from scratch with no orphans. Cases: 5xx-on-finance, 401-on-finance (composes with ERR-04 from Plan 05-04), network-failure-on-finance, 5xx-on-purchases."
    - "ERR-06 fail-whole-refresh invariant is gated structurally, not behaviorally: Phase-4's 16-step entry.lua RefreshAccount already enforces it via Lua's lexical scoping (in-flight `purchases_by_uuid`, `payments_by_uuid`, `fees_by_date` tables disappear on early return). Plan 05-05 adds NO source change to src/entry.lua — only the gating spec. If the spec FAILS, that's a Phase-4 regression (escalate; do not silently rewrite)."
    - "spec/refresh_log_redaction_spec.lua extended with one new gate (Gate D) covering the D-68 retry log lines from Plan 05-03: queue a 503-503-503 sequence on finance fetch; capture the print stream; assert (1) no `Bearer eyJ` substring appears in ANY captured line (SEC-03 invariant preserved across retry log lines), (2) exactly TWO `HTTP retry: attempt=` lines appear in the captured stream (attempt=2/3 + attempt=3/3 — the first attempt has no retry log; the third attempt's retry log fires before the final fail), (3) the format string is the Plan-05-03 documented format (regex match), (4) the URL field in each retry log contains `finance.izettle.com` but NO bearer fragment."
    - "spec/phase3_surface_preservation_spec.lua extended with `Phase-4 surface preservation` describe block asserting: (a) M_finance.fetch / fetch_all / fetch_account_state / parse_transaction public signatures unchanged from Plan 04-02/04-03; (b) M_mapping.purchase_to_transaction byte-identity for `purchase_simple_sale.json` (no multi-rate VAT, no card metadata) preserved across Phase-5 changes; (c) the entry.lua 14-step sequence count unchanged (count via grep of step-number comments); (d) the new 401-direct-check from Plan 05-04 in purchases.lua + finance.lua does NOT affect any non-401 code path (test queues 200 then asserts behavior matches Phase-4 baseline byte-identically)."
    - "docs/adr/0005-resilience-invariants.md transitions Status: Proposed → Status: ACCEPTED with the live Plan-05-02..05-04 results pinned. Sections added/updated: (1) Implementation pin — i18n keys actually added (`error.server_busy`, `error.token_revoked`); M_http retry constants actually used (1s/2s/4s/60s cap/30s default); 401-direct-check call sites (4 total). (2) Carve-out 3 (NEW): SSL handshake failures bypass ERR-05 pcall per ADR-0003 Q8 inheritance — documented as out-of-scope for v1.0.0; TLS 1.2+ minimum + OS-level certificate pinning are the mitigations. (3) D-64 collapse rationale (silent re-mint infeasible under assertion-grant + SEC-03; Phase-7 OAuth Authorization-Code flow reintroduces refresh_token-based silent refresh). (4) Fail-whole-refresh invariant — explicit prose from RESEARCH §5 plus the gating spec citation."
    - "Coverage on the new fail-whole spec branches >= 95%; coverage on the new SEC-03 Gate D >= 95%; overall coverage maintains Phase-4's 100% on dist/paypal-pos.lua (Phase 5 may dip to 98-99% if defensive branches in M_http retry become dead under test fixtures — document any dip in SUMMARY)."
    - "luacheck clean on the 4 modified spec files + docs/adr/0005 (luacheck ignores ADR markdown but lint checker should not flag the .md)."
    - "Reproducible build SHA unchanged from Plan 05-04 (Plan 05-05 is spec + ADR only — no src/ delta). If the SHA DOES change, that's a sign Plan 05-05 accidentally touched a source file — investigate before committing."
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution; Conventional Commits: `test(05-05):` for spec extensions; `docs(05-05):` for ADR-0005 finalization + SUMMARY."
  artifacts:
    - path: "spec/refresh_fail_whole_spec.lua"
      provides: "ERR-06 gating spec — 4 cases (5xx-on-finance / 401-on-finance / network-on-finance / 5xx-on-purchases); each asserts error-string-return + no-transactions + second-call-re-runs"
      contains: "ERR-06"
    - path: "spec/refresh_log_redaction_spec.lua"
      provides: "Gate D (NEW) — SEC-03 covers D-68 retry log lines; queue 503-503-503 + assert no Bearer + exactly 2 retry log lines + URL contains host but no bearer"
      contains: "Gate D"
    - path: "spec/phase3_surface_preservation_spec.lua"
      provides: "Phase-4 surface preservation describe block — M_finance signatures + M_mapping byte-identity + entry.lua step-count + Plan 05-04 401-check non-interference"
      contains: "Phase-4 surface preservation"
    - path: "docs/adr/0005-resilience-invariants.md"
      provides: "Status: ACCEPTED with pinned implementation details + Carve-out 3 (SSL bypass) + D-64 collapse rationale + fail-whole invariant prose"
      contains: "Status: ACCEPTED"
  key_links:
    - from: "Plan 05-05 spec/refresh_fail_whole_spec.lua"
      to: "Phase-4 entry.lua 16-step structural invariant"
      via: "Lua lexical scoping — in-flight tables discarded on early return"
      pattern: "ERR-06"
    - from: "Plan 05-05 spec/refresh_log_redaction_spec.lua Gate D"
      to: "Plan 05-03 D-68 INFO log lines"
      via: "M_log.redact applied per existing Phase-1 invariant; Gate D verifies invariant holds for new log lines too"
      pattern: "Bearer eyJ"
---

<objective>
Close Phase 5 with three gating specs + ADR-0005 finalization:

1. ERR-06 fail-whole-refresh gating spec — fills the Plan-05-02 scaffold with 4 failure-mid-pipeline cases that prove the structural invariant from Phase-4's 16-step entry.lua holds under every Phase-5 error path.

2. SEC-03 Gate D — extends the existing log-redaction spec to cover the D-68 retry INFO log lines from Plan 05-03, proving Bearer-absence holds across retry-storm scenarios.

3. Phase-3/4 surface preservation re-audit — extends the existing snapshot spec (created in Plan 04-06) with Phase-4-surface assertions, proving Plan 05-04's 401-direct-check additions do not affect any non-401 code path.

4. ADR-0005 finalization — Status: Proposed → ACCEPTED with the live Plan-05-02..05-04 implementation pinned, plus the SSL handshake bypass Carve-out, the D-64 collapse rationale, and the fail-whole invariant prose.

This plan is spec + ADR only. NO source code changes. The reproducible build SHA must be identical to Plan 05-04's recorded SHA; if it changes, Plan 05-05 has accidentally touched a source file.

</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-resilience-error-handling/05-CONTEXT.md
@.planning/phases/05-resilience-error-handling/05-RESEARCH.md §5 (fail-whole-refresh prose) + §Pattern-2 (D-64 collapse rationale) + §Carve-out 3 (SSL bypass)
@.planning/phases/05-resilience-error-handling/05-PATTERNS.md
@.planning/phases/05-resilience-error-handling/05-02-PLAN.md (Plan 05-02 created spec/refresh_fail_whole_spec.lua scaffold with 4 pending blocks)
@.planning/phases/05-resilience-error-handling/05-03-PLAN.md (Plan 05-03 D-68 retry INFO log format string — your Gate D regex matches it)
@.planning/phases/05-resilience-error-handling/05-04-PLAN.md (Plan 05-04 ERR-04 401-direct-check — your Phase-4 surface spec asserts non-interference)
@.planning/phases/05-resilience-error-handling/05-01-PLAN.md (Plan 05-01 created docs/adr/0005-resilience-invariants.md stub with Status: Proposed)
@CLAUDE.md
@spec/refresh_fail_whole_spec.lua (Plan 05-02 scaffold — fill the 4 pending blocks)
@spec/refresh_log_redaction_spec.lua (Phase-3 + Plan-04-05 extensions; add Gate D here)
@spec/phase3_surface_preservation_spec.lua (Plan 04-06 created; extend with Phase-4 surface)
@docs/adr/0005-resilience-invariants.md (Plan 05-01 stub; transition to ACCEPTED)
@spec/helpers/mm_mocks.lua (Mocks.push_response queue + captured_requests + captured_prints)
</context>

<tasks>

<task type="test:green" gate="fill-scaffold-from-prior-wave">
  <name>Task 1: fill spec/refresh_fail_whole_spec.lua with 4 ERR-06 cases</name>
  <read_first>
    - spec/refresh_fail_whole_spec.lua (Plan 05-02 scaffold — preserve the describe header + pending block structure)
    - spec/refresh_idempotency_spec.lua (the analogous spec from Phase 3+4 — same Mocks.push_response sequencing pattern)
    - .planning/phases/05-resilience-error-handling/05-02-PLAN.md (Plan 05-02 must_haves listing the 4 pending case names — your it() titles match exactly)
    - src/entry.lua (Phase-4's 16-step RefreshAccount — confirm the pipeline-step ordering for the failure-injection points)
    - spec/fixtures/finance/*.json (failure fixtures — use Plan-04-02 fixtures plus inline failure responses)
  </read_first>
  <action>
    Flip the 4 pending() blocks in spec/refresh_fail_whole_spec.lua to passing it() blocks:

    Case 1 — `it("ERR-06: 5xx on finance after purchase success returns error string and zero transactions", ...)`:
      - queue 200 OK purchase response (`Fixtures.load("purchases/purchase_simple_sale")`)
      - queue 503 + empty body × 3 (consumed by Plan 05-03's retry-with-backoff)
      - call RefreshAccount, assert return value is STRING containing the German `error.server_busy`
      - assert `Mocks._captured_requests` count >= 4 (1 purchase + 3 finance retries)
      - call RefreshAccount with same `since` again with 200 mint + 200 purchase + 200 finance — assert all transactions appear

    Case 2 — `it("ERR-06: 401 on finance (post-mint token revoked) returns error.token_revoked", ...)`:
      - queue 200 purchase, then 401 on first finance call
      - assert return value is STRING containing German `error.token_revoked` (composes with ERR-04 from Plan 05-04)
      - assert no second finance call happens (fail-whole stops the pipeline immediately)
      - second-call retry with fresh bearer succeeds

    Case 3 — `it("ERR-06: network failure on finance returns error.network", ...)`:
      - queue 200 purchase, then `Mocks.push_response({ content = "" })` to trigger the existing nil-status branch in M_http._infer_status
      - assert return value is STRING containing German `error.network`
      - assert second-call with normal responses succeeds

    Case 4 — `it("ERR-06: 5xx on purchases (first pipeline step) returns error string with no finance calls", ...)`:
      - queue 503 × 3 on first purchase call (Plan 05-03 retry exhausts)
      - assert return value is STRING containing `error.server_busy`
      - assert `Mocks._captured_requests` shows ONLY purchases calls (no finance.izettle.com URLs) — proves the fail-whole abort stopped before finance

    Commit: `test(05-05): fill ERR-06 fail-whole gating spec — 4 cases proving structural invariant`
  </action>
  <acceptance_criteria>
    - All 4 cases PASS (busted shows + 4 successes vs Plan 05-04 baseline)
    - `grep -c 'ERR-06' spec/refresh_fail_whole_spec.lua` >= 4
    - `grep -c 'pending(' spec/refresh_fail_whole_spec.lua` == 0 (all pending blocks were flipped to it blocks)
    - Commit GPG-signed
  </acceptance_criteria>
</task>

<task type="test:green" gate="sec-03-extension">
  <name>Task 2: extend spec/refresh_log_redaction_spec.lua with Gate D for retry log lines</name>
  <read_first>
    - spec/refresh_log_redaction_spec.lua (Phase-3 + Plan-04-05 — preserve existing gates A, B, C byte-identically; add D at the end)
    - .planning/phases/05-resilience-error-handling/05-03-PLAN.md (D-68 retry INFO log format string — your regex matches)
    - src/log.lua (M_log.redact behavior — extend gating coverage)
  </read_first>
  <action>
    Add a new `describe("Gate D: SEC-03 retry log Bearer redaction (D-68)", ...)` block after the existing Gate C in spec/refresh_log_redaction_spec.lua:

    `it("Gate D: 503-storm retry log lines contain no Bearer fragment", ...)`:
      - queue 200 mint + 200 purchase + 503 × 3 on first finance call
      - capture print stream via `Mocks._captured_prints` (or whatever the Phase-3+4 spec uses)
      - call RefreshAccount, drain prints
      - assert FOR EVERY captured line: no `Bearer eyJ` substring (SEC-03 invariant)
      - assert exactly 2 lines match the `HTTP retry: attempt=` prefix (attempt=2/3 + attempt=3/3 — first call has no retry log; the third call's retry log fires before the loop break)
      - assert each retry log line contains `finance.izettle.com` (the URL field is populated) but NO `eyJ` fragment in any position

    Commit: `test(05-05): SEC-03 Gate D — retry log line Bearer redaction (D-68)`
  </action>
  <acceptance_criteria>
    - busted shows +1 success
    - `grep -c 'Gate D' spec/refresh_log_redaction_spec.lua` >= 1
    - The 3 pre-existing gates A, B, C continue to pass
    - Commit GPG-signed
  </acceptance_criteria>
</task>

<task type="test:green" gate="phase-4-surface-audit">
  <name>Task 3: extend spec/phase3_surface_preservation_spec.lua with Phase-4 surface block</name>
  <read_first>
    - spec/phase3_surface_preservation_spec.lua (Plan 04-06 created; preserve existing assertions)
    - src/finance.lua, src/mapping.lua, src/entry.lua (the surfaces being asserted)
    - .planning/phases/05-resilience-error-handling/05-04-PLAN.md (Plan 05-04's 401-direct-check additions — the non-interference assertion proves they don't regress non-401 paths)
  </read_first>
  <action>
    Add a new `describe("Phase-4 surface preservation", ...)` block to spec/phase3_surface_preservation_spec.lua:

    1. `it("M_finance public surface unchanged from Plan 04-03", ...)`: assert `type(M_finance.fetch) == "function"`, `type(M_finance.fetch_all) == "function"`, `type(M_finance.fetch_account_state) == "function"`, `type(M_finance.parse_transaction) == "function"`.

    2. `it("M_mapping.purchase_to_transaction byte-identity preserved for purchase_simple_sale.json", ...)`: load the fixture, call purchase_to_transaction directly (NOT through RefreshAccount), assert the resulting transaction table is byte-identical to a Phase-4 snapshot (replicate the Plan 04-04 snapshot assertion).

    3. `it("entry.lua RefreshAccount step-count unchanged from Phase 4", ...)`: read src/entry.lua, count occurrences of the `-- Step N` comment pattern (or whatever step-marker Phase-4 uses), assert count == 16 (Plan 04-03's 14-step sequence + 2 internal substeps; verify against the actual file).

    4. `it("Plan 05-04 401-direct-check does NOT affect non-401 paths in purchases.lua / finance.lua", ...)`: queue 200 mint + 200 purchase + 200 finance (all happy), call RefreshAccount, assert the returned table has the expected balance + pendingBalance + transactions count (matches a Phase-4 baseline assertion).

    Commit: `test(05-05): extend surface preservation spec with Phase-4 assertions + 401-non-interference`
  </action>
  <acceptance_criteria>
    - busted shows +4 successes
    - `grep -c 'Phase-4 surface preservation' spec/phase3_surface_preservation_spec.lua` >= 1
    - Existing Phase-3 surface assertions continue to pass
    - Commit GPG-signed
  </acceptance_criteria>
</task>

<task type="docs:adr">
  <name>Task 4: finalize ADR-0005 Status: Proposed → ACCEPTED with pinned implementation</name>
  <read_first>
    - docs/adr/0005-resilience-invariants.md (Plan 05-01 stub — preserve structure, transition Status)
    - .planning/phases/05-resilience-error-handling/05-02-SUMMARY.md (Plan 05-02 i18n + M_errors deltas to pin)
    - .planning/phases/05-resilience-error-handling/05-03-SUMMARY.md (Plan 05-03 M_http retry semantics to pin)
    - .planning/phases/05-resilience-error-handling/05-04-SUMMARY.md (Plan 05-04 ERR-04 401-direct-check call sites to pin)
    - .planning/phases/05-resilience-error-handling/05-RESEARCH.md §Carve-out 3 (SSL handshake bypass) + §Pattern-2 (D-64 collapse)
  </read_first>
  <action>
    Edit docs/adr/0005-resilience-invariants.md:

    1. Change frontmatter Status from `Proposed` to `Accepted`. Add Date: 2026-06-22.

    2. Update Decision section with PINNED implementation details:
       - i18n keys actually added (cite from Plan 05-02 SUMMARY): `error.server_busy` German value, `error.token_revoked` German value
       - M_http retry constants used (cite from Plan 05-03 SUMMARY): `_MAX_ATTEMPTS = 3`, `_BACKOFF_SECONDS = {1, 2, 4}`, `_RETRY_AFTER_CAP = 60`, `_RATE_LIMIT_DEFAULT = 30`
       - 401-direct-check call sites (cite from Plan 05-04 SUMMARY): 4 call sites total (purchases.lua × 1 + finance.lua × 3)

    3. Add Carve-out 3: "SSL handshake failures bypass ERR-05" — per ADR-0003 Q8 finding: MoneyMoney's Connection() raises SSL/TLS handshake errors above the pcall surface; the extension cannot wrap them. Mitigations: TLS 1.2+ minimum (enforced by Connection() default per Phase-1 Q8), OS-level certificate pinning (macOS keychain). Documented as out-of-scope for v1.0.0 fail-whole-refresh contract; the extension surfaces a generic MoneyMoney connection error in this path, which is structurally acceptable because (a) it's distinguishable from API-level errors by the absence of a German error string, and (b) the user's next refresh re-attempts with the same `since`.

    4. Add D-64 collapse rationale section: silent re-mint via M_auth.exchange_assertion requires persisting the api_key in memory across InitializeSession2 → RefreshAccount, which violates AUTH-05 + SEC-03 (api_key must never appear outside the credentials dialog). v1.0.0 surfaces post-mint 401 immediately as `error.token_revoked`; user re-enters API key via MoneyMoney's account dialog. Phase 7 (POST-v1, ROADMAP.md) introduces OAuth Authorization-Code flow with refresh_token-based silent refresh.

    5. Add fail-whole-refresh invariant prose (from RESEARCH §5): Phase-4's 16-step entry.lua RefreshAccount enforces the invariant structurally via Lua's lexical scoping. Plan 05-05's gating spec (`spec/refresh_fail_whole_spec.lua`) is the regression gate.

    Commit: `docs(05-05): finalize ADR-0005 Status ACCEPTED with pinned implementation`
  </action>
  <acceptance_criteria>
    - `grep -c '^Status: Accepted$\|Status:.*Accepted' docs/adr/0005-resilience-invariants.md` >= 1
    - `grep -c 'Carve-out 3' docs/adr/0005-resilience-invariants.md` >= 1
    - `grep -c 'D-64 collapse\|D-64' docs/adr/0005-resilience-invariants.md` >= 1
    - Commit GPG-signed
  </acceptance_criteria>
</task>

<task type="docs:summary">
  <name>Task 5: write 05-05-SUMMARY.md + update STATE.md to READY-FOR-VERIFIER</name>
  <read_first>
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-06-SUMMARY.md (gold-standard Wave-N SUMMARY shape — frontmatter + decisions + metrics + hand-off)
    - .planning/STATE.md (Current Position section to update)
  </read_first>
  <action>
    Write `.planning/phases/05-resilience-error-handling/05-05-SUMMARY.md`:
    - Frontmatter: phase, plan: "05", subsystem: resilience + sec-03 + surface-audit + adr, tags, dependency_graph requires=[05-02, 05-03, 05-04], tech_stack added=[], key_files (4 modified files), decisions (the Phase-5 close summary), metrics
    - Body: ERR-06 4-case results, Gate D pass, Phase-4 surface preservation pass, ADR-0005 ACCEPTED
    - Hand-off: Phase 5 implementation COMPLETE — READY-FOR-VERIFIER

    Update `.planning/STATE.md` Current Position section:
    - Phase 5 status: PLANNED ✅ → IMPLEMENTATION COMPLETE; READY-FOR-VERIFIER
    - Add Plan-05 commits list (will be populated by SUMMARY)
    - Phase 6 + Phase 6.1 still BLOCKED on Phase-5 ship

    Commit: `docs(05-05): Plan 05-05 summary + STATE updated to READY-FOR-VERIFIER`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/05-resilience-error-handling/05-05-SUMMARY.md` exists
    - STATE.md Current Position reflects Phase 5 IMPLEMENTATION COMPLETE
    - Commit GPG-signed
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
New / modified files:
- `spec/refresh_fail_whole_spec.lua` — 4 pending blocks flipped to passing it blocks (4 ERR-06 cases)
- `spec/refresh_log_redaction_spec.lua` — Gate D added (SEC-03 retry log Bearer redaction)
- `spec/phase3_surface_preservation_spec.lua` — Phase-4 surface preservation describe block added (4 it cases)
- `docs/adr/0005-resilience-invariants.md` — Status: Proposed → Accepted; pinned implementation; Carve-out 3 (SSL bypass); D-64 collapse rationale; fail-whole prose
- `.planning/phases/05-resilience-error-handling/05-05-SUMMARY.md` — NEW
- `.planning/STATE.md` — Current Position updated to Phase 5 IMPLEMENTATION COMPLETE

New public symbols: NONE. Plan 05-05 is spec + ADR only.

Yves-blockers SURFACED by this plan: NONE.

Files NOT modified: any src/*.lua (Plan 05-05 is spec + ADR only; if the build SHA changes, that's a bug).

</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-resilience-error-handling/05-05-SUMMARY.md` summarising:
- The 4 ERR-06 cases that gate the fail-whole structural invariant
- The SEC-03 Gate D extension covering D-68 retry log Bearer redaction
- The Phase-4 surface preservation audit results (4 it cases)
- ADR-0005 finalization (Status: ACCEPTED + Carve-out 3 + D-64 rationale + fail-whole prose)
- Final test count, build SHA (must equal Plan 05-04's SHA — Plan 05-05 is spec + ADR only), coverage
- Hand-off: Phase 5 READY-FOR-VERIFIER. Recommended next steps: gsd-verifier run + parallel gsd-code-reviewer + loop-security-engineer + fix-batch if needed + PR + squash merge per feedback_gpg_signed_pr_merge memory.
</output>
