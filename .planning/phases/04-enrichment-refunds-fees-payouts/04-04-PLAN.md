---
phase: 04-enrichment-refunds-fees-payouts
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/mapping.lua
  - spec/mapping_spec.lua
  - spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json
  - spec/fixtures/purchases/purchase_umlauts_purpose.json
autonomous: true
requirements: [META-01, SALE-07, TEST-02]
tags: [wave-3, pure-logic, mapping, vat, card-tail, i18n, mvp]

must_haves:
  truths:
    - "src/mapping.lua _format_purpose is extended ADDITIVELY: it now emits per-rate German VAT lines when groupedVatAmounts has 2+ rates (D-53 / META-01); falls through to Phase-3's single MwSt line when 0 or 1 rate; produces output BYTE-IDENTICAL to Phase 3 for fixtures with no multi-rate VAT and no card metadata"
    - "Per-rate VAT lines are sorted DESCENDING by numeric rate (RESEARCH §5.2); format is `<rate>% MwSt: <amount_de> EUR` per line — e.g. `19% MwSt: 3,83 EUR` then `7% MwSt: 1,40 EUR`; whole-number rates drop the `.0` (e.g., `19% MwSt`, NOT `19.0% MwSt`); fractional rates are preserved (`7.5% MwSt`)"
    - "Per-rate VAT lines READ groupedVatAmounts decimal-string keys (`19.0`, `7.0`, `0.0` per RESEARCH §5.1; tonumber accepts both `19.0` and `19` integer-string forms defensively so the regenerated and legacy fixture forms both work)"
    - "src/mapping.lua _format_purpose is extended with a card-brand+entry-mode tail line: when payments[1].attributes.cardType AND payments[1].attributes.cardPaymentEntryMode are both present, emit `Zahlart: <brand_de> (<entry_mode_de>)` (e.g., `Zahlart: Visa (kontaktlos)`); when only cardType present, emit `Zahlart: <brand_de>` (no parens); when only cardPaymentEntryMode present, emit `Zahlart: Kartenzahlung (<entry_mode_de>)`; when both absent, the line is OMITTED (no `Zahlart: unbekannt (unbekannt)` noise per RESEARCH §6.3)"
    - "Card-brand display uses Phase-3 BRAND_MAP from src/mapping.lua (VISA→Visa, MASTERCARD→Mastercard, AMEX→Amex, MAESTRO→Maestro, GIROCARD→girocard, UNIONPAY→UnionPay); unknown brands fall through to capitalized literal per Phase-3 convention"
    - "Entry-mode display uses new i18n keys from Plan 04-02 (account.purpose.payment_method.kontaktlos / .chip / .swipe / .ecommerce / .manual / .unknown); API → i18n mapping per RESEARCH §6.2: CONTACTLESS_EMV → kontaktlos, ICC → Chip, MSR → Magnetstreifen, ECOMMERCE → Online, MANUAL → Manuell, any other → unbekannt (the _unknown fallback per RESEARCH §6.2 last row)"
    - "Card-brand+entry-mode tail line is inserted in _format_purpose AFTER the Netto line and BEFORE the `Beleg #<purchaseNumber>` receipt line — per Claude's-Discretion recommendation in CONTEXT (separate line, easier to grep visually) confirmed by RESEARCH §6.3"
    - "Phase-3 surface preservation: for fixtures WITHOUT multi-rate VAT AND WITHOUT card metadata (e.g., purchase_simple_sale.json), _format_purpose output is BYTE-IDENTICAL to Phase 3 (verified by snapshot assertion in spec/mapping_spec.lua per RESEARCH §Pitfall 8)"
    - "spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json exists: single purchase with payments[1].attributes.cardType=\"VISA\" + cardPaymentEntryMode=\"CONTACTLESS_EMV\" + maskedPan, and the _format_purpose output is `Brutto: ... EUR\\nMwSt: ... EUR\\nNetto: ... EUR\\nZahlart: Visa (kontaktlos)\\nBeleg #...`"
    - "spec/fixtures/purchases/purchase_umlauts_purpose.json exists: single purchase whose merchant name OR receipt fields contain umlauts (e.g., 'Beispiel-Café') and whose _format_purpose output preserves UTF-8 round-trip (no Mojibake; assertable via `txn.purpose:find('Café', 1, true)` returning non-nil)"
    - "spec/mapping_spec.lua extended with passing tests: (1) META-01 multi-rate VAT split with both rates sorted descending and decimal-string keys; (2) META-01 fallback to single MwSt line when 0 or 1 rate in groupedVatAmounts; (3) META-01 integer-string key `19` is accepted equivalently to `19.0` (defensive against the Phase-3 fixture-bug R-5); (4) META-01 negative VAT amount on refund record renders with leading `-` (e.g., `19% MwSt: -0,57 EUR`); (5) SALE-07 card tail with both fields produces `Zahlart: Visa (kontaktlos)`; (6) SALE-07 card tail with only cardType produces `Zahlart: Visa` (no parens); (7) SALE-07 card tail OMITTED when both fields absent; (8) SALE-07 entry-mode unknown value (`SOMETHING_NEW`) maps to `unbekannt`; (9) Phase-3 surface preservation snapshot — purchase_simple_sale.json produces byte-identical purpose to Phase 3"
    - "lua tools/build.lua --verify reports OK: reproducible across two consecutive builds; luacheck clean on src/mapping.lua + spec/mapping_spec.lua; commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642 with Conventional Commits prefix `feat(04-04):` and no AI attribution"
  artifacts:
    - path: "src/mapping.lua"
      provides: "_format_purpose extended with per-rate VAT block (D-53 / META-01) and card-brand+entry-mode tail block (D-57 / SALE-07); existing single-VAT and Beleg lines preserved"
      contains: "groupedVatAmounts"
    - path: "spec/mapping_spec.lua"
      provides: "Extended with 9 new tests covering META-01 sort/fallback/integer-key-defense/negative; SALE-07 both-fields/partial/omit/unknown; Phase-3 surface snapshot"
      contains: "Zahlart"
    - path: "spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json"
      provides: "NEW fixture: card metadata with CONTACTLESS_EMV entry mode"
      contains: "CONTACTLESS_EMV"
    - path: "spec/fixtures/purchases/purchase_umlauts_purpose.json"
      provides: "NEW fixture: UTF-8 umlauts (Café merchant name) for round-trip preservation test"
      contains: "Caf"
  key_links:
    - from: "src/mapping.lua _format_purpose VAT block"
      to: "purchase.groupedVatAmounts map iteration + sort descending + drop-`.0`"
      via: "pairs() walk into {rate, amount, label} entries; table.sort by rate DESC; _format_amount for amount formatting"
      pattern: "groupedVatAmounts"
    - from: "src/mapping.lua _format_purpose card-tail block"
      to: "payments[1].attributes.cardType + cardPaymentEntryMode + ENTRY_MODE_MAP local table"
      via: "guarded chained access; BRAND_MAP lookup for cardType; ENTRY_MODE_MAP lookup for entry-mode → i18n key suffix"
      pattern: "cardPaymentEntryMode"
    - from: "src/mapping.lua ENTRY_MODE_MAP"
      to: "account.purpose.payment_method.* i18n keys (Plan 04-02 additions)"
      via: "table lookup keyed by uppercase API value → i18n key suffix (`kontaktlos`/`chip`/`swipe`/`ecommerce`/`manual`/`unknown`)"
      pattern: "CONTACTLESS_EMV"
---

<objective>
Wave 3 extends src/mapping.lua `_format_purpose` with two ADDITIVE features that round out the bookkeeping picture: per-rate German VAT lines (D-53 / META-01) and the card-brand + entry-mode tail line (D-57 / SALE-07). Both extensions are pure-logic edits to the existing formatter — no new public API surface — so the wave runs in parallel with Plan 04-03 (only dependency is Plan 04-02's i18n key additions and the regenerated purchase_with_vat_and_tip.json fixture).

Purpose: META-01 is the "shows merchant the right VAT picture when they ring up mixed-rate baskets" feature; SALE-07 is the "shows merchant the card brand + how the customer paid" feature. Both extend `_format_purpose` rather than adding new mappers because the data lives on existing purchase records — Phase 3 already maps purchases to transactions, this plan just enriches the `purpose` field.

The acceptance bar for this wave is Phase-3 surface preservation per RESEARCH §Pitfall 8: for any purchase fixture that has NO multi-rate VAT (single rate or empty `groupedVatAmounts`) AND NO card metadata, the `_format_purpose` output must be BYTE-IDENTICAL to Phase 3's. The snapshot assertion in spec/mapping_spec.lua is the gate against silent regression.

Output: src/mapping.lua with `_format_purpose` extended additively (per-rate VAT block + card-tail block); 2 new fixtures (CONTACTLESS_EMV + umlauts); 9 new tests in spec/mapping_spec.lua all GREEN; Phase-3 surface preserved byte-identically; reproducible build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-PATTERNS.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-02-PLAN.md
@src/mapping.lua
@src/i18n.lua
@spec/mapping_spec.lua
@spec/fixtures/purchases/purchase_simple_sale.json
@spec/fixtures/purchases/purchase_with_vat_and_tip.json
@spec/fixtures/purchases/purchase_with_card_metadata.json
@spec/fixtures/purchases/purchase_vat_split_19_7.json
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 2 new fixtures (CONTACTLESS_EMV + umlauts) and add RED scaffolds for the META-01 + SALE-07 + Phase-3-snapshot tests</name>
  <read_first>
    - spec/fixtures/purchases/purchase_with_card_metadata.json (existing card-metadata shape — payments[1].attributes.cardType + maskedPan + cardPaymentEntryMode pattern to mirror for the new kontaktlos fixture; the existing fixture uses cardPaymentEntryMode=ICC per Plan-04-02 RESEARCH read)
    - spec/fixtures/purchases/purchase_simple_sale.json (the empty-payments fixture used for the Phase-3 snapshot assertion in Task 2)
    - spec/fixtures/purchases/purchase_with_vat_and_tip.json (the regenerated fixture from Plan 04-02 with `"19.0"` decimal-string VAT key; serves as the single-rate fallback test)
    - spec/fixtures/purchases/purchase_vat_split_19_7.json (the multi-rate fixture from Plan 04-02 with both `"19.0": 318` and `"7.0": 140`; serves as the META-01 happy-path test)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §5.1-§5.4 (groupedVatAmounts shape, render rule with `.0`-drop, edge cases including refund-negative-amount and integer-string key defensive accept), §6 (card tail), §Pitfall 8 (Phase-3 snapshot preservation)
    - spec/mapping_spec.lua (existing Phase-3 tests; the snapshot assertion is ADDED here, not modified from Phase 3)
  </read_first>
  <files>
    spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json,
    spec/fixtures/purchases/purchase_umlauts_purpose.json,
    spec/mapping_spec.lua
  </files>
  <action>
    Step 1 — Create fixture spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json. Mirror the shape of spec/fixtures/purchases/purchase_with_card_metadata.json but set `payments[1].attributes.cardType = "VISA"` and `payments[1].attributes.cardPaymentEntryMode = "CONTACTLESS_EMV"` and `payments[1].attributes.maskedPan = "411111******1111"`. Use deterministic UUID `40404040-4040-4040-4040-404040404040`, purchaseNumber 5001, amount 995, currency "EUR", `vatAmount: 159` (single rate), timestamp `"2026-06-15T11:00:00.000+0000"`, empty groupedVatAmounts `{}`. The fixture wraps as `{"_source": "spec/fixtures/purchases — synthetic; CONTACTLESS_EMV entry mode per RESEARCH §6", "purchases": [<this>], "lastPurchaseHash": ""}`. Validate with `dkjson` round-trip.

    Step 2 — Create fixture spec/fixtures/purchases/purchase_umlauts_purpose.json. Single purchase with `purchaseNumber: 6001`, `purchaseUUID1: "50505050-5050-5050-5050-505050505050"`, `amount: 1295`, `currency: "EUR"`, `vatAmount: 207`, `timestamp: "2026-06-15T12:00:00.000+0000"`, no card metadata, empty `payments: []`, AND an explicit `userDisplayName: "Beispiel-Café"` field at root (Zettle's spec documents `userDisplayName` as the merchant name). Even though _format_purpose doesn't read userDisplayName directly (Phase 3 doesn't surface it), the fixture's purpose is to gate UTF-8 round-trip THROUGH dkjson — load the fixture, decode it, re-encode, assert "Café" survives. The fixture's actual `_format_purpose` output goes through standard Brutto/MwSt/Netto/Beleg lines (no umlauts in those German words alone, but the round-trip-through-dkjson assertion is the test target). The fixture wraps as `{"_source": "...", "purchases": [<this>], "lastPurchaseHash": ""}`.

    Step 3 — Add RED scaffolds to spec/mapping_spec.lua (RED until Task 2 lands the impl):

    Inside the existing top-level describe block (the file already has 18+ Phase-3 it() blocks per Plan 03-03 + Plan 04-02 extensions). Append a new sub-describe (or just append new it() blocks at the end with descriptive names) for the Phase-4 enrichments. Use `it("...", function() ... end)` not `pending(...)` — the assertions reference behavior that doesn't exist yet, so they FAIL RED until Task 2:

    1. `it("META-01: groupedVatAmounts with two rates produces two MwSt lines sorted descending", function() ... end)` — load purchase_vat_split_19_7; call M_mapping.purchase_to_transaction(p); assert.truthy(txn.purpose:find("19%% MwSt: 3,18 EUR", 1, true)); assert.truthy(txn.purpose:find("7%% MwSt: 1,40 EUR", 1, true)); also assert the 19% line appears BEFORE the 7% line in the string (`txn.purpose:find("19%% MwSt", 1, true) < txn.purpose:find("7%% MwSt", 1, true)`). NOTE: the literal `%` in `find` must be escaped as `%%` only if using Lua pattern matching; with the plain-text flag (4th arg = true), use literal `%`. The assertion strings use literal `%` here for clarity — adjust if needed.

    2. `it("META-01: groupedVatAmounts with single rate falls through to Phase-3 single MwSt line", function() ... end)` — load purchase_with_vat_and_tip (single rate "19.0": 318); call mapping; assert exactly ONE MwSt line in txn.purpose (count via `select(2, txn.purpose:gsub("MwSt:", "")) == 1`); assert.truthy(txn.purpose:find("MwSt: 3,18 EUR", 1, true)); assert NO `19%` substring (the per-rate-prefix format is suppressed for single-rate path).

    3. `it("META-01: groupedVatAmounts with empty map falls through to Phase-3 single MwSt line", function() ... end)` — load purchase_with_card_metadata (empty groupedVatAmounts, single vatAmount); assert similar to test 2.

    4. `it("META-01: integer-string key '19' is accepted equivalently to decimal-string '19.0' (defensive — R-5)", function() ... end)` — inline a purchase table with `groupedVatAmounts = {["19"] = 318, ["7"] = 140}` and assert the same multi-rate output as test 1 (modulo amounts).

    5. `it("META-01: negative VAT amounts on refund records render with leading minus", function() ... end)` — inline a purchase record with refund=true and `groupedVatAmounts = {["19.0"] = -57, ["7.0"] = -30}`; call refund_to_transaction(p); assert.truthy(txn.purpose:find("19% MwSt: -0,57 EUR", 1, true)); assert.truthy(txn.purpose:find("7% MwSt: -0,30 EUR", 1, true)).

    6. `it("SALE-07: both cardType and cardPaymentEntryMode present produces 'Zahlart: Visa (kontaktlos)' line", function() ... end)` — load purchase_with_card_metadata_kontaktlos; call mapping; assert.truthy(txn.purpose:find("Zahlart: Visa %(kontaktlos%)") OR with plain-flag `txn.purpose:find("Zahlart: Visa (kontaktlos)", 1, true)`).

    7. `it("SALE-07: only cardType present produces 'Zahlart: Visa' (no parens)", function() ... end)` — inline a purchase with payments[1].attributes.cardType="VISA" and NO cardPaymentEntryMode; assert txn.purpose:find("Zahlart: Visa", 1, true) and NOT txn.purpose:find("(", txn.purpose:find("Zahlart"), true).

    8. `it("SALE-07: both fields absent — Zahlart line is OMITTED", function() ... end)` — load purchase_simple_sale (empty payments); call mapping; assert.is_nil(txn.purpose:find("Zahlart", 1, true)).

    9. `it("SALE-07: unknown cardPaymentEntryMode maps to 'unbekannt' fallback", function() ... end)` — inline a purchase with cardType="VISA" and cardPaymentEntryMode="SOMETHING_NEW_NOT_IN_MAP"; assert txn.purpose:find("Zahlart: Visa (unbekannt)", 1, true).

    10. `it("Phase-3 surface preservation: purchase_simple_sale produces byte-identical purpose to Phase 3 — RESEARCH §Pitfall 8", function() ... end)` — load purchase_simple_sale; call mapping; assert.equals the txn.purpose to a literal expected string captured here as the Phase-3 golden output. The expected string for purchase_simple_sale (per Phase-3 implementation, currency=EUR, amount=500, vatAmount=0, payments=[], purchaseNumber=1001) is:
       ```
       Brutto: 5,00 €
       Netto: 5,00 €
       Beleg #1001
       ```
       (One Brutto line, no MwSt, no Trinkgeld, Netto = Brutto, then Beleg. Verify by running the existing spec against Phase-3 main BEFORE this task lands, OR by inspecting the existing Plan 03-03 test "purchase_to_transaction omits MwSt line when vatAmount = 0 (D-34)" which asserts the same substrings.) The literal expected string MUST be locked at the planner level — Task 2 must NOT change purchase_simple_sale's output.

    11. `it("META-01: UTF-8 umlauts round-trip via dkjson then preserve in purpose", function() ... end)` — load purchase_umlauts_purpose; call mapping; assert.is_not_nil(txn) (sanity: fixture loads and parses); decode the fixture's raw JSON via dkjson and re-encode, assert "Café" substring survives (test the harness's UTF-8 path more than the mapping itself — this is a defensive gate against the regenerated `purchase_with_vat_and_tip.json` from Plan 04-02 also being UTF-8 clean).

    Run `./.luarocks/bin/busted spec/mapping_spec.lua` — expect the 11 new tests to FAIL RED (`_format_purpose` doesn't yet produce per-rate VAT lines or card tail; Phase-3 snapshot test passes immediately because the Phase-3 behavior is intact). Document in commit that 10/11 new tests are RED (the snapshot test passes); after Task 2 they all turn GREEN.

    Step 4 — Build + verify. `lua tools/build.lua && lua tools/build.lua --verify` OK: reproducible. Validate the two new fixtures parse via dkjson (smoke test).

    Step 5 — Commit. `test(04-04): add fixtures + RED scaffolds for META-01 per-rate VAT + SALE-07 card tail + Phase-3 snapshot`. GPG-signed, no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; ls spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json spec/fixtures/purchases/purchase_umlauts_purpose.json &amp;&amp; grep -q 'CONTACTLESS_EMV' spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json &amp;&amp; grep -q 'Beispiel-Caf' spec/fixtures/purchases/purchase_umlauts_purpose.json &amp;&amp; grep -c 'Zahlart' spec/mapping_spec.lua | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }' &amp;&amp; grep -c 'groupedVatAmounts' spec/mapping_spec.lua | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; grep -c 'Phase-3 surface preservation' spec/mapping_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ! ./.luarocks/bin/busted spec/mapping_spec.lua 2&gt;&amp;1 | grep -qE '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - Both new fixtures exist; both parse via dkjson without error; both are valid UTF-8
    - `grep -E '"cardPaymentEntryMode"[[:space:]]*:[[:space:]]*"CONTACTLESS_EMV"' spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json` succeeds
    - `grep -E 'Beispiel-Caf' spec/fixtures/purchases/purchase_umlauts_purpose.json` succeeds (UTF-8 umlaut survives)
    - `grep -c 'Zahlart' spec/mapping_spec.lua` reports >= 4 (Tests 6-9 use this token)
    - `grep -c 'groupedVatAmounts' spec/mapping_spec.lua` reports >= 2 (Tests 1, 4)
    - `grep -c 'Phase-3 surface preservation' spec/mapping_spec.lua` reports >= 1 (Test 10)
    - `busted spec/mapping_spec.lua` exits NON-ZERO (at least the META-01 and SALE-07 tests are RED until Task 2)
    - `lua tools/build.lua --verify` reports `OK: reproducible (sha256: ...)`
    - Commit GPG-signed; no AI attribution
  </acceptance_criteria>
  <done>
    Two new fixtures created; 11 new test cases added to spec/mapping_spec.lua and currently RED. Commit: `test(04-04): add fixtures + RED scaffolds for META-01 per-rate VAT + SALE-07 card tail + Phase-3 snapshot`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/mapping.lua _format_purpose with per-rate VAT block + card-brand+entry-mode tail block (RED → GREEN)</name>
  <read_first>
    - src/mapping.lua (whole file — locate the existing _format_purpose function; understand the Brutto / MwSt / Trinkgeld / Netto / Beleg line order; identify the exact insertion points for the per-rate VAT block (REPLACES the existing single-MwSt branch when groupedVatAmounts has >= 2 rates) and the card-tail block (INSERTS between Netto and Beleg lines)
    - src/mapping.lua BRAND_MAP (already a module-local table; reuse byte-identically)
    - src/i18n.lua (the 12 new keys from Plan 04-02 — verify keys for `account.purpose.payment_method.kontaktlos / .chip / .swipe / .ecommerce / .manual / .unknown` exist and translate as expected)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §5.2 (_format_vat_lines pseudo-code; the `e.rate == math.floor(e.rate)` whole-number check for dropping `.0`), §5.3 (edge cases), §6.2 (ENTRY_MODE_MAP per RESEARCH table), §6.3 (rendering format), §6.4 (i18n key shape)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §Pitfall 8 (Phase-3 surface preservation — fixtures without multi-rate VAT AND without card metadata MUST produce byte-identical output)
    - spec/mapping_spec.lua (the RED scaffolds from Task 1 — Task 2 turns them GREEN)
  </read_first>
  <files>src/mapping.lua, spec/mapping_spec.lua</files>
  <behavior>
    - _format_purpose for sale path: emits Brutto line → (per-rate VAT lines OR single MwSt line OR no VAT line per existing logic) → Trinkgeld line (when applicable) → Netto line → (card-tail line when applicable) → Beleg line
    - Per-rate VAT path (D-53 / META-01): when `groupedVatAmounts` has 2+ entries (counted via pairs() walk into a temp array), emit one `<rate>% MwSt: <amount_de> EUR` line per rate, sorted DESCENDING by numeric rate; drop the `.0` for whole-number rates (`19%`, not `19.0%`); preserve fractional rates (`7.5%`). Skip the Phase-3 single-MwSt branch when this path fires.
    - Single VAT fallback path: when `groupedVatAmounts` has 0 or 1 entries, fall through to Phase-3's existing single `MwSt: <vatAmount> €` line emission (only emits when `vatAmount > 0` per Phase-3 D-34)
    - Card-tail path (D-57 / SALE-07): inspect `p.payments[1].attributes.cardType` and `p.payments[1].attributes.cardPaymentEntryMode`. If BOTH present: `Zahlart: <brand_de> (<entry_mode_de>)`. If ONLY cardType: `Zahlart: <brand_de>`. If ONLY entry-mode: `Zahlart: Kartenzahlung (<entry_mode_de>)`. If NEITHER: OMIT the line entirely (no `Zahlart: unbekannt (unbekannt)` noise per RESEARCH §6.3).
    - brand_de: use BRAND_MAP lookup (VISA→Visa etc.); fall back to `cardType:sub(1,1):upper() .. cardType:sub(2):lower()` for unknown brands (Phase-3 BRAND_MAP fallback pattern)
    - entry_mode_de: use new local ENTRY_MODE_MAP: `{ CONTACTLESS_EMV = "kontaktlos", ICC = "chip", MSR = "swipe", ECOMMERCE = "ecommerce", MANUAL = "manual" }` then call `M_i18n.t("account.purpose.payment_method." .. mapped)`; for any value not in the map, use `M_i18n.t("account.purpose.payment_method.unknown")` (= "unbekannt")
    - Refund path (_format_purpose with opts.kind="refund"): per-rate VAT lines and card-tail line apply to refunds the same way (refund records carry groupedVatAmounts with negative values per RESEARCH §5.1; tail line behaves identically based on echoed cardType/entry-mode from original)
    - Phase-3 surface preservation: for purchase_simple_sale.json (empty payments, empty groupedVatAmounts, vatAmount=0), _format_purpose output is exactly `Brutto: 5,00 €\nNetto: 5,00 €\nBeleg #1001` — BYTE-IDENTICAL to Phase 3
  </behavior>
  <action>
    Step 1 — Open src/mapping.lua. Locate the `_format_purpose(p, opts)` function (existing Phase-3 implementation; lives as a `local function` inside the do…end block per Phase-3 D-40). Verify the current line-by-line structure:

    Current Phase-3 structure of _format_purpose (paraphrased):
    1. Initialize `local lines = {}`
    2. (Refund kind only) Insert `lines[#lines+1] = M_i18n.t("account.purpose.refund_for", opts.original_receipt or p.refundsPurchaseUUID1)`
    3. Insert `lines[#lines+1] = M_i18n.t("account.purpose.gross", _format_amount(p.amount))`
    4. If `p.vatAmount and p.vatAmount ~= 0`: insert MwSt line `M_i18n.t("account.purpose.vat", _format_amount(p.vatAmount))`
    5. Compute `tip_sum = sum(p.payments[].gratuityAmount)`; if > 0: insert Trinkgeld line
    6. Insert Netto line (amount - vat - tip)
    7. Insert Beleg line via M_i18n.t("account.purpose.receipt_number", tostring(p.purchaseNumber))
    8. Return `table.concat(lines, "\n")`

    Step 2 — Add a module-local helper above _format_purpose:

    ```lua
    -- D-57 / SALE-07: API value → i18n key suffix for cardPaymentEntryMode (RESEARCH §6.2).
    local ENTRY_MODE_MAP = {
      CONTACTLESS_EMV = "kontaktlos",
      ICC             = "chip",
      MSR             = "swipe",
      ECOMMERCE       = "ecommerce",
      MANUAL          = "manual",
    }
    ```

    Step 3 — Extend the _format_purpose function body. The order of operations: REPLACE step 4 (the single-MwSt branch) with a per-rate-aware emitter, then INSERT a new card-tail block BETWEEN step 6 (Netto) and step 7 (Beleg).

    The new step 4 (per-rate VAT or fall-through):

    ```lua
    -- D-53 / META-01: per-rate VAT block (multi-rate path) or fall-through to Phase-3 single line.
    local gva = type(p.groupedVatAmounts) == "table" and p.groupedVatAmounts or {}
    local rate_entries = {}
    for k, v in pairs(gva) do
      local rate_num = tonumber(k)  -- accepts "19.0" decimal-string AND "19" integer-string (R-5 defensive)
      if rate_num and type(v) == "number" then
        rate_entries[#rate_entries + 1] = { rate = rate_num, amount = v }
      end
    end
    if #rate_entries >= 2 then
      -- META-01 multi-rate path: sort descending by rate, emit one line per rate.
      table.sort(rate_entries, function(a, b) return a.rate > b.rate end)
      for _, e in ipairs(rate_entries) do
        local rate_display
        if e.rate == math.floor(e.rate) then
          rate_display = string.format("%d", e.rate)
        else
          rate_display = string.format("%g", e.rate)
        end
        lines[#lines + 1] = string.format("%s%% MwSt: %s EUR", rate_display, _format_amount(e.amount))
      end
    else
      -- Phase-3 single-VAT fallback (preserves byte-identity for non-multi-rate fixtures).
      local vat = type(p.vatAmount) == "number" and p.vatAmount or 0
      if vat ~= 0 then
        lines[#lines + 1] = M_i18n.t("account.purpose.vat", _format_amount(vat))
      end
    end
    ```

    The new card-tail block (insert between Netto line and Beleg line):

    ```lua
    -- D-57 / SALE-07: card-brand + entry-mode tail line.
    do
      local pm = type(p.payments) == "table" and p.payments[1]
      local attrs = type(pm) == "table" and pm.attributes
      local card_type = type(attrs) == "table" and type(attrs.cardType) == "string" and #attrs.cardType > 0 and attrs.cardType or nil
      local entry_mode = type(attrs) == "table" and type(attrs.cardPaymentEntryMode) == "string" and #attrs.cardPaymentEntryMode > 0 and attrs.cardPaymentEntryMode or nil

      if card_type or entry_mode then
        local brand_de
        if card_type then
          brand_de = BRAND_MAP[card_type:upper()]
          if not brand_de then
            -- Unknown brand: capitalized literal (Phase-3 BRAND_MAP fallback convention).
            brand_de = card_type:sub(1, 1):upper() .. card_type:sub(2):lower()
          end
        else
          -- Entry-mode only: fall back to generic Kartenzahlung label.
          brand_de = M_i18n.t("account.name.card_payment")
        end

        if entry_mode then
          local mode_key = ENTRY_MODE_MAP[entry_mode:upper()] or "unknown"
          local mode_de = M_i18n.t("account.purpose.payment_method." .. mode_key)
          lines[#lines + 1] = "Zahlart: " .. brand_de .. " (" .. mode_de .. ")"
        else
          -- Brand only, no entry mode: no parens.
          lines[#lines + 1] = "Zahlart: " .. brand_de
        end
      end
      -- else: BOTH absent → OMIT the Zahlart line entirely (RESEARCH §6.3)
    end
    ```

    Step 4 — Verify Phase-3 surface preservation. Build + run the snapshot test:

    `./.luarocks/bin/busted spec/mapping_spec.lua -t "Phase-3 surface preservation"` MUST be GREEN (purchase_simple_sale produces the expected literal `Brutto: 5,00 €\nNetto: 5,00 €\nBeleg #1001`). If this regresses, the per-rate VAT block or the card-tail block has changed Phase-3 behavior on a no-VAT-no-card fixture — investigate before committing.

    Step 5 — Run all 11 new tests + the existing Phase-3 suite. All RED → GREEN.

    Step 6 — Build + verify. `lua tools/build.lua && lua tools/build.lua --verify` OK: reproducible. `busted spec/` — only the still-RED Plan 04-05 specs may fail. `luacheck src/mapping.lua` clean.

    Step 7 — Commit. `feat(04-04): per-rate VAT lines + card-brand+entry-mode tail in _format_purpose (META-01, SALE-07, D-53, D-57)`. GPG-signed, no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'ENTRY_MODE_MAP' src/mapping.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'CONTACTLESS_EMV' src/mapping.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'rate_entries' src/mapping.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c '"Zahlart: "' src/mapping.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ./.luarocks/bin/busted spec/mapping_spec.lua 2&gt;&amp;1 | grep -qE '0 failures' &amp;&amp; ./.luarocks/bin/busted spec/mapping_schema_spec.lua spec/dst_table_spec.lua spec/i18n_spec.lua spec/finance_parse_transaction_spec.lua spec/pagination_offset_spec.lua spec/pagination_spec.lua spec/purchases_spec.lua spec/finance_spec.lua spec/finance_account_state_spec.lua spec/entry_spec.lua spec/refresh_log_redaction_spec.lua spec/auth_spec.lua spec/http_spec.lua spec/errors_spec.lua spec/mm_mocks_spec.lua spec/build_spec.lua 2&gt;&amp;1 | grep -qE '0 failures' &amp;&amp; ./.luarocks/bin/luacheck src/mapping.lua 2&gt;&amp;1 | grep -qE '(Total: 0 warnings|0 warnings / 0 errors)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ENTRY_MODE_MAP' src/mapping.lua` reports >= 1
    - `grep -c 'CONTACTLESS_EMV' src/mapping.lua` reports >= 1 (the API value is referenced in the map)
    - `grep -c 'rate_entries' src/mapping.lua` reports >= 1 (the per-rate accumulator)
    - `grep -c '"Zahlart: "' src/mapping.lua` reports >= 1 (the tail-line prefix string)
    - `busted spec/mapping_spec.lua -t "Phase-3 surface preservation"` is GREEN (snapshot preserved)
    - `busted spec/mapping_spec.lua -t "META-01"` all GREEN
    - `busted spec/mapping_spec.lua -t "SALE-07"` all GREEN
    - `busted spec/mapping_spec.lua` is fully GREEN (all 11 new tests + all Phase-3 + Plan-04-02 tests)
    - All other Wave-2 specs still GREEN (spec/mapping_schema_spec.lua, spec/i18n_spec.lua, spec/finance_*.lua, spec/pagination_*.lua, spec/entry_spec.lua, spec/refresh_log_redaction_spec.lua, etc.)
    - `busted spec/` exits non-zero ONLY due to still-RED Plan-04-05 specs (META-03 invariant + extended idempotency D-58)
    - `luacheck src/mapping.lua` is clean
    - `lua tools/build.lua --verify` reports `OK: reproducible`
    - Commit GPG-signed; no AI attribution; Conventional Commits prefix `feat(04-04):`
  </acceptance_criteria>
  <done>
    src/mapping.lua _format_purpose extended with per-rate VAT block + card-brand+entry-mode tail block ADDITIVELY; Phase-3 surface preserved byte-identically; 11 new spec tests all GREEN. Commit: `feat(04-04): per-rate VAT lines + card-brand+entry-mode tail in _format_purpose`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| purchase.groupedVatAmounts map → _format_purpose iteration | Already parsed by M_http.get_json; treated as untrusted Lua table; every key/value type-checked via tonumber + type(v) == "number" |
| purchase.payments[].attributes → card-tail block chained access | Each level guarded with type() == "table" and #string > 0 checks; absent fields OMIT the tail line cleanly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W3-01 | Tampering | groupedVatAmounts attacker-controlled key like `"19; os.execute('rm -rf /')"` | mitigate | tonumber(k) returns nil for non-numeric strings; the rate_entries accumulator silently discards non-numeric keys; no loadstring or Lua-pattern execution path |
| T-04-W3-02 | Information Disclosure | card-tail line in purpose | accept | Only cardType (string from API) and entry-mode label (i18n constant) reach the line; no maskedPan, no UUID, no Bearer; merchant's own data echoed back |
| T-04-W3-03 | Tampering | Phase-3 surface preservation gate | mitigate | The snapshot test in spec/mapping_spec.lua asserts byte-identical output for purchase_simple_sale; any per-rate VAT or card-tail block change that perturbs the no-VAT-no-card path fails the gate |
| T-04-W3-04 | Tampering | ENTRY_MODE_MAP unknown value handling | mitigate | `ENTRY_MODE_MAP[entry_mode:upper()] or "unknown"` always returns a known i18n key; the .unknown fallback is part of the locked i18n surface (Plan 04-02 D-57); no Lua-pattern injection risk because we only call .upper() and table lookup |
| T-04-W3-05 | Tampering | Lua table.sort instability for equal VAT rates | accept | Distinct German VAT rates (0%, 7%, 19%) are all distinct floats; equal-rate degenerate case impossible for valid Zettle data per RESEARCH §5.4 |
| T-04-W3-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Reproducible build:
lua tools/build.lua && lua tools/build.lua --verify

# luacheck clean:
./.luarocks/bin/luacheck src/mapping.lua spec/mapping_spec.lua

# All mapping tests GREEN (Phase 3 + Plan 04-02 + Plan 04-04 extensions):
./.luarocks/bin/busted spec/mapping_spec.lua spec/mapping_schema_spec.lua spec/dst_table_spec.lua

# Phase-3 surface preservation explicitly:
./.luarocks/bin/busted spec/mapping_spec.lua -t "Phase-3 surface preservation"
./.luarocks/bin/busted spec/mapping_spec.lua -t "META-01"
./.luarocks/bin/busted spec/mapping_spec.lua -t "SALE-07"

# Full suite excluding Plan 04-05 RED specs:
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(failures|0 failures)'

# Confirm Phase-3 fixtures produce same purpose as before:
lua -e 'dofile("dist/paypal-pos.lua"); local F = require "spec.helpers.fixtures"; local _, fx = F.load("purchases/purchase_simple_sale"); local txn = M_mapping.purchase_to_transaction(fx.purchases[1]); print(txn.purpose)'
# Expected output:
# Brutto: 5,00 €
# Netto: 5,00 €
# Beleg #1001
```
</verification>

<success_criteria>
- src/mapping.lua _format_purpose has ADDITIVE per-rate VAT block (D-53 / META-01) and card-brand+entry-mode tail block (D-57 / SALE-07)
- 2 new fixtures exist: spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json and spec/fixtures/purchases/purchase_umlauts_purpose.json
- 11 new test cases in spec/mapping_spec.lua all GREEN: META-01 (multi-rate sort, single-rate fallback, empty-map fallback, integer-key defense, negative VAT on refund); SALE-07 (both-present, brand-only, neither-present omit, unknown-entry-mode); Phase-3 snapshot preservation; UTF-8 umlaut round-trip
- Phase-3 surface preserved byte-identically for no-multi-rate, no-card-metadata fixtures (snapshot assertion green)
- Full spec suite GREEN except still-RED Plan-04-05 specs (META-03 invariant + extended idempotency D-58 cases)
- `lua tools/build.lua --verify` reports OK: reproducible
- luacheck clean on src/mapping.lua + spec/mapping_spec.lua
- All commits GPG-signed; no AI attribution; Conventional Commits prefix `feat(04-04):`
- 2 new fixtures land for Plan 04-05's META-03 forbidden-strings invariant spec to ALSO scan (the spec walks fixtures? — no, it walks src/*.lua only per RESEARCH §7.2; fixtures are not in scope)
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `src/mapping.lua` — _format_purpose extended ADDITIVELY with per-rate VAT block + card-brand+entry-mode tail block; existing Phase-3 single-MwSt branch preserved as fallback; ENTRY_MODE_MAP local table added
- `spec/mapping_spec.lua` — EXTENDED with 11 new test cases (META-01 × 5; SALE-07 × 4; Phase-3 snapshot × 1; UTF-8 round-trip × 1)
- `spec/fixtures/purchases/purchase_with_card_metadata_kontaktlos.json` — NEW fixture: Visa + CONTACTLESS_EMV
- `spec/fixtures/purchases/purchase_umlauts_purpose.json` — NEW fixture: Beispiel-Café merchant name for UTF-8 round-trip

New public symbols: NONE (this plan is pure _format_purpose extension; no new M_mapping.* API surface added)

New module-internal helpers:
- `ENTRY_MODE_MAP` (local table in src/mapping.lua) — maps API cardPaymentEntryMode values to i18n key suffixes per RESEARCH §6.2

New i18n keys: NONE (the 12 keys consumed by this plan were added in Plan 04-02; this plan just READS them via the ENTRY_MODE_MAP lookup)

New transactionCode prefixes: NONE (Plans 04-02 + 04-03 locked the prefixes; this plan only formats purpose text)

Files NOT modified by this plan: src/finance.lua, src/entry.lua, src/pagination.lua, src/purchases.lua, src/i18n.lua, src/webbanking_header.lua, tools/manifest.txt, src/http.lua, src/auth.lua, src/errors.lua, src/log.lua, CHANGELOG.md, README.md, docs/adr/*. Existing fixtures (other than the regenerated purchase_with_vat_and_tip.json from Plan 04-02) untouched.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-enrichment-refunds-fees-payouts/04-04-SUMMARY.md` summarising:
- src/mapping.lua _format_purpose LoC delta (additive lines only)
- spec/mapping_spec.lua final test count (Phase-3 ~18 + Plan 04-02 ~13 + Plan 04-04 ~11 = ~42)
- Phase-3 surface preservation: snapshot assertion result for purchase_simple_sale (must be byte-identical)
- META-01 test results: sort, fallback, integer-defense, negative-on-refund (all GREEN)
- SALE-07 test results: both-present, brand-only, neither-present, unknown-mode (all GREEN)
- UTF-8 round-trip confirmation (Café fixture)
- Reproducible-build SHA after this plan
- ENTRY_MODE_MAP keys + their i18n key suffix mappings
- Confirmation that Plan 04-05 specs (META-03 invariant, extended idempotency D-58, extended log-redaction prefix gate) are still RED
</output>
</content>
</invoke>