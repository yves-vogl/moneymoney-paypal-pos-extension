---
phase: 06-release-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build.lua
  - src/webbanking_header.lua
  - .luacheckrc
  - spec/build_version_substitution_spec.lua
  - spec/meta_no_tax_classification_spec.lua
  - .github/workflows/ci.yml
  - .github/workflows/commit-lint.yml
autonomous: true
requirements: [BUILD-03, CI-01, CI-02, CI-03, CI-04, CI-05, CI-06, SEC-02, DOC-04]
tags: [wave-1, build, ci, gitleaks, commit-lint, meta-03, tdd, mvp]

must_haves:
  truths:
    - "tools/build.lua substitutes a literal `__VERSION__` token in src/webbanking_header.lua's `WebBanking{ version = __VERSION__, }` line with a Lua-numeric version derived from $GITHUB_REF_NAME (CI) or `git describe --tags --exact-match` (local) or `dev-<short-sha>` fallback (per D-73 + RESEARCH §3); the substitution is Lua-side (NOT sed/shell) so it is testable in busted and reproducible per CI-04"
    - "`__VERSION__` substitution algorithm per RESEARCH §3: `v1.0.0` → `1.00`, `v1.2.3` → `1.20` (patch dropped per MoneyMoney `<major>.<two-digit-minor>` community convention), `v0.10.0` → `0.10`, `v1.0.0-rc.1` → `1.00` (rc suffix matches `^v(%d+)%.(%d+)` and patch+rc are dropped), `dev-<sha>` or no tag → `0.00` (development placeholder; release.yml gates against dev builds via BUILD-04 signed-tag requirement so 0.00 never ships)"
    - "src/webbanking_header.lua line 24 changes from `version     = 0.00,` to `version     = __VERSION__,`; `.luacheckrc` `globals` list adds `__VERSION__` so luacheck does not flag the placeholder as undefined; the raw src/webbanking_header.lua is Lua-syntax-INVALID until the build substitutes (acceptable — file is never loaded directly, only via the built artifact)"
    - "spec/build_version_substitution_spec.lua is RED-FIRST per TDD discipline (commit the spec BEFORE the build/header edits; suite shows failures naming the missing substitution) then GREEN after edits; spec covers 5 cases: v1.0.0→1.00, v1.2.3→1.20, v0.10.0→0.10, v1.0.0-rc.1→1.00 (rc dropped), unset GITHUB_REF_NAME → dev fallback (0.00 OR a numeric local tag — tolerate both per RESEARCH §3 portability), plus a sixth case asserting two consecutive `--verify` invocations with the same GITHUB_REF_NAME exit 0 (CI-04 reproducibility under substitution)"
    - "spec/meta_no_tax_classification_spec.lua walker extension (Pitfall 6 mitigation) — adds DOC_TARGETS array enumerating README.md, README.de.md, CONTRIBUTING.md, CHANGELOG.md, plus dynamic `ls docs/adr/*.md` enumeration; the new `it(...)` block tolerates file-not-found (early in Wave 1 README.de.md / CONTRIBUTING.md do not yet exist) but asserts zero forbidden-phrase hits for every file that DOES exist; current README.md + CHANGELOG.md + docs/adr/0001/0003/0004/0005 are dry-run-audited BEFORE committing the walker extension to confirm zero hits — if a hit surfaces (e.g., `steuerlich` substring in any existing doc) revise the doc IN THE SAME COMMIT or stop and surface the issue"
    - ".github/workflows/ci.yml extends with three new gates that join the existing test job's gate chain (DEBUG / egress / no-AI-attribution): (a) gitleaks `secret-scan` job per RESEARCH §4 (free for personal-account repo `yves-vogl`; `fetch-depth: 0`; no `GITLEAKS_LICENSE` needed), (b) D-79 `no-raw-print()-calls` grep added as a step inside the existing `test` job AFTER the egress allowlist step, (c) the existing test job is preserved byte-identically except for the added step — luacheck/busted/coverage/build/--verify/SEC-04/egress/no-AI all stay intact"
    - ".github/workflows/commit-lint.yml is a NEW separate workflow per RESEARCH §11 (cleaner separation from ci.yml since trigger is `pull_request` only); shell-only Conventional Commits regex `^(feat|fix|docs|test|refactor|chore|ci|build|perf|style|revert)(\\([^)]+\\))?: .+` walks `git rev-list BASE..HEAD` and exits non-zero on the first mismatch with a helpful message; the new workflow's job `name:` is `Commit-message lint` so 06-02's branch-protection script can reference it in its CHECKS array"
    - "Pitfall 5 mitigation (gitleaks fixture-blocking) — BEFORE pushing the W1 PR, run `gitleaks detect --no-banner --source .` locally to audit the existing tree; if any spec/fixtures/auth or spec/fixtures/jwt or src/auth.lua references trip the detector, add a `.gitleaksignore` file at repo root with line-by-line fingerprint entries (format `<fingerprint>:<path>:<line>` per RESEARCH §4) — NEVER add a blanket `paths` allowlist; if no fixtures trip, do NOT create `.gitleaksignore` (the file is conditional per PATTERNS item 23)"
    - "Pitfall 3 mitigation (dev-build banner) — when `version_to_number_string(resolve_version_string())` returns `0.00` (no tag), tools/build.lua prepends an extra comment line `-- paypal-pos amalgamated DEV BUILD (no tag) — not for release` AFTER the existing BANNER and BEFORE the first module; the second comment line of dist/paypal-pos.lua reads `DEV BUILD` for dev-builds, absent for tagged-builds; documented in CONTRIBUTING.md (06-02) but the implementation lives here"
    - "Reproducible-build SHA STAYS DETERMINISTIC per-tag — two consecutive `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` invocations produce byte-identical dist/paypal-pos.lua (the gsub is pure; the resolve_version_string output is fixed for a given env); local dev builds (no tag) are also reproducible per-sha but the SHA differs from any tagged build by exactly the `__VERSION__` literal + the optional DEV-BUILD banner line"
    - "Phase-2/3/4/5 surface UNTOUCHED — no src/*.lua other than webbanking_header.lua is modified; no spec/*.lua other than meta_no_tax_classification + the new build_version_substitution is modified; the existing 373/0 busted suite passes byte-identically post-W1 (with +N new specs from build_version_substitution_spec); reproducible SHA differs from Phase 5 baseline only because of the __VERSION__ token + optional DEV banner — documented in SUMMARY"
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution; Conventional Commits prefixes `test(06-01):` (RED spec), `feat(06-01):` (build + header GREEN flip), `test(06-01):` (META-03 walker extension), `ci(06-01):` (gitleaks + commit-lint + D-79 print-grep)"
  artifacts:
    - path: "tools/build.lua"
      provides: "resolve_version_string() + version_to_number_string() + __VERSION__ gsub in HEADER_MOD branch + DEV-BUILD banner prepend when version_number == '0.00' (BUILD-03 + Pitfall 3 mitigation)"
      contains: "resolve_version_string"
      min_lines: 450
    - path: "src/webbanking_header.lua"
      provides: "version = __VERSION__, placeholder (1-line change from `version = 0.00,`)"
      contains: "__VERSION__"
    - path: ".luacheckrc"
      provides: "Added __VERSION__ to globals[] so luacheck does not flag the unresolved-at-source token on webbanking_header.lua"
      contains: "__VERSION__"
    - path: "spec/build_version_substitution_spec.lua"
      provides: "BUILD-03 verification — 6 cases: v1.0.0→1.00, v1.2.3→1.20, v0.10.0→0.10, v1.0.0-rc.1→1.00, dev fallback, --verify reproducible per-tag"
      contains: "GITHUB_REF_NAME"
      min_lines: 50
    - path: "spec/meta_no_tax_classification_spec.lua"
      provides: "META-03 walker extended to scan README.md + README.de.md + CONTRIBUTING.md + CHANGELOG.md + docs/adr/*.md (tolerates file-not-found); Pitfall 6 mitigation"
      contains: "DOC_TARGETS"
    - path: ".github/workflows/ci.yml"
      provides: "Added gitleaks secret-scan job (RESEARCH §4) + D-79 no-raw-print() step inside existing test job (after egress allowlist)"
      contains: "gitleaks"
    - path: ".github/workflows/commit-lint.yml"
      provides: "New pull_request-only workflow enforcing Conventional Commits via shell regex on every commit in PR range (RESEARCH §11; D-78)"
      contains: "Conventional Commits"
  key_links:
    - from: "tools/build.lua resolve_version_string()"
      to: "$GITHUB_REF_NAME (CI) → git describe --tags --exact-match (local) → git rev-parse --short HEAD (dev fallback)"
      via: "3-tier env→git→sha resolution per RESEARCH §3"
      pattern: "GITHUB_REF_NAME"
    - from: "tools/build.lua version_to_number_string()"
      to: "src/webbanking_header.lua line 24 __VERSION__ literal"
      via: "string.gsub('__VERSION__', VERSION_NUMBER) applied inside the HEADER_MOD branch of build()"
      pattern: "__VERSION__"
    - from: "spec/meta_no_tax_classification_spec.lua DOC_TARGETS"
      to: "README.md + CHANGELOG.md + docs/adr/*.md (existing) + README.de.md + CONTRIBUTING.md (future from 06-02)"
      via: "Explicit static list + dynamic `ls docs/adr/*.md` enumeration; per-file existence check tolerates absent files"
      pattern: "DOC_TARGETS"
    - from: ".github/workflows/commit-lint.yml job name"
      to: "tools/setup-branch-protection.sh CHECKS array (06-02)"
      via: "Job `name: Commit-message lint` must match the CHECKS entry in the branch-protection script (Pitfall 4 mitigation)"
      pattern: "Commit-message lint"
    - from: ".github/workflows/ci.yml gitleaks job name"
      to: "tools/setup-branch-protection.sh CHECKS array (06-02)"
      via: "Job `name: gitleaks secret scan` must match the CHECKS entry (Pitfall 4)"
      pattern: "gitleaks secret scan"
---

<objective>
Land the Wave-1 scaffolding for Phase 6 that has no user-facing surface but unblocks every subsequent task in Phase 6: (a) `__VERSION__` substitution from `$GITHUB_REF_NAME` into the shipped artifact's `WebBanking{version}` field per D-73 / BUILD-03 (Lua-side, TDD-driven); (b) META-03 walker extension to cover markdown documentation per Pitfall 6 / DOC-04 (the discipline gate that protects README.de.md + CHANGELOG.md + ADRs as 06-02 + 06-03 land them); (c) gitleaks secret-scan job per CI-05 + D-76 with Pitfall-5-mitigating local dry-run; (d) commit-lint workflow per D-78; (e) D-79 raw-`print(` bypass grep (extends the existing egress allowlist); (f) re-asserts CI-01..06 + SEC-02 are still in place (existing ci.yml already covers; this plan extends without breaking).

Purpose: Wave 1 puts the test-and-lint gates in place BEFORE the user-facing content arrives in Wave 2. Two specific orderings matter: (1) the META-03 walker MUST land before README.de.md / CONTRIBUTING.md / new ADRs are written so the doc authors get immediate CI feedback on forbidden phrasing; (2) the `__VERSION__` substitution MUST land before the release.yml workflow (06-02) is wired so the release pipeline can pass `GITHUB_REF_NAME` and have the build pick it up.

Output: 4 GPG-signed commits across 4 files-touching tasks (RED spec → GREEN build/header → META-03 walker extension → CI extensions). Reproducible-build SHA recomputed and documented in SUMMARY (the SHA will differ from Phase 5 baseline `5dbcb8e…` because the `__VERSION__` placeholder + optional DEV-BUILD banner change the header bytes — by design).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-release-polish/06-CONTEXT.md
@.planning/phases/06-release-polish/06-RESEARCH.md
@.planning/phases/06-release-polish/06-PATTERNS.md
@.github/workflows/ci.yml
@.github/dependabot.yml
@.luacheckrc
@src/webbanking_header.lua
@tools/build.lua
@spec/meta_no_tax_classification_spec.lua
@CHANGELOG.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — author spec/build_version_substitution_spec.lua before any build/header edits (BUILD-03)</name>
  <read_first>
    - spec/meta_no_tax_classification_spec.lua (Phase-4 spec preamble pattern that invokes `os.execute("lua tools/build.lua 2>/dev/null")` before reading dist/paypal-pos.lua — mirror this idiom)
    - tools/build.lua (lines 22, 24, 62, 76, 85, 143, 152-172 — understand HEADER_MOD branch, BANNER constant, normalise(), ensure_trailing_newline(), parse_manifest(); the spec writes to this seam)
    - src/webbanking_header.lua (line 24 — `version     = 0.00,` is the literal that will become `version     = __VERSION__,` in Task 2)
    - .planning/phases/06-release-polish/06-RESEARCH.md §3 (substitution algorithm + 5 test cases verbatim) + §3 "Test in Phase 6" code excerpt
    - .planning/phases/06-release-polish/06-PATTERNS.md item 22 (spec outline with 6 cases including --verify reproducibility under substitution)
    - Pitfall 3 in 06-RESEARCH.md (DEV-BUILD banner expectation for dev builds — Task 2 implements; this spec MAY assert presence/absence of `DEV BUILD` text in dist/ for the dev-fallback case)
  </read_first>
  <behavior>
    - Test 1 (RED → GREEN after Task 2): `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua` → dist/paypal-pos.lua matches `version%s*=%s*1%.00,` AND `content:find("__VERSION__")` returns nil.
    - Test 2 (RED → GREEN): `GITHUB_REF_NAME=v1.2.3 lua tools/build.lua` → matches `version%s*=%s*1%.20,` (patch dropped).
    - Test 3 (RED → GREEN): `GITHUB_REF_NAME=v0.10.0 lua tools/build.lua` → matches `version%s*=%s*0%.10,`.
    - Test 4 (RED → GREEN): `GITHUB_REF_NAME=v1.0.0-rc.1 lua tools/build.lua` → matches `version%s*=%s*1%.00,` (rc + patch dropped per RESEARCH §3 `^v(%d+)%.(%d+)` capture).
    - Test 5 (RED → GREEN): `env -u GITHUB_REF_NAME lua tools/build.lua` → matches EITHER `version%s*=%s*0%.00,` (no local exact-match tag) OR `version%s*=%s*%d+%.%d+,` (local has a tag at HEAD); BOTH cases assert that `__VERSION__` token is absent from output; the DEV-BUILD banner is verified separately in Test 6.
    - Test 6 (RED → GREEN): when the resolved version is `0.00` (dev fallback), dist/paypal-pos.lua's second line (after the BANNER) contains `DEV BUILD` (Pitfall 3 mitigation); when a tag IS present, the second line is the first module's content (no DEV BUILD).
    - Test 7 (RED → GREEN): two consecutive `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` invocations both exit 0 (reproducible per CI-04 under substitution).
  </behavior>
  <files>spec/build_version_substitution_spec.lua</files>
  <action>
    Create spec/build_version_substitution_spec.lua as a busted spec. Use the file-read helper pattern observed in spec/meta_no_tax_classification_spec.lua (open dist/paypal-pos.lua in "rb", read("*a"), close). Implement 7 `it(...)` blocks per the `<behavior>` section above.

    Structure:

    1. Top-of-file local helper `read_dist()` that opens "dist/paypal-pos.lua" rb, reads, closes, returns content. Assert non-nil with helpful message if missing.
    2. Top-of-file local helper `run_build(env_prefix)` that calls `os.execute(env_prefix .. " lua tools/build.lua >/dev/null 2>&1")` and returns the return code; tests that want the artifact then call `read_dist()`.
    3. `describe("BUILD-03: __VERSION__ substitution (D-73)", function() ... end)` block wrapping all 7 it() cases.

    Each it() block:
    - Sets the env via the run_build helper.
    - Reads dist/paypal-pos.lua.
    - Asserts the expected `version = N.NN,` pattern via `assert.matches`.
    - For most cases: asserts `assert.is_nil(content:find("__VERSION__"), "__VERSION__ token still present after substitution")`.
    - For Test 6 (DEV BUILD banner): split content by newlines, inspect line 2, assert `DEV BUILD` substring presence/absence per behavior table.
    - For Test 7 (reproducibility): invoke `os.execute(...)` twice; assert both return non-zero codes equivalent to success (Lua's `os.execute` truthy-on-success convention varies by Lua version — use `assert.is_truthy(rc1)` AND `assert.is_truthy(rc2)`).

    Portability notes (RESEARCH §3 + PATTERNS item 22):
    - `env -u GITHUB_REF_NAME ...` works on both BSD (macOS) and GNU (CI ubuntu-24.04) `env` — both implementations support `-u`.
    - Lua 5.4 `os.execute` returns `true, exit, code` on success; treat any truthy return as success.
    - The spec does NOT require MoneyMoney mocks since it only invokes tools/build.lua and reads the resulting file — no `require` of any src/ module needed.

    Commit BEFORE any tools/build.lua or src/webbanking_header.lua edits exist:
    `test(06-01): RED scaffold for __VERSION__ substitution spec (BUILD-03)`

    After commit, run `busted spec/build_version_substitution_spec.lua` and CONFIRM the spec fails with messages identifying the missing substitution (current tools/build.lua emits `version = 0.00,` unconditionally so Test 1/2/3 all fail with "did not match `version = 1.00,`" etc.) — this is the RED state proving the spec discriminates.

    Do NOT proceed to Task 2 until the RED commit lands.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && test -f spec/build_version_substitution_spec.lua && grep -c 'GITHUB_REF_NAME' spec/build_version_substitution_spec.lua | awk '{ if ($1 >= 5) exit 0; else exit 1 }' && grep -c '__VERSION__' spec/build_version_substitution_spec.lua | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c 'DEV BUILD' spec/build_version_substitution_spec.lua | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '^[[:space:]]*it(' spec/build_version_substitution_spec.lua | awk '{ if ($1 >= 5) exit 0; else exit 1 }' && luacheck spec/build_version_substitution_spec.lua && ./.luarocks/bin/busted spec/build_version_substitution_spec.lua 2>&1 | grep -qE '(failure|failures|errors)' && ./.luarocks/bin/busted spec/build_version_substitution_spec.lua 2>&1 | grep -vqE '0 failures.*0 errors' || true</automated>
  </verify>
  <acceptance_criteria>
    - `test -f spec/build_version_substitution_spec.lua` succeeds
    - `grep -c '^[[:space:]]*it(' spec/build_version_substitution_spec.lua` reports >= 5 (the 6 behavior cases; Test 5/6 may be one it() each)
    - `grep -c 'GITHUB_REF_NAME' spec/build_version_substitution_spec.lua` reports >= 5
    - `grep -c '__VERSION__' spec/build_version_substitution_spec.lua` reports >= 2 (RED checks for absence post-substitution)
    - `grep -c 'DEV BUILD' spec/build_version_substitution_spec.lua` reports >= 1 (Test 6 Pitfall-3 banner case)
    - `luacheck spec/build_version_substitution_spec.lua` reports 0 warnings/errors
    - `busted spec/build_version_substitution_spec.lua` exits non-zero (RED — at least Test 1/2/3 fail because the current tools/build.lua has no substitution)
    - Commit is GPG-signed; no AI attribution; message starts with `test(06-01):` and references RED + BUILD-03
  </acceptance_criteria>
  <done>
    Spec exists with 5-7 `it()` cases covering BUILD-03 behavior; spec is RED (busted reports failures because Task 2 has not yet implemented the substitution); commit landed. Plan 06-01 Task 2 unblocked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement __VERSION__ substitution in tools/build.lua + flip header placeholder + luacheckrc (BUILD-03)</name>
  <read_first>
    - tools/build.lua (whole file — locate exact insertion points: after parse_manifest() definition for the helpers, inside build() at the `if mod == HEADER_MOD then` branch for the gsub, after the BANNER concat for the conditional DEV-BUILD line)
    - src/webbanking_header.lua (line 24 — exact literal `version     = 0.00,` with the spacing that the build's `normalise` function preserves; do NOT collapse the spaces)
    - .luacheckrc (globals[] list — add `__VERSION__` so luacheck does not flag the placeholder; observe the existing `globals[#globals+1] = "M_log"` append idiom)
    - .planning/phases/06-release-polish/06-RESEARCH.md §3 (exact Lua code for resolve_version_string + version_to_number_string)
    - .planning/phases/06-release-polish/06-PATTERNS.md item 5 (build.lua change recipe) + item 8 (header line change) + item 16 luacheckrc entry recommendation
    - Pitfall 3 in 06-RESEARCH.md (DEV-BUILD banner content)
    - spec/build_version_substitution_spec.lua from Task 1 — the RED test cases drive the GREEN implementation
  </read_first>
  <behavior>
    - After Task 2, `busted spec/build_version_substitution_spec.lua` GREEN (all 5-7 it() blocks pass).
    - `lua tools/build.lua` with no env emits dist/paypal-pos.lua whose second line contains `DEV BUILD` and whose webbanking_header substitutes `__VERSION__` → `0.00`.
    - `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` exits 0 and is reproducible across two consecutive invocations.
    - luacheck on the whole tree (including src/webbanking_header.lua now containing `__VERSION__`) reports clean (0 errors) because `__VERSION__` is in .luacheckrc globals[].
    - Full `busted spec/` passes — no regression in any pre-existing spec.
  </behavior>
  <files>tools/build.lua, src/webbanking_header.lua, .luacheckrc</files>
  <action>
    Step 1 — Edit tools/build.lua per RESEARCH §3:

    After `parse_manifest()` (around line 85-ish, before build()), add two new local functions:

    `resolve_version_string()` — try `os.getenv("GITHUB_REF_NAME")`; if it matches `^v%d` return it; else try `io.popen("git describe --tags --exact-match 2>/dev/null"):read("*l")`; if matches `^v%d` return it; else try `io.popen("git rev-parse --short HEAD 2>/dev/null"):read("*l")` and return `"dev-" .. sha` if non-empty; else return `"dev-unknown"`.

    `version_to_number_string(s)` — `local major, minor = s:match("^v(%d+)%.(%d+)")`; if both → `return string.format("%d.%02d", tonumber(major), tonumber(minor))`; else → `return "0.00"`.

    At module scope (after the two helpers), compute `local VERSION_NUMBER = version_to_number_string(resolve_version_string())`.

    In build() (around line 143-172), modify the assembly so the BANNER is appended unconditionally as before, and IF `VERSION_NUMBER == "0.00"` THEN append an additional comment line `"-- paypal-pos amalgamated DEV BUILD (no tag) — not for release\n"` BEFORE the first module's content (this becomes line 2 of the dist file, immediately after the BANNER). When VERSION_NUMBER != "0.00", the DEV-BUILD line is omitted so a tagged build's line 2 is the first module body (Test 6 discriminator).

    In the `if mod == HEADER_MOD then` branch (inside the `for _, mod in ipairs(modules) do` loop), AFTER `content = normalise(content)` and BEFORE `parts[#parts + 1] = ensure_trailing_newline(content)`, insert: `content = content:gsub("__VERSION__", VERSION_NUMBER)`. The gsub is anchored to the literal placeholder; the substitution is idempotent (no double-substitution because the substituted result `1.00` contains no `__VERSION__`).

    Step 2 — Edit src/webbanking_header.lua line 24:
    Change `  version     = 0.00,` to `  version     = __VERSION__,` (preserve exact spacing — `normalise()` collapses CRLF but not internal whitespace).

    Step 3 — Edit .luacheckrc — append `globals[#globals+1] = "__VERSION__"` after the existing `globals[#globals+1] = "DEBUG"` line. This prevents luacheck from flagging the unresolved-at-source token in src/webbanking_header.lua.

    Step 4 — Verify locally:
    - `luacheck .` clean (no new warnings).
    - `lua tools/build.lua` succeeds; `head -2 dist/paypal-pos.lua` shows BANNER + DEV-BUILD line.
    - `lua tools/build.lua --verify` reports `OK: reproducible` (dev-mode reproducible).
    - `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua && head -2 dist/paypal-pos.lua` shows BANNER then the first module content (no DEV-BUILD line); `grep -c 'version = 1.00,' dist/paypal-pos.lua` reports 1.
    - `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` reports reproducible (tagged-build reproducible).
    - `busted spec/build_version_substitution_spec.lua` GREEN (all 5-7 it() cases).
    - Full `busted spec/` GREEN end-to-end (no regression — the substitution does not affect any other spec because all other specs build dist/paypal-pos.lua via the same tools/build.lua and the substitution is consistent).

    Step 5 — Document the new reproducible-build SHA in the commit body. Compare against Phase-5 baseline `5dbcb8ea97ae2fb2b675442439ac93b342893e84b9e7849b29df07e9612b777e`: the new SHA will DIFFER because the header now contains `__VERSION__`-substituted bytes (`0.00` for dev / `1.00` etc. for tags) AND optionally the DEV-BUILD banner. Both differences are expected per RESEARCH §3 reproducibility note.

    Commit (single commit covering all 3 file changes — the three edits are interdependent and only meaningful together):
    `feat(06-01): __VERSION__ substitution from $GITHUB_REF_NAME in tools/build.lua + DEV-BUILD banner (BUILD-03 / D-73)`

    GPG-signed; no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && grep -q 'resolve_version_string' tools/build.lua && grep -q 'version_to_number_string' tools/build.lua && grep -q 'string.gsub.*__VERSION__\|gsub("__VERSION__"' tools/build.lua && grep -q 'DEV BUILD' tools/build.lua && grep -q 'version     = __VERSION__' src/webbanking_header.lua && grep -q '__VERSION__' .luacheckrc && luacheck . && lua tools/build.lua --verify 2>&1 | grep -qE 'OK: reproducible|reproducible' && head -2 dist/paypal-pos.lua | grep -q 'DEV BUILD' && GITHUB_REF_NAME=v1.0.0 lua tools/build.lua && grep -q 'version = 1.00,' dist/paypal-pos.lua && ! grep -q '__VERSION__' dist/paypal-pos.lua && GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify 2>&1 | grep -qE 'OK: reproducible|reproducible' && ./.luarocks/bin/busted spec/build_version_substitution_spec.lua 2>&1 | grep -qE '0 failures.*0 errors' && ./.luarocks/bin/busted spec/ 2>&1 | grep -qE '0 failures.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'resolve_version_string' tools/build.lua` >= 2 (definition + call)
    - `grep -c 'version_to_number_string' tools/build.lua` >= 2 (definition + call)
    - `grep -c 'gsub.*__VERSION__\|__VERSION__.*gsub' tools/build.lua` >= 1
    - `grep -c 'DEV BUILD' tools/build.lua` >= 1
    - `grep -q 'version     = __VERSION__' src/webbanking_header.lua` succeeds (with the exact 5-space gap preserved from the original)
    - `grep -q '__VERSION__' .luacheckrc` succeeds
    - `luacheck .` reports 0 errors and no new warnings (existing warnings preserved)
    - With no GITHUB_REF_NAME: `lua tools/build.lua --verify` reports reproducible; `head -2 dist/paypal-pos.lua | grep -q 'DEV BUILD'` succeeds; substituted version is `0.00`
    - With `GITHUB_REF_NAME=v1.0.0`: build succeeds; dist/ contains `version = 1.00,`; no `__VERSION__` literal remains; `--verify` reproducible
    - `busted spec/build_version_substitution_spec.lua` GREEN (all cases pass; the RED is flipped)
    - Full `busted spec/` GREEN end-to-end (no regression in any pre-existing spec)
    - Reproducible build SHA recorded in commit body (will differ from Phase-5 baseline by design)
    - Commit GPG-signed; no AI attribution; prefix `feat(06-01):`
  </acceptance_criteria>
  <done>
    `__VERSION__` substitution works under all 5-7 spec cases; DEV-BUILD banner appears for dev builds and is absent for tagged builds; reproducible per-tag; luacheck clean. Commit: `feat(06-01): __VERSION__ substitution from $GITHUB_REF_NAME (BUILD-03 / D-73)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: META-03 walker extension to scan markdown docs + dry-run audit existing files (DOC-04 / Pitfall 6)</name>
  <read_first>
    - spec/meta_no_tax_classification_spec.lua (whole file — observe the current `io.popen("ls src/*.lua")` enumeration pattern + `scan_file()` helper + `format_hits()` helper; the extension mirrors this for docs)
    - README.md (existing — current German engineering-draft; will become README.de.md in 06-02; AUDIT against 13-phrase list BEFORE extending walker)
    - CHANGELOG.md (existing — Phase-4 [0.2.0] content; AUDIT against 13-phrase list BEFORE extending walker — note line 57 mentions "steuerrechtliche Bewertung" which does NOT contain `steuerlich` as a substring; "GoBD-Bewertung" not "GoBD-konform"; verify both pass)
    - docs/adr/0001-amalgamator-design.md + 0003-sandbox-probe-results.md + 0004-finance-api-scope-and-fee-fallback.md + 0005-resilience-invariants.md (AUDIT each — ADR-0005 may reference "steuerlich" or similar in carve-out wording; dry-run grep before extending walker)
    - .planning/phases/06-release-polish/06-RESEARCH.md §Pitfall 6 (audit-before-extend warning + recommended Lua-only `it()` block tolerating absent files)
    - .planning/phases/06-release-polish/06-PATTERNS.md item 21 (recommended walker extension code with `DOC_TARGETS` static + dynamic enumeration)
  </read_first>
  <behavior>
    - Before extending the spec: dry-run grep `grep -E 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' README.md CHANGELOG.md docs/adr/*.md` MUST return zero lines. If any line matches, halt and surface the conflict to the user (do NOT silently revise existing committed content).
    - After extending the spec: a new `it("none of the documentation files contains a forbidden phrase", ...)` block runs and PASSES against the current tree (README.md + CHANGELOG.md + docs/adr/0001/0003/0004/0005.md are all clean; README.de.md / CONTRIBUTING.md absent → tolerated).
    - Existing `it("none of src/*.lua ...")` block continues to pass byte-identically (Phase-4 META-03 invariant preserved).
    - Existing `it("dist/paypal-pos.lua ...")` block continues to pass byte-identically.
    - `luacheck spec/meta_no_tax_classification_spec.lua` clean.
  </behavior>
  <files>spec/meta_no_tax_classification_spec.lua</files>
  <action>
    Step 1 — Pre-extension audit (run BEFORE editing the spec):
    ```bash
    grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' README.md CHANGELOG.md docs/adr/*.md
    ```
    Expected output: EMPTY (zero matches). If any line matches, STOP — surface the offending file/line to the user via the next response. Do NOT auto-edit existing committed German wording without explicit Yves approval (the wording was lektor-vetted in Phase 4 SUMMARY).

    Step 2 — Edit spec/meta_no_tax_classification_spec.lua per PATTERNS item 21:

    After the existing `FORBIDDEN` table (around line 41), BEFORE the `scan_file()` helper, add the DOC_TARGETS enumeration:

    ```lua
    -- DOC_TARGETS: documentation files Phase 6 extends META-03 coverage to.
    -- Static list covers the canonical entries; dynamic enumeration adds every
    -- ADR. Files MAY be absent during W1 (README.de.md / CONTRIBUTING.md land
    -- in W2); the it() block tolerates missing files.
    local DOC_TARGETS = {
      "README.md",
      "README.de.md",
      "CONTRIBUTING.md",
      "CHANGELOG.md",
    }
    do
      local handle = io.popen("ls docs/adr/*.md 2>/dev/null")
      if handle then
        for path in handle:lines() do
          DOC_TARGETS[#DOC_TARGETS + 1] = path
        end
        handle:close()
      end
    end
    ```

    Inside the existing `describe("META-03: forbidden tax-classification phrases (D-55)", function() ... end)` block, append a third `it(...)` block AFTER the existing `dist/paypal-pos.lua` block:

    ```lua
    it("none of the documentation files contains a forbidden phrase (DOC-04 / Phase 6 extension)", function()
      local scanned = 0
      for _, path in ipairs(DOC_TARGETS) do
        local f = io.open(path, "rb")
        if f then
          f:close()
          scanned = scanned + 1
          local hits = scan_file(path)
          assert.equals(0, #hits, format_hits(path, hits))
        end
        -- Files that do not yet exist (W1 stage; README.de.md / CONTRIBUTING.md
        -- land in W2) are skipped silently; the assertion only fires for files
        -- that physically exist.
      end
      assert.is_true(scanned >= 1,
        "expected at least one documentation target to exist; got " .. tostring(scanned))
    end)
    ```

    Step 3 — Verify locally:
    - `busted spec/meta_no_tax_classification_spec.lua` GREEN (all 3 it() blocks pass; the third reports `scanned >= 1` for the existing docs).
    - `luacheck spec/meta_no_tax_classification_spec.lua` clean.
    - Full `busted spec/` GREEN end-to-end.
    - `lua tools/build.lua --verify` reproducible (no src/ touched).

    Step 4 — Commit:
    `test(06-01): extend META-03 walker to README/CHANGELOG/CONTRIBUTING/ADR markdown (DOC-04 / Pitfall 6)`
    GPG-signed; no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && ! grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' README.md CHANGELOG.md docs/adr/*.md && grep -q 'DOC_TARGETS' spec/meta_no_tax_classification_spec.lua && grep -q 'README.md' spec/meta_no_tax_classification_spec.lua && grep -q 'CONTRIBUTING.md' spec/meta_no_tax_classification_spec.lua && grep -q 'docs/adr/' spec/meta_no_tax_classification_spec.lua && grep -c '^[[:space:]]*it(' spec/meta_no_tax_classification_spec.lua | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && luacheck spec/meta_no_tax_classification_spec.lua && ./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua 2>&1 | grep -qE '0 failures.*0 errors' && ./.luarocks/bin/busted spec/ 2>&1 | grep -qE '0 failures.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - Pre-extension audit (grep over README.md + CHANGELOG.md + docs/adr/*.md) returns ZERO forbidden-phrase matches
    - `grep -q 'DOC_TARGETS' spec/meta_no_tax_classification_spec.lua` succeeds
    - `grep -c '^[[:space:]]*it(' spec/meta_no_tax_classification_spec.lua` reports >= 3 (src + dist + new docs block)
    - `luacheck spec/meta_no_tax_classification_spec.lua` clean
    - `busted spec/meta_no_tax_classification_spec.lua` GREEN — all 3 it() blocks pass; the new docs block asserts `scanned >= 1` for the existing tree
    - Full `busted spec/` GREEN end-to-end; reproducible-build SHA unchanged from Task 2 (no src/ change in Task 3)
    - Commit GPG-signed; no AI attribution; prefix `test(06-01):`
  </acceptance_criteria>
  <done>
    META-03 walker scans README/CHANGELOG/CONTRIBUTING/docs/adr/*.md (tolerating absent files); current tree is audited clean; suite GREEN. The walker now protects every Wave-2 doc author from accidentally introducing a D-55 forbidden phrase. Commit: `test(06-01): extend META-03 walker to documentation markdown (DOC-04)`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire gitleaks + commit-lint + D-79 raw-print() grep into CI (CI-05 / D-78 / D-79 / SEC-02 hardening)</name>
  <read_first>
    - .github/workflows/ci.yml (current state — locate the egress allowlist step at lines 83-120 + the no-AI-attribution gate at lines 122-139; the new D-79 step lands BETWEEN them, the new gitleaks job lands as a SIBLING of the existing test job)
    - .planning/phases/06-release-polish/06-RESEARCH.md §4 (gitleaks workflow snippet verbatim + Pitfall 5 local-dry-run instruction) + §11 (commit-lint shell regex verbatim) + §12 (D-79 raw-print() grep snippet verbatim)
    - .planning/phases/06-release-polish/06-PATTERNS.md item 2 (ci.yml extension recipe) + item 3 (commit-lint.yml NEW file recipe)
    - Pitfall 5 in 06-RESEARCH.md (DRY-RUN gitleaks locally BEFORE pushing the PR to avoid fixture-blocking)
    - dist/paypal-pos.lua (sanity-check via `grep -c '^[^-]*print(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--'` — expected zero raw `print(` calls per D-27/Phase-5 closure; D-79 codifies this as a CI gate)
  </read_first>
  <action>
    Step 1 — Local gitleaks dry-run (Pitfall 5 mitigation). Install if not present (`brew install gitleaks` on macOS dev or skip on CI runners). Run:
    ```bash
    gitleaks detect --no-banner --source . --report-path /tmp/gitleaks-report.json --report-format json 2>&1 | tee /tmp/gitleaks-stdout.log
    ```
    Inspect the report. If ZERO leaks, no `.gitleaksignore` is needed — proceed.
    If 1+ leaks, all should be in spec/fixtures/auth or spec/fixtures/jwt (JWT-shaped test data) — author `.gitleaksignore` at repo root with one line per fingerprint (format `<fingerprint>:<path>:<line>` per RESEARCH §4); commit `.gitleaksignore` separately with message `ci(06-01): allowlist fixture-only gitleaks fingerprints (Pitfall 5)`. NEVER add a blanket `paths` allowlist.

    Step 2 — Edit .github/workflows/ci.yml. Two changes:

    Change A: Inside the existing `test` job, AFTER the "Egress allowlist gate" step (the step ending at line 120 with `echo "OK: only allowlisted hosts in dist/paypal-pos.lua (scheme + scheme-less)"`) and BEFORE the "No-AI-attribution gate" step, insert per RESEARCH §12:

    ```yaml
          - name: D-79 — no raw print() calls in shipped artifact (must use M_log)
            run: |
              set -e
              # Lines that look like print( calls but ARE NOT comment-prefixed.
              # The artifact has no obfuscated print; a simple grep -E suffices.
              BAD=$(grep -nE '^[^-]*print\(' dist/paypal-pos.lua \
                | grep -v '^[[:space:]]*--' \
                || true)
              if [ -n "${BAD}" ]; then
                echo "FAIL: raw print() calls in dist/paypal-pos.lua (must use M_log):"
                echo "${BAD}"
                exit 1
              fi
              echo "OK: no raw print() calls in dist/paypal-pos.lua"
    ```

    Change B: Add a new sibling job named `secret-scan` (job key `secret-scan`, name `gitleaks secret scan` — the name MUST match what 06-02's branch-protection script's CHECKS array references) per RESEARCH §4:

    ```yaml
      secret-scan:
        name: gitleaks secret scan
        runs-on: ubuntu-24.04
        permissions:
          contents: read
          pull-requests: write   # so gitleaks can comment on PRs
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0
          - name: gitleaks scan
            uses: gitleaks/gitleaks-action@v2
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              # No GITLEAKS_LICENSE needed — yves-vogl is a personal account.
    ```

    Place this job below the existing `badge:` job (or beside it — YAML sibling order is cosmetic). Confirm via `gh api users/yves-vogl --jq .type` if accessible (per RESEARCH §4 A5) that the account is `User` not `Organization`; if Organization, the workflow needs a free `GITLEAKS_LICENSE` repo secret — surface this finding to Yves rather than guessing.

    Step 3 — Create .github/workflows/commit-lint.yml per RESEARCH §11 verbatim. Single file, pull_request-only trigger, ~30 LoC:

    ```yaml
    name: Commit-message lint

    on:
      pull_request:
        types: [opened, synchronize, reopened]

    permissions:
      contents: read

    jobs:
      lint:
        name: Commit-message lint
        runs-on: ubuntu-24.04
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0
              ref: ${{ github.event.pull_request.head.sha }}

          - name: Validate commit messages
            run: |
              set -euo pipefail
              BASE="${{ github.event.pull_request.base.sha }}"
              HEAD="${{ github.event.pull_request.head.sha }}"
              REGEX='^(feat|fix|docs|test|refactor|chore|ci|build|perf|style|revert)(\([^)]+\))?: .+'
              FAIL=0
              while IFS= read -r SHA; do
                SUBJECT=$(git log --format=%s -n 1 "${SHA}")
                if ! echo "${SUBJECT}" | grep -qE "${REGEX}"; then
                  echo "FAIL ${SHA:0:7}: '${SUBJECT}'"
                  FAIL=1
                fi
              done < <(git rev-list "${BASE}..${HEAD}")
              if [ "${FAIL}" -eq 1 ]; then
                echo ""
                echo "Conventional Commits required: <type>(<scope>): <subject>"
                echo "Allowed types: feat fix docs test refactor chore ci build perf style revert"
                exit 1
              fi
              echo "OK: all commit subjects follow Conventional Commits"
    ```

    Job `name: Commit-message lint` MUST match exactly — 06-02's `tools/setup-branch-protection.sh` references this string.

    Step 4 — Verify locally:
    - `yamllint .github/workflows/ci.yml .github/workflows/commit-lint.yml` if yamllint available; else `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/commit-lint.yml'))"` to validate YAML parsability.
    - Locally run the D-79 grep against current dist/: `grep -nE '^[^-]*print\(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--'` → expected empty.
    - Local gitleaks scan clean (or `.gitleaksignore` covers any fixtures).
    - Reproducible build still green (`lua tools/build.lua --verify` — workflow files are outside the artifact manifest).
    - `busted spec/` still GREEN end-to-end.

    Step 5 — Commit. Two commits (or one combined per Yves' convention; the changes are tightly coupled so a single combined commit is acceptable):
    Option A (combined): `ci(06-01): add gitleaks + commit-lint + D-79 raw-print() grep (CI-05 / D-78 / D-79)`
    Option B (split — recommended if `.gitleaksignore` is needed): commit 1 = `.gitleaksignore` (per Step 1); commit 2 = `ci(06-01): add gitleaks + commit-lint + D-79 raw-print() grep`.
    GPG-signed; no AI attribution.

    Step 6 — Document in the SUMMARY: list the new CI checks BY NAME ("gitleaks secret scan", "Commit-message lint") so 06-02 can include them verbatim in the branch-protection script's CHECKS array.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && test -f .github/workflows/ci.yml && test -f .github/workflows/commit-lint.yml && grep -q 'gitleaks/gitleaks-action@v2' .github/workflows/ci.yml && grep -q 'gitleaks secret scan' .github/workflows/ci.yml && grep -q 'D-79.*no raw print\|no raw print() calls' .github/workflows/ci.yml && grep -q 'Commit-message lint' .github/workflows/commit-lint.yml && grep -qE 'feat\|fix\|docs\|test' .github/workflows/commit-lint.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/commit-lint.yml'))" && BAD=$(grep -nE '^[^-]*print\(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--' || true) && [ -z "$BAD" ] && lua tools/build.lua --verify 2>&1 | grep -qE 'OK: reproducible|reproducible' && ./.luarocks/bin/busted spec/ 2>&1 | grep -qE '0 failures.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/commit-lint.yml` succeeds
    - `grep -q 'gitleaks/gitleaks-action@v2' .github/workflows/ci.yml` succeeds (new secret-scan job present)
    - `grep -q 'gitleaks secret scan' .github/workflows/ci.yml` succeeds (job name matches 06-02's branch-protection CHECKS expectation)
    - `grep -q 'no raw print' .github/workflows/ci.yml` succeeds (D-79 step present)
    - `grep -q 'Commit-message lint' .github/workflows/commit-lint.yml` succeeds (job name matches)
    - `python3 -c "import yaml; yaml.safe_load(...)"` on both YAML files succeeds (no syntax error)
    - Local D-79 grep (`grep -nE '^[^-]*print\(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--'`) returns empty — the artifact is clean today (Phase-5 closure invariant; D-79 codifies it)
    - Local gitleaks dry-run is clean OR `.gitleaksignore` covers documented fingerprints; SUMMARY records the disposition
    - `lua tools/build.lua --verify` reproducible (workflow files outside the artifact manifest — no SHA change)
    - Full `busted spec/` GREEN end-to-end
    - Commits GPG-signed; no AI attribution; prefix `ci(06-01):`
  </acceptance_criteria>
  <done>
    ci.yml has gitleaks `secret-scan` job + D-79 raw-print() grep step; commit-lint.yml exists with pull_request trigger + Conventional Commits regex; local gitleaks dry-run clean (or fingerprints allowlisted); CI-05 + D-78 + D-79 + SEC-02-hardening all wired. The branch-protection CHECKS array in 06-02 references exactly: `Lint + tests + reproducible build` (existing test job), `gitleaks secret scan` (new), `Commit-message lint` (new).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML → GitHub Actions runner | YAML executed in sandboxed VM; secrets injected via `${{ secrets.* }}` only |
| `$GITHUB_REF_NAME` env → tools/build.lua substitution | Untrusted-shaped input; tag regex `^v%d+%.%d+` validates before substitution; non-matching → safe `0.00` fallback |
| dist/paypal-pos.lua bytes → D-79 grep + egress allowlist + META-03 walker | Three independent gates over the same shipped artifact |
| spec/fixtures/* → gitleaks scanner | Detector may flag legitimate JWT-shaped test data; `.gitleaksignore` allowlist per-fingerprint (NEVER per-path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | tools/build.lua `__VERSION__` substitution | mitigate | Tag regex `^v%d+%.%d+` validates env input; non-matching → `0.00` (safe fallback); the gsub is anchored to a literal token (no pattern injection) |
| T-06-01-02 | Spoofing | $GITHUB_REF_NAME could be set locally to spoof a release | accept | Local builds are dev-only; BUILD-04 requires a GPG-signed tag at release.yml job 1; spoofed env never reaches a published release |
| T-06-01-03 | Information Disclosure | gitleaks scanner false-positive on fixture JWT shapes | mitigate | Pitfall-5 local dry-run BEFORE PR push; `.gitleaksignore` per-fingerprint allowlist (never per-path) |
| T-06-01-04 | Tampering | D-79 raw-print() bypass | mitigate | CI grep `^[^-]*print\(` over dist/paypal-pos.lua fails the build; D-27 redactor invariant preserved |
| T-06-01-05 | Tampering | commit-lint regex bypass via crafted PR title | accept | The regex walks every commit in the PR range (not just title); a non-Conventional commit fails fast; even a crafted title doesn't bypass per-commit walking |
| T-06-01-06 | Information Disclosure | META-03 walker miss on a new doc file added later | mitigate | DOC_TARGETS includes dynamic `ls docs/adr/*.md`; any new ADR is auto-scanned; new static targets require a spec edit (acceptable per RESEARCH §Pitfall 6 audit-before-extend discipline) |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new shipped deps. Two new GitHub Actions (`gitleaks/gitleaks-action@v2`, already in `softprops/action-gh-release@v2` queue for 06-02) — both approved per 06-RESEARCH §Package Legitimacy Audit (gitleaks-action: official org, OpenSSF Scorecard recommended) |
</threat_model>

<verification>
After all four tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Task 1 + 2: BUILD-03 substitution working end-to-end
test -f spec/build_version_substitution_spec.lua
GITHUB_REF_NAME=v1.0.0 lua tools/build.lua
grep -q 'version = 1.00,' dist/paypal-pos.lua
! grep -q '__VERSION__' dist/paypal-pos.lua
GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify

# Dev-build banner (Pitfall 3)
env -u GITHUB_REF_NAME lua tools/build.lua
head -2 dist/paypal-pos.lua | grep -q 'DEV BUILD'

# luacheckrc + global registered
grep -q '__VERSION__' .luacheckrc
luacheck .

# Task 3: META-03 walker extension
grep -q 'DOC_TARGETS' spec/meta_no_tax_classification_spec.lua
./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua

# Audit: no forbidden phrase in any existing doc (Pitfall 6 pre-flight)
! grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' README.md CHANGELOG.md docs/adr/*.md

# Task 4: CI gates wired
test -f .github/workflows/commit-lint.yml
grep -q 'gitleaks/gitleaks-action@v2' .github/workflows/ci.yml
grep -q 'no raw print' .github/workflows/ci.yml
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/commit-lint.yml'))"

# D-79 dist clean (Phase-5 invariant)
BAD=$(grep -nE '^[^-]*print\(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--' || true)
[ -z "$BAD" ]

# Final reproducible-build check + full suite
GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(failure|0 failures)'
```
</verification>

<success_criteria>
- spec/build_version_substitution_spec.lua exists with >= 5 it() cases; committed RED then flipped GREEN per TDD
- tools/build.lua has resolve_version_string + version_to_number_string helpers + __VERSION__ gsub in HEADER_MOD branch + conditional DEV-BUILD banner for 0.00 case
- src/webbanking_header.lua line 24 contains `version     = __VERSION__,` (exact whitespace preserved)
- .luacheckrc globals[] includes `__VERSION__`; `luacheck .` clean
- `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` reproducible; dist contains `version = 1.00,` and no `__VERSION__` token
- `env -u GITHUB_REF_NAME lua tools/build.lua` produces a dev build whose 2nd line contains `DEV BUILD` (Pitfall 3 mitigation)
- spec/meta_no_tax_classification_spec.lua has DOC_TARGETS + 3rd it() block; current tree (README.md + CHANGELOG.md + docs/adr/0001/0003/0004/0005.md) passes the extended walker
- .github/workflows/ci.yml has the gitleaks `secret-scan` job (name `gitleaks secret scan`) + the D-79 raw-print() step (between egress and no-AI-attribution gates)
- .github/workflows/commit-lint.yml exists with `pull_request` trigger + Conventional Commits regex + job name `Commit-message lint`
- Local gitleaks dry-run clean (Pitfall 5 mitigation); `.gitleaksignore` created only if fixture-leaks surfaced
- Full `busted spec/` GREEN end-to-end (no regression; the existing 373/0 suite gains the new BUILD-03 cases + the META-03 doc-extension case)
- Reproducible-build SHA recorded in commit body + SUMMARY (will differ from Phase-5 baseline due to __VERSION__ + DEV banner; both differences documented as expected)
- 4 GPG-signed commits (or 5 if .gitleaksignore is split); all `(06-01)` scoped Conventional Commits; no AI attribution
- 06-02's branch-protection script (Plan 06-02 Task 2) can reference these CI check names verbatim: `Lint + tests + reproducible build`, `gitleaks secret scan`, `Commit-message lint`
</success_criteria>

<artifacts_this_phase_produces>
New files:
- `spec/build_version_substitution_spec.lua` — BUILD-03 verification (5-7 it() cases)
- `.github/workflows/commit-lint.yml` — pull_request-only Conventional Commits regex check (D-78)
- `.gitleaksignore` (CONDITIONAL — only if Pitfall-5 dry-run flags fixtures)

Modified files:
- `tools/build.lua` — resolve_version_string + version_to_number_string + __VERSION__ gsub + conditional DEV-BUILD banner
- `src/webbanking_header.lua` — line 24 `version = 0.00,` → `version = __VERSION__,`
- `.luacheckrc` — globals[] adds `__VERSION__`
- `spec/meta_no_tax_classification_spec.lua` — DOC_TARGETS + 3rd it() block
- `.github/workflows/ci.yml` — D-79 raw-print() step (inside test job) + secret-scan job (sibling of test/badge)

New CI checks (referenced by 06-02 branch-protection script):
- `Lint + tests + reproducible build` (pre-existing — extended with D-79 step)
- `gitleaks secret scan` (NEW)
- `Commit-message lint` (NEW)

Reproducible-build SHA delta from Phase 5 baseline (`5dbcb8e…`):
- DIFFERS by design — header now substitutes `__VERSION__` (dev → `0.00`; tagged → `<major>.<minor>`); plus optional DEV-BUILD banner line for dev builds
- DETERMINISTIC per-tag — two consecutive `GITHUB_REF_NAME=v1.0.0 lua tools/build.lua --verify` invocations produce byte-identical dist/paypal-pos.lua
- Documented in 06-01-SUMMARY.md

Files NOT modified (Phase-2/3/4/5 surface preservation):
- Any src/*.lua except webbanking_header.lua
- Any spec/*.lua except build_version_substitution_spec.lua (NEW) and meta_no_tax_classification_spec.lua (extension only — existing assertions unchanged)
- tools/manifest.txt (module ordering unchanged)
- tools/probe.lua (Q9 block from Plan 05-01 untouched)
- All four ADRs (0001/0003/0004/0005 unchanged — 4 NEW ADRs land in 06-02)
- LICENSE / SECURITY.md / dependabot.yml / scorecard.yml (already in place per RESEARCH §Already in repo)

Yves-blockers in this plan: NONE (Wave 1 is fully autonomous; CP-1..CP-5 are post-merge or in 06-02/06-03)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-release-polish/06-01-SUMMARY.md` summarising:
- BUILD-03 substitution: 5-7 spec cases GREEN; reproducible per-tag; DEV-BUILD banner for dev builds
- META-03 walker: extended to README/CHANGELOG/CONTRIBUTING/ADRs; current tree audited zero hits
- CI extensions: gitleaks job (name `gitleaks secret scan`); commit-lint workflow (name `Commit-message lint`); D-79 raw-print() step added to test job
- Pitfall mitigations applied: 3 (DEV-BUILD banner), 5 (local gitleaks dry-run), 6 (pre-extension META-03 audit)
- Local gitleaks dry-run disposition (clean OR `.gitleaksignore` fingerprints listed)
- Reproducible-build SHA before vs after this plan (Phase-5 `5dbcb8e…` → new SHA); explain `__VERSION__` + DEV-BUILD delta is expected
- 4 (or 5) GPG-signed commits landed under `(06-01)` scope
- CI check names for 06-02 branch-protection CHECKS array: `Lint + tests + reproducible build`, `gitleaks secret scan`, `Commit-message lint`
- Plan 06-02 unblocked (release.yml + admin scripts + README split + ADRs)
</output>
