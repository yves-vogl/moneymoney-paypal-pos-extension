---
phase: 06.1-supply-chain-scorecard-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/workflows/scorecard.yml
  - .github/workflows/commit-lint.yml
autonomous: true
requirements: [SEC-06, SEC-07]
tags: [wave-1, ci, sha-pinning, persist-credentials, token-permissions, g-04, g-05, g-06, g-09, g-10]

must_haves:
  truths:
    - "Every `uses:` reference across all 4 workflows is pinned to a 40-char commit SHA followed by a `# vX.Y.Z` trailing comment (closes SEC-06, G-05). RESEARCH §2's resolved-SHA table is the starting point but every SHA is re-resolved at execution time via `gh api repos/<owner>/<repo>/git/ref/tags/<tag>` because Dependabot may have advanced the major-version pointer between research date (2026-06-23) and execution."
    - "Every `actions/checkout` step (5 of 6 occurrences — the scorecard.yml one is already correct) gains `with.persist-credentials: false` (closes G-06, SEC-07 partial). The scorecard.yml step is left untouched."
    - "ci.yml gains an explicit top-level `permissions: read-all` block immediately after the `env:` block (closes SEC-07 acceptance bar `every workflow declares permissions: read-all at top level`). The existing per-job `permissions:` blocks stay byte-identical and remain authoritative for write-scope escalation."
    - "commit-lint.yml's `lint` job gains an explicit per-job `permissions: { contents: read }` block (currently inherits from the workflow-level `contents: read`; making it explicit aligns with SEC-07 least-privilege textual posture)."
    - "ci.yml gains a new `test` job step `SEC-06 gate — every uses: reference is SHA-pinned` (per RESEARCH §2 acceptance command) that walks `.github/workflows/*.yml` and fails if any `uses:` line references a floating tag instead of a 40-hex SHA. This is the corollary gate to SEC-06 itself — protects against regressions when a new step adds `@vX` (PATTERNS §3 row 4)."
    - "ci.yml's existing D-79 raw-print() gate at lines 122-144 is EXTENDED (G-04) to also catch `io.write(` and obvious aliasing patterns (`local p = print`, `local p = io.write`); the existing M_log emission sentinel (`-- D-79-allowed:`) continues to suppress the legitimate call. The grep is one extra `-E` alternation; no separate step."
    - "release.yml line 139 `awk '{print $NF}' | sed 's/%//' | cut -d. -f1` is replaced by a single `awk` invocation (G-09 anti-pattern, PATTERNS §3 row 2). ci.yml lines 55-56 use the same anti-pattern — fix BOTH in this plan so they cannot drift. The replacement: `awk '/^Total/ { gsub(\"%\", \"\"); print int($NF); exit }'`. The shell-interpolation concern in G-09 is moot once `$NF` is processed entirely inside awk."
    - "release.yml `publish` job (line 238) `needs:` is verified to transitively depend on `verify-signed-tag` (currently chained via `build-test-coverage-repro` which `needs: verify-signed-tag` on line 103). Defense-in-depth per G-10: make the dependency explicit by adding `verify-signed-tag` to publish's `needs:` array directly (so a future refactor that drops the middle dependency does not silently let unsigned tags publish)."
    - "release.yml's `verify-signed-tag` job checkout at line 41 needs `fetch-depth: 0` PRESERVED (required for `git verify-tag` to see the tag object) AND gains `persist-credentials: false` — RESEARCH §3 verified `git verify-tag` operates on local objects only and does not need persisted credentials."
    - "ci.yml's `secret-scan` job checkout at line 244 gains `persist-credentials: false`. RESEARCH §3 verified the gitleaks-action uses `env.GITHUB_TOKEN` for PR commenting, not git-stored credentials — so dropping persisted credentials does not break PR commenting."
    - "Open Dependabot PRs #2, #3, #4, #12 (per RESEARCH §10 PR-1 task list) are CLOSED MANUALLY by Yves after the PR-1 merge supersedes them. The plan does NOT auto-close PRs — Yves closes via `gh pr close` after verifying the PR-1 commit pinned the same targets."
    - "Reproducible-build SHA on `dist/paypal-pos.lua` is UNCHANGED — no `src/*.lua` edits, no `tools/build.lua` edits, no spec edits. The plan touches only `.github/**`. Both `lua tools/build.lua --verify` (local) and the CI's reproducible-build-diff gate must remain green at commit boundaries."
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no AI/Claude attribution. Conventional Commits: `ci(06.1): pin actions, persist-credentials, top-level read-all`."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "SHA-pinned uses: × 5 (lines 23, 28, 33, 244, 248); top-level permissions: read-all; persist-credentials: false × 2 (lines 23, 244); extended D-79 print-gate with io.write + aliasing; SEC-06 grep gate as new test step; G-09 awk-safety fix at lines 55-56"
      contains: "permissions: read-all"
      min_lines: 250
    - path: ".github/workflows/release.yml"
      provides: "SHA-pinned uses: × 7 (lines 41, 109, 114, 119, 228, 246, 252); persist-credentials: false × 2 (lines 41, 109); G-09 awk fix at line 139; G-10 explicit verify-signed-tag in publish.needs"
      contains: "needs:"
      min_lines: 260
    - path: ".github/workflows/scorecard.yml"
      provides: "SHA-pinned uses: × 4 (lines 33, 38, 48, 55); persist-credentials: false untouched (already present line 35)"
      contains: "ossf/scorecard-action@"
      min_lines: 55
    - path: ".github/workflows/commit-lint.yml"
      provides: "SHA-pinned uses: × 1 (line 23); persist-credentials: false on checkout; explicit per-job permissions: { contents: read }"
      contains: "actions/checkout@"
  key_links:
    - from: ".github/workflows/ci.yml SEC-06 grep gate"
      to: "All .github/workflows/*.yml files"
      via: "grep -EnR 'uses:[[:space:]]*[^#@[:space:]]+@(v[0-9]+(\\.[0-9]+)*|main|master|[a-zA-Z]+)' filtered against the 40-hex-char SHA pattern"
      pattern: "uses:.*@[a-f0-9]{40}"
    - from: "release.yml publish job"
      to: "verify-signed-tag job"
      via: "explicit needs: [verify-signed-tag, build-test-coverage-repro] (G-10 defense in depth)"
      pattern: "needs:"
    - from: "ci.yml D-79 extended print-gate"
      to: "dist/paypal-pos.lua emission lines"
      via: "grep -E '^[^-]*(print|io\\.write)\\(' filtered against the D-79-allowed sentinel"
      pattern: "D-79-allowed"
---

<objective>
SHA-pin every GitHub Action reference across all four workflows; add `persist-credentials: false` to every checkout that does not need persisted credentials; add explicit top-level `permissions: read-all` to ci.yml and explicit per-job `permissions: { contents: read }` to commit-lint.yml; extend the D-79 print-gate to catch `io.write` and aliasing bypasses; replace the brittle `awk | sed | cut` anti-pattern at ci.yml:55-56 and release.yml:139 with a single safe `awk` invocation; tighten release.yml `publish.needs:` to explicitly depend on `verify-signed-tag` for defense in depth; and add a CI gate that prevents future floating-tag regressions.

Purpose: Closes SEC-06 (Pinned-Dependencies → 10/10), SEC-07 (Token-Permissions → 10/10), G-04 (raw-print gate completeness), G-05 (SHA-pinning), G-06 (persist-credentials), G-09 (awk safety), G-10 (publish needs defense in depth). This is the foundation PR — every subsequent plan in Phase 6.1 inherits the SHA-pinned SHA + persist-credentials patterns.

Output: Four modified workflow files; zero new files; zero changes to `src/**`, `tools/**`, or `spec/**`. Reproducible-build SHA invariant: post-merge `dist/paypal-pos.lua` SHA matches the pre-merge baseline byte-for-byte.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-CONTEXT.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-RESEARCH.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.github/workflows/scorecard.yml
@.github/workflows/commit-lint.yml
</context>

<artifacts_this_phase_produces>
Files modified by this plan (no new files):

- `.github/workflows/ci.yml` — 5 SHA pins + 2 persist-credentials + top-level permissions + D-79 extension + SEC-06 grep gate + G-09 awk fix at lines 55-56
- `.github/workflows/release.yml` — 7 SHA pins + 2 persist-credentials + G-09 awk fix at line 139 + G-10 publish needs hardening
- `.github/workflows/scorecard.yml` — 4 SHA pins (persist-credentials already correct)
- `.github/workflows/commit-lint.yml` — 1 SHA pin + persist-credentials + explicit per-job permissions

Files explicitly NOT touched (blacklist):
- `src/*.lua` — reproducible-build SHA invariant
- `tools/build.lua` — reproducible-build SHA invariant
- `spec/**` — no behaviour change
- `.gitleaks.toml` (does not exist yet — Plan 02 territory)
- `.github/dependabot.yml` — Plan 05 territory
- `.github/workflows/sast.yml` (does not exist yet — Plan 03 territory)
- `.github/workflows/docs.yml` (does not exist yet — Plan 06 territory)
- `tools/setup-branch-protection.sh` — Plan 04 territory
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-resolve SHA targets and run pin-github-action on all four workflows</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/commit-lint.yml</files>
  <action>
    Re-resolve the resolved-SHA table from RESEARCH §2 (it was captured 2026-06-23 — some SHAs may have advanced). For each `uses:` reference:

    1. Read the table at RESEARCH §2 lines 135-145 to know the action + currently-floating tag.
    2. Re-resolve the SHA via `gh api repos/<owner>/<repo>/git/ref/tags/<tag> --jq '.object.sha'` (per RESEARCH Pitfall 1 — always resolve against the parent repo, never a sub-path).
    3. Apply the pin manually with the resolved SHA + `# vX.Y.Z` trailing comment. Do NOT auto-install `pin-github-action` via `npx` — the project bans `npx --yes` per memory `feedback_orchestrator_role`. Either (a) install once with `npm install -g pin-github-action@2` if Yves has a global node toolchain, or (b) hand-edit each `uses:` line with the resolved SHA. Determinism matters more than the tool (per D-36 Claude's Discretion).

    The 17 `uses:` references to pin (per RESEARCH §2 + verified via `grep -nE "uses:" .github/workflows/*.yml`):

    | File:Line | Currently | Target |
    |---|---|---|
    | ci.yml:23 | `actions/checkout@v4` | `actions/checkout@<sha> # v4.2.2` (or v7.0.0 if closing PR #12 in same PR) |
    | ci.yml:28 | `leafo/gh-actions-lua@v13` | `leafo/gh-actions-lua@6919171ccf181b826f44b9bca76307b577217377 # v13.0.0` |
    | ci.yml:33 | `leafo/gh-actions-luarocks@v6.1.0` | `leafo/gh-actions-luarocks@35d062def313a7699a0d513e996bcf4352f05389 # v6.1.0` |
    | ci.yml:244 | `actions/checkout@v4` | same as line 23 (must be identical SHA) |
    | ci.yml:248 | `gitleaks/gitleaks-action@v2` | resolve via `gh api repos/gitleaks/gitleaks-action/git/ref/tags/v2.x.x` for current v2.x |
    | release.yml:41 | `actions/checkout@v4` | same checkout SHA as ci.yml:23 |
    | release.yml:109 | `actions/checkout@v4` | same |
    | release.yml:114 | `leafo/gh-actions-lua@v13` | same as ci.yml:28 |
    | release.yml:119 | `leafo/gh-actions-luarocks@v6.1.0` | same as ci.yml:33 |
    | release.yml:228 | `actions/upload-artifact@v4` | resolve via `gh api repos/actions/upload-artifact/git/ref/tags/v4.x.x` (per RESEARCH §2 PR #2 target is v7.0.0 — Yves' call whether to bump major in this PR; recommend stick with v4.x to minimise blast radius) |
    | release.yml:246 | `actions/download-artifact@v4` | resolve current v4.x; must match upload-artifact major (currently both v4) |
    | release.yml:252 | `softprops/action-gh-release@v2` | resolve current v2.x |
    | scorecard.yml:33 | `actions/checkout@v4` | same checkout SHA as everywhere else |
    | scorecard.yml:38 | `ossf/scorecard-action@v2.4.0` | `ossf/scorecard-action@99c09fe975337306107572b4fdf4db224cf8e2f2 # v2.4.3` (RESEARCH §2 — closes PR #3 target) |
    | scorecard.yml:48 | `actions/upload-artifact@v4` | same SHA as release.yml:228 |
    | scorecard.yml:55 | `github/codeql-action/upload-sarif@v3` | resolve via `gh api repos/github/codeql-action/git/ref/tags/v3.x.x` for current v3.x (per RESEARCH Pitfall 1: pin to the PARENT codeql-action repo SHA, not the upload-sarif sub-path) |
    | commit-lint.yml:23 | `actions/checkout@v4` | same checkout SHA |

    Per PATTERNS §3 row 4: every `uses:` line MUST end with a 40-hex SHA followed by space-hash-vX.Y.Z. No exceptions.
  </action>
  <verify>
    <automated>
      # Acceptance gate (this exact grep is what Task 2 wires into ci.yml as a permanent CI step):
      set -e
      BAD=$(grep -EnR "uses:[[:space:]]*[^#@[:space:]]+@(v[0-9]+(\.[0-9]+)*|main|master|[a-zA-Z]+)" .github/workflows/ \
        | grep -v "@[a-f0-9]\{40\}" || true)
      if [ -n "$BAD" ]; then
        echo "FAIL: unpinned actions detected:"
        echo "$BAD"
        exit 1
      fi
      # Count check — there must be 17 SHA-pinned uses: references.
      COUNT=$(grep -EhR "uses:[[:space:]]*[^[:space:]]+@[a-f0-9]{40}" .github/workflows/ | wc -l | tr -d ' ')
      if [ "$COUNT" -ne 17 ]; then
        echo "FAIL: expected 17 SHA-pinned uses: refs, found $COUNT"
        exit 1
      fi
      echo "OK: all 17 uses: refs are SHA-pinned with vX.Y.Z trailing comment"
    </automated>
  </verify>
  <done>
    All 17 `uses:` references across the four workflows are 40-hex-SHA + `# vX.Y.Z` comment. Grep gate above returns exit 0 with `OK: all 17 uses: refs ...`. No `src/**`, `tools/**`, or `spec/**` file is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add persist-credentials false, top-level permissions, explicit per-job permissions, SEC-06 grep gate, D-79 print-gate extension, G-09 awk fix, G-10 publish needs</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/commit-lint.yml</files>
  <action>
    All edits in this task are independent of the SHA-pinning task's specific SHAs (touches different YAML lines), so failures in re-resolution do not block this task. Apply the following surgical edits:

    Edit 2.1 — ci.yml top-level permissions (SEC-07 acceptance bar):
    - Insert an explicit `permissions: read-all` block immediately after the `env:` block (currently ends at line 10) and before the `jobs:` block at line 12. The block lives at workflow root, not inside any job. Per RESEARCH §3 line 199 diff sketch: two lines added (`permissions: read-all` + blank line).

    Edit 2.2 — ci.yml persist-credentials on test job checkout (G-06):
    - Line 23-25 currently:
      ```
            - name: Checkout
              uses: actions/checkout@<sha> # v4.x.y
              with:
                fetch-depth: 0
      ```
    - Append `persist-credentials: false` inside the `with:` block.

    Edit 2.3 — ci.yml persist-credentials on secret-scan job checkout (G-06):
    - Line 244-246 currently has the checkout with only `fetch-depth: 0`. Append `persist-credentials: false`. RESEARCH §3 verified gitleaks-action uses `env.GITHUB_TOKEN` for PR comments, not stored git credentials — safe.

    Edit 2.4 — ci.yml D-79 print-gate extension (G-04):
    - Line 135 currently: `BAD=$(grep -nE '^[^-]*print\(' dist/paypal-pos.lua \`
    - Change to: `BAD=$(grep -nE '^[^-]*(print|io\.write)\(' dist/paypal-pos.lua \`
    - The existing `grep -v '^[[:space:]]*--'` and `grep -v 'D-79-allowed'` filters in the pipeline stay byte-identical. The D-79-allowed sentinel in `src/log.lua` continues to suppress the legitimate emission line.
    - Also extend the `BAD=$(...)` to catch obvious aliasing: add a second pipeline step `| grep -v 'local p = print'` is the wrong approach (would whitelist alias declarations). Instead, add a separate parallel grep for `local[[:space:]]+[a-z_]+[[:space:]]*=[[:space:]]*(print|io\.write)` and union the results. Implementation: introduce two BAD_* vars and concat. Pseudocode:
      ```
      BAD_CALLS=$(grep -nE '^[^-]*(print|io\.write)\(' dist/paypal-pos.lua | grep -v '^[[:space:]]*--' | grep -v 'D-79-allowed' || true)
      BAD_ALIAS=$(grep -nE 'local[[:space:]]+[A-Za-z_][A-Za-z0-9_]*[[:space:]]*=[[:space:]]*(print|io\.write)[[:space:]]*$' dist/paypal-pos.lua | grep -v '^[[:space:]]*--' || true)
      BAD="${BAD_CALLS}${BAD_ALIAS:+\n$BAD_ALIAS}"
      ```
    - Update the FAIL message to mention "print/io.write/aliased" instead of just "print".

    Edit 2.5 — ci.yml SEC-06 corollary grep gate (new step in test job, after the existing no-AI-attribution gate at line 163):
    - Insert a new step `- name: SEC-06 — every uses: reference is SHA-pinned` that runs the acceptance grep from Task 1's verify block. Make it permanent so future PRs that add `@vX` floating tags get caught at PR time. Body identical to the verify block in Task 1.

    Edit 2.6 — ci.yml G-09 awk fix at lines 55-56:
    - Current shell-piped extraction:
      ```
      TOTAL_LINE=$(grep -E "^Total" luacov.report.out | tail -1)
      TOTAL_COV=$(echo "$TOTAL_LINE" | awk '{print $NF}' | sed 's/%//' | cut -d. -f1)
      ```
    - Replace with single awk invocation (PATTERNS §3 row 2):
      ```
      TOTAL_COV=$(awk '/^Total/ { gsub("%", "", $NF); split($NF, a, "."); print a[1]; exit }' luacov.report.out)
      ```
    - Preserve the empty-string guard immediately after (`if [ -z "$TOTAL_COV" ]; then ... fi`) byte-for-byte.

    Edit 2.7 — release.yml persist-credentials on verify-signed-tag checkout (G-06, line 41-44):
    - Current `with:` block has `fetch-depth: 0` + `ref: ${{ github.ref }}`. Append `persist-credentials: false`. RESEARCH §3 + Assumption A8 verified `git verify-tag` uses local objects only.

    Edit 2.8 — release.yml persist-credentials on build-test-coverage-repro checkout (G-06, line 109-111):
    - Current `with:` block has `fetch-depth: 0`. Append `persist-credentials: false`.

    Edit 2.9 — release.yml G-09 awk fix at line 139:
    - Current: `TOTAL_COV=$(echo "$TOTAL_LINE" | awk '{print $NF}' | sed 's/%//' | cut -d. -f1)`
    - Replace with the same single-awk pattern as Edit 2.6. Lines 138-139 (TOTAL_LINE + TOTAL_COV) collapse to one line:
      `TOTAL_COV=$(awk '/^Total/ { gsub("%", "", $NF); split($NF, a, "."); print a[1]; exit }' luacov.report.out)`
    - Preserve the empty-string guard byte-for-byte. Both ci.yml and release.yml fixes ship in the same commit so they cannot drift (per PATTERNS §3 row 2 "do this in both ... in the same PR — drifting them is worse than the current state").

    Edit 2.10 — release.yml G-10 publish needs hardening:
    - Line 238-240 currently:
      ```
        publish:
          name: Publish GitHub Release
          needs: build-test-coverage-repro
      ```
    - Change `needs: build-test-coverage-repro` to `needs: [verify-signed-tag, build-test-coverage-repro]`. Defense in depth: a future refactor that removes the middle dependency cannot silently let an unsigned tag publish.

    Edit 2.11 — commit-lint.yml explicit per-job permissions (SEC-07 textual posture):
    - The `lint` job at line 19-21 currently inherits from the workflow-level `contents: read` (line 15-16). Add an explicit per-job `permissions: { contents: read }` block before `steps:` for consistency with the other workflows' shape. Two lines added.

    Edit 2.12 — commit-lint.yml persist-credentials on checkout (G-06):
    - Line 23-26 currently has `with: fetch-depth: 0, ref: ${{ ... }}`. Append `persist-credentials: false`.
  </action>
  <verify>
    <automated>
      set -e

      # 2.1 — ci.yml top-level permissions: read-all present
      head -15 .github/workflows/ci.yml | grep -qE '^permissions:[[:space:]]*read-all$' \
        || { echo "FAIL: ci.yml top-level permissions: read-all missing"; exit 1; }

      # 2.2/2.3 — both ci.yml checkouts carry persist-credentials: false
      PC_COUNT=$(grep -c 'persist-credentials: false' .github/workflows/ci.yml)
      if [ "$PC_COUNT" -ne 2 ]; then
        echo "FAIL: ci.yml expects 2 persist-credentials: false, found $PC_COUNT"
        exit 1
      fi

      # 2.4 — D-79 gate references io.write
      grep -qE 'print\|io\\.write' .github/workflows/ci.yml \
        || grep -qE 'print\|io\.write' .github/workflows/ci.yml \
        || { echo "FAIL: ci.yml D-79 gate does not include io.write alternation"; exit 1; }

      # 2.5 — SEC-06 grep gate step exists in ci.yml
      grep -qE 'SEC-06.*SHA-pinned' .github/workflows/ci.yml \
        || { echo "FAIL: ci.yml SEC-06 grep gate step missing"; exit 1; }

      # 2.6 + 2.9 — single-awk pattern present in both files; old anti-pattern absent
      # Filter out comment lines so the prose CLAUDE-noise of inline rationale doesn't trip the grep.
      BAD_PIPE_CI=$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -cE "awk '\\{print \\\$NF\\}' \\| sed 's/%//' \\| cut -d\\. -f1" || true)
      BAD_PIPE_REL=$(grep -v '^[[:space:]]*#' .github/workflows/release.yml | grep -cE "awk '\\{print \\\$NF\\}' \\| sed 's/%//' \\| cut -d\\. -f1" || true)
      if [ "${BAD_PIPE_CI:-0}" != "0" ] || [ "${BAD_PIPE_REL:-0}" != "0" ]; then
        echo "FAIL: old awk|sed|cut anti-pattern still present (ci=$BAD_PIPE_CI release=$BAD_PIPE_REL)"
        exit 1
      fi
      # New pattern present in both
      grep -qE 'gsub\("%"' .github/workflows/ci.yml \
        || { echo "FAIL: ci.yml awk gsub pattern missing"; exit 1; }
      grep -qE 'gsub\("%"' .github/workflows/release.yml \
        || { echo "FAIL: release.yml awk gsub pattern missing"; exit 1; }

      # 2.7/2.8 — both release.yml checkouts carry persist-credentials: false
      PC_REL=$(grep -c 'persist-credentials: false' .github/workflows/release.yml)
      if [ "$PC_REL" -ne 2 ]; then
        echo "FAIL: release.yml expects 2 persist-credentials: false, found $PC_REL"
        exit 1
      fi

      # 2.10 — publish job needs both verify-signed-tag and build-test-coverage-repro
      awk '/^  publish:/,/^  [a-z]+:/' .github/workflows/release.yml \
        | grep -qE 'needs:.*verify-signed-tag.*build-test-coverage-repro' \
        || awk '/^  publish:/,/^  [a-z]+:/' .github/workflows/release.yml | grep -qE 'needs:.*\[.*verify-signed-tag.*\]' \
        || { echo "FAIL: release.yml publish.needs does not explicitly include verify-signed-tag"; exit 1; }

      # 2.11 — commit-lint.yml lint job has explicit per-job permissions
      awk '/^  lint:/,/steps:/' .github/workflows/commit-lint.yml | grep -qE 'permissions:' \
        || { echo "FAIL: commit-lint.yml lint job missing explicit per-job permissions"; exit 1; }

      # 2.12 — commit-lint.yml checkout carries persist-credentials: false
      grep -q 'persist-credentials: false' .github/workflows/commit-lint.yml \
        || { echo "FAIL: commit-lint.yml checkout missing persist-credentials: false"; exit 1; }

      # scorecard.yml — persist-credentials was already there; verify no regression
      grep -q 'persist-credentials: false' .github/workflows/scorecard.yml \
        || { echo "FAIL: scorecard.yml lost persist-credentials: false"; exit 1; }

      # GLOBAL — 5 of 6 checkouts have persist-credentials: false (badge job has no checkout — it does git init in mktemp)
      TOTAL_PC=$(grep -hc 'persist-credentials: false' .github/workflows/*.yml | awk '{s+=$1} END {print s}')
      if [ "$TOTAL_PC" -ne 6 ]; then
        echo "FAIL: expected 6 persist-credentials: false (ci x2, release x2, scorecard x1, commit-lint x1), found $TOTAL_PC"
        exit 1
      fi

      echo "OK: Task 2 edits verified"
    </automated>
  </verify>
  <done>
    All 11 sub-edits applied and verified. ci.yml has explicit top-level `permissions: read-all`, the D-79 gate catches both `print(` and `io.write(` and obvious aliasing, the SEC-06 corollary grep gate is a permanent CI step, the awk anti-pattern is replaced byte-identically in both ci.yml and release.yml, the release.yml publish job explicitly depends on `verify-signed-tag`, and every checkout that does not need credentials carries `persist-credentials: false`. Reproducible-build SHA on `dist/paypal-pos.lua` unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runtime → maintainer code | A compromised third-party action (e.g. tj-actions/changed-files 2025-style attack) can exfiltrate secrets or inject malicious commits if pinned by floating tag. |
| GitHub Actions runtime → repo `.git/config` | `actions/checkout` persists an authenticated git token in `.git/config` by default; any subsequent step in the same job can exfiltrate it. |
| Workflow YAML parser → CI gate enforcement | The CI gates (DEBUG=false, egress allowlist, no-AI-attribution, SEC-06 SHA-pinning) are themselves the trust boundary — a regression in the gate is a regression in the policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Tampering | All `uses:` references in `.github/workflows/*.yml` | mitigate | SHA-pin every reference + add a CI grep gate (Task 2 Edit 2.5) that fails any future PR introducing a floating tag. Closes G-05, SEC-06. |
| T-06.1-02 | Information Disclosure | `actions/checkout` persisted git credentials | mitigate | Add `persist-credentials: false` to every checkout except the badge-job push (which uses `https://x-access-token:${GITHUB_TOKEN}@` URL injection, never touches `.git/config`). Closes G-06. |
| T-06.1-03 | Elevation of Privilege | Workflow tokens with broader scope than the job requires | mitigate | Top-level `permissions: read-all` on ci.yml + explicit per-job `permissions: { contents: read }` on commit-lint.yml. Closes SEC-07. |
| T-06.1-04 | Tampering | release.yml publish job dependency chain | mitigate | Explicit `needs: [verify-signed-tag, build-test-coverage-repro]` instead of chained-via-middle (G-10). Defense in depth: a future refactor that removes the middle dependency cannot silently bypass tag-signature verification. |
| T-06.1-05 | Tampering | Shell parsing of luacov.report.out total line | mitigate | Single `awk` invocation replaces brittle `awk \| sed \| cut` pipeline in both ci.yml and release.yml (G-09). Eliminates the theoretical shell-meta-injection vector when `$NF` is processed entirely inside awk. |
| T-06.1-06 | Tampering | D-79 raw-print gate bypass via `io.write` or aliasing | mitigate | Extend the grep alternation in the D-79 gate (G-04). Aliasing (`local p = print`) caught by a second parallel grep. Suppression sentinel `-- D-79-allowed:` unchanged. |
| T-06.1-SC | Tampering | npm `pin-github-action` install (if used) | accept | Optional tooling, run once interactively. If installed via `npm install -g pin-github-action`, verify on npmjs.com that author is `mheap` and version is current 2.x. The hand-edited fallback (resolve via `gh api`) requires no third-party tool installation and is the documented safe path. |
</threat_model>

<verification>
1. `git diff --stat` shows only the four workflow files modified; zero changes under `src/`, `tools/`, `spec/`, `docs/`.
2. Full CI gate replays locally:
   ```
   for f in .github/workflows/*.yml; do
     grep -EnR "uses:[[:space:]]*[^#@[:space:]]+@(v[0-9]+|main|master|[a-zA-Z]+)" "$f" | grep -v "@[a-f0-9]\{40\}" && exit 1 || true
   done
   echo "OK: SHA-pin gate green"
   ```
3. `lua tools/build.lua && lua tools/build.lua --verify` exits 0 and `sha256sum dist/paypal-pos.lua` matches the pre-merge baseline byte-for-byte (zero regression on the reproducible-build invariant).
4. Push branch, watch CI run — all 3 required checks (`Lint + tests + reproducible build`, `gitleaks secret scan`, `Commit-message lint`) green.
5. Manually close Dependabot PRs #2, #3, #4, #12 via `gh pr close <number> --comment "Superseded by phase-6.1/scorecard-hardening — see 06.1-01-PLAN"`.
</verification>

<success_criteria>
- 17 `uses:` references SHA-pinned + `# vX.Y.Z` comment across all 4 workflows
- 6 `persist-credentials: false` declarations (ci.yml ×2, release.yml ×2, scorecard.yml ×1 pre-existing, commit-lint.yml ×1)
- 1 explicit `permissions: read-all` at ci.yml top level
- 1 explicit per-job `permissions: { contents: read }` in commit-lint.yml lint job
- SEC-06 corollary grep gate exists as a permanent step in ci.yml's test job
- D-79 print-gate catches `io.write(` and aliasing patterns
- release.yml + ci.yml awk extraction is one `awk` invocation, not `awk | sed | cut`
- release.yml publish job explicitly `needs: [verify-signed-tag, build-test-coverage-repro]`
- All CI gates pass on PR
- Dependabot PRs #2/#3/#4/#12 closed by Yves with reference to this PR
- `dist/paypal-pos.lua` SHA256 unchanged from pre-merge baseline
</success_criteria>

<output>
Create `.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-01-SUMMARY.md` when done.
</output>

<pitfalls>
- **Pitfall 1 — SHA-pinning sub-path actions (RESEARCH Pitfall 1):** `github/codeql-action/upload-sarif@<sha>` must pin to the PARENT `github/codeql-action` repo's SHA, never the sub-path's. `gh api repos/github/codeql-action/git/ref/tags/<tag>` is the correct resolver.
- **Pitfall 2 — Dependabot PR conflict (RESEARCH Pitfall 3):** Dependabot PRs #2/#3/#4/#12 already pin some of these SHAs. After PR-1 merges, manually close them with `gh pr close <N> --comment "Superseded by 06.1-01-PLAN"`. Do NOT merge them first (their commit messages do not carry the `ci(06.1)` Conventional-Commits scope).
- **Pitfall 3 — Tag-vs-SHA drift in a follow-up step (PATTERNS §3 row 4):** A contributor adding a new step with `@vX` after this PR merges would silently regress the Pinned-Dependencies score. The SEC-06 grep gate added in Edit 2.5 catches this at PR time.
- **Pitfall 4 — awk fix breaking the `[ -z "$TOTAL_COV" ]` guard:** The empty-string guard remains correct because the new `awk` invocation prints exactly one integer or nothing (when `/^Total/` does not match). Preserve the guard byte-for-byte.
- **Pitfall 5 — persist-credentials breaking a downstream `git push`:** Verified per RESEARCH Pitfall 4: the badge job at ci.yml:209-227 uses `https://x-access-token:${GITHUB_TOKEN}@` URL injection, not stored credentials. No other job pushes. Safe.
- **Pitfall 6 — Top-level `permissions: read-all` on release.yml accidentally upgraded to `write-all`:** Do NOT touch release.yml's top-level `permissions: contents: read` (it is already correct per RESEARCH §3 and Pitfall 5 — keep it minimal explicit, not `read-all`).
- **Pitfall 7 — D-79 sentinel string drift:** The existing `-- D-79-allowed:` sentinel in `src/log.lua` is load-bearing for the extended gate. Do NOT touch `src/log.lua` — only the gate grep changes.
- **Pitfall 8 — commit-lint.yml explicit permissions accidentally widened:** The new per-job block must be exactly `permissions: { contents: read }` (single scope, read-only). Do not include any write scopes.
</pitfalls>

<closes>
- SEC-06 (Pinned-Dependencies → 10/10 expected)
- SEC-07 (Token-Permissions → 10/10 expected)
- G-04 (D-79 print-gate bypass paths — io.write + aliasing covered)
- G-05 (SHA-pin actions)
- G-06 (persist-credentials false on every applicable checkout)
- G-09 (release.yml awk shell-interpolation safety)
- G-10 (release.yml publish.needs defense in depth)
</closes>
