---
phase: 02-authenticated-network-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spec/helpers/mm_mocks.lua
  - spec/helpers/fixtures.lua
  - spec/fixtures/auth/token_ok.json
  - spec/fixtures/auth/token_invalid_grant.json
  - spec/fixtures/auth/users_self_ok.json
  - spec/fixtures/auth/users_self_unauthorized.json
  - spec/fixtures/auth/token_rate_limited.json
  - spec/fixtures/auth/network_timeout.json
  - spec/auth_spec.lua
  - spec/http_spec.lua
  - spec/errors_spec.lua
autonomous: true
requirements: [AUTH-02, AUTH-04, AUTH-05, AUTH-06, SEC-03, ACCT-02, ACCT-04]
requirements_addressed: [AUTH-02, AUTH-04, AUTH-05, AUTH-06, SEC-03, ACCT-02, ACCT-04]
tags: [test-infra, fixtures, mocks, base64, lua]

must_haves:
  truths:
    - "Hand-rolled JSON fixtures exist for every Phase-2 OAuth response shape so specs can queue them via Mocks.push_response (per D-28)"
    - "MM.base64decode mock is a real base64 decoder (not identity) so _decode_jwt_payload tests can construct genuine base64url JWT segments (per Risk R-6)"
    - "Mocks.push_response accepts an optional status field for test ergonomics (production code derives status from body per Risk R-1)"
    - "Spec scaffolds for auth_spec.lua, http_spec.lua, errors_spec.lua exist with pending tests for every Wave 1+ test command listed in 02-VALIDATION.md per-task verification map"
    - "Fixtures.load(\"auth/token_ok\") resolves to spec/fixtures/auth/token_ok.json without code edits to fixtures.lua (Wave 0 verifies the existing concat works)"
    - "API key never appears in any spec fixture content (only synthesized JWT shapes that look real but carry no real merchant credentials)"
    - "Egress allowlist {oauth.zettle.com, purchase.izettle.com, finance.izettle.com} not violated — fixtures contain no other hosts"
    - "All user-facing strings remain via M_i18n.t — fixtures are server-side payloads only"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in commit message, code, or comments"
  artifacts:
    - path: "spec/fixtures/auth/token_ok.json"
      provides: "Successful token-endpoint JSON response shape (access_token, expires_in, token_type)"
      contains: "access_token"
    - path: "spec/fixtures/auth/token_invalid_grant.json"
      provides: "Bad-assertion error response (error=invalid_grant, error_description)"
      contains: "invalid_grant"
    - path: "spec/fixtures/auth/users_self_ok.json"
      provides: "Successful merchant profile (uuid, organizationUuid, publicName)"
      contains: "organizationUuid"
    - path: "spec/fixtures/auth/users_self_unauthorized.json"
      provides: "Scope-failure profile error (error=unauthorized)"
      contains: "unauthorized"
    - path: "spec/fixtures/auth/token_rate_limited.json"
      provides: "Rate-limit response body for 429 inference test"
      contains: "rate_limit"
    - path: "spec/fixtures/auth/network_timeout.json"
      provides: "Empty body / network anomaly signal"
      min_lines: 0
    - path: "spec/helpers/mm_mocks.lua"
      provides: "Real MM.base64decode implementation + optional status field on push_response"
      contains: "base64decode"
    - path: "spec/auth_spec.lua"
      provides: "Test scaffold for M_auth (pending tests for AUTH-02/04/06)"
      contains: "describe"
    - path: "spec/http_spec.lua"
      provides: "Test scaffold for M_http (pending tests for post_form/get_json/shutdown)"
      contains: "describe"
    - path: "spec/errors_spec.lua"
      provides: "Test scaffold for M_errors.from_http_status (pending tests for D-24 six cases)"
      contains: "describe"
  key_links:
    - from: "src/auth.lua (Wave 1+)"
      to: "MM.base64decode"
      via: "called inside _b64url_decode (RESEARCH §Pattern 1)"
      pattern: "MM%.base64decode"
    - from: "spec/auth_spec.lua (Wave 1+)"
      to: "spec/fixtures/auth/token_ok.json"
      via: "Fixtures.load(\"auth/token_ok\")"
      pattern: "Fixtures.load.*auth/"
    - from: "spec/helpers/mm_mocks.lua"
      to: "spec/helpers/fixtures.lua"
      via: "Wave 0 verifies nested-path concat works (no edit needed if path concat works as is)"
      pattern: "spec/fixtures/"
---

<objective>
Stand up Wave 0 test infrastructure so every downstream Phase-2 task (Waves 1–4) has fixtures, a real base64 decoder mock, and failing/pending spec scaffolds to populate. This plan unblocks parallel work on `M_errors`, `M_auth`, `M_http`, and the entry-integration tests.

Purpose: Per RESEARCH.md §"Wave 0 Gaps" and Risk R-6, the current `spec/helpers/mm_mocks.lua` ships an identity-stub `MM.base64decode` (L144–146) that makes any genuine base64url JWT-decoding test untrustworthy. Phase 2's `_extract_client_id` and the SEC-03 gating tests both need a real decoder. Additionally, the six hand-rolled fixtures under `spec/fixtures/auth/` (D-28) are required by every Wave 1+ spec. Without this plan, Waves 1–4 cannot execute.

Output: Six fixture JSON files, an extended `mm_mocks.lua` with a real base64 decoder, a verified nested-path `Fixtures.load("auth/token_ok")` API, and three spec scaffolds (`auth_spec.lua`, `http_spec.lua`, `errors_spec.lua`) wired with `before_each(Mocks.setup + dofile)` and at least one `pending(...)` test per requirement they will cover.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-RESEARCH.md
@.planning/phases/02-authenticated-network-layer/02-PATTERNS.md
@.planning/phases/02-authenticated-network-layer/02-VALIDATION.md
@.planning/phases/01-foundations-sandbox-probes/CONTEXT.md
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@spec/log_redaction_spec.lua
@spec/entry_spec.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author six hand-rolled JSON fixtures under spec/fixtures/auth/</name>
  <read_first>
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md (D-28 — fixture file list)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Auth Round-Trip Details" L686–716 (verbatim Zettle JSON shapes)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Common Pitfalls" Pitfall 7 (strict JSON has no comments — use `_source` key inside the JSON or sibling .md)
    - .planning/phases/02-authenticated-network-layer/02-PATTERNS.md §"spec/fixtures/auth/*.json" L182–195
    - spec/helpers/fixtures.lua (current Fixtures.load contract — strict JSON; dkjson.decode will error on `//` comments)
  </read_first>
  <files>
    spec/fixtures/auth/token_ok.json,
    spec/fixtures/auth/token_invalid_grant.json,
    spec/fixtures/auth/users_self_ok.json,
    spec/fixtures/auth/users_self_unauthorized.json,
    spec/fixtures/auth/token_rate_limited.json,
    spec/fixtures/auth/network_timeout.json
  </files>
  <action>
    Create six hand-rolled JSON fixtures under spec/fixtures/auth/ per D-28 with the exact shapes documented in 02-RESEARCH.md §"Auth Round-Trip Details" L686–716. Strict-JSON only (dkjson rejects `//` comments per Pitfall 7) — embed citation as a top-level `_source` string key inside each JSON document. Files:

    1. `token_ok.json`: keys `access_token` (a JWT-shaped string `"eyJhbGciOiJSUzI1NiJ9.eyJzdWIiOiJ0ZXN0In0.sig"` — synthetic, never a real token), `expires_in` (integer 7200), `token_type` (`"Bearer"`), `_source` ("iZettle/api-documentation/authorization.md"). Per AUTH-02.
    2. `token_invalid_grant.json`: keys `error` (`"invalid_grant"`), `error_description` (`"bad assertion"`), `_source`. Per AUTH-03 / D-24 400→LoginFailed mapping.
    3. `users_self_ok.json`: keys `uuid` (UUID-shaped string), `organizationUuid` (UUID-shaped string — used by D-23a as `accountNumber`), `publicName` (`"Beispiel Café GmbH"` — German with umlaut to also exercise UTF-8 round-trip per ADR-0003 Q4), `_source` ("iZettle/api-documentation/faq.adoc"). Per ACCT-01/ACCT-02/ACCT-04.
    4. `users_self_unauthorized.json`: keys `error` (`"unauthorized"`), `error_description`, `_source`. Per D-21 scope-failure path → LoginFailed.
    5. `token_rate_limited.json`: keys `error` (`"rate_limit"`), `error_description`, `_source`. Used to drive the inferred-status path in `_infer_status` toward the 429 branch via `from_http_status`.
    6. `network_timeout.json`: a single empty JSON object `{"_source":"network anomaly — empty body case"}` for the empty/network-failure inference path; the spec that loads it MUST also be prepared to push an actually-empty `content` string via `Mocks.push_response({ content = "" })`.

    Format rules: pretty-printed JSON, two-space indent, LF line endings, single trailing newline (matches the build's normalisation). Place `_source` as the FIRST key in each document. NEVER include the maintainer's real API key or any real Zettle response capture — these are synthetic shapes.

    Do NOT add fenced code blocks for the fixture contents in this file; the executor authors the JSON directly per the field list above.
  </action>
  <behavior>
    - Each fixture is valid strict JSON parseable by dkjson with no errors
    - Each fixture contains the `_source` key citing the iZettle documentation page
    - `users_self_ok.json.publicName` contains a German umlaut to ensure UTF-8 round-trip
    - No fixture contains a real API key, real merchant UUID, or any production data
    - No fixture references hosts outside the egress allowlist
  </behavior>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua -e 'local d=require("dkjson"); for _,n in ipairs({"token_ok","token_invalid_grant","users_self_ok","users_self_unauthorized","token_rate_limited","network_timeout"}) do local f=io.open("spec/fixtures/auth/"..n..".json","r"); local raw=f:read("*a"); f:close(); local t,_,err=d.decode(raw); assert(not err, n..": "..tostring(err)); assert(type(t._source)=="string", n..": missing _source"); end; print("OK")' | grep -q '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/fixtures/auth/*.json | wc -l` reports 6
    - Each file parses via `dkjson.decode` without error
    - Each file has a top-level string-valued `_source` key
    - `grep -l 'eyJhbGciOiJSUzI1NiJ9' spec/fixtures/auth/token_ok.json` finds the synthetic JWT shape
    - `grep -l 'invalid_grant' spec/fixtures/auth/token_invalid_grant.json` succeeds
    - `grep -l 'organizationUuid' spec/fixtures/auth/users_self_ok.json` succeeds
    - `grep -l 'Café' spec/fixtures/auth/users_self_ok.json` succeeds (UTF-8 umlaut present)
    - `grep -L -E '(zettle\.com|izettle\.com)' spec/fixtures/auth/*.json` (none of the fixtures embed hostnames — they are bodies, not URLs)
  </acceptance_criteria>
  <done>
    Six valid JSON fixtures under spec/fixtures/auth/ matching the shapes in 02-RESEARCH.md §"Auth Round-Trip Details"; each carries a `_source` citation; UTF-8 umlaut present in users_self_ok.json.publicName; no real merchant data.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace MM.base64decode identity stub with real base64 decoder + extend push_response with status field</name>
  <read_first>
    - spec/helpers/mm_mocks.lua (current state — especially L39–48 push_response, L144–146 identity base64/base64decode stubs)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Risks / Open Questions / Landmines" Risk R-6 L1501–L1512 (mandate to replace identity stub with pure-Lua decoder; rationale)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Risks / Open Questions / Landmines" Risk R-1 (status field is for test ergonomics only — production code derives status from body)
    - .planning/phases/02-authenticated-network-layer/02-PATTERNS.md §"spec/helpers/mm_mocks.lua (Wave 0 extension)" L243–275
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-25 (M_http return contract — status is the inferred integer)
    - src/log.lua (existing redaction patterns — not directly modified, but the decoder must produce bytes that the existing `_redact` JWT pass would NOT redact when used inside a fixture body assembled for an SEC-03 test)
  </read_first>
  <files>spec/helpers/mm_mocks.lua</files>
  <behavior>
    - Test 1: `MM.base64decode("YWJj")` returns `"abc"` (standard base64, no padding required when length already multiple of 4)
    - Test 2: `MM.base64decode("YWJjZA==")` returns `"abcd"` (with `=` padding)
    - Test 3: `MM.base64decode("eyJhdWQiOiJjbGllbnQteCJ9")` returns `'{"aud":"client-x"}'` (the JSON-payload round-trip that `_extract_client_id` depends on)
    - Test 4: `MM.base64decode("")` returns `""` (empty input is benign)
    - Test 5: `Mocks.push_response({ content = "{}", status = 400 })` does NOT alter the 5-tuple returned by `conn:request` (status is stored on the queue entry only — production code never reads it because it isn't in the 5-tuple)
    - Test 6: `MM.base64` may remain identity (Phase 2 does not encode in production; the SEC-03 spec uses hand-rolled mid-segment strings or hardcoded base64 of `{"aud":"client-x"}` per Pitfall 8). The mock keeps identity to avoid surprise.
  </behavior>
  <action>
    Edit spec/helpers/mm_mocks.lua in place. Two changes:

    Change A (Risk R-6 — REAL decoder): Replace the identity `MM.base64decode = function(s) return s end` at L146 with a pure-Lua RFC 4648 standard-base64 decoder. Keep the implementation as a `local function _base64decode(s)` defined ABOVE the `Mocks.setup()` body, then assign `base64decode = _base64decode` inside the `MM` table. The decoder must: tolerate or strip `=` padding, accept the standard alphabet `[A-Za-z0-9+/]`, return `""` for empty input, ignore whitespace, and return the decoded byte string. Pure Lua 5.4 only — no external rocks. ~25–35 LoC implementation; consult `mime.unb64` source for shape or implement the 4-char-group → 3-byte loop directly. Keep `MM.base64` as the existing identity stub (no callers in Phase 2 production code; SEC-03 spec uses hardcoded base64 strings per RESEARCH.md Pitfall 8).

    Change B (test ergonomics for Risk R-1): Extend `Mocks.push_response(opts)` to accept an optional integer `opts.status` field. Store it on the queue entry as `entry.headers.status` (re-using the headers sub-table; does NOT change the 5-tuple shape returned by `conn:request`). The 5-tuple return signature stays exactly `(content, charset, mime, filename, headers)` per the documented MoneyMoney `Connection():request` contract (RESEARCH §"Connection() semantics"). Specs may inspect `Mocks._response_queue[i].headers.status` for assertions; production code MUST NOT read it. Add a 3-line comment block above `push_response` explaining the Risk R-1 contract.

    Do NOT add `pcall` wrappers around any production-facing call. Do NOT change `_make_connection` or `conn:request` signature. Preserve existing `-- luacheck: ignore 431` annotations and module-table layout.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua -e 'package.path="./?.lua;./?/init.lua;"..package.path; local M=require("spec.helpers.mm_mocks"); M.setup(); assert(MM.base64decode("YWJj")=="abc","abc"); assert(MM.base64decode("YWJjZA==")=="abcd","abcd"); assert(MM.base64decode("eyJhdWQiOiJjbGllbnQteCJ9")==[[{"aud":"client-x"}]],"jwt-payload"); assert(MM.base64decode("")=="","empty"); M.push_response({content="{}",status=400}); local e=M._response_queue[1]; assert(e.headers.status==400,"status-stored"); M.teardown(); print("OK")' | grep -q '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - The above lua one-liner exits 0 and prints `OK`
    - `grep -n 'function _base64decode' spec/helpers/mm_mocks.lua` finds the decoder definition
    - `grep -c 'return s end' spec/helpers/mm_mocks.lua` shows the original L146 identity `base64decode = function(s) return s end` has been replaced (only `MM.base64` may keep identity)
    - `grep -n 'opts.status' spec/helpers/mm_mocks.lua` finds the new status handling
    - `grep -n '5-tuple\|Risk R-1\|status is' spec/helpers/mm_mocks.lua` finds the explanatory comment
    - `busted spec/mm_mocks_spec.lua` continues to pass (no Phase-1 mock regressions)
    - `luacheck spec/helpers/mm_mocks.lua` exits 0
  </acceptance_criteria>
  <done>
    `MM.base64decode` is a real RFC 4648 standard-base64 decoder; `Mocks.push_response` accepts an optional `status` field stored on the queue entry's headers; the 5-tuple `conn:request` return contract is unchanged; all Phase-1 mocks_spec tests still pass; luacheck clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create pending spec scaffolds (auth_spec, http_spec, errors_spec) + verify Fixtures.load nested path works</name>
  <read_first>
    - spec/log_redaction_spec.lua (template for `before_each` + `Mocks.setup()` + `dofile("dist/paypal-pos.lua")` pattern — see Pattern Map §"Analog A")
    - spec/entry_spec.lua L11–34 (alternate setup-once template; Phase-2 specs prefer before_each per RESEARCH §Pattern Assignments)
    - spec/helpers/fixtures.lua (current implementation — concatenates `"spec/fixtures/" .. name .. ".json"`; nested path `auth/token_ok` already works on macOS POSIX)
    - .planning/phases/02-authenticated-network-layer/02-VALIDATION.md §"Per-Task Verification Map" (test name list — every `❌ W0` row needs a pending spec entry)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Wave 0 Gaps" L1396–L1412
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-24 (six errors_spec cases), D-25 (http_spec contract), D-21+D-22 (auth_spec scope)
  </read_first>
  <files>spec/auth_spec.lua, spec/http_spec.lua, spec/errors_spec.lua</files>
  <action>
    Create three new spec files mirroring `spec/log_redaction_spec.lua`'s `before_each(Mocks.setup + dofile)` pattern. Each file starts with the standard build+dofile setup, declares one outer `describe(...)` block, and contains `pending("...")` (busted's `pending(name, fn)`) entries for every test command listed in 02-VALIDATION.md whose status is `❌ W0`. Pending tests do NOT implement assertions — they document the contract Waves 1+ will fill. Use the exact test names from the validation matrix so `busted -t "..."` filters match.

    `spec/auth_spec.lua` pending tests (Wave 2 will green these):
    - "exchange_assertion posts grant_type" (AUTH-02)
    - "exchange_assertion posts client_id and assertion" (AUTH-02)
    - "exchange_assertion includes Accept: application/json header" (AUTH-02 / Pitfall 1)
    - "fetch_profile posts Bearer header" (D-21 leg 2)
    - "cached_token expiry" (AUTH-04 — returns nil when now >= expires_at - 60)
    - "cached_token returns access_token when fresh" (AUTH-04)
    - "cache survives reload via flat fallback" (AUTH-06)
    - "_decode_jwt_payload returns nil for malformed input" (D-22 edge cases)
    - "_decode_jwt_payload returns nil for non-JSON payload" (D-22 Pitfall 2)
    - "_extract_client_id reads aud claim" (D-22)
    - "_extract_client_id falls back to client_id claim" (D-22)
    - "_extract_client_id returns nil when neither aud nor client_id" (D-22)
    - "persist_session writes both nested and flat cache entries" (D-23c)
    - "two orgs coexist in cache" (ACCT-04 / Pitfall 6)

    `spec/http_spec.lua` pending tests (Wave 2 will green these):
    - "post_form constructs sorted form body with url-encoded values" (D-25)
    - "post_form always sends Accept: application/json header" (Pitfall 1)
    - "post_form returns 5-tuple destructured correctly" (Risk R-1)
    - "post_form passes raw body through M_log.redact before debug log" (SEC-01 / AUTH-05)
    - "post_form returns nil status for empty body" (D-24 network case)
    - "_infer_status maps invalid_grant body to 400" (Risk R-1 / Pitfall 5)
    - "_infer_status maps success body to 200" (D-24)
    - "get_json never logs the Bearer header value" (defense-in-depth per RESEARCH §"Anti-Patterns to Avoid")
    - "shutdown nils the module-local Connection" (D-25)
    - "shutdown is idempotent" (defensive)

    `spec/errors_spec.lua` pending tests (Wave 1 plan-02 will green these):
    - "nil status returns network string with dash placeholder" (D-24)
    - "200 returns nil" (D-24)
    - "299 returns nil" (D-24 boundary)
    - "400 returns LoginFailed" (D-24 / AUTH-03)
    - "401 returns LoginFailed" (D-24)
    - "403 returns LoginFailed" (D-24)
    - "429 returns rate_limit string" (D-24)
    - "500 returns network string with status" (D-24)
    - "599 returns network string with status" (D-24 boundary)
    - "999 returns network string with status" (D-24 catch-all)

    Each file's top must include:
      - `local Mocks = require("spec.helpers.mm_mocks")`
      - Build-once `do … end` block calling `os.execute("lua tools/build.lua 2>/dev/null")`
      - `local function load_artifact() dofile("dist/paypal-pos.lua") end`
      - `describe("...", function() before_each(function() Mocks.setup(); load_artifact() end); after_each(function() Mocks.teardown() end); ...end)`

    Also add ONE non-pending sanity test to each spec that confirms the artifact builds and the relevant module table is present: e.g. `it("M_errors module table is exposed", function() assert.is_table(M_errors) end)` — this ensures the spec actually exercises `dofile` and is not silently empty.

    No edits to `spec/helpers/fixtures.lua` are needed; the nested-path `Fixtures.load("auth/token_ok")` already resolves via existing string concat on macOS POSIX (PATTERNS.md L290). The sanity test in `auth_spec.lua` MUST include a non-pending call `Fixtures.load("auth/token_ok")` to verify nested-path support empirically (`assert.is_string(raw)` after).
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; busted spec/auth_spec.lua spec/http_spec.lua spec/errors_spec.lua 2>&amp;1 | grep -E '(Success|Pending|Failure)' | tee /tmp/wave0_pending.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/wave0_pending.log &amp;&amp; grep -q 'Pending' /tmp/wave0_pending.log</automated>
  </verify>
  <acceptance_criteria>
    - `busted spec/auth_spec.lua spec/http_spec.lua spec/errors_spec.lua` exits with no failures
    - `busted spec/auth_spec.lua -t "exchange_assertion posts grant_type"` reports the test as Pending
    - `busted spec/http_spec.lua -t "post_form always sends Accept: application/json header"` reports Pending
    - `busted spec/errors_spec.lua -t "400 returns LoginFailed"` reports Pending
    - `busted spec/auth_spec.lua -t "M_auth module table is exposed"` PASSES (sanity test confirms dofile worked)
    - `busted spec/auth_spec.lua -t "Fixtures.load reads auth/token_ok"` PASSES (sanity test confirms nested-path concat)
    - `luacheck spec/auth_spec.lua spec/http_spec.lua spec/errors_spec.lua` exits 0
    - All three spec files contain `local Mocks = require("spec.helpers.mm_mocks")` on a line near the top
    - All three spec files contain `dofile("dist/paypal-pos.lua")` in `before_each`
  </acceptance_criteria>
  <done>
    Three new spec files (`spec/auth_spec.lua`, `spec/http_spec.lua`, `spec/errors_spec.lua`) exist with the correct `before_each`/`after_each` plumbing, one sanity test confirming `dofile` + module-table presence, and `pending(...)` entries for every `❌ W0` row in 02-VALIDATION.md per-task verification map. Nested-path `Fixtures.load("auth/token_ok")` is empirically verified by one passing sanity test in auth_spec.lua. The full suite (`busted spec/`) still passes with no failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec test code → mock surface | Spec files load the artifact via `dofile`; mocks substitute for MoneyMoney globals. No external network. |
| fixture JSON files → dkjson decoder | Static, hand-rolled JSON; no attacker-controlled input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-W0-01 | Information Disclosure | spec/fixtures/auth/*.json | mitigate | Fixtures contain only synthetic JWT shapes and synthetic UUIDs; never real merchant credentials. CI grep on `.planning/` confirms no real-key paste. (AUTH-05 defense-in-depth.) |
| T-02-W0-02 | Tampering | spec/helpers/mm_mocks.lua base64decode replacement | mitigate | Pure-Lua RFC 4648 implementation — no external rock dependency means no supply-chain surface. luacheck enforces no `require("mime")` or similar. |
| T-02-W0-03 | Spoofing | Mocks.push_response status field | accept | Status field is test-ergonomics only; production code derives status from body shape (Risk R-1). Mismatch between mock status and inferred status is intentional documentation of the contract gap. |
| T-02-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs in this plan — all changes are pure Lua source edits. No package-legitimacy gate applies. |
</threat_model>

<verification>
After all three tasks complete, run the full Phase-2 baseline:

```bash
cd /Users/yves/Development/paypal-pos-plugin
lua tools/build.lua --verify  # reproducible build still green
luacheck src/ spec/
busted spec/                  # full suite green, new pending tests visible
```

Manual sanity check (Claude reads, no human needed):
- `ls spec/fixtures/auth/` → six `.json` files
- `head -5 spec/fixtures/auth/token_ok.json` → starts with `{`, contains `_source`
- `grep -n 'base64decode = function' spec/helpers/mm_mocks.lua` → references `_base64decode` local, NOT identity
</verification>

<success_criteria>
- All six fixtures parse as strict JSON and contain `_source` citations
- `MM.base64decode` round-trips `"eyJhdWQiOiJjbGllbnQteCJ9"` → `'{"aud":"client-x"}'`
- `Mocks.push_response({status=400})` stores 400 on the queue entry without altering the 5-tuple return contract
- Three new spec files exist with the `before_each` + `dofile` pattern, sanity tests passing, and `pending(...)` entries for every `❌ W0` row in 02-VALIDATION.md
- `busted spec/` reports zero failures (pending tests are reported as Pending, not Failure)
- `luacheck src/ spec/` exits 0
- `lua tools/build.lua --verify` reports reproducible build still green
</success_criteria>

<output>
Create `.planning/phases/02-authenticated-network-layer/02-01-SUMMARY.md` when done, summarising:
- Fixture file list with `_source` citation per file
- Confirmation that `MM.base64decode` is now real (cite the test that round-trips the JWT payload)
- Confirmation that `Mocks.push_response.status` is stored on `entry.headers.status` but is NOT returned by `conn:request` (Risk R-1 contract preserved)
- Pending test count per spec file
- Any unexpected discoveries about `Fixtures.load` nested-path behaviour on the host filesystem
</output>
