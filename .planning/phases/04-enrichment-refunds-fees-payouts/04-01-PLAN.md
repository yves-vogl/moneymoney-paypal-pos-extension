---
phase: 04-enrichment-refunds-fees-payouts
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/adr/0003-sandbox-probe-results.md
autonomous: false
requirements: [ACCT-03]
tags: [wave-0, probe, human, finance-api, adr]

must_haves:
  truths:
    - "docs/adr/0003-sandbox-probe-results.md Q3 row has been transitioned from DEFERRED → ACCEPTED (or REJECTED with replan note) by Yves' live probe against https://finance.izettle.com/v2/accounts/liquid/transactions"
    - "The ADR row records the observed HTTP status code (200 if PASS), the observed response wrapper shape (the literal JSON key path — e.g., 'response is {\"data\":[...]}' or whatever was actually returned), and the date of the probe — values redacted per RESEARCH §8.4 PII rules"
    - "If the probe returned 401 with the user's existing Phase-2 Bearer, the ADR row notes whether the failure was scope-related (READ:FINANCE missing) vs key-invalidity; if scope-related, ADR-0004 backlog item is logged inside the row for Plan 04-06 to draft"
    - "Plans 04-02..04-06 are unblocked once this plan is marked complete in the phase tracker; Plans 04-02..04-06 assume `finance.izettle.com` per RESEARCH §1.1 OpenAPI confirmation, and only this plan blocks on the live verification"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in the commit message, ADR text, or the redacted-response excerpt"
  artifacts:
    - path: "docs/adr/0003-sandbox-probe-results.md"
      provides: "Q3 row transitioned from DEFERRED → ACCEPTED (or REJECTED) with observed response wrapper shape pasted (redacted)"
      contains: "Q3"
  key_links:
    - from: "docs/adr/0003-sandbox-probe-results.md Q3"
      to: "Plans 04-02..04-06 URL constants and fixture shapes"
      via: "live-probe-result-records-observed-shape; planner replans only if the probe rejects the assumed host"
      pattern: "finance\\.izettle\\.com"
---

<objective>
Close ADR-0003 Q3 (the single open Phase-1 probe blocking Phase 4) by having Yves execute one live GET against `https://finance.izettle.com/v2/accounts/liquid/transactions` with his sandbox PayPal POS API key and recording the observed HTTP status + redacted response wrapper shape into the ADR row.

Purpose: The CONTEXT D-46 + RESEARCH §1.1 both recommend `finance.izettle.com` based on Zettle's OpenAPI spec, but neither has executed the call against Yves' real sandbox key. This is the final empirical gate before Plans 04-02..04-06 lock URL constants and fixture shapes. **This plan is intentionally Yves-blocking and cannot be done autonomously** — it requires a real sandbox token only Yves holds. Plans 04-02..04-06 do NOT chain on this plan in `depends_on` (per CONTEXT D-60 wave structure they proceed under the research-recommended assumption); this plan exists so Yves can RECORD the observed truth as a permanent ADR row, and so a planner replan is triggered on the rare event the probe fails.

Output: docs/adr/0003-sandbox-probe-results.md Q3 row transitioned from DEFERRED → ACCEPTED with the observed (redacted) response wrapper shape pasted; OR transitioned to REJECTED with a brief note triggering `/gsd-plan-phase 4 --gaps` to replan Plans 04-02..04-03 against the actual host.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-PATTERNS.md
@docs/adr/0003-sandbox-probe-results.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Yves executes the Q3 live probe with sandbox API key</name>
  <read_first>
    - docs/adr/0003-sandbox-probe-results.md Q3 row (current DEFERRED text — must locate the row for the edit in Task 2)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §1.1 ("Base host" — confirms `finance.izettle.com` is the OpenAPI-published production host) and §1.3 ("List account transactions" — the exact query parameter rules; `start` + `end` are REQUIRED and use `YYYY-MM-DDThh:mm:ss` format with NO `Z` suffix and NO milliseconds)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §1.2 (READ:FINANCE scope requirement — R-1 in §9; the existing Phase-2 sandbox key may not have this scope and the probe may surface a 401 that demands re-minting before the probe can complete)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §8.4 (PII-scrubbing rule — `originatingTransactionUuid` values, merchant org IDs, names, emails MUST be redacted to `<UUID-REDACTED>` / `<ORG-REDACTED>` / `<NAME-REDACTED>` before being pasted into the ADR; only the shape — field names, types, response wrapper — is recorded)
  </read_first>
  <what-built>
    Nothing automated by Claude in this task — this task is a manual probe Yves performs against his own sandbox tenant. Claude has prepared two things to make Yves' job mechanical:

    1. **The exact `curl` command Yves should run** (UTC date inserted by Yves at run time; the placeholder `__TODAY_UTC__` is a literal placeholder Yves replaces with today's date in `YYYY-MM-DD` form):

       `curl -i -H "Authorization: Bearer $SANDBOX_BEARER" "https://finance.izettle.com/v2/accounts/liquid/transactions?start=__TODAY_MINUS_20_UTC__T00:00:00&end=__TODAY_UTC__T00:00:00&limit=1&includeTransactionType=PAYMENT"`

       Notes for Yves: `$SANDBOX_BEARER` is the access_token Yves' MoneyMoney sandbox extension currently holds (extractable via `defaults read` on the LocalStorage plist or by running the extension once with a debug print of `cached_token` — Yves picks whichever is faster). The date params use the no-`Z`, no-millis format per RESEARCH §1.3.

    2. **The four observable outcomes Yves should record in the ADR Q3 row** (one row, one bullet list of the four facts below):
       a) HTTP status code returned (200 expected on PASS; 401 if scope-missing or key-invalid; 404 if host wrong; other = anomaly)
       b) Top-level JSON wrapper shape (e.g., `{"data":[...]}` — just the wrapper key, NOT contents)
       c) For a non-empty response, the bare field names of the first record (per RESEARCH §1.6 expected: `timestamp`, `amount`, `originatorTransactionType`, `originatingTransactionUuid`) — values redacted to `<REDACTED>`
       d) Date of probe in `YYYY-MM-DD` form
  </what-built>
  <how-to-verify>
    Yves performs the following steps in his terminal and then either edits the ADR himself OR resumes Claude with the observed outputs for Claude to do the ADR edit in Task 2:

    1. Open Terminal on Yves' macOS machine where the paypal-pos-plugin repo lives.
    2. Source the sandbox bearer into `$SANDBOX_BEARER` (Yves' preferred method — extracting from MoneyMoney LocalStorage, re-minting via `/oauth/token` with the sandbox `client_id` + sandbox API key, or copy-pasting from a recent MoneyMoney debug session).
    3. Replace `__TODAY_MINUS_20_UTC__` and `__TODAY_UTC__` placeholders with literal dates (e.g., `2026-06-01` and `2026-06-21`).
    4. Execute the curl command. Observe:
       - The HTTP status line (e.g., `HTTP/2 200`).
       - The body — copy the structure but redact any UUID/org-id/name values.
    5. Decide the verdict:
       - **PASS** (HTTP 200 + body matches `{"data":[...]}` wrapper from RESEARCH §1.6): mark Q3 ACCEPTED, paste the redacted shape, type "approved" to resume Claude.
       - **PASS with empty data** (HTTP 200 + body is `{"data":[]}`): same — mark ACCEPTED, paste `{"data":[]}`, note that the probe ran against an empty time window which is also a valid PASS.
       - **FAIL on 401** (HTTP 401): the key likely lacks `READ:FINANCE` scope (RESEARCH §1.2 R-1). Re-mint a sandbox API key at `https://my.zettle.com/apps/api-keys?scopes=READ:PURCHASE+READ:FINANCE`, re-acquire bearer, re-run the curl. If still 401, mark Q3 REJECTED with `scope_issue: confirmed` and resume Claude with "reject scope" so Task 2 records the truth and Plan 04-06 picks up the migration ADR-0004 task as critical-priority.
       - **FAIL on 404 or different shape**: mark Q3 REJECTED with the observed status + shape. Resume Claude with "reject host". This triggers a `/gsd-plan-phase 4 --gaps` replan of Plans 04-02..04-03 against the actual host.

    Expected behavior: a sandbox key minted with `READ:PURCHASE+READ:FINANCE` returns 200 + `{"data":[]}` (empty array on a quiet sandbox tenant) within ~500ms.

    Hard-fail conditions (resume Claude with the specific failure mode):
    - The curl never gets a TLS handshake (DNS resolution failure for `finance.izettle.com`) — record `dns_failure` and treat as REJECTED.
    - 5xx response — re-try once after 30s; if still 5xx, record `sandbox_unreliable` and DEFER the probe by one day rather than rejecting (Zettle's sandbox has known instability windows).
  </how-to-verify>
  <resume-signal>Type one of: `approved` (PASS, host confirmed) | `reject scope` (401 with scope missing, ADR-0004 escalated) | `reject host` (404 or different shape, replan needed) | `defer probe` (sandbox unreachable today, retry tomorrow). Then paste the redacted four facts (a/b/c/d) directly into chat so Task 2 can record them.</resume-signal>
  <acceptance_criteria>
    - Yves has typed one of the four resume-signal verbs into chat
    - Yves has pasted the four observable facts (HTTP status, wrapper shape, first-record field names, probe date) into chat — values redacted
    - Claude has captured the verdict + the four facts and proceeds to Task 2 (or escalates per the reject/defer path)
  </acceptance_criteria>
  <done>
    Yves has executed the probe against his sandbox tenant and reported the four observable facts to Claude with a verdict tag. No commit yet — Task 2 makes the ADR edit and commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record Q3 probe result into docs/adr/0003-sandbox-probe-results.md</name>
  <read_first>
    - docs/adr/0003-sandbox-probe-results.md (full file — needs to locate the Q3 row, observe the existing DEFERRED markup style, and edit in place preserving MADR table-formatting)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §8.4 (PII-scrubbing rule — the values pasted MUST be the redacted shape Yves provided in Task 1, never raw UUIDs or merchant identifiers)
    - The four observable facts Yves pasted in chat from Task 1 (HTTP status, wrapper shape, first-record field names, probe date)
  </read_first>
  <files>docs/adr/0003-sandbox-probe-results.md</files>
  <action>
    Open docs/adr/0003-sandbox-probe-results.md and locate the Q3 row (the row whose Question field references `finance.izettle.com` and whose Status field is currently `DEFERRED`). Update that row in place. Do NOT add a new ADR; do NOT renumber other rows; do NOT touch the other 7 Q rows.

    Apply the verdict Yves provided in Task 1's resume-signal:

    **If `approved`:**
    - Status: `DEFERRED` → `ACCEPTED`
    - Observed: paste a short multi-line block under the row using whatever markdown style the file already uses (likely a bullet list or a sub-table), containing:
      - `HTTP status: <code Yves observed>` (typically 200)
      - `Response wrapper: <shape Yves observed>` (typically `{"data":[...]}` per RESEARCH §1.6)
      - `First-record field names observed: <list>` (e.g., `timestamp, amount, originatorTransactionType, originatingTransactionUuid` — names only, not values)
      - `Probe date: <YYYY-MM-DD>`
      - `Probed by: Yves (sandbox tenant; values redacted per RESEARCH §8.4)`
    - Add a one-line concluding sentence directly underneath that says: "Plans 04-02..04-06 may now lock `finance.izettle.com` as the Finance API base host per the OpenAPI confirmation in 04-RESEARCH.md §1.1."

    **If `reject scope`:**
    - Status: `DEFERRED` → `ACCEPTED (host confirmed, scope-upgrade required for end users)` — i.e., the host IS correct, the scope rejection is a user-side concern not a host problem.
    - Observed: paste the same observable block (HTTP status will be `401`, wrapper shape is whatever the 401 body was — Zettle typically returns `{"developerMessage":"..."}` or similar, name only, not full text), and add:
      - `Scope verdict: READ:FINANCE missing on this key — Plan 04-06 must draft ADR-0004 documenting the dual-scope requirement and the v0.1.0 → v0.2.0 user-side re-mint migration`

    **If `reject host`:**
    - Status: `DEFERRED` → `REJECTED — observed host is <whatever Yves saw>`
    - Observed: paste the observable block and explicitly call out: "Plans 04-02..04-06 MUST be replanned via `/gsd-plan-phase 4 --gaps` before execution proceeds. Recommended action: re-research the Finance API host with the observed evidence."
    - Surface this as a blocker in the SUMMARY.md output.

    **If `defer probe`:**
    - Do NOT modify the ADR. Instead update the SUMMARY.md output with `verdict: deferred — Yves retries Task 1 tomorrow when sandbox is reachable; Plans 04-02..04-06 remain on the research-recommended `finance.izettle.com` assumption with the explicit caveat that the host is unverified empirically`.

    Then run `lua tools/build.lua --verify` to confirm the artifact is still reproducible (the ADR edit MUST NOT affect the shipped artifact — ADRs live under `docs/adr/` which is not in the manifest; the verify is purely a safety check that nothing else got touched). Commit with message `docs(04-01): close ADR-0003 Q3 probe (finance.izettle.com)` using HEREDOC commit body per CLAUDE.md GPG-signed convention; no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; grep -A 5 -B 1 'Q3' docs/adr/0003-sandbox-probe-results.md | grep -qE '(ACCEPTED|REJECTED)' &amp;&amp; ! grep -A 2 'Q3' docs/adr/0003-sandbox-probe-results.md | grep -q 'DEFERRED' &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; git log -1 --format='%G?' | grep -qE '^(G|U)$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Q3' docs/adr/0003-sandbox-probe-results.md` no longer shows `DEFERRED` (Status is one of `ACCEPTED`, `ACCEPTED (host confirmed, scope-upgrade required for end users)`, or `REJECTED — ...`)
    - `grep -E 'HTTP status:' docs/adr/0003-sandbox-probe-results.md` finds the recorded status line in the Q3 row area
    - `grep -E 'Probe date:' docs/adr/0003-sandbox-probe-results.md` finds the recorded date
    - `git diff HEAD~1 docs/adr/0003-sandbox-probe-results.md | wc -l` reports >= 6 lines changed (substantive edit, not a trivial whitespace change)
    - `git log -1 --format='%G?'` reports `G` or `U` (GPG-signed commit)
    - `git log -1 --format='%s'` matches the conventional-commit prefix `docs(04-01):`
    - `! git log -1 --format='%B' | grep -iE 'claude|anthropic|co-authored.*claude'` succeeds (no AI attribution)
    - `lua tools/build.lua --verify` still reports `OK: reproducible (sha256: ...)` (ADR edit doesn't perturb the shipped artifact)
  </acceptance_criteria>
  <done>
    docs/adr/0003-sandbox-probe-results.md Q3 row carries Yves' observed verdict + redacted observable shape; commit GPG-signed; reproducible build still green. SUMMARY.md notes the verdict tag so downstream planner runs (Plan 04-06's ADR-0004 escalation, or a `--gaps` replan on REJECTED) can act on it. Commit: `docs(04-01): close ADR-0003 Q3 probe (finance.izettle.com)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yves' sandbox tenant → docs/adr/0003-sandbox-probe-results.md | Observed response body crosses from a real Zettle sandbox into a public-Markdown ADR; redaction MUST be applied before paste (RESEARCH §8.4) |
| The `$SANDBOX_BEARER` env var Yves uses for the probe → terminal history | Once Yves runs the curl, the bearer may sit in shell history; Yves' responsibility, not Claude's |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Information Disclosure | docs/adr/0003-sandbox-probe-results.md Q3 redacted block | mitigate | Yves redacts at paste-time per RESEARCH §8.4; Claude does NOT echo or store the unredacted values; Task 2 explicitly cites the PII-scrubbing rule in `<read_first>` |
| T-04-W0-02 | Information Disclosure | terminal shell history holding `$SANDBOX_BEARER` | accept | Yves' machine; no extension code path leaks the bearer |
| T-04-W0-03 | Tampering | docs/adr/0003-sandbox-probe-results.md Q3 row | mitigate | Commit is GPG-signed (FDE07046A6178E89ADB57FD3DE300C53D8E18642); ADR row records the date so future readers can audit when the probe ran |
| T-04-W0-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs; no package manager run; pure curl + Markdown edit |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# ADR Q3 has been transitioned out of DEFERRED:
grep -B 1 -A 5 'Q3' docs/adr/0003-sandbox-probe-results.md

# Reproducible build still green (ADR edit must not perturb the artifact):
lua tools/build.lua --verify

# Commit is GPG-signed:
git log -1 --format='%G? %s' -- docs/adr/0003-sandbox-probe-results.md

# Phase-3 specs still pass (no source change in this plan, sanity check only):
./.luarocks/bin/busted spec/ 2>&1 | grep -E '0 failures'
```
</verification>

<success_criteria>
- docs/adr/0003-sandbox-probe-results.md Q3 row carries Yves' verdict (ACCEPTED / REJECTED) + the four observable facts (HTTP status, wrapper shape, first-record field names, probe date) in redacted form
- Commit is GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642 and uses Conventional Commits prefix `docs(04-01):`
- No AI/Claude attribution anywhere in commit, ADR text, or the redacted excerpt
- Reproducible build SHA unchanged (lua tools/build.lua --verify passes; the ADR is not in the shipped artifact)
- The phase tracker notes Plan 04-01 complete; if the verdict was `reject scope` then Plan 04-06's ADR-0004 task is flagged critical; if the verdict was `reject host` then a `--gaps` replan of Plans 04-02..04-03 is queued
</success_criteria>

<artifacts_this_phase_produces>
Modified files:
- `docs/adr/0003-sandbox-probe-results.md` — Q3 row transitioned from DEFERRED to ACCEPTED (or REJECTED) with observed redacted shape

New public symbols: none

New i18n keys: none

New ADR numbers: none (ADR-0003 row updated in place; ADR-0004 may be drafted by Plan 04-06 if the verdict was `reject scope`)

Yves-blockers cleared by this plan: Q3 (single blocker — once recorded, Plans 04-02..04-06 are unblocked under either the confirmed `finance.izettle.com` host or a replan trigger)

Yves-blockers REMAINING after this plan (still queued in CONTEXT.md): D-49 Option A vs Option B (surfaced in Plan 04-03 preamble), D-55 META-03 forbidden-strings list confirmation (surfaced in Plan 04-05 preamble)

Files NOT modified: any `src/*.lua`, any `spec/*.lua`, any fixture, `tools/manifest.txt`, `src/webbanking_header.lua`, CHANGELOG.md, README.md.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-enrichment-refunds-fees-payouts/04-01-SUMMARY.md` summarising:
- Q3 verdict tag from Yves (approved / reject scope / reject host / defer probe)
- The four observed facts as pasted (already redacted)
- ADR Q3 status after edit (ACCEPTED | REJECTED | DEFERRED)
- Whether Plan 04-06 ADR-0004 escalation flag is set (only set on `reject scope`)
- Whether a `/gsd-plan-phase 4 --gaps` replan is triggered (only on `reject host`)
- Confirmation that no `src/` or `spec/` file was touched and the reproducible build SHA is unchanged
- The next plan to execute (Plan 04-02; OR if `reject host`: HOLD and request replan)
</output>
</content>
</invoke>