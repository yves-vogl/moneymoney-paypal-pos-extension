---
phase: 06.1-supply-chain-scorecard-hardening
plan: 07
type: execute
wave: 4
depends_on: ["06.1-06"]
files_modified:
  - README.md
  - README.de.md
autonomous: false
requirements: [BUILD-07]
tags: [wave-4, docs, cii-best-practices, badge, d-35, d-40, checkpoint]

must_haves:
  truths:
    - "Yves completes the OpenSSF Best Practices passing-level questionnaire at https://www.bestpractices.dev (~2 h off-repo per D-35 + RESEARCH §6 estimate). The questionnaire produces a numeric project ID (e.g. 8731). Recorded in Yves CP-6.1-D."
    - "After Yves CP-6.1-D, two surgical README edits add the CII Best Practices badge to BOTH `README.md` AND `README.de.md` per D-40 (CONTEXT addendum corrects original D-35 single-README target: Phase 6 shipped README.de.md). Badge inserted in the badge cluster (lines 5-14 in both files) immediately AFTER the OpenSSF Scorecard badge — groups all OpenSSF-ecosystem badges together."
    - "Badge URL format per RESEARCH §6 + PATTERNS §1 row 11: `[![CII Best Practices](https://www.bestpractices.dev/projects/<ID>/badge)](https://www.bestpractices.dev/projects/<ID>)`. Two markdown lines per README. `<ID>` is the numeric ID from CP-6.1-D."
    - "CII passing acceptance bar per D-35 + ROADMAP success criterion #6: `Scorecard CII-Best-Practices.score >= 5` post-merge (passing level). Silver (~7/10) is explicitly DEFERRED per D-35 + ADR-0009 (Plan 06.1-08) tracks the backlog issue."
    - "After badge URLs are live (post-merge), the next Scorecard run picks up the badge and the `CII-Best-Practices` check reports the numeric score. Expected: 5/10 (passing tier). Per RESEARCH §6: ~24-48 h Scorecard cache delay; Yves can trigger a manual rerun via `gh workflow run \"OpenSSF Scorecard\"` after the README badges land."
    - "Nothing else changes in either README. Both files were finalized in Phase 6 R1+R2+lektor (D-70/D-71/D-72/D-78); the only edit is the +1 badge line per file. Plan 06.1-06's Documentation badge edit (lines 7-8 of each cluster) is preserved unchanged."
    - "NO `src/**`, `tools/**`, `spec/**` edits. Reproducible-build SHA on `dist/paypal-pos.lua` unchanged."
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no AI/Claude attribution. Conventional Commits: `docs(06.1): add CII Best Practices passing badge to bilingual READMEs`."
  artifacts:
    - path: "README.md"
      provides: "CII Best Practices badge in badge cluster (single-line insertion, position: after OpenSSF Scorecard badge)"
      contains: "bestpractices.dev/projects"
    - path: "README.de.md"
      provides: "CII Best Practices badge in badge cluster (single-line insertion, position: after OpenSSF Scorecard badge, identical badge URL — international standard, no German variant of the badge image)"
      contains: "bestpractices.dev/projects"
  key_links:
    - from: "README.md + README.de.md badge cluster"
      to: "https://www.bestpractices.dev/projects/<ID>"
      via: "Static markdown image badge wired to bestpractices.dev's badge-by-project-ID endpoint; auto-updates as Yves' tier changes (in-progress / passing / silver / gold)"
      pattern: "bestpractices.dev/projects"
    - from: "bestpractices.dev project ID"
      to: "Scorecard CII-Best-Practices check"
      via: "Scorecard scans README for the badge URL; if present, queries bestpractices.dev for the project's current tier and translates to a 0-10 score (passing → 5, silver → 7, gold → 10)"
      pattern: "CII-Best-Practices"
---

<objective>
Insert the OpenSSF Best Practices passing-level badge into both `README.md` and `README.de.md` (per D-40) after Yves completes the off-repo questionnaire at bestpractices.dev.

Purpose: Closes BUILD-07 + the Scorecard `CII-Best-Practices` check (target ≥ 5/10 = passing per D-35). The questionnaire is off-repo work (~2 h Yves) — this plan ships the resulting badge edits.

Output: Two single-line additions, one per README. No other file modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-CONTEXT.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-RESEARCH.md
@.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-PATTERNS.md
@README.md
@README.de.md
</context>

<artifacts_this_phase_produces>
Files modified:
- `README.md` — single-line badge insertion (line ~8, after OpenSSF Scorecard badge)
- `README.de.md` — single-line badge insertion (line ~8, after OpenSSF Scorecard badge)

Files explicitly NOT touched (blacklist):
- All other files. This plan ships the smallest possible diff.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Yves CP-6.1-D: Complete CII Best Practices passing questionnaire</name>
  <what-needs-doing>
    Register the project at bestpractices.dev and complete the passing-level (≥5/10) questionnaire. The questionnaire takes ~2 h per D-35 + RESEARCH §6 estimate; most criteria are direct copy-paste from existing repo artifacts (RESEARCH §6 enumerates the criteria-to-artifact mapping). The output is a numeric project ID needed for the badge URL.
  </what-needs-doing>
  <how-to-perform>
    1. Open https://www.bestpractices.dev/en/users/new (or sign in via GitHub OAuth if you already have a bestpractices.dev account).
    2. Click "Add a new project". Repository URL: `https://github.com/yves-vogl/moneymoney-paypal-pos-extension`.
    3. Complete the passing-level questionnaire (~67 criteria across 6 categories — basics, change-control, reporting, quality, security, analysis). Use RESEARCH §6 lines 442-457 as the criterion-to-artifact mapping cheat sheet.
    4. Two known gaps to handle:
       - **Static analysis** — answer "Yes, Semgrep" with URL `https://github.com/yves-vogl/moneymoney-paypal-pos-extension/blob/main/.github/workflows/sast.yml` (Plan 06.1-03 must have merged first; if not, defer this question or check "N/A" with rationale until SAST workflow lands).
       - **Dynamic analysis** — answer "N/A, with justification: Lua wrapper around remote API; no parser, codec, or local protocol stack to fuzz." Per RESEARCH §6.
    5. Submit. The project receives a numeric ID (e.g. `8731`). RECORD THIS ID — needed for Task 1.
    6. Verify your project's status page shows "Passing" badge: open `https://www.bestpractices.dev/projects/<ID>` and confirm the green Passing pill appears at the top.

    [CITED: https://www.bestpractices.dev/en/criteria/0 — passing-level criteria]
  </how-to-perform>
  <resume-signal>Type "passing badge earned: ID=<NNNN>" with the actual numeric ID.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Insert CII badge into README.md and README.de.md badge clusters</name>
  <files>README.md, README.de.md</files>
  <action>
    Surgical single-line additions. Both READMEs have a badge cluster at lines 5-14 (post Plan 06.1-06, the Documentation badge is now also in the cluster). Insert the CII badge IMMEDIATELY AFTER the OpenSSF Scorecard badge — groups OpenSSF-ecosystem badges together.

    Let `<ID>` be the numeric ID from CP-6.1-D.

    README.md edit:
    - Find the line `[![OpenSSF Scorecard](...)](...)`
    - Insert IMMEDIATELY AFTER it (before the next badge):
      ```
      [![CII Best Practices](https://www.bestpractices.dev/projects/<ID>/badge)](https://www.bestpractices.dev/projects/<ID>)
      ```

    README.de.md edit:
    - Find the same OpenSSF Scorecard line.
    - Insert the IDENTICAL badge markdown immediately after it. The badge image is internationally branded (English text on the badge); no German variant exists. Same URL, same target. This is the bilingual-cluster pattern from D-40.

    No other edits. Verify both files' line counts increase by exactly +1.
  </action>
  <verify>
    <automated>
      set -e

      # CII badge URL present in both files
      grep -q 'bestpractices.dev/projects/' README.md \
        || { echo "FAIL: README.md missing CII badge URL"; exit 1; }
      grep -q 'bestpractices.dev/projects/' README.de.md \
        || { echo "FAIL: README.de.md missing CII badge URL"; exit 1; }

      # Badge URL has a numeric ID (not the literal placeholder <ID>)
      grep -qE 'bestpractices.dev/projects/[0-9]+/badge' README.md \
        || { echo "FAIL: README.md CII badge URL has placeholder ID instead of numeric"; exit 1; }
      grep -qE 'bestpractices.dev/projects/[0-9]+/badge' README.de.md \
        || { echo "FAIL: README.de.md CII badge URL has placeholder ID instead of numeric"; exit 1; }

      # IDs match between the two READMEs (single project, single badge)
      ID_EN=$(grep -oE 'bestpractices.dev/projects/[0-9]+/badge' README.md | head -1 | grep -oE '[0-9]+')
      ID_DE=$(grep -oE 'bestpractices.dev/projects/[0-9]+/badge' README.de.md | head -1 | grep -oE '[0-9]+')
      if [ "$ID_EN" != "$ID_DE" ]; then
        echo "FAIL: README.md ID ($ID_EN) does not match README.de.md ID ($ID_DE)"
        exit 1
      fi

      # Line-count check — README.md should grow by exactly 1; README.de.md likewise
      # Post-Plan-06.1-06: README.md ~55 lines, README.de.md ~213 lines. After this plan: +1 each.
      EN_LINES=$(wc -l < README.md | tr -d ' ')
      DE_LINES=$(wc -l < README.de.md | tr -d ' ')
      if [ "$EN_LINES" -gt 57 ] || [ "$EN_LINES" -lt 55 ]; then
        echo "FAIL: README.md line count $EN_LINES outside expected [55-57] post Plan 06.1-06 + Plan 06.1-07"
        exit 1
      fi
      if [ "$DE_LINES" -gt 215 ] || [ "$DE_LINES" -lt 213 ]; then
        echo "FAIL: README.de.md line count $DE_LINES outside expected [213-215] post Plan 06.1-06 + Plan 06.1-07"
        exit 1
      fi

      # Position check — CII badge appears AFTER OpenSSF Scorecard badge in both files
      OSSF_LINE_EN=$(grep -n 'OpenSSF Scorecard' README.md | head -1 | cut -d: -f1)
      CII_LINE_EN=$(grep -n 'CII Best Practices' README.md | head -1 | cut -d: -f1)
      if [ "$CII_LINE_EN" -le "$OSSF_LINE_EN" ]; then
        echo "FAIL: README.md CII badge (line $CII_LINE_EN) not after OpenSSF Scorecard (line $OSSF_LINE_EN)"
        exit 1
      fi
      OSSF_LINE_DE=$(grep -n 'OpenSSF Scorecard' README.de.md | head -1 | cut -d: -f1)
      CII_LINE_DE=$(grep -n 'CII Best Practices' README.de.md | head -1 | cut -d: -f1)
      if [ "$CII_LINE_DE" -le "$OSSF_LINE_DE" ]; then
        echo "FAIL: README.de.md CII badge (line $CII_LINE_DE) not after OpenSSF Scorecard (line $OSSF_LINE_DE)"
        exit 1
      fi

      echo "OK: CII badge present in both READMEs with matching numeric ID, positioned after OpenSSF Scorecard"
    </automated>
  </verify>
  <done>
    Both `README.md` and `README.de.md` have one new CII Best Practices badge line each, immediately after the OpenSSF Scorecard badge. Both use the same numeric project ID. Both link to `https://www.bestpractices.dev/projects/<ID>`. No other line changed in either file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bestpractices.dev → external badge image hosting | A compromised bestpractices.dev could serve a misleading badge image. Mitigation: the URL is a well-known public-good infrastructure under OpenSSF; the project's tier is also independently queryable via the bestpractices.dev API. |
| Scorecard CII-Best-Practices check → README badge URL detection | Scorecard scans README files for the badge URL pattern. A typo in the badge URL (e.g. `bestpractices.coreinfrastructure.org` instead of `bestpractices.dev`) causes the check to return 0. Mitigation: verify the URL pattern matches what Scorecard expects (RESEARCH §6 + check Scorecard source at https://github.com/ossf/scorecard/blob/main/checks/cii_best_practices.go). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-07-01 | Information Disclosure | Questionnaire answers exposing internal architecture | accept | Answers are public on the project's bestpractices.dev page. Content matches what's already in README + ADRs — no new disclosure. |
| T-06.1-07-02 | Tampering | Compromised badge image misleading viewers | accept | Industry-standard infrastructure; project owner can re-issue badge if compromised. |
| T-06.1-07-03 | Information Disclosure | Yves account credentials at bestpractices.dev | accept | GitHub OAuth path — no separate password. Same security posture as the GitHub account itself. |
</threat_model>

<verification>
1. CP-6.1-D complete — numeric project ID recorded.
2. Both READMEs have CII badge with matching numeric ID.
3. Badge positioned after OpenSSF Scorecard badge in both.
4. Line counts: README.md +1, README.de.md +1.
5. CI green on PR.
6. After merge: trigger a Scorecard run (`gh workflow run "OpenSSF Scorecard"`). Within ~24 h, verify `CII-Best-Practices.score == 5` via `curl -s 'https://api.securityscorecards.dev/projects/github.com/yves-vogl/moneymoney-paypal-pos-extension' | jq '.checks[] | select(.name=="CII-Best-Practices") | .score'`.
7. Reproducible-build SHA unchanged.
</verification>

<success_criteria>
- CP-6.1-D satisfied (bestpractices.dev project page shows green Passing pill)
- README.md +1 CII badge line, positioned after OpenSSF Scorecard
- README.de.md +1 CII badge line, positioned after OpenSSF Scorecard
- Numeric ID matches between both READMEs
- CI green on PR
- Post-merge Scorecard run reports `CII-Best-Practices.score >= 5`
- No other file modified
</success_criteria>

<output>
Create `.planning/phases/06.1-supply-chain-scorecard-hardening/06.1-07-SUMMARY.md` when done.
</output>

<pitfalls>
- **Pitfall 1 — Questionnaire deferred:** Yves cannot complete CP-6.1-D until Plan 06.1-03 (Semgrep SAST) has merged — the "static analysis" question needs a real workflow URL. If Plan 06.1-03 is still in flight, defer CP-6.1-D until 06.1-03 merges. The `depends_on: [06.1-06]` enforces ordering at the orchestrator level, but the implicit dependency on 06.1-03 is documented here.
- **Pitfall 2 — Stale bestpractices.coreinfrastructure.org URLs:** Some older docs reference `bestpractices.coreinfrastructure.org` — that URL redirects but Scorecard's check expects `bestpractices.dev`. Use the `bestpractices.dev` form per RESEARCH §6.
- **Pitfall 3 — Badge image vs project page URL confusion:** The badge IMAGE URL is `https://www.bestpractices.dev/projects/<ID>/badge` (renders SVG); the LINKED project page URL is `https://www.bestpractices.dev/projects/<ID>` (HTML). Both forms are used in the markdown — image inside `![...](...)`, link inside `[![...](image_url)](page_url)`.
- **Pitfall 4 — Numeric ID typo across READMEs:** Manual transcription risk. The verify gate compares IDs between the two READMEs and fails on mismatch.
- **Pitfall 5 — Wrong cluster position:** Placing the badge AT THE TOP of the cluster or AT THE BOTTOM breaks the OpenSSF-ecosystem grouping. The verify gate enforces "after OpenSSF Scorecard line" position.
- **Pitfall 6 — Scorecard cache delay:** Post-merge, the next CII-Best-Practices score takes up to ~24 h to flip from 0 to 5. RESEARCH Pitfall 6 documents this. Yves can manually trigger a Scorecard rerun to shorten the lag.
- **Pitfall 7 — Silver-as-backlog:** D-35 + RESEARCH §6 + this plan's plan-level summary all defer Silver to a backlog issue. ADR-0009 (Plan 06.1-08) will create the backlog issue link. Yves CP-6.1-D answers questions ONLY at the passing tier; do not over-shoot to silver in the same session.
</pitfalls>

<closes>
- BUILD-07 (CII Best Practices passing badge, ≥5/10, rendered in both READMEs per D-40)
</closes>
