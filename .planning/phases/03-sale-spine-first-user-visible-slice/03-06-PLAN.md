---
phase: 03-sale-spine-first-user-visible-slice
plan: 06
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
files_modified:
  - src/entry.lua
  - spec/entry_spec.lua
autonomous: true
requirements: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-05, SALE-06, SALE-08, I18N-01, TEST-03, TEST-04]
requirements_addressed: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-05, SALE-06, SALE-08, I18N-01, TEST-03, TEST-04]
tags: [wave-4, entry, refresh-account, integration, mvp]

must_haves:
  truths:
    - "src/entry.lua RefreshAccount is rewired to drive the real Phase-3 pipeline: orgUuid → bearer via M_auth.cached_token → since clamp per D-33 → M_purchases.fetch_all → M_mapping mapping per purchase → return {balance, transactions}"
    - "The Phase-2 surface contract for SupportsBank, InitializeSession2, ListAccounts, EndSession is FROZEN — Phase 3 does NOT touch those functions in src/entry.lua"
    - "The Phase-1 RefreshAccount fixture body (returning a hardcoded 9.95 EUR fixture transaction with booked=true) is REMOVED — its replacement is the Phase-3 pipeline"
    - "Step 1 (orgUuid): orgUuid = account.accountNumber; guard `if type(orgUuid) ~= \"string\" or orgUuid == \"\" then return M_i18n.t(\"error.network\", \"missing_account\") end`"
    - "Step 2 (cached_token): bearer = M_auth.cached_token(orgUuid); if not bearer then return M_i18n.t(\"error.network\", \"no_token\") end — D-41 nil-token guard"
    - "Step 3 (since clamp per D-33): NINETY_DAYS = 90 * 86400; effective_since = math.max(since or 0, os.time() - NINETY_DAYS) — clamp visible at the entry-point boundary (RESEARCH Pitfall 5)"
    - "Step 4 (fetch): purchases, fetch_err = M_purchases.fetch_all(effective_since, bearer); if fetch_err then return fetch_err end"
    - "Step 5 (map): iterate purchases via ipairs; for each p: if p.refund == true call M_mapping.refund_to_transaction(p), else call M_mapping.purchase_to_transaction(p); push non-nil results into transactions array (D-37 nil → skip)"
    - "Step 6 (return): return { balance = account.balance, transactions = transactions } — balance unchanged per CONTEXT specifics (Phase 4 wires Finance API)"
    - "ERR-06 fail-whole-refresh invariant (Phase-3 partial coverage): any non-nil fetch_err aborts the refresh — partial transactions are NEVER returned alongside an error; the return is either a transactions-table OR the error string (mutually exclusive)"
    - "REFRESH log line: a single M_log.info call at the top of RefreshAccount logs only the account orgUuid prefix and the effective_since clamp result — never the bearer (existing redaction by M_log already covers it but the call site MUST follow the Phase-2 SEC-03 no-Bearer-in-logs convention)"
    - "RefreshAccount preserves the existing `-- luacheck: ignore 431` annotation on its function header (callback arg shadowing)"
    - "spec/entry_spec.lua is EXTENDED (not replaced) with new RefreshAccount integration tests; pre-existing Phase-2 tests for SupportsBank / InitializeSession2 / ListAccounts / EndSession remain unchanged"
    - "spec/refresh_idempotency_spec.lua (Wave 1 RED gate) TURNS FULLY GREEN — all four tests pass: three double-refresh stability tests + D-41 nil-token error test"
    - "spec/mapping_schema_spec.lua (Wave 1 + 2 already green) STAYS GREEN — no regression from the entry-level integration"
    - "src/entry.lua manifest position unchanged (still last in tools/manifest.txt, emitted verbatim at top-level after the do…end blocks)"
    - "tools/manifest.txt is NOT modified"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts:
    - path: "src/entry.lua"
      provides: "RefreshAccount rewired to drive Phase-3 real pipeline; SupportsBank, InitializeSession2, ListAccounts, EndSession unchanged"
      contains: "RefreshAccount"
      min_lines: 100
    - path: "spec/entry_spec.lua"
      provides: "Integration tests for the new RefreshAccount pipeline (D-31, D-32, D-33, D-37, D-41, ERR-06 partial)"
      contains: "RefreshAccount"
  key_links:
    - from: "src/entry.lua RefreshAccount Step 2"
      to: "M_auth.cached_token(orgUuid)"
      via: "Phase-2 D-23d / D-41 — cached_token returns access_token or nil"
      pattern: "M_auth.cached_token"
    - from: "src/entry.lua RefreshAccount Step 3"
      to: "math.max(since, os.time() - 90 * 86400)"
      via: "D-33 since clamp at boundary"
      pattern: "NINETY_DAYS\\|90 %* 86400"
    - from: "src/entry.lua RefreshAccount Step 4"
      to: "M_purchases.fetch_all(effective_since, bearer)"
      via: "Plan 03-05 — drives M_pagination.iterate"
      pattern: "M_purchases.fetch_all"
    - from: "src/entry.lua RefreshAccount Step 5"
      to: "M_mapping.purchase_to_transaction(p) / M_mapping.refund_to_transaction(p)"
      via: "Plan 03-03 — per-purchase mapping; nil result skipped (D-37)"
      pattern: "M_mapping.purchase_to_transaction\\|M_mapping.refund_to_transaction"
    - from: "spec/refresh_idempotency_spec.lua (Wave 1 RED)"
      to: "src/entry.lua RefreshAccount (Wave 4)"
      via: "double-refresh comparison turns GREEN once the pipeline is real"
      pattern: "RefreshAccount"
---

<objective>
Rewire src/entry.lua RefreshAccount to drive the real Phase-3 pipeline (orgUuid → cached_token → since clamp → fetch_all → map → return), removing the Phase-1 hardcoded fixture body. Extend spec/entry_spec.lua with integration tests covering D-31, D-32, D-33, D-37, D-41, and ERR-06's Phase-3 partial coverage. Make spec/refresh_idempotency_spec.lua (Wave 1 RED gate) turn fully GREEN.

Purpose: This is the integration milestone — every Wave-3 module (mapping, pagination, purchases.fetch) gets composed at the RefreshAccount boundary. Per CONTEXT specifics Pitfall 5, the since clamp belongs at the entry boundary (not inside purchases.fetch) so it stays visible for debugging. After this plan, a maintainer with a real PayPal POS API key can build dist/paypal-pos.lua, drop it in MoneyMoney's Extensions folder, click "Aktualisieren", and see real card sales appear — the Core Value of the project is delivered.

Output: src/entry.lua with RefreshAccount rewired (other four callbacks untouched); spec/entry_spec.lua extended with new it() tests covering each step of the pipeline; spec/refresh_idempotency_spec.lua all four tests GREEN; spec/mapping_schema_spec.lua stays GREEN; spec/mapping_spec.lua + pagination_spec.lua + purchases_spec.lua stay GREEN; reproducible build still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@src/entry.lua
@src/auth.lua
@src/purchases.lua
@src/pagination.lua
@src/mapping.lua
@src/i18n.lua
@src/errors.lua
@spec/entry_spec.lua
@spec/refresh_idempotency_spec.lua
@spec/mapping_schema_spec.lua
@spec/helpers/mm_mocks.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire src/entry.lua RefreshAccount (the load-bearing integration)</name>
  <read_first>
    - src/entry.lua (current state — Phase-1 fixture body at L129-149; the other four callbacks are FROZEN per Phase-2 contract)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"src/entry.lua — RefreshAccount rewire only" (sequential orchestration pattern from L56-88 of the same file — InitializeSession2's two-call probe is the structural template)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-31 (booked=false), D-32 (refund branch), D-33 (since clamp at boundary), D-37 (non-EUR skip — but the skip happens INSIDE M_mapping, not in entry.lua; entry.lua just filters nil), D-40 (file layout), D-41 (nil-token guard), D-42 / D-43 (transport + error reuse)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md Pitfall 5 (clamp at boundary, not inside fetch)
    - src/auth.lua M_auth.cached_token (returns access_token string or nil; uses 60s pre-expiry guard per D-23d)
    - src/purchases.lua M_purchases.fetch_all (Plan 03-05 — returns (all_purchases|nil, error_string|nil))
    - src/mapping.lua M_mapping.purchase_to_transaction + refund_to_transaction (Plan 03-03 — returns table or nil for non-EUR; deterministic transactionCode)
    - src/i18n.lua (existing error.network envelope reused; account.purpose.* keys added in Plan 03-03)
    - spec/refresh_idempotency_spec.lua (the Wave 1 gating contract this plan must satisfy)
    - spec/mapping_schema_spec.lua (Wave 2 already green — this plan must not regress)
  </read_first>
  <files>src/entry.lua</files>
  <behavior>
    - RefreshAccount(account, since) signature unchanged; `-- luacheck: ignore 431` preserved
    - Step 1: orgUuid = account.accountNumber; guard non-empty string; if missing return M_i18n.t("error.network", "missing_account")
    - Step 2: bearer = M_auth.cached_token(orgUuid); if nil return M_i18n.t("error.network", "—") per D-41 (use the SAME envelope value the idempotency spec Test 4 asserts: `M_i18n.t("error.network", "—")`)
    - Step 3: NINETY_DAYS = 90 * 86400 (module-local constant OR inline — either is acceptable); effective_since = math.max(since or 0, os.time() - NINETY_DAYS)
    - Step 4: local purchases, fetch_err = M_purchases.fetch_all(effective_since, bearer); if fetch_err then return fetch_err end (ERR-06: never return partial transactions alongside an error)
    - Step 5: build transactions = {}; loop ipairs purchases; per p: if p.refund == true then txn = M_mapping.refund_to_transaction(p) else txn = M_mapping.purchase_to_transaction(p); if txn ~= nil then transactions[#transactions+1] = txn end (the nil case covers D-37 non-EUR silent skip plus any future skip-conditions)
    - Step 6: return { balance = account.balance, transactions = transactions }
    - Single M_log.info call at the top: "RefreshAccount called for org=<first 8 chars of orgUuid> since=<effective_since>" (NEVER include bearer, never include full orgUuid for ACCT-04 multi-merchant privacy)
    - For an empty purchases array (since past all purchases), the return is { balance = account.balance, transactions = {} } — SALE-06 incremental empty-refresh
  </behavior>
  <action>
    Edit src/entry.lua in-place. Keep SupportsBank, InitializeSession2, ListAccounts, EndSession EXACTLY as they are (lines 5-128 + 151-155 in the current file). Replace the function body of `RefreshAccount(account, since)` (currently lines 129-149) with the Phase-3 pipeline.

    Step-by-step replacement for the body of `function RefreshAccount(account, since) -- luacheck: ignore 431`:

    1. Module-local constant declaration ONCE at the file scope (place near the top, after the file-level comment block, BEFORE SupportsBank): `local NINETY_DAYS = 90 * 86400`. Alternatively, inline `90 * 86400` inside RefreshAccount — either is acceptable. RECOMMENDED: module-local for readability and to make the constant findable by a future Phase-5/6 "force-full-sync" flag.

    2. Inside `RefreshAccount(account, since)`:

       a. `local orgUuid = account and account.accountNumber`
       b. `if type(orgUuid) ~= "string" or orgUuid == "" then return M_i18n.t("error.network", "missing_account") end`
       c. `local effective_since = math.max(since or 0, os.time() - NINETY_DAYS)`
       d. `M_log.info("RefreshAccount called for org=" .. tostring(orgUuid):sub(1, 8) .. " since=" .. tostring(effective_since))` — sub(1,8) prefix only per ACCT-04 multi-merchant privacy convention from spec/log_redaction_spec.lua
       e. `local bearer = M_auth.cached_token(orgUuid)`
       f. `if not bearer then return M_i18n.t("error.network", "—") end` — the literal "—" matches the D-41 contract test in spec/refresh_idempotency_spec.lua Test 4
       g. `local purchases, fetch_err = M_purchases.fetch_all(effective_since, bearer)`
       h. `if fetch_err then return fetch_err end` — ERR-06 fail-whole-refresh
       i. `local transactions = {}`
       j. `for _, p in ipairs(purchases or {}) do local txn; if p.refund == true then txn = M_mapping.refund_to_transaction(p) else txn = M_mapping.purchase_to_transaction(p) end; if txn ~= nil then transactions[#transactions + 1] = txn end end`
       k. `return { balance = account.balance, transactions = transactions }`

    Add a one-line comment above Step 6 explaining the Phase-3 contract: `-- D-31: balance unchanged in Phase 3 (Finance API is Phase 4 ACCT-03)`.

    Do NOT touch any other callback. Do NOT add pcall around any M_* call (every M_* function has its own internal pcall where needed). Do NOT add any new require() (D-02). Do NOT introduce a new egress host (only purchase.izettle.com flows through M_purchases.fetch_all; oauth.zettle.com only from Phase-2 surface).

    Run `lua tools/build.lua && lua tools/build.lua --verify` after the edit — confirm SHA prints OK: reproducible.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'M_auth.cached_token' src/entry.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'M_purchases.fetch_all' src/entry.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'M_mapping.purchase_to_transaction\|M_mapping.refund_to_transaction' src/entry.lua | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; grep -c 'NINETY_DAYS\|90 \* 86400' src/entry.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'function RefreshAccount' src/entry.lua` finds exactly 1
    - `grep -L 'balance = 9.95' src/entry.lua` succeeds (the Phase-1 fixture is REMOVED)
    - `grep -L 'fixture-0001' src/entry.lua` succeeds (the Phase-1 fixture transactionCode is REMOVED)
    - `grep -n 'M_auth.cached_token' src/entry.lua` finds the Step 2 call
    - `grep -n 'M_purchases.fetch_all' src/entry.lua` finds the Step 4 call
    - `grep -n 'M_mapping.purchase_to_transaction' src/entry.lua` finds the Step 5 sale branch
    - `grep -n 'M_mapping.refund_to_transaction' src/entry.lua` finds the Step 5 refund branch
    - `grep -n 'p.refund == true' src/entry.lua` finds the refund-branch discriminator
    - `grep -n 'math.max' src/entry.lua` finds the D-33 since clamp
    - `grep -n 'NINETY_DAYS\|90 %* 86400' src/entry.lua` finds the 90-day constant
    - `grep -L 'finance.izettle.com' src/entry.lua` succeeds (no Phase-4 host)
    - `grep -L 'require(' src/entry.lua` succeeds (D-02)
    - `grep -L 'pcall' src/entry.lua` succeeds (no pcall around M_* calls)
    - `lua tools/build.lua --verify` reports OK: reproducible
    - The four other callbacks (SupportsBank, InitializeSession2, ListAccounts, EndSession) are byte-identical to their pre-Wave-4 state — `git diff src/entry.lua` shows changes ONLY inside the RefreshAccount body (plus the optional NINETY_DAYS declaration at file scope)
    - The pre-existing entry_spec.lua tests for SupportsBank / InitializeSession2 / ListAccounts / EndSession still pass with the new RefreshAccount in place (no regression)
  </acceptance_criteria>
  <done>
    src/entry.lua RefreshAccount drives the real Phase-3 pipeline; SupportsBank/InitializeSession2/ListAccounts/EndSession unchanged; reproducible build green. Commit: `feat(03-06): rewire RefreshAccount to drive Phase-3 pipeline (SALE-01..06+08, D-31/33/41)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend spec/entry_spec.lua with new RefreshAccount integration tests</name>
  <read_first>
    - spec/entry_spec.lua (full current state — preserve the existing describe block + before_each/after_each pattern; ADD a new describe block at the end for Phase-3 RefreshAccount tests)
    - spec/refresh_idempotency_spec.lua (Wave 1 RED gate — its tests will turn green once the entry.lua rewire is in place; this Task 2 extends entry_spec.lua with COMPLEMENTARY tests that exercise the individual pipeline steps in isolation)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-33, D-37, D-41 (the boundary contracts entry_spec.lua must explicitly cover)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md (fixture inventory; the seed_token helper pattern from refresh_idempotency_spec.lua applies)
    - src/entry.lua after Task 1 (the implementation under test)
    - spec/helpers/mm_mocks.lua Mocks._last_request fields
  </read_first>
  <files>spec/entry_spec.lua</files>
  <behavior>
    - Pre-existing tests in entry_spec.lua (SupportsBank, InitializeSession2 challenge, etc.) remain unchanged and passing
    - New describe block "RefreshAccount Phase-3 pipeline" appended at the end with the standard before_each(Mocks.setup + load_artifact) / after_each(Mocks.teardown) pattern shared by the file
    - At least 6 new it() tests covering: missing accountNumber, nil cached_token (D-41), happy-path simple sale, refund-branch dispatch (D-32), non-EUR silent skip (D-37), empty-result (SALE-06 incremental empty-refresh), since clamp boundary (D-33)
  </behavior>
  <action>
    Edit spec/entry_spec.lua. Locate the closing `end)` of the outer describe block (currently at L347 in the file). BEFORE that closing `end)`, append a new sibling describe block titled "RefreshAccount Phase-3 pipeline (SALE-01..06+08 / D-31 / D-33 / D-37 / D-41)". Use the same `before_each(function() Mocks.setup(); load_artifact() end)` and `after_each(function() Mocks.teardown() end)` already established at the file head.

    Inside the new describe, declare the same seed_token helper used by spec/refresh_idempotency_spec.lua (or a local equivalent): `local function seed_token(orgUuid) LocalStorage.zettle = {}; LocalStorage["zettle:" .. orgUuid] = JSON():set({access_token="AT-VALID", expires_at=os.time()+7200, obtained_at=os.time(), client_id="client-x", uuid="u-1", publicName="Beispiel Café"}):json() end`.

    Tests:

    1. `it("RefreshAccount returns error string when accountNumber is missing", function() local result = RefreshAccount({balance=0}, 0); assert.is_string(result); assert.truthy(result:find("missing_account", 1, true) or result:find("Netzwerkfehler", 1, true), "expected German error.network envelope, got: " .. tostring(result)) end)` — asserts D-41-adjacent guard

    2. `it("RefreshAccount returns error.network when cached_token is nil (D-41)", function() local result = RefreshAccount({accountNumber="org-no-token", currency="EUR", balance=0}, 0); assert.is_string(result); assert.equals(M_i18n.t("error.network", "—"), result) end)` — locks the exact German envelope so refresh_idempotency_spec Test 4 also passes

    3. `it("RefreshAccount returns transactions for purchase_simple_sale fixture (happy path)", function() seed_token("org-1"); local raw, _ = Fixtures.load("purchases/purchase_simple_sale"); Mocks.push_response({content=raw}); local result = RefreshAccount({accountNumber="org-1", currency="EUR", balance=0}, 0); assert.is_table(result); assert.is_table(result.transactions); assert.equals(1, #result.transactions); assert.equals(5.00, result.transactions[1].amount); assert.equals("zettle:sale:11111111-1111-1111-1111-111111111111", result.transactions[1].transactionCode); assert.is_false(result.transactions[1].booked) end)`

    4. `it("RefreshAccount dispatches refund_to_transaction for refund records (D-32)", function() seed_token("org-2"); local raw, _ = Fixtures.load("purchases/purchase_refund"); Mocks.push_response({content=raw}); local result = RefreshAccount({accountNumber="org-2", currency="EUR", balance=0}, 0); assert.is_table(result); assert.equals(1, #result.transactions); assert.is_true(result.transactions[1].amount < 0); assert.truthy(result.transactions[1].transactionCode:find("^zettle:refund:")) end)`

    5. `it("RefreshAccount silently skips non-EUR purchases (D-37)", function() seed_token("org-3"); local raw, _ = Fixtures.load("purchases/purchase_non_eur"); Mocks.push_response({content=raw}); local result = RefreshAccount({accountNumber="org-3", currency="EUR", balance=0}, 0); assert.is_table(result); assert.is_table(result.transactions); assert.equals(0, #result.transactions, "non-EUR purchase must produce 0 transactions") end)`

    6. `it("RefreshAccount returns empty transactions for empty fixture (SALE-06 incremental empty-refresh)", function() seed_token("org-4"); local raw, _ = Fixtures.load("purchases/purchases_empty"); Mocks.push_response({content=raw}); local result = RefreshAccount({accountNumber="org-4", currency="EUR", balance=0}, os.time() - 60); assert.is_table(result); assert.is_table(result.transactions); assert.equals(0, #result.transactions) end)`

    7. `it("RefreshAccount clamps since to 90 days back when caller passes 0 (D-33)", function() seed_token("org-5"); local raw, _ = Fixtures.load("purchases/purchases_empty"); Mocks.push_response({content=raw}); local before = os.time(); RefreshAccount({accountNumber="org-5", currency="EUR", balance=0}, 0); local url = Mocks._last_request.url; assert.truthy(url:find("startDate=", 1, true), "URL must include startDate query param"); -- The startDate must reflect a clamped timestamp roughly NINETY_DAYS ago, not the epoch -- The exact assertion: extract the startDate value and verify it parses to within [now - 90*86400 - 60, now - 90*86400 + 60] tolerance for clock skew. For the v0.1 spec, simply assert the URL does NOT contain '1970' or '%3A00%3A00' alone at the epoch position. local ok = not url:find("startDate=1970", 1, true); assert.is_true(ok, "startDate must be clamped, not 1970 epoch: " .. url) end)`

    8. `it("RefreshAccount passes through Recent since unchanged when newer than 90-day window (D-33)", function() seed_token("org-6"); local raw, _ = Fixtures.load("purchases/purchases_empty"); Mocks.push_response({content=raw}); local recent = os.time() - 3600; RefreshAccount({accountNumber="org-6", currency="EUR", balance=0}, recent); local url = Mocks._last_request.url; assert.truthy(url:find("startDate=", 1, true)); -- recent value should round-trip into the URL within a 60-second window (URL-encoded ":" → "%3A") local recent_iso = os.date("!%Y-%m-%dT%H:%M:%SZ", recent); -- Tolerance: the year and month substring should appear in the URL. local year_month = recent_iso:sub(1, 7); assert.truthy(url:find(year_month, 1, true), "URL must contain " .. year_month .. ", got: " .. url) end)`

    9. `it("RefreshAccount preserves account.balance in return value (Phase 3 ACCT-03 not-yet-wired)", function() seed_token("org-7"); local raw, _ = Fixtures.load("purchases/purchases_empty"); Mocks.push_response({content=raw}); local result = RefreshAccount({accountNumber="org-7", currency="EUR", balance=123.45}, os.time() - 60); assert.is_table(result); assert.equals(123.45, result.balance) end)`

    Use `assert.equals`, `assert.is_string`, `assert.is_table`, `assert.is_true`, `assert.is_false`, `assert.truthy(s:find(...))`. Add diagnostic strings on every assert. The new describe block must close cleanly before the outer describe's `end)`.

    DO NOT modify pre-existing tests or helpers; the new block is additive.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/entry_spec.lua spec/refresh_idempotency_spec.lua spec/mapping_schema_spec.lua 2>&amp;1 | tee /tmp/wave4.log &amp;&amp; grep -qE '0 failures' /tmp/wave4.log &amp;&amp; ! grep -E 'Pending|Error' /tmp/wave4.log &amp;&amp; grep -c '^  it(' spec/entry_spec.lua | awk '{ if ($1 &gt;= 25) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RefreshAccount Phase-3 pipeline' spec/entry_spec.lua` reports 1 (the new describe block exists)
    - `grep -c '^  it(' spec/entry_spec.lua` reports >= 25 (existing tests + 9 new ones)
    - `busted spec/entry_spec.lua` is GREEN (no regression on existing tests + 9 new tests pass)
    - `busted spec/refresh_idempotency_spec.lua` is FULLY GREEN — all 4 Wave-1 tests pass (the gating contract is now satisfied)
    - `busted spec/mapping_schema_spec.lua` is still GREEN
    - `busted spec/mapping_spec.lua spec/pagination_spec.lua spec/purchases_spec.lua` is GREEN
    - `busted spec/` (full suite) reports `0 failures` and `0 errors` for the FIRST time in Phase 3 — the entire phase is functionally complete
    - `lua tools/build.lua --verify` reports OK: reproducible
  </acceptance_criteria>
  <done>
    spec/entry_spec.lua extended with 9 new RefreshAccount integration tests; spec/refresh_idempotency_spec.lua fully GREEN; full suite GREEN. Commit: `test(03-06): extend entry spec with Phase-3 RefreshAccount integration tests`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MoneyMoney → src/entry.lua RefreshAccount(account, since) | account is supplied by MoneyMoney; account.accountNumber treated as orgUuid (Phase-2 D-23a); guarded against missing/empty before any cache lookup. |
| LocalStorage → M_auth.cached_token | Pre-existing Phase-2 surface; no new exposure in this plan. |
| Zettle JSON → M_purchases.fetch_all → M_mapping | Already covered by Phase-2 pcall in M_http.get_json + Phase-3 mapping guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W4-01 | Information Disclosure | src/entry.lua RefreshAccount M_log.info line | mitigate | The log line includes only the first 8 chars of orgUuid (orgUuid:sub(1,8)) and the effective_since integer; no Bearer, no API key, no full UUID. SEC-03 invariant preserved. |
| T-03-W4-02 | Tampering | account.accountNumber injection | mitigate | Guarded as non-empty string before any cache lookup; missing/empty returns German error.network envelope. No way for MoneyMoney to drive a Lua-level crash via malformed account table. |
| T-03-W4-03 | Information Disclosure | Partial-transactions-with-error return | mitigate | ERR-06 fail-whole-refresh: `if fetch_err then return fetch_err end` — return is mutually exclusive (transactions table OR error string), never both. MoneyMoney's `since` watermark cannot advance past undelivered data. |
| T-03-W4-04 | Tampering | math.max since clamp boundary | mitigate | Clamp is at the entry boundary per Pitfall 5, visible for debug; can't be silently bypassed by passing a negative `since`. |
| T-03-W4-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Pure-Lua source edit + spec extension only. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin
lua tools/build.lua && lua tools/build.lua --verify

# All Phase-3 specs GREEN for the first time:
./.luarocks/bin/busted spec/ 2>&1 | grep -E '0 failures'

# Specifically the Wave-1 idempotency gate now passes:
./.luarocks/bin/busted spec/refresh_idempotency_spec.lua 2>&1 | grep -E '0 failures'

# Pre-existing Phase-2 specs unchanged:
./.luarocks/bin/busted spec/auth_spec.lua spec/http_spec.lua spec/log_redaction_spec.lua 2>&1 | grep -E '0 failures'

# Egress sanity check:
grep -E 'https?://[a-z.]+' dist/paypal-pos.lua | sort -u
# Expected: https://oauth.zettle.com/token, /users/self + https://purchase.izettle.com/purchases/v2
```
</verification>

<success_criteria>
- src/entry.lua RefreshAccount drives the real pipeline (cached_token → clamp → fetch_all → map → return)
- Phase-1 fixture body (balance=9.95, fixture-0001 transactionCode) is removed
- SupportsBank, InitializeSession2, ListAccounts, EndSession byte-identical to pre-Wave-4 state (only diff inside RefreshAccount body)
- spec/entry_spec.lua has 25+ tests (pre-existing + 9 new); all green
- spec/refresh_idempotency_spec.lua is FULLY GREEN — 4 tests pass
- spec/mapping_schema_spec.lua + mapping_spec.lua + pagination_spec.lua + purchases_spec.lua remain GREEN
- Full suite `busted spec/` reports 0 failures, 0 errors for the first time
- `lua tools/build.lua --verify` is reproducible
- The end-of-phase demo is now possible: a maintainer with a real PayPal POS API key can install dist/paypal-pos.lua, see card sales appear, refresh twice, see zero duplicates
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `src/entry.lua` — RefreshAccount rewired; other four callbacks unchanged
- `spec/entry_spec.lua` — extended with new "RefreshAccount Phase-3 pipeline" describe block (9 tests)

New module-level state in src/entry.lua:
- `NINETY_DAYS = 90 * 86400` (module-local constant, file scope)

Public symbols touched (signature unchanged):
- `RefreshAccount(account, since)` — body completely replaced; signature and luacheck annotation preserved

Files NOT modified: `tools/manifest.txt`, `src/webbanking_header.lua`, `src/mapping.lua`, `src/pagination.lua`, `src/purchases.lua`, `src/i18n.lua`, `src/auth.lua`, `src/http.lua`, `src/errors.lua`, `src/log.lua`, all helper / pre-existing spec files.

Status of gating specs after this plan:
- spec/refresh_idempotency_spec.lua → FULLY GREEN (the Phase-3 load-bearing gate is now satisfied)
- spec/mapping_schema_spec.lua → STILL GREEN
- spec/mapping_spec.lua, pagination_spec.lua, purchases_spec.lua, dst_table_spec.lua, i18n_spec.lua → STILL GREEN
- Full Phase-3 acceptance: 0 failures, 0 errors across the entire suite for the first time
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-06-SUMMARY.md` summarising:
- RefreshAccount pipeline steps (1 orgUuid guard, 2 cached_token, 3 since clamp, 4 fetch_all, 5 map, 6 return)
- Confirmation that SupportsBank / InitializeSession2 / ListAccounts / EndSession are byte-identical to pre-Wave-4
- entry_spec.lua new-test count (9)
- spec/refresh_idempotency_spec.lua final status (4 GREEN — gating contract satisfied)
- Full-suite status (0/0/0 for the first time in Phase 3)
- Reproducible build SHA after this plan
- Egress sanity output (3 URLs expected: oauth.zettle.com/token, /users/self, purchase.izettle.com/purchases/v2)
- Any subtle bookkeeping notes for the maintainer's first end-of-phase real-key demo (per VALIDATION.md "Manual-Only Verifications")
</output>
