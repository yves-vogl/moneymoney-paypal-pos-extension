---
phase: 03-sale-spine-first-user-visible-slice
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/mapping.lua
  - spec/mapping_spec.lua
autonomous: true
requirements: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-08, I18N-01, TEST-04]
requirements_addressed: [SALE-01, SALE-02, SALE-03, SALE-04, SALE-08, I18N-01, TEST-04]
tags: [wave-2, mapping, pure-logic, dst, i18n, mvp]

must_haves:
  truths:
    - "src/mapping.lua fills the Phase-1 stub with M_mapping.purchase_to_transaction(p) and M_mapping.refund_to_transaction(p) — both pure functions returning a MoneyMoney transaction table or nil (D-40)"
    - "purchase_to_transaction returns nil when p.currency ~= \"EUR\" (D-37 silent skip)"
    - "purchase_to_transaction sets booked = false on every Phase-3 transaction; valueDate is OMITTED (no key written), not set to nil explicitly — Phase 4 will fill these without breaking the dedup contract (D-31)"
    - "purchase_to_transaction sets transactionCode = \"zettle:sale:\" .. p.purchaseUUID1; refund_to_transaction sets transactionCode = \"zettle:refund:\" .. p.purchaseUUID1 (D-38)"
    - "purchase_to_transaction sets amount = p.amount / 100 (Lua number, ADR-0003 Q4 confirmed; SALE-01); refund_to_transaction sets amount = p.amount / 100 — Zettle already returns a negative amount on refund records so we DO NOT negate it (D-32, RESEARCH §1 amount-field semantic)"
    - "purchase_to_transaction sets bookingDate = _to_berlin_local_time(_parse_iso8601_utc(p.timestamp)) — a Berlin-local-seconds POSIX integer (D-36 / SALE-04)"
    - "purchase_to_transaction sets name via _format_label(p.payments): default \"Kartenzahlung\" via M_i18n.t(\"account.name.card_payment\"); upgrade to \"<Brand> •••• <last4>\" when payments[1].attributes.cardType and payments[1].attributes.maskedPan are present per RESEARCH §1 Payment Sub-Object correction over CONTEXT D-35 wording (cardBrand / cardLastFour are NOT used)"
    - "refund_to_transaction sets name by composing the sale-name plus \" Rückerstattung\" suffix (D-32 / D-35)"
    - "purchase_to_transaction sets purpose via _format_purpose(p) producing the D-34 multi-line German bookkeeping string: line 1 Brutto: X,YY € (always), line 2 MwSt: X,YY € (when p.vatAmount > 0), line 3 Trinkgeld: X,YY € (when sum of payments[].gratuityAmount > 0), line 4 Netto: X,YY € (always), final line Beleg #<purchaseNumber>"
    - "refund_to_transaction sets purpose first line via M_i18n.t(\"account.purpose.refund_for\", <original-purchaseNumber-if-resolvable-else-refundsPurchaseUUID1>); final line Beleg #<refund-own-purchaseNumber>"
    - "_format_amount(minor_units) converts integer minor units to German-formatted string (comma decimal separator, no thousands separator below 10000 — D-34)"
    - "Six new i18n keys added to STRINGS.de and STRINGS.en in src/i18n.lua: account.purpose.gross, account.purpose.vat, account.purpose.tip, account.purpose.net, account.purpose.refund_for, account.purpose.receipt_number, account.name.card_payment"
    - "DST table inlined inside src/mapping.lua per RESEARCH §2b — 21 rows for years 2020-2040 generated by deterministic last_sunday_utc helper at the planner level (see action for the exact 21 inline values); _to_berlin_local_time(utc_posix) walks DST_TABLE, picks 7200 offset when utc_posix is inside a [summer_start, summer_end) interval, 3600 otherwise"
    - "_parse_iso8601_utc(s) handles both \"%Y-%m-%dT%H:%M:%S%.?%d*+0000\" and \"%Y-%m-%dT%H:%M:%S%.?%d*Z\" forms (Pitfall 3 in RESEARCH §3); returns integer POSIX seconds via os.time({...}) — Lua os.time is documented as TZ-independent when fed UTC components (ADR-0003 Q1 carryover)"
    - "spec/mapping_spec.lua fills its Wave-0 pending scaffolds — every pending test becomes a passing it(); spec/dst_table_spec.lua also turns green via the inlined DST_TABLE; spec/mapping_schema_spec.lua Tests 1-4 and 7-8 turn green (Tests 5+6 require DST math which is also Wave 2)"
    - "tools/manifest.txt is NOT modified — DST table is inlined inside src/mapping.lua (D-40 planner discretion exercised; no src/timezone.lua hoisted)"
    - "src/webbanking_header.lua is NOT modified — M_mapping is already pre-declared"
    - "src/mapping.lua follows the standard module preamble (ownership comment, M_* table pre-declared note, no require() of siblings — D-02); pure helpers are local function inside the do…end block"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts:
    - path: "src/mapping.lua"
      provides: "M_mapping.purchase_to_transaction, M_mapping.refund_to_transaction (public) plus _format_amount / _format_purpose / _format_label / _to_berlin_local_time / _parse_iso8601_utc (private)"
      contains: "M_mapping.purchase_to_transaction"
      min_lines: 150
    - path: "src/i18n.lua"
      provides: "Six new account.purpose.* and account.name.card_payment keys in STRINGS.de + STRINGS.en"
      contains: "account.purpose.gross"
    - path: "spec/mapping_spec.lua"
      provides: "Filled unit specs for M_mapping.* covering SALE-01 / SALE-02 / SALE-04 / SALE-08 / I18N-01 / D-32 / D-34 / D-35 / D-37 / D-38"
      contains: "purchase_to_transaction"
  key_links:
    - from: "src/mapping.lua _format_label"
      to: "p.payments[1].attributes.cardType + p.payments[1].attributes.maskedPan"
      via: "guarded chained access; :sub(-4) for last four characters of maskedPan"
      pattern: "attributes%.cardType"
    - from: "src/mapping.lua purchase_to_transaction"
      to: "M_i18n.t(\"account.purpose.gross\", ...) and account.purpose.* family"
      via: "string.format via M_i18n.t with pre-formatted German amount via _format_amount"
      pattern: "account%.purpose%."
    - from: "src/mapping.lua _to_berlin_local_time"
      to: "DST_TABLE inline literal"
      via: "linear scan picks 7200 offset when utc_posix in summer interval, 3600 otherwise"
      pattern: "DST_TABLE"
    - from: "spec/dst_table_spec.lua (Wave 0)"
      to: "src/mapping.lua _to_berlin_local_time"
      via: "pending tests now pass with the boundary cases 2026-06 / 2026-01"
      pattern: "_to_berlin_local_time"
    - from: "spec/mapping_schema_spec.lua (Wave 1)"
      to: "src/mapping.lua M_mapping.purchase_to_transaction + refund_to_transaction"
      via: "Tests 1-4 + 7-8 (and Tests 5+6 once DST table inlined) turn green"
      pattern: "M_mapping"
---

<objective>
Fill src/mapping.lua with the pure-logic Phase-3 mapping: purchase_to_transaction, refund_to_transaction, the four private helpers (_format_amount, _format_purpose, _format_label, _to_berlin_local_time), and the inline DST_TABLE for years 2020-2040 per D-36. Extend src/i18n.lua with the six new German keys per D-34 / D-35. Fill spec/mapping_spec.lua so every Wave-0 pending becomes a passing it(); turn spec/dst_table_spec.lua green; turn spec/mapping_schema_spec.lua Tests 1-4 and 7-8 green (Tests 5+6 also green via the DST math).

Purpose: This is the central pure-logic milestone of Phase 3 — every downstream wave (pagination, purchases fetch, entry rewire) feeds purchases through this mapping. The schema gate (TEST-04) and idempotency gate (TEST-03) both partially turn green once mapping deterministically emits the seven required fields. Wave 3 then wires HTTP fetch + pagination; Wave 4 rewires entry.lua RefreshAccount; the gating specs go fully green.

Output: src/mapping.lua with two public functions and 5 private helpers including the 21-row inline DST_TABLE; src/i18n.lua with six additional keys (de + en); spec/mapping_spec.lua filled with passing unit tests; spec/dst_table_spec.lua green; spec/mapping_schema_spec.lua Tests 1-4 and 5-8 green; Wave-1 idempotency spec partially advances (mapping deterministic, but entry.lua still returns fixture body until Wave 4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-02-PLAN.md
@src/mapping.lua
@src/i18n.lua
@src/auth.lua
@src/errors.lua
@src/webbanking_header.lua
@spec/mapping_spec.lua
@spec/dst_table_spec.lua
@spec/mapping_schema_spec.lua
@spec/refresh_idempotency_spec.lua
@spec/auth_spec.lua
@docs/adr/0003-sandbox-probe-results.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend src/i18n.lua with six new German keys (D-34 / D-35 / I18N-01)</name>
  <read_first>
    - src/i18n.lua (current STRINGS.de and STRINGS.en layout; existing key names use "purpose.gross", "purpose.vat_line" with `%.2f` float format — Phase 3 ADDS new "account.purpose.*" keys using `%s` because amounts are pre-formatted via _format_amount)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §5 "i18n Keys (Phase 3 Additions)" — verbatim key list
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-34 / D-35
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"src/i18n.lua — extend STRINGS tables"
    - spec/i18n_spec.lua (current i18n spec; verify nothing existing breaks by adding keys)
  </read_first>
  <files>src/i18n.lua, spec/i18n_spec.lua</files>
  <behavior>
    - M_i18n.t("account.purpose.gross", "9,95") returns "Brutto: 9,95 €" (de) or "Gross: 9,95 €" (en)
    - M_i18n.t("account.purpose.vat", "1,59") returns "MwSt: 1,59 €" / "VAT: 1,59 €"
    - M_i18n.t("account.purpose.tip", "1,00") returns "Trinkgeld: 1,00 €" / "Tip: 1,00 €"
    - M_i18n.t("account.purpose.net", "7,36") returns "Netto: 7,36 €" / "Net: 7,36 €"
    - M_i18n.t("account.purpose.refund_for", "1001") returns "Rückerstattung zu Beleg #1001" / "Refund for receipt #1001"
    - M_i18n.t("account.purpose.receipt_number", "1001") returns "Beleg #1001" / "Receipt #1001"
    - M_i18n.t("account.name.card_payment") returns "Kartenzahlung" / "Card payment"
    - Existing keys ("purpose.gross", "purpose.vat_line", etc.) are NOT modified — Wave 3 mapping uses the new "account.purpose.*" family because the old keys hard-code "%.2f" floats with dot separators
  </behavior>
  <action>
    Edit src/i18n.lua. Locate the STRINGS.de table. After the existing `["purpose.refund_of"] = "Erstattung zu Beleg %s"` line, add the seven new keys with `%s` placeholders (because the mapping module pre-formats the amount via _format_amount to get German comma separator). Add the same seven keys to STRINGS.en. Do NOT modify existing keys (purpose.gross, purpose.vat_line, purpose.tip, purpose.uuid, purpose.refund_of, account.name, transaction.name.*, error.*, credential.api_key.label) — those serve the Phase-1 fixture and adjacent specs.

    Keys to add (apply to both STRINGS.de and STRINGS.en, German strings normative per RESEARCH §5):
    - de: `["account.purpose.gross"] = "Brutto: %s €"` / en: `["account.purpose.gross"] = "Gross: %s €"`
    - de: `["account.purpose.vat"] = "MwSt: %s €"` / en: `["account.purpose.vat"] = "VAT: %s €"`
    - de: `["account.purpose.tip"] = "Trinkgeld: %s €"` / en: `["account.purpose.tip"] = "Tip: %s €"`
    - de: `["account.purpose.net"] = "Netto: %s €"` / en: `["account.purpose.net"] = "Net: %s €"`
    - de: `["account.purpose.refund_for"] = "Rückerstattung zu Beleg #%s"` / en: `["account.purpose.refund_for"] = "Refund for receipt #%s"`
    - de: `["account.purpose.receipt_number"] = "Beleg #%s"` / en: `["account.purpose.receipt_number"] = "Receipt #%s"`
    - de: `["account.name.card_payment"] = "Kartenzahlung"` / en: `["account.name.card_payment"] = "Card payment"`

    Extend spec/i18n_spec.lua with a new describe block titled "Phase 3 account.purpose.* keys (D-34 / D-35 / I18N-01)" containing seven tests, one per new key, asserting the German output equals the expected literal. Use the existing spec preamble pattern (no Mocks needed — i18n is pure).
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/i18n_spec.lua 2>&amp;1 | grep -E '(Success|Failure)' | grep -qE '0 failures' &amp;&amp; lua -e 'dofile("dist/paypal-pos.lua"); assert(M_i18n.t("account.purpose.gross", "9,95") == "Brutto: 9,95 €", "gross"); assert(M_i18n.t("account.purpose.vat", "1,59") == "MwSt: 1,59 €", "vat"); assert(M_i18n.t("account.purpose.tip", "1,00") == "Trinkgeld: 1,00 €", "tip"); assert(M_i18n.t("account.purpose.net", "7,36") == "Netto: 7,36 €", "net"); assert(M_i18n.t("account.purpose.refund_for", "1001") == "Rückerstattung zu Beleg #1001", "refund_for"); assert(M_i18n.t("account.purpose.receipt_number", "1001") == "Beleg #1001", "receipt"); assert(M_i18n.t("account.name.card_payment") == "Kartenzahlung", "card"); print("OK")' | grep -q '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"account.purpose.gross"' src/i18n.lua` reports 2 (de + en)
    - `grep -c '"account.name.card_payment"' src/i18n.lua` reports 2
    - All seven new keys appear in BOTH STRINGS.de and STRINGS.en
    - `busted spec/i18n_spec.lua` is GREEN (no regression to existing keys + new tests pass)
    - `lua tools/build.lua --verify` is still reproducible (the new keys are byte-identical across two consecutive builds)
    - `luacheck spec/i18n_spec.lua src/i18n.lua 2>/dev/null || true` (env exception)
  </acceptance_criteria>
  <done>
    src/i18n.lua has the seven new keys in both STRINGS.de and STRINGS.en; spec/i18n_spec.lua has seven new passing tests; reproducible build still green. Commit: `feat(03-03): add German i18n keys for Phase 3 mapping (D-34 / D-35 / I18N-01)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill src/mapping.lua with public + private functions and inline DST_TABLE</name>
  <read_first>
    - src/mapping.lua (current Phase-1 stub — three lines)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §1 (purchase JSON fields, especially the Payment Sub-Object correction: card metadata under payments[].attributes.cardType + maskedPan)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2b (DST table shape + _to_berlin_local_time signature + last_sunday_utc helper) — note the [ASSUMED] timestamps in RESEARCH §2b are SUPERSEDED by the planner's pre-computed deterministic values in the action below
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §3 (Anti-Patterns: never use os.date("*t") on raw UTC; never use cardBrand/cardLastFour direct access; ISO-8601 pattern must tolerate optional ".SSS" fraction)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-31, D-32, D-34, D-35, D-36, D-37, D-38
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"src/mapping.lua" (pure-function shape from src/auth.lua _decode_jwt_payload / _extract_client_id)
    - src/auth.lua (private helper scoping: `local function _b64url_decode(s)` inside do…end; same convention applies to _format_amount / _format_purpose / _format_label / _to_berlin_local_time / _parse_iso8601_utc)
    - src/i18n.lua (after Task 1 — for M_i18n.t calls)
    - docs/adr/0003-sandbox-probe-results.md Q1 (Lua os.time is TZ-independent when fed UTC component table) and Q4 (JSON integer round-trip — amount stored as Lua number)
    - spec/auth_spec.lua (pure-logic spec style — inline-table inputs + Fixtures.load for complex cases)
  </read_first>
  <files>src/mapping.lua</files>
  <behavior>
    - M_mapping.purchase_to_transaction(p) returns nil when type(p.currency) ~= "string" or p.currency ~= "EUR" (D-37); otherwise returns a table {name, amount, currency, bookingDate, purpose, transactionCode, booked}
    - M_mapping.purchase_to_transaction(p) emits booked = false; does NOT write a valueDate key
    - M_mapping.purchase_to_transaction(p) emits transactionCode = "zettle:sale:" .. p.purchaseUUID1
    - M_mapping.purchase_to_transaction(p) emits amount = p.amount / 100 (Lua number)
    - M_mapping.purchase_to_transaction(p) emits bookingDate = _to_berlin_local_time(_parse_iso8601_utc(p.timestamp))
    - M_mapping.purchase_to_transaction(p) emits name via _format_label(p.payments)
    - M_mapping.purchase_to_transaction(p) emits purpose via _format_purpose(p) producing the D-34 multi-line German string
    - M_mapping.refund_to_transaction(p) returns a table with the same 7 fields; transactionCode = "zettle:refund:" .. p.purchaseUUID1; amount = p.amount / 100 (Zettle delivers negative amount for refunds per RESEARCH §1); name has " Rückerstattung" suffix; purpose first line uses M_i18n.t("account.purpose.refund_for", original_receipt_or_uuid_fallback); final purpose line uses M_i18n.t("account.purpose.receipt_number", refund_own_purchaseNumber); booked = false
    - _format_amount(995) returns "9,95"; _format_amount(1995) returns "19,95"; _format_amount(500) returns "5,00"; _format_amount(0) returns "0,00"; _format_amount(-995) returns "-9,95"
    - _format_label(nil) returns "Kartenzahlung"; _format_label({}) returns "Kartenzahlung"; _format_label({{type="IZETTLE_CARD", attributes={cardType="VISA", maskedPan="411111******1111"}}}) returns "Visa •••• 1111" (U+2022 bullets)
    - _format_label brand map: VISA→Visa, MASTERCARD→Mastercard, AMEX→Amex, MAESTRO→Maestro, GIROCARD→girocard, UNIONPAY→UnionPay; unknown brand → capitalised literal (e.g. DISCOVER→Discover)
    - _format_label guards: payments[1] and payments[1].attributes and payments[1].attributes.cardType — every level guarded; missing maskedPan or maskedPan not 16+ chars falls back to "Kartenzahlung"
    - _parse_iso8601_utc("2026-06-19T23:55:00.000+0000") returns POSIX integer for that UTC instant; same for "Z" suffix
    - _parse_iso8601_utc returns nil for malformed input
    - _to_berlin_local_time(utc_posix) returns utc_posix + 7200 when utc_posix is inside any DST_TABLE[year] = {summer_start, summer_end} interval, else utc_posix + 3600
    - _to_berlin_local_time on POSIX(2026-06-19T23:55:00Z) = 1781913300 returns 1781920500 (CEST +02:00); decomposing via os.date("!*t", 1781920500) yields y=2026 m=6 d=20 h=1 min=55
    - _to_berlin_local_time on POSIX(2026-01-31T23:55:00Z) = 1769903700 returns 1769907300 (CET +01:00); decomposing yields y=2026 m=2 d=1 h=0 min=55
  </behavior>
  <action>
    Rewrite src/mapping.lua in full (the file is currently a three-line stub). Follow the standard Phase-2 module preamble (PATTERNS.md §"Module preamble"). Structure of the file:

    Module-level comment block (lines 1-9): cite ownership of SALE-01, SALE-02, SALE-03 (partial — Phase 3 emits booked=false half per D-31), SALE-04, SALE-08, I18N-01, D-31, D-32, D-34, D-35, D-36, D-37, D-38. "Provides: M_mapping.purchase_to_transaction(p), M_mapping.refund_to_transaction(p)". "The M_mapping table is predeclared in src/webbanking_header.lua." "NO require() of sibling modules (D-02)."

    Module-local DST_TABLE (use these EXACT integer pairs — precomputed deterministically by the planner via last_sunday_utc(year, 3, 1) and last_sunday_utc(year, 10, 1); 21 rows for years 2020-2040):
      `{1585443600, 1603587600}`  -- 2020 (Mar 29 / Oct 25)
      `{1616893200, 1635642000}`  -- 2021 (Mar 28 / Oct 31)
      `{1648342800, 1667091600}`  -- 2022 (Mar 27 / Oct 30)
      `{1679792400, 1698541200}`  -- 2023 (Mar 26 / Oct 29)
      `{1711846800, 1729990800}`  -- 2024 (Mar 31 / Oct 27)
      `{1743296400, 1761440400}`  -- 2025 (Mar 30 / Oct 26)
      `{1774746000, 1792890000}`  -- 2026 (Mar 29 / Oct 25)
      `{1806195600, 1824944400}`  -- 2027 (Mar 28 / Oct 31)
      `{1837645200, 1856394000}`  -- 2028 (Mar 26 / Oct 29)
      `{1869094800, 1887843600}`  -- 2029 (Mar 25 / Oct 28)
      `{1901149200, 1919293200}`  -- 2030 (Mar 31 / Oct 27)
      `{1932598800, 1950742800}`  -- 2031 (Mar 30 / Oct 26)
      `{1964048400, 1982797200}`  -- 2032 (Mar 28 / Oct 31)
      `{1995498000, 2014246800}`  -- 2033 (Mar 27 / Oct 30)
      `{2026947600, 2045696400}`  -- 2034 (Mar 26 / Oct 29)
      `{2058397200, 2077146000}`  -- 2035 (Mar 25 / Oct 28)
      `{2090451600, 2108595600}`  -- 2036 (Mar 30 / Oct 26)
      `{2121901200, 2140045200}`  -- 2037 (Mar 29 / Oct 25)
      `{2153350800, 2172099600}`  -- 2038 (Mar 28 / Oct 31)
      `{2184800400, 2203549200}`  -- 2039 (Mar 27 / Oct 30)
      `{2216250000, 2234998800}`  -- 2040 (Mar 25 / Oct 28)

    Each row's left integer is POSIX of "last Sunday of March at 01:00 UTC" for the year, right integer is POSIX of "last Sunday of October at 01:00 UTC". Verified independently: row 7 (2026) gives `{1774746000, 1792890000}` so POSIX(2026-06-19T23:55Z) = 1781913300 sits inside the interval and gets +7200 offset → local POSIX 1781920500 → local 2026-06-20T01:55; POSIX(2026-01-31T23:55Z) = 1769903700 sits BEFORE 1774746000 so gets +3600 → local 1769907300 → local 2026-02-01T00:55.

    Private helpers (each `local function` inside the do…end block, scoped to the module):

    `local function _format_amount(minor_units)` — divides by 100 to get euros, formats with `string.format("%.2f", euros)` then replaces "." with "," via gsub. Handles negative input by formatting then placing the minus sign before the integer part (or simpler: format as-is — `string.format("%.2f", -9.95)` already yields "-9.95" which becomes "-9,95"). No thousands separator below 10000 (D-34) — for the v0.1.0 release, omit thousands separator entirely (revisit if a real user has a transaction over €10,000).

    `local function _parse_iso8601_utc(s)` — guards `type(s) == "string"`; matches the pattern `"(%d+)-(%d+)-(%d+)T(%d+):(%d+):(%d+)"` with optional fraction `"%.?%d*"` and either `Z` or `+0000` suffix (Pitfall 3); on match, calls `os.time({year=Y, month=M, day=D, hour=H, min=Mi, sec=S})` and returns the integer; on no match, returns nil. ADR-0003 Q1 confirms Lua os.time treats the components as UTC by default on the MoneyMoney runtime; on CI (Linux UTC) this is also correct. For belt-and-suspenders correctness across non-UTC dev machines, ALSO subtract the system's TZ offset from the result using `os.time(os.date("*t", reference)) - os.time(os.date("!*t", reference))` for a known reference, then add it back — or use the deterministic days-since-epoch arithmetic from RESEARCH §2b's last_sunday_utc helper. Pick whichever is simpler to test on macOS + Linux CI. Note in code comment: "POSIX result is UTC-anchored; _to_berlin_local_time adds Berlin offset".

    `local function _to_berlin_local_time(utc_posix)` — linear-scans DST_TABLE; if utc_posix >= entry[1] and utc_posix < entry[2] then offset = 7200 else continues; default 3600. Returns utc_posix + offset.

    `local function _format_label(payments)` — guards payments table; reads `payments[1]`, then `payments[1].attributes`, then `payments[1].attributes.cardType`, then `payments[1].attributes.maskedPan`. **CRITICAL**: uses `attributes.cardType` and `attributes.maskedPan` per RESEARCH §1 correction over CONTEXT D-35 wording. Brand map: VISA→"Visa", MASTERCARD→"Mastercard", AMEX→"Amex", MAESTRO→"Maestro", GIROCARD→"girocard", UNIONPAY→"UnionPay"; unknown brand falls through to the capitalized literal via `cardType:sub(1,1):upper() .. cardType:sub(2):lower()`. last_four = `maskedPan:sub(-4)` only when `type(maskedPan) == "string" and #maskedPan >= 4`; otherwise falls back to "Kartenzahlung". Bullet character U+2022 = `"\xe2\x80\xa2"` (UTF-8 byte sequence). Result string: `brand .. " " .. "\xe2\x80\xa2\xe2\x80\xa2\xe2\x80\xa2\xe2\x80\xa2" .. " " .. last_four`. Default return: `M_i18n.t("account.name.card_payment")`.

    `local function _format_purpose(p, opts)` — opts is a small table with the kind: `{kind="sale"}` or `{kind="refund", original_receipt=<int-or-nil>}`. For sales: build lines = {}; insert M_i18n.t("account.purpose.gross", _format_amount(p.amount)); compute vat = p.vatAmount or 0; if type(vat) == "number" and vat > 0 then insert M_i18n.t("account.purpose.vat", _format_amount(vat)); compute tip_sum by iterating p.payments and summing gratuityAmount (guard nils); if tip_sum > 0 then insert M_i18n.t("account.purpose.tip", _format_amount(tip_sum)); net = p.amount - vat - tip_sum; insert M_i18n.t("account.purpose.net", _format_amount(net)); insert M_i18n.t("account.purpose.receipt_number", tostring(p.purchaseNumber)); table.concat(lines, "\n"). For refunds: first line uses M_i18n.t("account.purpose.refund_for", tostring(opts.original_receipt or p.refundsPurchaseUUID1)); then the same Brutto/MwSt/Trinkgeld/Netto/Beleg lines using p.amount (which is already negative on a refund record — _format_amount returns a "-X,YY" string).

    Public functions:

    `function M_mapping.purchase_to_transaction(p)` — guard `if type(p) ~= "table" then return nil end`; D-37 check `if type(p.currency) ~= "string" or p.currency ~= "EUR" then M_log.info("M_mapping.purchase_to_transaction: skipping non-EUR purchase currency=" .. tostring(p.currency)); return nil end`; build txn = {}: txn.name = _format_label(p.payments); txn.amount = (p.amount or 0) / 100; txn.currency = "EUR"; local utc = _parse_iso8601_utc(p.timestamp); txn.bookingDate = utc and _to_berlin_local_time(utc) or os.time(); txn.purpose = _format_purpose(p, {kind="sale"}); txn.transactionCode = "zettle:sale:" .. tostring(p.purchaseUUID1); txn.booked = false; return txn. (Do NOT set txn.valueDate — omit the key entirely per D-31.)

    `function M_mapping.refund_to_transaction(p)` — same guards as above; build txn: txn.name = _format_label(p.payments) .. " Rückerstattung" (or M_i18n.t for the suffix if a key is added later — for Phase 3 inline is acceptable, but PREFER a small i18n key extension only if needed; Phase 4 may revisit); txn.amount = (p.amount or 0) / 100; txn.currency = "EUR"; txn.bookingDate = utc and _to_berlin_local_time(utc) or os.time(); txn.purpose = _format_purpose(p, {kind="refund"}); txn.transactionCode = "zettle:refund:" .. tostring(p.purchaseUUID1); txn.booked = false; return txn.

    Apply `-- luacheck: ignore <code>` annotations where loop variables shadow outer scope (rare in this file). Do NOT add `require()` of siblings (D-02). Do NOT add `pcall` around any operation (no attacker-controlled input here — input is already parsed by M_http.get_json's pcall + dkjson).

    Run `lua tools/build.lua` after the edit and confirm the artifact builds. The manifest stays unchanged.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/dst_table_spec.lua spec/mapping_schema_spec.lua 2>&amp;1 | tee /tmp/wave2_mapping.log ; grep -qE '0 failures' /tmp/wave2_mapping.log &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'DST_TABLE' src/mapping.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/mapping.lua` reports >= 150 lines (substantial implementation)
    - `grep -c 'DST_TABLE' src/mapping.lua` reports >= 1 (table declaration present)
    - `grep -c '{1774746000, 1792890000}' src/mapping.lua` reports 1 (the 2026 boundary row — sanity check the table is intact)
    - `grep -n 'attributes.cardType' src/mapping.lua` finds the corrected path access (RESEARCH §1)
    - `grep -L 'cardBrand\|cardLastFour' src/mapping.lua` succeeds (deprecated D-35 names MUST NOT appear)
    - `grep -n 'zettle:sale:' src/mapping.lua` finds the sale prefix
    - `grep -n 'zettle:refund:' src/mapping.lua` finds the refund prefix
    - `grep -n 'booked = false' src/mapping.lua` appears at least twice (sale + refund)
    - `grep -L 'valueDate' src/mapping.lua` succeeds (no valueDate key written — D-31)
    - `grep -L 'require(' src/mapping.lua` succeeds (no sibling require, D-02)
    - `grep -L 'pcall' src/mapping.lua` succeeds (no pcall in this module per Phase-2 carryover)
    - `lua tools/build.lua --verify` reports `OK: reproducible (sha256: ...)`
    - `busted spec/dst_table_spec.lua` is GREEN (all five+ pending tests now pass)
    - `busted spec/mapping_schema_spec.lua` is GREEN (all 8 tests now pass — schema gate fully turned)
    - `busted spec/` exits non-zero ONLY due to spec/refresh_idempotency_spec.lua (Wave 4 fix) and pending scaffolds for purchases/pagination (Wave 3 fix); no previously-passing spec regresses
  </acceptance_criteria>
  <done>
    src/mapping.lua filled with M_mapping.purchase_to_transaction + refund_to_transaction + the inline 21-row DST_TABLE + the four/five private helpers; reproducible build green; spec/dst_table_spec.lua and spec/mapping_schema_spec.lua both turn fully GREEN. Commit: `feat(03-03): implement purchase/refund mapping with inline DST table (SALE-01/02/03/04/08, D-31/32/34/35/36/37/38)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill spec/mapping_spec.lua (replace Wave-0 pending tests with passing units)</name>
  <read_first>
    - spec/mapping_spec.lua (Wave 0 pending scaffold — 16+ pending tests with verbatim names)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/mapping_spec.lua" — pure-logic spec pattern from spec/auth_spec.lua L175-261
    - spec/auth_spec.lua (pure-logic call pattern: load fixture, call function directly with controlled input, assert specific fields)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md (fixture inventory — which fixture has VAT, which has tip, which has card metadata)
    - src/mapping.lua (after Task 2 — the implementation under test)
    - src/i18n.lua (after Task 1 — for cross-checking the German strings in purpose tests)
  </read_first>
  <files>spec/mapping_spec.lua</files>
  <behavior>
    - All 16+ pending tests from Wave 0 (03-01-PLAN.md Task 3) become passing it() with explicit assertions
    - Test names remain verbatim so `busted -t "<name>"` filters from VALIDATION.md still match
    - The sanity test "M_mapping module table is exposed" stays
    - The Fixtures.load nested-path verification test stays
    - Tests use a mix of fixture-driven input (via Fixtures.load) and inline-table input (for nil-guard / edge cases)
  </behavior>
  <action>
    Replace each `pending(name, function() end)` block in spec/mapping_spec.lua with `it(name, function() <assertions> end)` keeping the EXACT test name. Use Fixtures.load("purchases/<name>") for fixture-driven tests; inline `local p = { ... }` for unit-test edge cases. Common asserter helpers (declare local inside describe):

    `local function load_first(name) local _, f = Fixtures.load("purchases/"..name); return f.purchases[1] end`

    Test-by-test assertions:

    1. "purchase_to_transaction maps amount to EUR float (SALE-01)" — load purchase_simple_sale; txn = M_mapping.purchase_to_transaction(p); assert.equals(p.amount / 100, txn.amount); assert.equals(p.amount / 100, 5.00).

    2. "purchase_to_transaction sets transactionCode = zettle:sale:<purchaseUUID1> (SALE-02 / D-38)" — txn = M_mapping.purchase_to_transaction(p); assert.equals("zettle:sale:11111111-1111-1111-1111-111111111111", txn.transactionCode).

    3. "purchase_to_transaction sets booked = false and omits valueDate (D-31 / Phase 3 contract)" — assert.is_false(txn.booked); assert.is_nil(txn.valueDate); also assert that the raw table does NOT contain a valueDate key: `assert.is_nil(rawget(txn, "valueDate"))` if necessary.

    4. "purchase_to_transaction sets bookingDate via Berlin local time (SALE-04 / D-36)" — load purchase_dst_boundary_summer; txn = M_mapping.purchase_to_transaction(p); local t = os.date("!*t", txn.bookingDate); assert.equals(2026, t.year); assert.equals(6, t.month); assert.equals(20, t.day); assert.equals(1, t.hour); assert.equals(55, t.min).

    5. "purchase_to_transaction defaults name to Kartenzahlung when payments empty (SALE-08 / D-35)" — load purchase_simple_sale (payments = []); txn = M_mapping.purchase_to_transaction(p); assert.equals("Kartenzahlung", txn.name).

    6. "purchase_to_transaction upgrades name to <Brand> •••• <last4> when payments[0].attributes.cardType present (SALE-08 / D-35 corrected)" — load purchase_with_card_metadata; txn = M_mapping.purchase_to_transaction(p); assert.equals("Visa \xe2\x80\xa2\xe2\x80\xa2\xe2\x80\xa2\xe2\x80\xa2 1111", txn.name) (exact UTF-8 bullet sequence).

    7. "purchase_to_transaction purpose contains Brutto / MwSt / Trinkgeld / Netto / Beleg German lines (I18N-01 / D-34)" — load purchase_with_vat_and_tip; txn = M_mapping.purchase_to_transaction(p); assert.truthy(txn.purpose:find("Brutto: 19,95 €", 1, true)); assert.truthy(txn.purpose:find("MwSt: 3,18 €", 1, true)); assert.truthy(txn.purpose:find("Trinkgeld: 1,00 €", 1, true)); assert.truthy(txn.purpose:find("Netto: 15,77 €", 1, true)); assert.truthy(txn.purpose:find("Beleg #1002", 1, true)).

    8. "purchase_to_transaction omits MwSt line when vatAmount = 0 (D-34)" — load purchase_simple_sale (vatAmount=0); txn = M_mapping.purchase_to_transaction(p); assert.is_falsy(txn.purpose:find("MwSt", 1, true)); assert.truthy(txn.purpose:find("Brutto: 5,00 €", 1, true)); assert.truthy(txn.purpose:find("Netto: 5,00 €", 1, true)).

    9. "purchase_to_transaction omits Trinkgeld line when payments[].gratuityAmount sums to 0 (D-34)" — inline a purchase with empty payments and vatAmount=0; assert no Trinkgeld substring in purpose.

    10. "purchase_to_transaction returns nil for non-EUR purchase (D-37)" — load purchase_non_eur (currency="USD"); txn = M_mapping.purchase_to_transaction(p); assert.is_nil(txn).

    11. "refund_to_transaction returns negative amount (D-32)" — load purchase_refund (amount=-995); txn = M_mapping.refund_to_transaction(p); assert.is_true(txn.amount < 0); assert.equals(-9.95, txn.amount).

    12. "refund_to_transaction sets transactionCode = zettle:refund:<purchaseUUID1> (D-38)" — assert.equals("zettle:refund:33333333-3333-3333-3333-333333333333", txn.transactionCode).

    13. "refund_to_transaction purpose references original purchaseNumber via refundsPurchaseUUID1 (D-32)" — txn.purpose should contain "Rückerstattung zu Beleg #" followed by either the original receipt (if resolvable on the page — Phase 3 mapper falls back to the refundsPurchaseUUID1 UUID since same-page lookup is not implemented yet); assert.truthy(txn.purpose:find("Rückerstattung zu Beleg #", 1, true)). NOTE: Phase 4 will resolve refundsPurchaseUUID1 → original purchaseNumber via a same-page index; Phase 3 ships with the UUID literal in the purpose, which is acceptable per CONTEXT D-32 fallback.

    14. "refund_to_transaction name appends Rückerstattung suffix (D-32 / D-35)" — assert.truthy(txn.name:find("Rückerstattung", 1, true)). If payments are empty on the refund fixture, the name reads "Kartenzahlung Rückerstattung".

    15. "_format_amount renders 1995 minor units as 19,95 with German comma decimal (D-34)" — call M_mapping via a small re-export OR test the format through txn.purpose strings (since _format_amount is private). RECOMMENDED: test indirectly via the purpose substring "Brutto: 19,95 €" rather than re-exporting the private helper. This keeps the helper private (PATTERNS.md §"Private helper scoping").

    16. "_format_amount renders 500 minor units as 5,00 (D-34)" — same: test indirectly via purpose substring "Brutto: 5,00 €" using purchase_simple_sale.

    Also keep the sanity test `it("M_mapping module table is exposed", function() assert.is_table(M_mapping) end)` and the Fixtures.load nested-path empirical proof.

    Use `assert.equals` for exact equality, `assert.truthy(s:find(...))` for substring presence, `assert.is_falsy` for absence, `assert.is_nil` for nil checks. Add diagnostic strings on every assert ("expected ..., got: " .. tostring(actual)) so debug output is actionable.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/mapping_spec.lua 2>&amp;1 | tee /tmp/wave2_unit.log &amp;&amp; grep -qE '0 failures' /tmp/wave2_unit.log &amp;&amp; ! grep -E '(Pending|Error)' /tmp/wave2_unit.log &amp;&amp; grep -c '^  it(' spec/mapping_spec.lua | awk '{ if ($1 &gt;= 18) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  it(' spec/mapping_spec.lua` reports >= 18 (16+ Wave-0 pending now it() + 2 sanity tests)
    - `grep -c '^  pending(' spec/mapping_spec.lua` reports 0 (every Wave-0 pending replaced with it())
    - `busted spec/mapping_spec.lua` is GREEN (all tests pass)
    - `busted spec/mapping_spec.lua -t "purchase_to_transaction maps amount to EUR float (SALE-01)"` passes
    - `busted spec/mapping_spec.lua -t "purchase_to_transaction upgrades name to <Brand>"` passes (uses literal `•••• ` not pattern characters — adjust grep)
    - `busted spec/mapping_spec.lua -t "refund_to_transaction returns negative amount (D-32)"` passes
    - `busted spec/mapping_spec.lua -t "purchase_to_transaction returns nil for non-EUR purchase (D-37)"` passes
    - The full suite `busted spec/` is non-zero only due to spec/refresh_idempotency_spec.lua (Wave 4) and the Wave-0 pending scaffolds for pagination/purchases (Wave 3) — NO previously-passing spec regresses
    - `lua tools/build.lua --verify` is still reproducible
  </acceptance_criteria>
  <done>
    spec/mapping_spec.lua has 16+ passing unit tests covering SALE-01/02/04/08, I18N-01, D-32/D-34/D-35/D-37/D-38 mapping behaviors; no pending entries remain; full suite passes except the two Wave-1 RED specs and Wave-0 pending scaffolds for purchases/pagination. Commit: `test(03-03): fill mapping unit specs (SALE-01/02/04/08, I18N-01, D-32/34/35/37/38)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zettle Purchase API JSON → src/mapping.lua input | Already parsed by M_http.get_json's pcall + dkjson; mapping treats input as untrusted but well-typed Lua tables; every field access is guarded |
| Operator-readable purpose strings → MoneyMoney UI | Pure German bookkeeping format; no user input is echoed verbatim except the amount integer (SALE-01) and purchaseNumber (D-34) — both come from the same authenticated API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W2-01 | Tampering | src/mapping.lua _to_berlin_local_time DST_TABLE | mitigate | 21 rows precomputed deterministically by the planner via `last_sunday_utc` algorithm; verified independently — POSIX(2026-06-19T23:55Z) yields local 2026-06-20 (CEST), POSIX(2026-01-31T23:55Z) yields local 2026-02-01 (CET). spec/dst_table_spec.lua + spec/mapping_schema_spec.lua Tests 5+6 are the gate against silent boundary drift. |
| T-03-W2-02 | Tampering | src/mapping.lua _format_label cardType brand-map | accept | Unknown brands fall through to capitalized literal; no Lua pattern injection risk because we use `:upper()` and `:lower()` on byte ranges, not gsub patterns. |
| T-03-W2-03 | Information Disclosure | src/mapping.lua _format_purpose receipt content | mitigate | Only the purchaseNumber integer and amount integer reach the purpose string. NO API key, NO Bearer, NO UUID-with-original-customer-data appears. UUID-as-fallback in refund purpose is a public UUID (same UUID merchant sees in Zettle's own UI). |
| T-03-W2-04 | Tampering | src/mapping.lua _parse_iso8601_utc | mitigate | Pattern guard: nil on malformed input → mapping falls back to os.time() so the transaction still has a valid bookingDate (cannot crash). Any malformed timestamp is logged at INFO via M_log (no Bearer leak per Phase-2 D-29). |
| T-03-W2-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Pure-Lua source edits + spec authoring only. |
</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Build artifact + reproducible-build assertion (manifest unchanged):
lua tools/build.lua && lua tools/build.lua --verify

# Wave-1 schema gating spec now GREEN (Tests 1-8 all pass):
./.luarocks/bin/busted spec/mapping_schema_spec.lua 2>&1 | grep -E '0 failures'

# Wave-0 DST table spec now GREEN:
./.luarocks/bin/busted spec/dst_table_spec.lua 2>&1 | grep -E '0 failures'

# Wave-2 mapping unit spec now GREEN:
./.luarocks/bin/busted spec/mapping_spec.lua 2>&1 | grep -E '0 failures'

# Wave-1 idempotency spec STILL RED (Wave 4 fixes it — entry.lua still returns fixture body):
./.luarocks/bin/busted spec/refresh_idempotency_spec.lua 2>&1 | grep -E 'failures'

# Wave-0 pending scaffolds for pagination + purchases still pending (Wave 3 fixes):
./.luarocks/bin/busted spec/pagination_spec.lua spec/purchases_spec.lua 2>&1 | grep Pending

# Pre-existing Phase-1/2 specs still pass:
./.luarocks/bin/busted spec/auth_spec.lua spec/entry_spec.lua spec/http_spec.lua spec/log_redaction_spec.lua spec/i18n_spec.lua spec/errors_spec.lua spec/mm_mocks_spec.lua spec/build_spec.lua 2>&1 | grep -E '0 failures'
```
</verification>

<success_criteria>
- src/mapping.lua has substance (>= 150 lines including DST_TABLE) and follows the standard Phase-2 module preamble
- DST_TABLE has all 21 rows for 2020-2040; row 7 is `{1774746000, 1792890000}` (the 2026 boundary)
- Card metadata extraction uses `payments[1].attributes.cardType` and `payments[1].attributes.maskedPan` — NOT the deprecated cardBrand / cardLastFour
- src/i18n.lua has the seven new keys in both STRINGS.de and STRINGS.en
- spec/dst_table_spec.lua is GREEN (Wave-0 pending tests pass)
- spec/mapping_schema_spec.lua is GREEN (all 8 Wave-1 tests pass — schema gate fully turned)
- spec/mapping_spec.lua has 16+ passing unit tests; zero pending entries
- spec/i18n_spec.lua passes (no regression + seven new tests)
- `lua tools/build.lua --verify` is still reproducible
- Pre-existing Phase-1/2 specs unchanged
- spec/refresh_idempotency_spec.lua STILL RED (Wave 4 is the closer)
- spec/pagination_spec.lua + spec/purchases_spec.lua still pending (Wave 3 closer)
- `grep -L 'cardBrand\|cardLastFour' src/mapping.lua` confirms the deprecated D-35 names do not appear
- `grep -L 'valueDate' src/mapping.lua` confirms D-31 valueDate omission
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `src/mapping.lua` — filled with M_mapping.purchase_to_transaction, refund_to_transaction, _format_amount, _format_purpose, _format_label, _to_berlin_local_time, _parse_iso8601_utc, inline DST_TABLE (21 rows)
- `src/i18n.lua` — extended with 7 new keys in both STRINGS.de and STRINGS.en
- `spec/mapping_spec.lua` — filled (Wave-0 pending blocks → passing it() tests)
- `spec/i18n_spec.lua` — extended with 7 new key-translation tests

New public symbols:
- `M_mapping.purchase_to_transaction(p)` — pure function returning MoneyMoney transaction table or nil (non-EUR skip)
- `M_mapping.refund_to_transaction(p)` — pure function returning MoneyMoney refund transaction table or nil

New i18n keys (de + en parity):
- `account.purpose.gross`, `account.purpose.vat`, `account.purpose.tip`, `account.purpose.net`, `account.purpose.refund_for`, `account.purpose.receipt_number`, `account.name.card_payment`

Module-internal (private — not exported on M_mapping):
- `_format_amount`, `_format_purpose`, `_format_label`, `_to_berlin_local_time`, `_parse_iso8601_utc`, `DST_TABLE`

Files NOT modified: `tools/manifest.txt`, `src/webbanking_header.lua` (no new module declarations needed), `src/entry.lua` (Wave 4), `src/purchases.lua` and `src/pagination.lua` (Wave 3).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-03-SUMMARY.md` summarising:
- DST table verified: row 7 = {1774746000, 1792890000} for 2026; spec/dst_table_spec.lua green
- Mapping spec test count + pass count
- Schema gating spec status (Tests 1-8 all green)
- i18n new-key count + de/en parity confirmed
- Card metadata path confirmed at attributes.cardType + maskedPan (not cardBrand / cardLastFour)
- Reproducible build SHA after this plan
- Confirmation that refresh_idempotency_spec.lua remains RED (Wave 4 closer)
- Any tricky discoveries about ISO-8601 parsing on macOS vs Linux CI
</output>
