---
phase: 02-authenticated-network-layer
plan: 07
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - spec/log_redaction_spec.lua
  - tools/manifest.txt
autonomous: true
requirements: [AUTH-05, SEC-03]
requirements_addressed: [AUTH-05, SEC-03]
tags: [security, sec-03, redaction, gating, manifest, coverage, lua]

must_haves:
  truths:
    - "SEC-03 gating spec threads a REAL auth failure through entry.lua → auth.lua → http.lua → errors.lua and asserts the returned MoneyMoney string contains NO JWT-shape (eyJ[A-Za-z0-9_-]+), NO 'Bearer' literal, and NO base64url segment of the input API key (per D-29)"
    - "SEC-03 gating spec also walks every value in LocalStorage recursively and asserts no value contains the input API key or any of its three JWT segments (AUTH-05 + SEC-03)"
    - "SEC-03 gating spec asserts the captured print stream (M_log path) contains no JWT-shape, no Bearer, no input-key fragment after a full successful auth round-trip"
    - "tools/manifest.txt order is preserved AND verified — auth, http, errors are listed BEFORE entry per the build's concatenation contract; no new src/ modules introduced in Phase 2 means no manifest change is required, but the verification gate is run nonetheless"
    - "Full Phase-2 spec suite achieves >=85% line coverage on src/ excluding webbanking_header.lua per Phase-1 D-06 / CI-02 (which still gates after Phase 6 ships CI but is verified locally in Phase 2)"
    - "Reproducible build holds — sha256 across two consecutive lua tools/build.lua --verify runs is identical (BUILD-02 carryover from Phase 1)"
    - "CI egress-grep continues to pass — dist/paypal-pos.lua contains only oauth.zettle.com/purchase.izettle.com/finance.izettle.com host strings (Phase-1 D-12 / D-26 / SEC-02 carryover)"
    - "API key never appears anywhere — in returned strings, in any LocalStorage value, in any captured print, in any debug log output"
    - "Egress allowlist {oauth.zettle.com, purchase.izettle.com, finance.izettle.com} structurally verified by grep"
    - "All user-facing strings via M_i18n.t (German primary)"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in commit message, code, or comments"
  artifacts:
    - path: "spec/log_redaction_spec.lua"
      provides: "Three SEC-03 gating tests (D-29) per RESEARCH §SEC-03 Gating Test L1014–L1129"
      contains: "SEC-03"
    - path: "tools/manifest.txt"
      provides: "(unchanged) — verification confirms auth/http/errors are listed before entry per concatenation contract"
      contains: "auth"
  key_links:
    - from: "spec/log_redaction_spec.lua SEC-03 tests"
      to: "InitializeSession2 → exchange_assertion → from_http_status"
      via: "fixture-driven invalid_grant path captured end-to-end"
      pattern: "InitializeSession2"
    - from: "spec/log_redaction_spec.lua SEC-03 tests"
      to: "LocalStorage walk"
      via: "recursive string-value scan after successful auth"
      pattern: "LocalStorage"
    - from: "tools/manifest.txt"
      to: "tools/build.lua amalgamation order"
      via: "auth/http/errors emitted between webbanking_header and entry"
      pattern: "auth"
---

<objective>
Land the SEC-03 gating spec (D-29) that proves no API key fragment ever appears in any returned error string, any LocalStorage value, or any captured print output — across the full Phase-2 integration path (InitializeSession2 → auth.lua → http.lua → errors.lua). Verify the manifest and reproducible-build contracts still hold. This is the defense-in-depth + manifest-verification gate that closes the phase.

Purpose: AUTH-05 + SEC-03 collectively demand that the API key live ONLY in MoneyMoney's credential store — never in our extension's persistent state or observable side effects. Plans 02-02 through 02-06 each contributed structural guarantees (M_errors body-never-echoed, persist_session shape locked, redaction-before-log, headers-never-concatenated); Plan 02-07 verifies the combined invariant end-to-end with the canonical SEC-03 gating spec from RESEARCH §"SEC-03 Gating Test" L1014–L1129. Additionally, the manifest gate ensures tools/build.lua emits auth/http/errors in the correct order (no changes expected in Phase 2 since the manifest already lists all three, but the verification is mandatory per Wave-4 checklist).

Output: Three new gating tests appended to spec/log_redaction_spec.lua per the D-29 verbatim sketch; verification that tools/manifest.txt's order is unchanged; verification that coverage and reproducibility hold across the full Phase-2 src/ tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-RESEARCH.md
@.planning/phases/02-authenticated-network-layer/02-PATTERNS.md
@.planning/phases/02-authenticated-network-layer/02-06-SUMMARY.md
@spec/log_redaction_spec.lua
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@src/log.lua
@src/auth.lua
@src/http.lua
@src/errors.lua
@src/entry.lua
@tools/manifest.txt
@tools/build.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SEC-03 gating spec to log_redaction_spec.lua per D-29 (three tests)</name>
  <read_first>
    - spec/log_redaction_spec.lua (current Phase-1 redaction tests — preserve verbatim; APPEND a new describe block for SEC-03)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-29 (verbatim test contract)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"SEC-03 Gating Test" L1014–L1129 (verbatim test sketch with three it() blocks)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Common Pitfalls" Pitfall 8 (use hardcoded base64url strings since MM.base64 mock is identity; MM.base64decode is real after Plan 02-01)
    - src/log.lua L11–L36 (redaction passes — JWT-shape, Bearer, assertion=, access_token=)
    - src/auth.lua, src/http.lua, src/errors.lua, src/entry.lua (the full integration path the spec exercises)
    - spec/fixtures/auth/token_ok.json, token_invalid_grant.json, users_self_ok.json (Wave 0 fixtures)
    - spec/helpers/mm_mocks.lua (Mocks._captured_prints; LocalStorage reset; print override per L226–L235)
  </read_first>
  <files>spec/log_redaction_spec.lua</files>
  <behavior>
    Three SEC-03 gating tests per RESEARCH §"SEC-03 Gating Test" L1014–L1129:

    Test 1: "rejects a malformed JWT without echoing it anywhere"
    - Construct a JWT-SHAPED but invalid input that passes the regex check but fails JSON decode (e.g. "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.bm90anNvbg.signature" — middle "bm90anNvbg" decodes to "notjson")
    - Call InitializeSession2 with credentials {{value=fake_jwt}}; push NO HTTP responses (extraction fails before any network call per D-22)
    - Assert result equals M_i18n.t("error.invalid_grant")
    - Assert result:find("eyJ", 1, true) is falsy AND result:find("Bearer", 1, true) is falsy
    - For each segment of fake_jwt (split on "."), assert result:find(seg, 1, true) is falsy
    - For each captured print line, assert it does NOT contain fake_jwt as a literal AND does NOT contain any individual segment

    Test 2: "rejects an invalid_grant from /token without echoing the assertion"
    - Construct a JWT whose middle segment is precomputed base64url of {"aud":"client-x"} — use hardcoded constant "eyJhdWQiOiJjbGllbnQteCJ9" (per Pitfall 8)
    - fake_jwt = "header." + mid + ".sig"
    - Push token_invalid_grant fixture; call InitializeSession2 with {{value=fake_jwt}}
    - Assert result equals LoginFailed
    - Assert result:find(fake_jwt, 1, true) is falsy; for each segment of fake_jwt, assert result:find(seg, 1, true) is falsy
    - For each captured print line, assert line:find(fake_jwt, 1, true) is falsy AND line:find(mid, 1, true) is falsy

    Test 3: "never writes the API key to LocalStorage even after a successful auth"
    - mid = "eyJhdWQiOiJjbGllbnQteCJ9"; fake_jwt = "header." + mid + ".sig"
    - Push token_ok then users_self_ok (successful round-trip)
    - Call InitializeSession2 with {{value=fake_jwt}}; assert result is nil
    - Walk every value in LocalStorage recursively (use a `walk(t, visit)` helper); for each string value, assert value:find(fake_jwt, 1, true) is falsy AND for each segment of fake_jwt assert value:find(seg, 1, true) is falsy
    - This is the structural AUTH-05 + SEC-03 assertion at the integration layer
  </behavior>
  <action>
    Edit spec/log_redaction_spec.lua. APPEND a new outer describe block at the end of the file (preserve all Phase-1 redaction tests):

    describe("SEC-03 — API key never leaks (D-29)", function()
      before_each(function()
        Mocks.setup()
        load_artifact()  -- reuses the file-level helper from L37–L40
      end)
      after_each(function()
        Mocks.teardown()
      end)

      -- THREE it() blocks per behavior above
    end)

    Implementation notes:

    - For Test 1 use the literal fake_jwt = "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.bm90anNvbg.signature" (matches the redactor's JWT-shape pattern at src/log.lua L21–L24 so the redactor WOULD strip it if echoed; the test verifies it is never even reached)
    - For Tests 2 and 3 use mid = "eyJhdWQiOiJjbGllbnQteCJ9" as a hardcoded constant (precomputed base64url of {"aud":"client-x"} — the test does NOT call MM.base64 to encode it, since the mm_mocks.lua MM.base64 remains identity per Plan 02-01 Pitfall 8)
    - For segment iteration use a Lua loop: for seg in fake_jwt:gmatch("[^.]+") do … end
    - For LocalStorage walk use a local recursive function: local function walk(t, visit) for _, v in pairs(t) do if type(v)=="table" then walk(v, visit) elseif type(v)=="string" then visit(v) end end end
    - For the credentials shape pass {{value=fake_jwt}} (a positional array with one challenge-style table) — matches the L31–L33 credential-extraction branch in entry.lua

    Reuse the file-level `load_artifact()` helper at spec/log_redaction_spec.lua L37–L40 — no need to redeclare it inside the describe block.

    NO new spec helper files. NO new fixtures. NO changes to mm_mocks.lua or fixtures.lua.

    No fenced code blocks in <action>.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/log_redaction_spec.lua --filter "SEC-03" 2>&amp;1 | tee /tmp/sec03.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/sec03.log &amp;&amp; ! grep -q 'Pending' /tmp/sec03.log &amp;&amp; grep -c 'describe.*SEC-03' spec/log_redaction_spec.lua | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/log_redaction_spec.lua reports all SEC-03 tests passing; zero pending; zero failures
    - busted spec/log_redaction_spec.lua --filter "rejects a malformed JWT without echoing it anywhere" PASSES
    - busted spec/log_redaction_spec.lua --filter "rejects an invalid_grant from /token without echoing the assertion" PASSES
    - busted spec/log_redaction_spec.lua --filter "never writes the API key to LocalStorage even after a successful auth" PASSES
    - All Phase-1 redaction tests continue to pass (no regression)
    - grep -c 'SEC-03' spec/log_redaction_spec.lua is at least 1 (the describe block label)
    - grep -c 'fake_jwt' spec/log_redaction_spec.lua is at least 3 (one per test)
    - grep -c 'walk(LocalStorage' spec/log_redaction_spec.lua is at least 1 (the recursive walk in Test 3)
    - luacheck spec/log_redaction_spec.lua exits 0
    - busted spec/ (full suite) reports zero failures
  </acceptance_criteria>
  <done>
    spec/log_redaction_spec.lua includes three SEC-03 gating tests per D-29 covering: malformed-JWT no-echo, invalid_grant /token failure no-echo (including the mid segment), and successful-auth LocalStorage no-echo. All three pass; Phase-1 redaction tests still pass; full suite green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify manifest order, full coverage gate, reproducible build, and egress allowlist on the Phase-2 artifact</name>
  <read_first>
    - tools/manifest.txt (current order; auth/http/errors must precede entry; no Phase-2 src/ additions expected)
    - tools/build.lua (concatenation algorithm; verifies no banned calls, no DEBUG=true in shipped artifact)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-26 (egress allowlist)
    - .planning/phases/01-foundations-sandbox-probes/CONTEXT.md D-06 (coverage gate ≥85% on src/ excluding webbanking_header.lua), D-12 (egress allowlist)
    - .planning/phases/02-authenticated-network-layer/02-VALIDATION.md §"Sampling Rate" (full suite + coverage gate before /gsd-verify-work)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Module Inventory Delta" L878–L900 (no new src/ modules; existing manifest covers all Phase-2 work)
  </read_first>
  <files>tools/manifest.txt</files>
  <action>
    This is a VERIFICATION task — no source edits are expected. The executor runs the following checks and confirms each:

    1. Manifest order check: `grep -n 'webbanking_header\|errors\|http\|auth\|entry' tools/manifest.txt` confirms the order is: webbanking_header → ... → errors → ... → http → auth → ... → entry. (errors must come before http and auth because auth.lua references M_errors via the global table; http must come before auth because auth.lua references M_http; entry must come last because entry.lua references M_auth, M_http, M_errors, M_i18n. The existing manifest L5-L17 already satisfies this; the task asserts it didn't drift.)

    2. Build the artifact and verify reproducibility: `lua tools/build.lua --verify` must report `OK: reproducible (sha256: …)`.

    3. Egress allowlist verification: confirm dist/paypal-pos.lua contains only the three allow-listed hosts and no others. Run:
       - `grep -oE 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u` should yield ONLY URLs whose host is in {oauth.zettle.com, purchase.izettle.com, finance.izettle.com}. Phase 2 should add exactly two: https://oauth.zettle.com/token and https://oauth.zettle.com/users/self
       - `grep -cE '(purchase|finance)\.izettle\.com' dist/paypal-pos.lua` is 0 (Phase 2 does not introduce purchase/finance hosts)

    4. Full coverage gate: `busted --coverage spec/` then `luacov` and inspect `luacov.report.out`. Confirm `src/auth.lua`, `src/http.lua`, `src/errors.lua`, `src/entry.lua`, `src/log.lua`, `src/i18n.lua`, `src/model.lua` all show >=85% line coverage (excluding `src/webbanking_header.lua` per Phase-1 D-06).

    5. Full suite green: `busted spec/` exits 0.

    6. luacheck clean: `luacheck src/ spec/` exits 0.

    If ANY check fails, the executor MUST diagnose and resolve before declaring done. If the manifest order check fails (auth listed before http etc.), edit tools/manifest.txt to reorder. If the coverage gate fails on a specific file, add a focused test to the relevant spec file to lift coverage above 85% on that file.

    Do NOT edit src/ files in this task — coverage gaps should be addressed by extending existing spec files (auth_spec, http_spec, errors_spec, entry_spec) with one or two additional `it()` blocks covering uncovered branches. If the gap is structural (e.g. an unreachable defensive branch), document it in the SUMMARY as an accepted gap with rationale.

    Document each check's outcome in the plan's SUMMARY file.

    No fenced code blocks in <action>.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua --verify | grep -q '^OK: reproducible' &amp;&amp; busted spec/ 2>&amp;1 | tee /tmp/full.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/full.log &amp;&amp; luacheck src/ spec/ &amp;&amp; busted --coverage spec/ &amp;&amp; luacov &amp;&amp; lua -e 'local f=io.open("luacov.report.out","r"); local r=f:read("*a"); f:close(); for fname in r:gmatch("(src/[a-z_]+%.lua)[^\n]*\n") do io.write(fname.."\n") end' &amp;&amp; grep -oE 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u | tee /tmp/hosts.log &amp;&amp; ! grep -vE '(oauth\.zettle\.com|purchase\.izettle\.com|finance\.izettle\.com)' /tmp/hosts.log | grep -v '^$'</automated>
  </verify>
  <acceptance_criteria>
    - lua tools/build.lua --verify exits 0 and reports "OK: reproducible (sha256: …)"
    - busted spec/ exits 0 across all spec files with zero failures and zero pending
    - luacheck src/ spec/ exits 0
    - busted --coverage spec/ exits 0 and luacov generates luacov.report.out
    - luacov.report.out shows >=85% line coverage on each of: src/auth.lua, src/http.lua, src/errors.lua, src/entry.lua, src/log.lua, src/i18n.lua, src/model.lua
    - grep -oE 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u shows ONLY URLs on the three allow-listed hosts; no other hosts present
    - dist/paypal-pos.lua contains exactly the URLs https://oauth.zettle.com/token and https://oauth.zettle.com/users/self (Phase 2's only new endpoints)
    - tools/manifest.txt order is unchanged from Phase 1 (webbanking_header, log, errors, i18n, model, http, auth, pagination, purchases, payouts, balance, mapping, entry)
    - The SEC-03 gating spec from Plan 02-07 Task 1 passes within the full suite
  </acceptance_criteria>
  <done>
    Manifest order verified unchanged; reproducible build sha256-confirmed; full spec suite green with zero failures and zero pending; luacheck clean; coverage gate ≥85% across every src/ file except webbanking_header.lua; egress allowlist structurally verified on dist/paypal-pos.lua; Phase 2 ready for /gsd-verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec end-to-end integration → captured prints + LocalStorage | Spec asserts negative invariants on all observable side effects of a real auth round-trip |
| dist/paypal-pos.lua → egress allowlist | grep-based structural verification of host strings in the shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | API key leak through ANY observable channel after full integration | mitigate | SEC-03 gating spec asserts negative invariants on (a) the InitializeSession2 return string, (b) every value in LocalStorage walked recursively, (c) every captured print line. Three independent it() blocks cover malformed-JWT, invalid_grant, and success paths. This is the canonical D-29 test. |
| T-02-07-02 | Tampering | Manifest reorder breaking the M_* global table predeclare→use contract | mitigate | Plan 02-07 Task 2 verifies the manifest order didn't drift; the Phase-1 ordering (errors before http before auth) is correct for Phase 2's cross-module call patterns. |
| T-02-07-03 | Information Disclosure | Egress to a non-allowlisted host introduced accidentally | mitigate | Structural grep on dist/paypal-pos.lua confirms only oauth.zettle.com URLs are present; sort -u catches duplicates and unexpected new hosts. Phase 6 will encode this as a CI gate; Plan 02-07 verifies it locally. |
| T-02-07-04 | Tampering | Coverage regression hiding untested branches | mitigate | Coverage gate ≥85% per src/ file (excluding webbanking_header.lua per D-06); if a file drops below 85%, the task author must add focused tests rather than lowering the bar. |
| T-02-07-05 | Tampering | Reproducible build drift | mitigate | lua tools/build.lua --verify reports OK only on byte-identical second build; any drift fails the gate and the executor must investigate (likely a timestamp leakage in the source or test-runtime side effect). |
| T-02-07-SC | Tampering | npm/luarocks installs | accept | Zero installs in this plan — pure verification + Lua spec edits only. |
</threat_model>

<verification>
- All three SEC-03 gating tests in spec/log_redaction_spec.lua pass
- Reproducible build verified twice with identical sha256
- Full spec suite green (entry, auth, http, errors, log_redaction including SEC-03, mm_mocks, i18n, build)
- luacheck clean across src/ and spec/
- Coverage ≥85% on every src/ file except webbanking_header.lua
- Egress allowlist structurally verified on dist/paypal-pos.lua
- Manifest order unchanged from Phase 1
- The shipped artifact contains exactly the two new URLs Phase 2 introduces (https://oauth.zettle.com/token, https://oauth.zettle.com/users/self) and no other URLs
</verification>

<success_criteria>
- SEC-03 gating spec is the canonical D-29 test from RESEARCH §"SEC-03 Gating Test" L1014–L1129 — adapted only for the recursive LocalStorage walk helper
- Manifest, reproducible build, coverage, egress allowlist all verified
- Phase 2 ready for /gsd-verify-work and the maintainer's end-of-phase manual install in real MoneyMoney
</success_criteria>

<output>
Create `.planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md` recording:
- SEC-03 gating spec test count (3) and the integration path each exercises
- Reproducible build sha256 of dist/paypal-pos.lua
- Per-file coverage percentages from luacov.report.out (record at least src/auth.lua, src/http.lua, src/errors.lua, src/entry.lua)
- Egress allowlist verification: the unique URLs found in dist/paypal-pos.lua
- Manifest order confirmation (unchanged from Phase 1)
- Final dist/paypal-pos.lua LoC (expected ~550–600 per RESEARCH §Module Inventory Delta L900)
- Any accepted coverage gaps (with rationale) — only if structural unreachables exist
- Confirmation that Phase 2 is ready for /gsd-verify-work
</output>
