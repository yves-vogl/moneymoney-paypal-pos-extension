---
phase: 03-sale-spine-first-user-visible-slice
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/pagination.lua
  - spec/pagination_spec.lua
autonomous: true
requirements: [SALE-06]
requirements_addressed: [SALE-06]
tags: [wave-3, pagination, cursor, mvp]

must_haves:
  truths:
    - "src/pagination.lua fills the stub with M_pagination.iterate(fetch_page_fn, initial_params) returning (all_purchases|nil, error|nil) per RESEARCH §2a"
    - "iterate uses repeat…until with defensive termination: both empty purchases[] AND absent lastPurchaseHash are terminal (RESEARCH §3 Anti-Pattern 1)"
    - "iterate routes every fetch_page_fn return through M_errors.from_http_status(status, raw) per D-43; any err propagates as (nil, err)"
    - "iterate guards page shape: nil page or non-table page.purchases → returns (nil, M_i18n.t(\"error.network\", \"bad_page\"))"
    - "iterate copies initial_params locally (no mutation of caller's table); updates only params.lastPurchaseHash between iterations"
    - "MAX_PAGES = 50 guard: on exceedance, logs a German WARN/INFO line via M_log and returns (nil, M_i18n.t(\"error.network\", \"max_pages\")) — no new error.* i18n key, reuses Phase-2 error.network envelope"
    - "iterate makes ZERO network calls — transport is owned by the injected fetch_page_fn (Plan 03-05's M_purchases.fetch is the Wave-3 transport injection)"
    - "spec/pagination_spec.lua fills every Wave-0 pending with a passing it() using inline fetch_fn closures (pure orchestration testable without real HTTP)"
    - "src/pagination.lua follows the Phase-2 module preamble: ownership citing SALE-06 / D-43; M_pagination predeclared in webbanking_header.lua; no require() of siblings (D-02)"
    - "tools/manifest.txt is NOT modified (pagination is already in the manifest between auth and purchases)"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts:
    - path: "src/pagination.lua"
      provides: "M_pagination.iterate cursor loop with MAX_PAGES guard"
      contains: "M_pagination.iterate"
      min_lines: 50
    - path: "spec/pagination_spec.lua"
      provides: "Passing cursor-termination tests (empty, missing-hash, MAX_PAGES, error routing, cursor handoff)"
      contains: "M_pagination.iterate"
  key_links:
    - from: "src/pagination.lua iterate"
      to: "M_errors.from_http_status"
      via: "Phase-2 D-43 reuse — no new error cases"
      pattern: "M_errors.from_http_status"
    - from: "src/pagination.lua iterate"
      to: "M_i18n.t error.network"
      via: "bad_page + max_pages reuse the existing Phase-2 envelope"
      pattern: "error.network"
    - from: "spec/pagination_spec.lua"
      to: "spec/fixtures/purchases/purchase_page1.json + purchase_page2.json"
      via: "Fixtures.load smoke test threads fixtures through iterator"
      pattern: "purchase_page"
---

<objective>
Fill src/pagination.lua with M_pagination.iterate — the pure cursor-loop orchestrator that drives Zettle's lastPurchaseHash pagination. Then fill spec/pagination_spec.lua so every Wave-0 pending test passes. This plan runs in parallel with Plan 03-05 (purchases fetch) — different files, both must complete before Wave 4 wires entry.lua RefreshAccount.

Purpose: Per CONTEXT D-40 and RESEARCH §2a, pagination is a reusable concern orthogonal to URL construction. Hoisting the loop keeps M_purchases.fetch a single-page primitive and lets Phase 4 reuse the iterator with a different fetch_page_fn for the Finance API.

Output: src/pagination.lua with M_pagination.iterate following RESEARCH §2a's repeat…until shape; defensive termination on BOTH empty-array and absent-cursor; MAX_PAGES = 50 guard reusing the Phase-2 error.network envelope; spec/pagination_spec.lua with passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@src/pagination.lua
@src/errors.lua
@src/log.lua
@src/i18n.lua
@spec/pagination_spec.lua
@spec/http_spec.lua
@spec/entry_spec.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill src/pagination.lua with M_pagination.iterate</name>
  <read_first>
    - src/pagination.lua (Phase-1 stub)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2a (recommended repeat…until shape)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §3 Anti-Pattern 1 (check BOTH terminal conditions)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"src/pagination.lua" (loop-with-guard pattern from src/entry.lua L57-74; module-local constant pattern from src/http.lua L12)
    - src/errors.lua M_errors.from_http_status (route every fetch_page_fn return)
    - src/log.lua (verify which log method exists: M_log.debug / M_log.info / M_log.warn — use whichever is present)
    - src/i18n.lua (reuse existing error.network envelope; no new keys)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-43
  </read_first>
  <files>src/pagination.lua</files>
  <behavior>
    - iterate(fetch_page_fn, initial_params) returns (all_purchases_table, nil) on success; (nil, error_string) on failure
    - Loop calls fetch_page_fn(params); each call returns (page, status, raw)
    - Route through M_errors.from_http_status(status, raw); non-nil err → return (nil, err)
    - Guard page shape: nil or page.purchases not a table → return (nil, M_i18n.t("error.network", "bad_page"))
    - Accumulate page.purchases via ipairs into all_purchases
    - Terminal when #page.purchases == 0 OR type(page.lastPurchaseHash) ~= "string" OR lastPurchaseHash == ""
    - MAX_PAGES = 50: on exceedance, log via M_log.warn (fallback M_log.info), return (nil, M_i18n.t("error.network", "max_pages"))
    - initial_params not mutated — local copy made before loop
  </behavior>
  <action>
    Rewrite src/pagination.lua in full. Header comment (lines 1-9) cites ownership of SALE-06 and D-43; declares the public surface "Provides: M_pagination.iterate(fetch_page_fn, initial_params)"; states "The M_pagination table is predeclared in src/webbanking_header.lua." and "NO require() of sibling modules (D-02)".

    Module-local constant: `local MAX_PAGES = 50` immediately after the header.

    Public function `M_pagination.iterate(fetch_page_fn, initial_params)`:
    1. `local all_purchases = {}`.
    2. `local params = {}` then copy each k,v from initial_params (caller-defensive).
    3. `local page_count = 0`.
    4. `repeat`:
       a. `page_count = page_count + 1`
       b. MAX_PAGES guard: if page_count > MAX_PAGES then log warning ("M_pagination.iterate: MAX_PAGES exceeded, aborting") and return (nil, M_i18n.t("error.network", "max_pages")). Use `M_log.warn` if it exists, else `M_log.info` — verify by reading src/log.lua.
       c. `local page, status, raw = fetch_page_fn(params)`
       d. `local err = M_errors.from_http_status(status, raw); if err then return nil, err end`
       e. Page-shape guard: `if type(page) ~= "table" or type(page.purchases) ~= "table" then return nil, M_i18n.t("error.network", "bad_page") end`
       f. Accumulate: `for _, p in ipairs(page.purchases) do all_purchases[#all_purchases + 1] = p end`
       g. has_more = (#page.purchases > 0) and (type(page.lastPurchaseHash) == "string") and (page.lastPurchaseHash ~= "")
       h. if has_more then params.lastPurchaseHash = page.lastPurchaseHash else params.lastPurchaseHash = nil end
    5. `until not (type(params.lastPurchaseHash) == "string" and params.lastPurchaseHash ~= "")`
    6. `return all_purchases, nil`

    Apply `-- luacheck: ignore 431` only if loop-variable shadowing is flagged. Do NOT add pcall (input parsed upstream). Do NOT make any network call (transport owned by fetch_page_fn). Do NOT modify tools/manifest.txt.

    Run `lua tools/build.lua` after; confirm reproducible build still green.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'M_pagination.iterate' src/pagination.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'MAX_PAGES' src/pagination.lua | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/pagination.lua` reports >= 50
    - `grep -n 'M_pagination.iterate' src/pagination.lua` finds the definition
    - `grep -c 'MAX_PAGES' src/pagination.lua` reports >= 2
    - `grep -n 'M_errors.from_http_status' src/pagination.lua` finds error routing (D-43)
    - `grep -n 'lastPurchaseHash' src/pagination.lua` finds cursor handling
    - `grep -L 'conn:request\|Connection(' src/pagination.lua` succeeds (no network)
    - `grep -L 'require(' src/pagination.lua` succeeds (D-02)
    - `grep -L 'pcall' src/pagination.lua` succeeds
    - `lua tools/build.lua --verify` reports OK
    - Pre-existing Phase-1/2/Wave-2 specs unchanged
  </acceptance_criteria>
  <done>
    src/pagination.lua filled with M_pagination.iterate per RESEARCH §2a; reproducible build green; zero new egress hosts touched. Commit: `feat(03-04): implement M_pagination.iterate with MAX_PAGES guard (SALE-06 / D-43)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill spec/pagination_spec.lua (Wave-0 pending → passing)</name>
  <read_first>
    - spec/pagination_spec.lua (Wave-0 pending scaffold — 7+ pending tests)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/pagination_spec.lua" (multi-response queue pattern from spec/entry_spec.lua L122-128; empty-response termination from spec/http_spec.lua L138-143)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md (fixture inventory)
    - src/pagination.lua (after Task 1 — the implementation under test)
    - spec/entry_spec.lua L122-128 (multi-push-response pattern)
    - spec/http_spec.lua L138-143 (empty-body / nil-status pattern)
  </read_first>
  <files>spec/pagination_spec.lua</files>
  <behavior>
    - All 7+ Wave-0 pending tests become passing it() with the same verbatim names
    - Sanity test "M_pagination module table is exposed" preserved
    - Tests use inline fetch_fn closures (pure orchestration testable without real HTTP)
    - One smoke test threads real fixtures through the iterator
  </behavior>
  <action>
    Replace each pending block in spec/pagination_spec.lua with a passing it() preserving the exact name. Use inline closures for the fetch_fn so tests are self-contained; use Fixtures.load only for the end-to-end smoke test.

    Test 1 ("iterate accumulates purchases across two pages until empty (SALE-06)"): build `local pages = { {purchases={{purchaseUUID1="aaa"}}, lastPurchaseHash="hash-1"}, {purchases={{purchaseUUID1="bbb"}}, lastPurchaseHash="hash-2"}, {purchases={}}}`; closure pops one per call and returns (p, 200, "{}"); call iterate({limit=200}); assert.is_nil(err), #all == 2, all[1].purchaseUUID1 == "aaa", all[2].purchaseUUID1 == "bbb".

    Test 2 ("iterate terminates immediately on empty purchases[] array (RESEARCH §2a Pitfall 1)"): pages = { {purchases={}, lastPurchaseHash="hash-x"} } (cursor present but empty); call; assert.is_nil(err), #all == 0. Proves empty-array beats cursor.

    Test 3 ("iterate terminates when lastPurchaseHash is absent on a non-empty page (defensive)"): pages = { {purchases={{purchaseUUID1="cc"}}} }; call; assert #all == 1, err is nil.

    Test 4 ("iterate updates params.lastPurchaseHash between pages (cursor handoff)"): build `seen_params = {}`; closure records `seen_params[#seen_params+1] = params.lastPurchaseHash` BEFORE returning each page; pages = two-page setup as Test 1; call; assert.is_nil(seen_params[1]), assert.equals("hash-1", seen_params[2]).

    Test 5 ("iterate aborts on MAX_PAGES guard and logs a warning"): fetch_fn = function() return {purchases={{purchaseUUID1="x"}}, lastPurchaseHash="never-stops"}, 200, "{}" end; call; assert.is_nil(all); assert.is_string(err); assert.truthy(err:find("max_pages", 1, true)).

    Test 6 ("iterate routes HTTP error via M_errors.from_http_status"): fetch_fn returns (nil, nil, "") (simulates network failure); call; assert.is_nil(all); assert.is_string(err); assert.equals(M_i18n.t("error.network", "—"), err).

    Test 7 ("iterate returns error string when page shape is invalid"): fetch_fn = function() return {}, 200, "{}" end; call; assert.is_nil(all); assert.is_string(err); assert.truthy(err:find("bad_page", 1, true)).

    Smoke test 8 ("iterate accumulates fixture-driven pages from purchase_page1 + purchase_page2"): use Fixtures.load to read decoded page1 and page2; closure indexes through the two; call iterate; assert.is_nil(err); assert.equals(1, #all); assert.equals("44444444-4444-4444-4444-444444444444", all[1].purchaseUUID1).

    Use `assert.equals`, `assert.is_nil`, `assert.is_string`, `assert.truthy(s:find(...))`. Add diagnostic messages.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/pagination_spec.lua 2>&amp;1 | tee /tmp/wave3_pag.log &amp;&amp; grep -qE '0 failures' /tmp/wave3_pag.log &amp;&amp; ! grep -E 'Pending' /tmp/wave3_pag.log &amp;&amp; grep -c '^  it(' spec/pagination_spec.lua | awk '{ if ($1 &gt;= 8) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  it(' spec/pagination_spec.lua` reports >= 8
    - `grep -c '^  pending(' spec/pagination_spec.lua` reports 0
    - `busted spec/pagination_spec.lua` is GREEN
    - `busted spec/pagination_spec.lua -t "iterate aborts on MAX_PAGES guard and logs a warning"` passes
    - `busted spec/pagination_spec.lua -t "iterate routes HTTP error via M_errors.from_http_status"` passes
    - Full suite `busted spec/` still passes for Phase-1/2/Wave-0/Wave-2 specs; the only remaining failures are spec/refresh_idempotency_spec.lua (Wave 4) and spec/purchases_spec.lua pending (Plan 03-05)
  </acceptance_criteria>
  <done>
    spec/pagination_spec.lua filled with 8 passing it() tests covering SALE-06 cursor termination, MAX_PAGES guard, error routing, page-shape guard, cursor handoff, and a fixture-driven smoke test. Commit: `test(03-04): fill pagination unit specs (SALE-06 / D-43)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch_page_fn return → src/pagination.lua iterator | Already parsed by upstream M_http.get_json's pcall+dkjson; iterator treats page as untrusted but well-typed table; every field access guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W3a-01 | Denial of Service | M_pagination.iterate infinite loop on malformed response | mitigate | MAX_PAGES = 50 guard returns the German error.network envelope and logs a warning; no infinite loop possible even on adversarial server. |
| T-03-W3a-02 | Tampering | M_pagination.iterate caller params mutation | mitigate | initial_params is copied locally; caller's table is never mutated. Prevents state leakage between repeated RefreshAccount calls in Wave 4. |
| T-03-W3a-03 | Information Disclosure | M_log.warn("MAX_PAGES exceeded") | accept | Log line contains no Bearer, no API key, no PII — just the constant string. SEC-03 invariant preserved. |
| T-03-W3a-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Pure-Lua source edits + spec authoring only. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin
lua tools/build.lua && lua tools/build.lua --verify

# Wave-3a pagination spec GREEN:
./.luarocks/bin/busted spec/pagination_spec.lua 2>&1 | grep -E '0 failures'

# Pre-existing specs still pass; only refresh_idempotency_spec (Wave 4) + purchases_spec (Plan 05) still not-green:
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(Success|Failure)'
```
</verification>

<success_criteria>
- src/pagination.lua has substantial implementation (>= 50 lines) with M_pagination.iterate, MAX_PAGES guard, and explicit error routing
- spec/pagination_spec.lua has 8 passing it() tests; zero pending; full Wave-0 pending-list replaced
- Pre-existing Phase-1/2/Wave-2 specs unchanged
- `lua tools/build.lua --verify` is still reproducible
- spec/refresh_idempotency_spec.lua remains RED (Wave 4 is the closer)
- spec/purchases_spec.lua remains pending (Plan 03-05 closes it in parallel)
- `grep -L 'conn:request' src/pagination.lua` succeeds (no transport in this module)
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `src/pagination.lua` — filled with M_pagination.iterate + MAX_PAGES constant
- `spec/pagination_spec.lua` — filled (Wave-0 pending → passing)

New public symbols:
- `M_pagination.iterate(fetch_page_fn, initial_params)` — pure cursor-loop orchestrator

Module-internal:
- `MAX_PAGES = 50` (module-local constant)

Files NOT modified: `tools/manifest.txt`, `src/webbanking_header.lua`, `src/purchases.lua` (Plan 03-05), `src/mapping.lua` (Plan 03-03 owns), `src/entry.lua` (Plan 03-06).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-04-SUMMARY.md` summarising:
- M_pagination.iterate implementation choices (M_log.warn vs M_log.info — which was available)
- Pagination spec test count + pass count
- Confirmation that no transport (conn:request, Connection(), etc.) appears in src/pagination.lua
- Reproducible build SHA after this plan
- Confirmation that pagination_spec.lua threads real fixtures via the smoke test
</output>
