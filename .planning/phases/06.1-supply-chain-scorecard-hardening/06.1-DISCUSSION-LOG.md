# Phase 6.1: Supply-chain & Scorecard hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-17
**Phase:** 06.1-supply-chain-scorecard-hardening
**Areas discussed:** Sigstore/SLSA scope, Branch-protection + permissions refactor, SAST gate strictness + ruleset, CII level + requirement-IDs

---

## Sigstore / SLSA scope (Area A)

| Option | Description | Selected |
|--------|-------------|----------|
| Komplett in Phase 6 (Proposal-Empfehlung) | Phase 6 implementiert GPG-tag-trigger + cosign sign-blob + SLSA-Generator. Phase 6.1 fasst Signed-Releases gar nicht an. BUILD-01/02 werden NICHT in 6.1 geändert. | ✓ |
| Cosign-Signing in 6.1, SLSA-Provenance in Phase 6 | 6.1 ergänzt cosign sign-blob auf dem dist-Artifact (~30min). SLSA-Generator bleibt in Phase 6. | |
| Beides in 6.1 verschieben | BUILD-01/02 in 6.1 final aufgebohrt; Phase 6 enthält nur noch GPG-tag, README, ADRs. | |

**User's choice:** Komplett in Phase 6
**Notes:** Aligns with sprint-proposal Plan 6.5-06 verbatim. Keeps Phase 6.1 focused on the five fixable items + docs; no doubling of release-pipeline work. Locked as **D-30** in CONTEXT.md.

---

## Branch-protection rules (Area B-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Maximal strict: PR + signed commits + linear history + force-push/delete blocked + Status-Checks | Schreibt die Solo-Maintainer-Praxis fest. coverage-badge bleibt unberührt. Signed-commits OK weil Phase-1 D-13 alles GPG-signed. | ✓ |
| Strict ohne signed-commits | PR + linear history + force-push/delete blocked + Status-Checks. Signed-commits weggelassen. Score-Auswirkung minimal. | |
| Strict + signed-commits + Admin-Bypass erlaubt | Wie Option 1, aber Admin (Yves) darf im Notfall überbiegen. Scorecard zieht ≤0.5 ab. | |

**User's choice:** Maximal strict
**Notes:** Solo-maintainer commitment to live with the same rules as any external contributor. No admin-bypass. Locked as **D-31** in CONTEXT.md.

---

## Permissions refactor of ci.yml (Area B-2)

| Option | Description | Selected |
|--------|-------------|----------|
| Test+Badge in zwei Jobs splitten (Proposal-Empfehlung) | Top-level read-all. `test` (contents:read). `badge` (needs:test, if:push to main, contents:write). Saubere 10/10 auf Token-Permissions. | ✓ |
| Monolithisch lassen, Step-Level-Token-Scoping | Ein Job, explizite env-token nur im push-Step. Weniger Refactor, aber Scorecard wertet Job-level. | |
| Badge-Push in eigenen Workflow ausgliedern | Neuer `.github/workflows/coverage-badge.yml` triggered von `workflow_run`. Sauber, aber race-anfällig. | |

**User's choice:** Test+Badge split
**Notes:** Matches the proposal's recommendation. All four existing CI gates (DEBUG=false, egress-allowlist, no-AI-attribution, build --verify) move into the `test` job verbatim. Locked as **D-32** in CONTEXT.md.

---

## SAST gate strictness (Area C-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Blockierend: required status check, semgrep ci --error | ERROR-findings brechen den PR. Required check auf main. Falsch-positiv-Risiko initial. | ✓ |
| Advisory: SARIF-Upload only | Findings landen in code-scanning UI, blockieren aber keinen Merge. Scorecard SAST-Check trotzdem 10/10. | |
| Blockierend NUR auf p/secrets, p/security-audit advisory | Secret-Leaks (SEC-01) blockieren hart; Code-Quality-Findings advisory. | |

**User's choice:** Blockierend mit `semgrep ci --error`
**Notes:** Planner must sequence: any required Semgrep suppressions (`# nosemgrep:<rule>`) committed BEFORE adding the SAST workflow to the required-status-checks set. Locked as **D-33** in CONTEXT.md.

---

## SAST ruleset (Area C-2)

| Option | Description | Selected |
|--------|-------------|----------|
| p/security-audit + p/secrets (Proposal-Default) | Generic security rules + key/token-leak patterns. Stable, gut gewartet. | ✓ |
| p/security-audit + p/secrets + p/ci | Plus GHA-hardening rules (set-env, untrusted PR-target, expression injection). | |
| Eigene Lua-Rules dazu (Community-Repo) | semgrep.dev/r?lang=lua hat ~15 Rules, meist low-signal für API-Wrapper. | |

**User's choice:** p/security-audit + p/secrets
**Notes:** `p/ci` is partially redundant with SEC-06 + SEC-07; Lua-community rules are too thin to warrant inclusion in v1. Locked as **D-34** in CONTEXT.md.

---

## CII Best Practices badge level (Area D-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Passing (≥5/10) — Roadmap-Default | ~2h Fragebogen. Badge in README. Scorecard CII-Check = 5/10. | |
| Silver (~7/10) — Proposal-Empfehlung | +3h Fragebogen. +0.3 Aggregat-Score. | |
| Passing jetzt, Silver als Followup-Issue | 6.1 zieht passing durch. Silver wird als Backlog-Issue dokumentiert. | ✓ |

**User's choice:** Passing jetzt, Silver als Followup-Issue
**Notes:** Roadmap acceptance #6 says "passing badge" verbatim — passing satisfies the bar. Silver tracked as a backlog issue linked from ADR-0004. Locked as **D-35** in CONTEXT.md.

---

## Requirement-IDs lock (Area D-2)

| Option | Description | Selected |
|--------|-------------|----------|
| Jetzt locken: SEC-05/06/07/08 + BUILD-03 mit Acceptance-Texten aus Roadmap | CONTEXT.md schreibt ID+Text fest. Planner zitiert verbatim. | ✓ |
| Im CONTEXT.md anchor-en, im Planner härten | CONTEXT.md sagt "wird in dieser Phase eingeführt; finale Wortlaute liefert PLAN.md". | |
| Nur die nicht-trivialen IDs jetzt locken (SEC-07 + SEC-08) | Selbsterklärende IDs (SEC-05, SEC-06, BUILD-03) im Planner; nur Branch-Protection + SAST jetzt. | |

**User's choice:** Jetzt locken
**Notes:** Mid-discussion finding: SEC-05 and BUILD-03 in the roadmap text **collide with existing REQUIREMENTS.md IDs** (SEC-05 = signed-commits branch protection — actually thematically aligned, so expanded in-place; BUILD-03 = `__VERSION__` substitution — unrelated, so the new CII badge req becomes BUILD-07). New IDs locked: **SEC-05 (expanded), SEC-06, SEC-07, SEC-08, BUILD-07, DOC-N**. Locked as **D-36** in CONTEXT.md.

---

## Claude's Discretion

- Ordering of the five hardening PRs vs the one docs PR (subject to: SAST suppressions committed BEFORE the SAST workflow becomes a required check; Token-Permissions refactor BEFORE or WITH the new SAST workflow).
- Concrete Semgrep `# nosemgrep:` suppressions (if any surface) — case-by-case with one-line rationale.
- Pinning tool selection for SEC-06 (pin-github-action CLI vs StepSecurity generator vs hand-typed) — planner picks; determinism matters more than the tool.
- ADR-0004 prose depth — planner drafts; the five accepted-gap rationales (Fuzzing, Code-Review, Packaging, Contributors, Maintained) plus the silver-CII-as-backlog pointer are mandatory.

## Deferred Ideas

- **CII Best Practices Silver** — backlog issue, revisit post-v1.0.0.
- **Bot-driven Code-Review subscription (CodeRabbit/Greptile/Diamond)** — deliberately rejected in ADR-0004; revisit only if sponsor or community size shifts the calculus.
- **OSS-Fuzz / ClusterFuzzLite** — out of scope per ADR-0004; property-based tests in busted cover the relevant decoder paths.
- **Lua-specific Semgrep rules** — defer until a Phase-2/3 false-negative motivates inclusion.
- **`p/ci` Semgrep ruleset** — defer; SEC-06 + SEC-07 cover the same threat model from a different angle.
