---
phase: 02-authenticated-network-layer
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - src/auth.lua
  - spec/auth_spec.lua
autonomous: true
requirements: [AUTH-02, AUTH-04, AUTH-05, AUTH-06, ACCT-01, ACCT-02, ACCT-04]
requirements_addressed: [AUTH-02, AUTH-04, AUTH-05, AUTH-06, ACCT-01, ACCT-02, ACCT-04]
tags: [auth, oauth, localstorage, cache, multi-merchant, lua]

must_haves:
  truths:
    - "M_auth.exchange_assertion(api_key, client_id) POSTs grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer&client_id=...&assertion=<api_key> to https://oauth.zettle.com/token (AUTH-02)"
    - "M_auth.fetch_profile(access_token) GETs https://oauth.zettle.com/users/self with Authorization: Bearer <token> (D-21 leg 2)"
    - "M_auth.persist_session writes BOTH nested LocalStorage.zettle[orgUuid] AND flat LocalStorage[\"zettle:\"..orgUuid] = JSON-string per D-23c"
    - "Cache entry shape is exactly {access_token, expires_at, obtained_at, client_id, publicName, uuid} — strings + integers only (D-23c)"
    - "expires_at = os.time() + (token.expires_in or 7200) — trust server-returned TTL per RESEARCH §Specifics"
    - "M_auth.cached_token(orgUuid) returns nil when now >= expires_at - 60 OR no entry; returns access_token when fresh (D-23d)"
    - "Cache READ tries nested first, falls through to flat-string on miss (D-23c)"
    - "Two orgs coexist in cache; neither overwrites the other (ACCT-04)"
    - "API key NEVER appears in LocalStorage, in any log line, or in any returned table (AUTH-05 / SEC-03 structural)"
    - "Egress allowlist {oauth.zettle.com, purchase.izettle.com, finance.izettle.com} respected"
    - "All user-facing strings via M_i18n.t — auth.lua surfaces no error strings directly"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts:
    - path: "src/auth.lua"
      provides: "M_auth.exchange_assertion, M_auth.fetch_profile, M_auth.persist_session, M_auth.cached_token (plus private _cache_read, _cache_write); existing _decode_jwt_payload / _extract_client_id from Plan 02-03 preserved"
      contains: "oauth.zettle.com"
      min_lines: 120
    - path: "spec/auth_spec.lua"
      provides: "Greened pending tests for exchange_assertion, fetch_profile, cached_token, persist_session, two-orgs-coexist, flat-fallback cache reload"
  key_links:
    - from: "src/auth.lua exchange_assertion"
      to: "M_http.post_form + https://oauth.zettle.com/token"
      via: "form body with grant_type, client_id, assertion"
      pattern: "oauth%.zettle%.com/token"
    - from: "src/auth.lua fetch_profile"
      to: "M_http.get_json + https://oauth.zettle.com/users/self"
      via: "Authorization: Bearer header"
      pattern: "oauth%.zettle%.com/users/self"
    - from: "src/auth.lua persist_session"
      to: "LocalStorage.zettle[orgUuid] AND LocalStorage[\"zettle:\"..orgUuid]"
      via: "D-23c double-write"
      pattern: "LocalStorage"
    - from: "src/auth.lua cached_token"
      to: "os.time() vs expires_at - 60"
      via: "D-23d pre-expiry guard"
      pattern: "expires_at"
---

<objective>
Implement the auth orchestration layer in src/auth.lua per D-21 / D-22 / D-23 / D-25: exchange_assertion (POST /token), fetch_profile (GET /users/self), persist_session (D-23c double-write), cached_token (D-23d 60s guard), plus private _cache_read / _cache_write. This plan composes Plans 02-02 (M_errors), 02-03 (JWT decoder), and 02-04 (M_http) into the contract Wave 3 (entry-integration) consumes.

Purpose: AUTH-02 demands the exact OAuth form body shape against oauth.zettle.com/token; D-21 requires the two-call probe; D-23c requires nested-then-flat cache writes so AUTH-06 survives whichever way Phase-1 ADR-0003 Q5 resolves; D-23d requires the 60s pre-expiry guard. Without this plan, Wave 3 cannot wire InitializeSession2.

Output: ~120 LoC delta in src/auth.lua; greened pending tests in spec/auth_spec.lua for the eight remaining Wave-0 names.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-RESEARCH.md
@.planning/phases/02-authenticated-network-layer/02-PATTERNS.md
@.planning/phases/02-authenticated-network-layer/02-03-SUMMARY.md
@.planning/phases/02-authenticated-network-layer/02-04-SUMMARY.md
@src/auth.lua
@src/http.lua
@src/errors.lua
@src/log.lua
@src/i18n.lua
@src/webbanking_header.lua
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@spec/auth_spec.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exchange_assertion and fetch_profile per D-21 leg 1 / leg 2</name>
  <read_first>
    - src/auth.lua (state after Plan 02-03; this task appends new functions)
    - src/http.lua (M_http.post_form, M_http.get_json contracts from Plan 02-04)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-21, D-22
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Auth Round-Trip Details" L666–L716 (verbatim URLs, form params, response shapes)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Code Examples" L1183–L1216 (canonical form-body, Bearer header)
    - spec/fixtures/auth/token_ok.json, users_self_ok.json (Wave 0 outputs)
    - spec/auth_spec.lua pending names: "exchange_assertion posts grant_type", "exchange_assertion posts client_id and assertion", "exchange_assertion includes Accept: application/json header", "fetch_profile posts Bearer header"
    - spec/helpers/mm_mocks.lua (post Plan 02-04: Mocks._last_request available)
  </read_first>
  <files>src/auth.lua, spec/auth_spec.lua</files>
  <behavior>
    - exchange_assertion("hdr.eyJhdWQiOiJjbGllbnQteCJ9.sig", "client-x") with token_ok pushed returns ({access_token=..., expires_in=7200, ...}, 200, raw)
    - Mocks._last_request.url == "https://oauth.zettle.com/token"
    - Mocks._last_request.body contains "grant_type=urn%3Aietf%3Aparams%3Aoauth%3Agrant-type%3Ajwt-bearer", "client_id=client-x", "assertion=hdr.eyJ..."
    - Mocks._last_request.contentType == "application/x-www-form-urlencoded"
    - Mocks._last_request.headers["Accept"] == "application/json" (structural via M_http._merge_headers)
    - fetch_profile("AT-12345") with users_self_ok pushed returns ({uuid=..., organizationUuid=..., publicName="Beispiel Café GmbH"}, 200, raw)
    - Mocks._last_request.url == "https://oauth.zettle.com/users/self"
    - Mocks._last_request.headers["Authorization"] == "Bearer AT-12345"
    - No captured print contains the access_token literal "AT-12345" (defense-in-depth — get_json structurally skips headers in logs)
  </behavior>
  <action>
    Edit src/auth.lua. APPEND below the existing _decode_jwt_payload / _extract_client_id from Plan 02-03:

    1. M_auth.exchange_assertion = function(api_key, client_id):
       - Build body table per RESEARCH §"Code Examples" L1194–L1198: grant_type = "urn:ietf:params:oauth:grant-type:jwt-bearer", client_id = client_id, assertion = api_key
       - return M_http.post_form("https://oauth.zettle.com/token", body, {})
       - No logging in this function — M_http handles redacted DEBUG line

    2. M_auth.fetch_profile = function(access_token):
       - return M_http.get_json("https://oauth.zettle.com/users/self", {Authorization = "Bearer " .. access_token})
       - No logging

    Both URLs are hard-coded literals. Per D-27 no sandbox toggle; only production endpoints. Per D-26 oauth.zettle.com is in the allowlist.

    Then GREEN spec/auth_spec.lua pending tests:
    - "exchange_assertion posts grant_type" — Fixtures.load("auth/token_ok"), push as content, call exchange_assertion; assert url is the token URL; assert body:find("grant_type=urn%%3Aietf%%3Aparams%%3Aoauth%%3Agrant%%-type%%3Ajwt%%-bearer", 1, false) succeeds. NOTE: `:` becomes `%3A`; `-` and `.` are NOT percent-encoded by MM.urlencode per its alphabet exclusion [A-Za-z0-9%-_%.~] at spec/helpers/mm_mocks.lua L179.
    - "exchange_assertion posts client_id and assertion" — assert body:find("client_id=client%-x", 1, false) AND body:find("assertion=hdr%.eyJhdWQiOiJjbGllbnQteCJ9%.sig", 1, false)
    - "exchange_assertion includes Accept: application/json header" — assert Mocks._last_request.headers["Accept"] == "application/json"
    - "fetch_profile posts Bearer header" — push users_self_ok; call fetch_profile("AT-12345"); assert url, Authorization header, returned profile.organizationUuid is a non-empty string

    Add TWO additional tests:
    - it("exchange_assertion content type is x-www-form-urlencoded", ...) — assert.equals("application/x-www-form-urlencoded", Mocks._last_request.contentType)
    - it("fetch_profile never echoes the access token in any captured print", ...) — push response; call fetch_profile("AT-SECRET-XYZ"); assert no entry in Mocks._captured_prints contains the literal "AT-SECRET-XYZ"

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/auth_spec.lua --filter "exchange_assertion|fetch_profile" 2>&amp;1 | tee /tmp/auth_t1.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/auth_t1.log &amp;&amp; ! grep -q 'Pending' /tmp/auth_t1.log</automated>
  </verify>
  <acceptance_criteria>
    - All exchange_assertion and fetch_profile tests pass; zero pending; zero failures
    - grep -c 'oauth.zettle.com/token' src/auth.lua is at least 1
    - grep -c 'oauth.zettle.com/users/self' src/auth.lua is at least 1
    - grep -cE '(purchase|finance)\.izettle\.com' src/auth.lua is exactly 0
    - grep -c 'urn:ietf:params:oauth:grant-type:jwt-bearer' src/auth.lua is exactly 1
    - grep -cE 'Bearer ' src/auth.lua is exactly 1 (in fetch_profile)
    - grep -cE '(M_log|print)' src/auth.lua is exactly 0 (Plan 02-05 Task 1 does not introduce log calls; cached_token will add ONE M_log.info in Task 2)
    - luacheck src/auth.lua spec/auth_spec.lua exits 0
    - lua tools/build.lua --verify reports reproducible build
  </acceptance_criteria>
  <done>
    M_auth.exchange_assertion and M_auth.fetch_profile attach to M_auth; hard-code their oauth.zettle.com URLs; delegate transport to M_http; spec greens four Wave-0 tests plus two additional structural tests; auth.lua emits no log lines in Task 1; reproducible build holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _cache_read, _cache_write, persist_session, cached_token per D-23c + D-23d</name>
  <read_first>
    - src/auth.lua (state after Task 1)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-23a, D-23b, D-23c, D-23d
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Pattern 3" L508–L563
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"LocalStorage semantics" L805–L872
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Code Examples / Cache cold-path" L1220–L1235 (exact entry shape)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Specifics" L107–L112 (trust server expires_in)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md Pitfall 6 (multi-merchant overwrite avoidance)
    - spec/auth_spec.lua pending names: "cached_token expiry", "cached_token returns access_token when fresh", "cache survives reload via flat fallback", "persist_session writes both nested and flat cache entries", "two orgs coexist in cache"
    - spec/helpers/mm_mocks.lua L203 (LocalStorage reset each setup)
  </read_first>
  <files>src/auth.lua, spec/auth_spec.lua</files>
  <behavior>
    - _cache_write(orgUuid, entry): LocalStorage.zettle defensively initialized, then nested write + flat-string write (D-23c)
    - _cache_read(orgUuid): nested first; falls through to flat-string + pcall JSON-decode; returns nil on miss
    - persist_session(token_table, profile, client_id): builds entry {access_token, obtained_at=os.time(), expires_at=os.time()+(tonumber(token_table.expires_in) or 7200), client_id, uuid=profile.uuid, publicName=profile.publicName}; calls _cache_write(profile.organizationUuid, entry)
    - cached_token(orgUuid): _cache_read; returns nil if entry missing OR access_token missing OR now >= expires_at - 60; else returns entry.access_token
    - Emits ONE M_log.info line when cache entry exists but is expired: "cached_token: expired for org=" + orgUuid:sub(1,8)
    - Two distinct orgUuids coexist in cache; neither overwrites the other
    - Flat-fallback path: if LocalStorage.zettle == nil but LocalStorage["zettle:org-1"] is a JSON-encoded string, cached_token still returns the access_token (AUTH-06 defensive)
    - persist_session never writes the input api_key into LocalStorage (entry contains only access_token, not the assertion)
  </behavior>
  <action>
    Edit src/auth.lua. APPEND below the Task-1 additions in this order inside the existing do…end block:

    1. local function _cache_write(orgUuid, entry):
       - LocalStorage.zettle = LocalStorage.zettle or {}
       - LocalStorage.zettle[orgUuid] = entry
       - LocalStorage["zettle:" .. orgUuid] = JSON():set(entry):json()

    2. local function _cache_read(orgUuid):
       - If LocalStorage.zettle and LocalStorage.zettle[orgUuid] is non-nil, return it
       - Else read raw = LocalStorage["zettle:" .. orgUuid]; if type(raw) == "string" and #raw > 0, pcall(function() return JSON(raw):dictionary() end); return parsed table on success
       - Return nil on miss

    3. function M_auth.persist_session(token_table, profile, client_id):
       - Build entry per RESEARCH §"Code Examples / Cache cold-path" L1224–L1231:
         access_token = token_table.access_token,
         obtained_at = os.time(),
         expires_at = os.time() + (tonumber(token_table.expires_in) or 7200),
         client_id = client_id,
         uuid = profile.uuid,
         publicName = profile.publicName  -- may be nil; ListAccounts handles fallback
       - Call _cache_write(profile.organizationUuid, entry)
       - Return nil

    4. function M_auth.cached_token(orgUuid):
       - entry = _cache_read(orgUuid); if not entry or not entry.access_token then return nil end
       - now = os.time()
       - if now >= (entry.expires_at or 0) - 60 then M_log.info("cached_token: expired for org=" .. tostring(orgUuid):sub(1, 8)); return nil end
       - return entry.access_token

    The api_key (assertion) is NOT a field of the entry. The cache entry's client_id is the public JWT aud claim. The cache entry's access_token is the Zettle-issued bearer token. The API key never enters this code path past exchange_assertion.

    Then GREEN spec/auth_spec.lua pending tests:

    - "cached_token expiry" — write entry with expires_at = os.time() - 100 (expired); assert cached_token returns nil. Write entry with expires_at = os.time() + 30 (within 60s guard); assert cached_token returns nil. Write entry with expires_at = os.time() + 3600; assert cached_token returns the access_token.
    - "cached_token returns access_token when fresh" — covered above
    - "persist_session writes both nested and flat cache entries" — persist_session({access_token="AT-1", expires_in=7200}, {uuid="user-1", organizationUuid="org-1", publicName="Beispiel Café"}, "client-x"); assert LocalStorage.zettle["org-1"].access_token == "AT-1"; assert type(LocalStorage["zettle:org-1"]) == "string"; round-trip JSON(LocalStorage["zettle:org-1"]):dictionary() and assert .access_token == "AT-1"
    - "two orgs coexist in cache" — persist_session twice with different organizationUuids; assert both cached_token("org-1") and cached_token("org-2") return their respective tokens; assert LocalStorage.zettle has both keys
    - "cache survives reload via flat fallback" — persist_session; then set LocalStorage.zettle = nil (simulates Q5 nested-table-lost scenario); assert cached_token("org-1") still returns "AT-1" via the flat path

    Add ONE additional test:
    - it("persist_session never writes the api_key anywhere in LocalStorage", ...) — call persist_session via the entry sequence (decode api_key, exchange_assertion, fetch_profile, persist_session); walk every string value in LocalStorage recursively; assert none contains "hdr.eyJ" (the synthetic api_key prefix from Plan 02-03 tests)

    No fenced code blocks.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/auth_spec.lua 2>&amp;1 | tee /tmp/auth_t2.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/auth_t2.log &amp;&amp; ! grep -q 'Pending' /tmp/auth_t2.log</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/auth_spec.lua reports zero pending, zero failures across all 20+ tests (Plan 02-03 JWT tests + Plan 02-05 Task 1 exchange/fetch tests + this task's cache tests)
    - grep -c 'function M_auth.persist_session' src/auth.lua is exactly 1
    - grep -c 'function M_auth.cached_token' src/auth.lua is exactly 1
    - grep -c 'local function _cache_write' src/auth.lua is exactly 1
    - grep -c 'local function _cache_read' src/auth.lua is exactly 1
    - grep -c 'LocalStorage.zettle' src/auth.lua is at least 3 (init in _cache_write, write to nested in _cache_write, read in _cache_read)
    - grep -c '"zettle:"' src/auth.lua is at least 2 (flat-key write + flat-key read)
    - grep -c 'expires_at' src/auth.lua is at least 2 (cache entry assembly + cached_token comparison)
    - grep -c 'M_log.info' src/auth.lua is exactly 1 (the cached_token expiry log line; no other log emissions in auth.lua per Plan 02-03 / 02-05 Task 1 constraints)
    - grep -c 'require' src/auth.lua is exactly 0
    - luacheck src/auth.lua spec/auth_spec.lua exits 0
    - lua tools/build.lua --verify reports reproducible build
    - busted spec/ (full suite) reports zero new failures
  </acceptance_criteria>
  <done>
    M_auth gains persist_session, cached_token, plus private _cache_read / _cache_write attached to the existing M_auth table; cache writes go to both nested and flat paths; cache reads try nested then flat; 60s pre-expiry guard active; two-org coexistence holds; flat-fallback reload works; api_key never written to LocalStorage; auth.lua has exactly one M_log.info line in the expired-entry path; reproducible build holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api_key passed into exchange_assertion | OUR credential; transits as the assertion form parameter; never persisted by our code |
| access_token returned by /token | Less sensitive than api_key (2h TTL, scope-limited); cached in LocalStorage; never logged |
| organizationUuid + publicName from /users/self | Non-secret metadata; safe to store as account identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | api_key written to LocalStorage | mitigate | Cache entry shape is locked to {access_token, expires_at, obtained_at, client_id, publicName, uuid}; assertion is structurally absent. Test "persist_session never writes the api_key anywhere in LocalStorage" gates via recursive string walk over LocalStorage. (AUTH-05 / SEC-03 structural.) |
| T-02-05-02 | Information Disclosure | access_token leaked through cached_token's expired-entry log line | mitigate | The log line includes only orgUuid:sub(1,8), never the access_token; M_log.redact further strips any Bearer/JWT-shape if present. Spec test "fetch_profile never echoes the access token in any captured print" gates the structural absence. |
| T-02-05-03 | Tampering | Multi-merchant cache overwrite (Pitfall 6) | mitigate | _cache_write keys by orgUuid (nested) and "zettle:"+orgUuid (flat); two distinct orgs always coexist. Test "two orgs coexist in cache" gates. |
| T-02-05-04 | Spoofing | Stale token used past expires_at | mitigate | cached_token enforces now >= expires_at - 60 returns nil; caller (Wave 3 entry.lua) re-mints via exchange_assertion. 60s guard avoids racing MoneyMoney's refresh against Zettle's clock. |
| T-02-05-05 | Tampering | LocalStorage.zettle nested-table lost on restart (Q5) | mitigate | Flat-string fallback path: LocalStorage["zettle:"+orgUuid] = JSON-string is written every persist_session; cached_token reads it when nested is missing. Test "cache survives reload via flat fallback" gates. |
| T-02-05-06 | Repudiation | Token-mint timing logged for forensics | accept | obtained_at is captured but never logged; only stored in cache entry. Sufficient for cache-validity logic; no audit log needed in v1. |
| T-02-05-SC | Tampering | npm/luarocks installs | accept | Zero installs in this plan — pure Lua source edits only. |
</threat_model>

<verification>
- busted spec/auth_spec.lua exits 0 with zero pending, zero failures (~22 tests total across Plans 02-03 + 02-05)
- busted spec/ (full suite) exits 0
- luacheck src/auth.lua spec/auth_spec.lua exits 0
- lua tools/build.lua --verify reports reproducible build
- grep -c 'oauth.zettle.com' src/auth.lua is at least 2 (token + users/self URLs)
- grep -cE '(purchase|finance)\.izettle\.com' src/auth.lua is exactly 0
- Recursive walk of LocalStorage after persist_session finds no "hdr.eyJ" substring (api_key never persisted)
</verification>

<success_criteria>
- All 8 remaining Wave-0 auth_spec pending tests are now green
- exchange_assertion + fetch_profile compose cleanly with M_http (no transport logic in auth.lua)
- persist_session double-writes nested + flat
- cached_token enforces 60s pre-expiry guard and falls through to flat-string when nested is lost
- API key never appears in LocalStorage, in any log, or in any returned table
- Reproducible build holds; full suite green
</success_criteria>

<output>
Create `.planning/phases/02-authenticated-network-layer/02-05-SUMMARY.md` recording:
- Final LoC of src/auth.lua (target ~180 total: ~60 from Plan 02-03 + ~120 from this plan)
- The exact cache-entry table shape (field order) used by persist_session
- The flat-fallback JSON encoding (so Wave 3 entry-integration tests can mimic the cross-restart simulation deterministically)
- Confirmation that grep -cE '(purchase|finance)\\.izettle\\.com' src/auth.lua is 0
- Confirmation that the api_key never appears in any LocalStorage value (cite the recursive-walk test)
</output>
