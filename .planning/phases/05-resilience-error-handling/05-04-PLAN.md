---
phase: 05-resilience-error-handling
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/purchases.lua
  - src/finance.lua
  - spec/auth_spec.lua
  - spec/refresh_idempotency_spec.lua
autonomous: true
requirements: [ERR-01, ERR-04]
tags: [wave-3, auth, token-revoked, login-failed, regression, tdd]

must_haves:
  truths:
    - "ERR-04 contract LOCKED — silent re-mint COLLAPSED per RESEARCH §Pattern-2: assertion-grant + SEC-03 forbid persisting the api_key (the only thing M_auth.exchange_assertion can use to re-mint) outside the InitializeSession2 credentials dialog. v1.0.0 surfaces post-mint 401 IMMEDIATELY as `error.token_revoked` German string. User re-enters API key via MoneyMoney's normal account dialog. Phase 7 (POST-v1 OAuth Authorization-Code flow per ROADMAP) reintroduces silent refresh via refresh_token grant."
    - "src/purchases.lua M_purchases.fetch: after `local parsed, status, raw = M_http.get_json(url, headers)` (Phase 3 line), check `if status == 401 then return nil, M_i18n.t('error.token_revoked') end` BEFORE the existing `M_errors.from_http_status(status, raw)` routing. Direct-check rationale documented inline: this is a justified exception to the D-43 'all errors via M_errors' invariant because the 401 → token-revoked mapping is semantic (the bearer was VALID at mint time but invalidated mid-session), not a status-code-class generic mapping. The exception applies ONLY to post-mint 401 from resource endpoints; the general D-43 routing is preserved for every other status."
    - "src/finance.lua M_finance.fetch + fetch_all + fetch_account_state: same 401-direct-check pattern as M_purchases.fetch. fetch_account_state issues TWO sequential GETs (`/v2/accounts/liquid/balance` + `/v2/accounts/preliminary/balance` per RESEARCH §1.2); on 401 from EITHER endpoint, return `nil, M_i18n.t('error.token_revoked')` and abort the dual-fetch sequence (do NOT issue the second call after a 401 on the first — saves one needless API call and matches the fail-whole invariant per D-66)."
    - "Plan 05-03's M_http retry-with-backoff does NOT affect ERR-04: a 401 status is NOT retryable. M_http returns the 401 status verbatim to the caller; the caller (purchases / finance) decides the semantic. The retry loop only fires for 5xx and 429."
    - "ERR-01 regression-verified: `M_auth.exchange_assertion` already returns the MoneyMoney `LoginFailed` constant on `invalid_grant` (Phase 2 D-22). spec/auth_spec.lua extends with an explicit ERR-01 gating test using the existing `auth_invalid_grant.json` fixture: queue the invalid_grant 400 → assert `M_auth.exchange_assertion` returns the literal `LoginFailed` constant (NOT a German string). If the regression test fails, Plan 05-04 surfaces this as a Phase-2 regression — do NOT silently rewrite the Phase-2 behavior; commit the failing test, then root-cause and fix in a separate `fix(02):` commit."
    - "ERR-04 vs ERR-01 distinction: ERR-01 = invalid_grant at MINT time (api_key bad) → MoneyMoney's `LoginFailed` constant (special UI affordance: re-prompts credentials). ERR-04 = 401 at RESOURCE call AFTER successful mint (token revoked mid-session) → German `error.token_revoked` string. Both result in the user re-entering the API key, but only `LoginFailed` triggers MoneyMoney's `Konto neu hinzufügen`-style prompt. The distinction matters because a server-side token revocation mid-refresh should NOT be misclassified as a bad API key (that would imply the merchant's stored credentials are wrong when they were correct at mint time)."
    - "spec/auth_spec.lua RED → GREEN sequence: RED commit adds the ERR-04 + ERR-01 tests using existing fixtures. ERR-04 test queues a 200 token-mint response, then queues a 401 on the next GET; calls RefreshAccount and asserts the return string contains the German `error.token_revoked` text from Plan 05-02 i18n additions. GREEN commit lands the 401-direct-check in src/purchases.lua + src/finance.lua."
    - "spec/refresh_idempotency_spec.lua extended with one new case: ERR-04 + retry refresh idempotency — refresh N returns the `error.token_revoked` string with no transactions; refresh N+1 (user re-entered API key, new fresh bearer) succeeds and emits the same transactions that would have appeared if refresh N had succeeded. Asserts: (a) `since` unchanged across the failed and retry refresh (passed back from MoneyMoney byte-identically), (b) no orphan `transactionCode` from refresh N polluting refresh N+1, (c) the German error string format matches the exact Plan 05-02 i18n value (no string drift)."
    - "Coverage: src/purchases.lua + src/finance.lua 401-check branches gated by tests. Coverage on src/auth.lua unchanged (no Phase-5 code change; regression-only)."
    - "luacheck clean on src/purchases.lua, src/finance.lua, spec/auth_spec.lua, spec/refresh_idempotency_spec.lua: no new warnings vs Phase-4 baseline."
    - "Reproducible build SHA changes per src/purchases.lua + src/finance.lua additions. New SHA recorded in Plan 05-04 SUMMARY; Plan 05-05 baseline."
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution; Conventional Commits: `test(05-04):` for spec extensions; `feat(05-04):` for src/purchases.lua + src/finance.lua 401-direct-check; `docs(05-04):` for SUMMARY."
  artifacts:
    - path: "src/purchases.lua"
      provides: "401-direct-check added to M_purchases.fetch (single-line gate before M_errors.from_http_status routing)"
      contains: "error.token_revoked"
    - path: "src/finance.lua"
      provides: "401-direct-check added to M_finance.fetch + fetch_all + fetch_account_state (4 call sites total — 2 in fetch_account_state dual-GET)"
      contains: "error.token_revoked"
    - path: "spec/auth_spec.lua"
      provides: "ERR-01 LoginFailed regression test (explicit gate) + ERR-04 token-revoked happy-path test (queue 200 mint then 401 resource)"
      contains: "ERR-04"
    - path: "spec/refresh_idempotency_spec.lua"
      provides: "ERR-04 + retry case — refresh N fails with token_revoked, refresh N+1 (fresh bearer) succeeds with no orphans"
      contains: "ERR-04 retry"
  key_links:
    - from: "M_purchases.fetch / M_finance.fetch / fetch_account_state"
      to: "M_i18n.t('error.token_revoked')"
      via: "direct-check on status == 401 BEFORE M_errors.from_http_status routing (justified exception to D-43)"
      pattern: "status == 401"
---

<objective>
Close the ERR-04 token-revoked-mid-session contract per RESEARCH §Pattern-2's material correction to CONTEXT D-64: silent re-mint is INFEASIBLE under assertion-grant + SEC-03 (no refresh token; api_key cannot be persisted per AUTH-05). v1.0.0 surfaces post-mint 401 IMMEDIATELY as the German `error.token_revoked` string; the user re-enters the API key in MoneyMoney's account dialog. Phase 7 (POST-v1 OAuth Authorization-Code flow) reintroduces silent refresh via refresh_token grant.

Also gate the ERR-01 invalid_grant → `LoginFailed` regression that Phase 2 already implemented but never explicitly speced. The test is regression-only; if Phase 2 has drifted, Plan 05-04 surfaces the drift without silently rewriting Phase 2.

This plan is purely additive: 4 call-site additions (one each in purchases, three in finance.lua) and 2 spec additions (ERR-01 + ERR-04). No public API surface change; the M_errors.from_http_status central dispatch is preserved for every status code EXCEPT post-mint 401 from resource endpoints (the documented justified exception).

</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-resilience-error-handling/05-CONTEXT.md
@.planning/phases/05-resilience-error-handling/05-RESEARCH.md §Pattern-2 (D-64 collapse) + §1 + §4
@.planning/phases/05-resilience-error-handling/05-PATTERNS.md
@.planning/phases/05-resilience-error-handling/05-02-PLAN.md (Plan 05-02 i18n additions: `error.token_revoked` German key)
@.planning/phases/05-resilience-error-handling/05-03-PLAN.md (Plan 05-03 M_http retry semantics — 401 is NOT retryable)
@CLAUDE.md
@src/purchases.lua
@src/finance.lua
@src/auth.lua (Phase-2 baseline; do NOT modify)
@spec/auth_spec.lua
@spec/refresh_idempotency_spec.lua
@spec/helpers/mm_mocks.lua
@spec/fixtures/auth/auth_invalid_grant.json (Phase-2 fixture — ERR-01 regression test consumes)
</context>

<tasks>

<task type="test:red" gate="red-spec-before-green">
  <name>Task 1 (RED): add ERR-04 token-revoked + ERR-01 LoginFailed regression tests</name>
  <read_first>
    - spec/auth_spec.lua (the file being extended — preserve existing tests byte-identically)
    - spec/refresh_idempotency_spec.lua (extending with ERR-04 retry case)
    - spec/helpers/mm_mocks.lua (Mocks.push_response queue API)
    - spec/fixtures/auth/auth_invalid_grant.json (the ERR-01 regression fixture)
    - .planning/phases/05-resilience-error-handling/05-02-PLAN.md (Plan 05-02 must_haves listing the i18n key `error.token_revoked` German value — your tests assert against this exact string)
  </read_first>
  <action>
    1. In `spec/auth_spec.lua`, add a new `describe("ERR-01 (Phase-5 regression) LoginFailed on invalid_grant", ...)` block with one `it("M_auth.exchange_assertion returns the LoginFailed constant on invalid_grant (ERR-01)", ...)` test that:
       - queues `Fixtures.load("auth/auth_invalid_grant")` as the next response with status 400
       - calls `M_auth.exchange_assertion(api_key, client_id)` (use the same call shape Phase 2 used in this file)
       - asserts the returned value is the literal `LoginFailed` constant (set up by `spec/helpers/mm_mocks.lua` — `LoginFailed = "LoginFailed"` typically). Use `assert.equals(LoginFailed, result)`.

    2. In `spec/auth_spec.lua`, add a second `describe("ERR-04 token-revoked on post-mint 401", ...)` block with one `it("RefreshAccount returns error.token_revoked German string when resource fetch 401s after successful mint (ERR-04)", ...)` test that:
       - queues a 200 token-mint response (`Mocks.push_response({ content = '{"access_token":"eyJtest","expires_in":7200}', mime = "application/json" })`)
       - queues a 401 on the next call (`Mocks.push_response({ content = '{"error":"invalid_token"}', mime = "application/json" })`) — `M_http._infer_status` will derive 401
       - calls RefreshAccount (use the same call shape Phase 4 entry_spec uses)
       - asserts the return value is a STRING (not a table) that contains the German `error.token_revoked` substring "Anmeldung verloren" (or the exact Plan-05-02 i18n value)
       - asserts no transactions reach MoneyMoney (the function returned a string, not a table)

    3. In `spec/refresh_idempotency_spec.lua`, extend the existing `describe` block with one `it("ERR-04 + retry: refresh N fails with token_revoked, refresh N+1 with fresh bearer succeeds without orphans (ERR-04 retry)", ...)` test that:
       - simulates refresh N: queues 200 mint + 401 resource → assert error string return + zero transactions
       - simulates refresh N+1: queues 200 mint (new bearer) + 200 purchase response + finance fixture set → assert the expected transactionCodes appear
       - asserts `since` parameter is unchanged across both refresh calls (the test passes the SAME since value both times — proving MoneyMoney's idempotent contract holds across the ERR-04 failure)
       - asserts no `zettle:sale:` / `zettle:refund:` / `zettle:fee:` / `zettle:payout:` orphans from refresh N pollute refresh N+1's output

    4. Commit RED:
       `test(05-04): add ERR-01 LoginFailed + ERR-04 token-revoked RED specs`
  </action>
  <acceptance_criteria>
    - `git log -1 --format='%s'` shows `test(05-04): add ERR-01 LoginFailed + ERR-04 token-revoked RED specs`
    - `git log -1 --format='%G?'` returns `G`
    - `./.luarocks/bin/busted spec/auth_spec.lua spec/refresh_idempotency_spec.lua 2>&1 | tail -3` shows the new tests as FAILING (RED — the GREEN impl hasn't landed yet)
    - The existing Phase-2 + Phase-3 + Phase-4 tests continue to pass (only the new tests fail)
    - `grep -c 'ERR-04' spec/auth_spec.lua` >= 1; `grep -c 'ERR-01' spec/auth_spec.lua` >= 1; `grep -c 'ERR-04 retry' spec/refresh_idempotency_spec.lua` >= 1
  </acceptance_criteria>
</task>

<task type="feat:green" gate="green-impl-after-red">
  <name>Task 2 (GREEN): add 401-direct-check to src/purchases.lua + src/finance.lua</name>
  <read_first>
    - src/purchases.lua (the file being modified — preserve Phase-3 byte-identity except for the 1-line addition per call site)
    - src/finance.lua (the file being modified — preserve Phase-4 byte-identity except for the 3-call-site additions in fetch + fetch_all + fetch_account_state's dual-GET)
    - src/i18n.lua (verify `error.token_revoked` key landed via Plan 05-02 — your check returns `M_i18n.t("error.token_revoked")`)
    - src/errors.lua (D-43 invariant — your 401 check is the documented justified exception; comment cites this)
  </read_first>
  <action>
    1. In `src/purchases.lua` M_purchases.fetch (after the `local parsed, status, raw = M_http.get_json(url, headers)` line, BEFORE the existing M_errors.from_http_status routing):
       ```
       if status == 401 then
         -- ERR-04 (D-64 collapsed per RESEARCH §Pattern-2): post-mint 401 from a
         -- resource endpoint means the bearer was VALID at mint time but
         -- invalidated mid-session (revoked / scope changed / merchant
         -- regenerated key in another tab). Surface immediately as
         -- error.token_revoked; user re-enters API key in MoneyMoney's
         -- account dialog. Justified exception to D-43 routing: the 401 →
         -- token-revoked mapping is semantic, not status-code-class generic.
         return nil, M_i18n.t("error.token_revoked")
       end
       ```

    2. In `src/finance.lua` M_finance.fetch: same 2-line check after `M_http.get_json` (single call site).

    3. In `src/finance.lua` M_finance.fetch_all: NO check needed if fetch_all just calls fetch in a loop and propagates errors verbatim (verify). If fetch_all does its own get_json call, add the check there too.

    4. In `src/finance.lua` M_finance.fetch_account_state: TWO call sites (liquid/balance + preliminary/balance). After the FIRST 401, abort the dual-fetch sequence and return `nil, M_i18n.t("error.token_revoked")` — do NOT issue the second call (saves one needless API request and matches D-66 fail-whole invariant).

    5. Verify suite goes GREEN:
       - `./.luarocks/bin/busted spec/` — must show 0 failures
       - `./.luarocks/bin/luacheck .` — clean
       - `lua tools/build.lua --verify` — record the new SHA

    6. Commit GREEN:
       `feat(05-04): direct-check post-mint 401 → error.token_revoked (ERR-04)`
  </action>
  <acceptance_criteria>
    - All Phase-2 + Phase-3 + Phase-4 + Phase-5 RED tests pass (the RED becomes GREEN)
    - `grep -c 'error.token_revoked' src/purchases.lua` >= 1
    - `grep -c 'error.token_revoked' src/finance.lua` >= 2 (fetch + fetch_account_state; +1 if fetch_all has its own check)
    - `git log -1 --format='%G?'` returns `G`
    - `./.luarocks/bin/luacheck src/purchases.lua src/finance.lua 2>&1` shows 0 warnings, 0 errors
    - `lua tools/build.lua --verify` returns OK across two runs with identical SHA
    - Commit message contains `(ERR-04)`
  </acceptance_criteria>
</task>

<task type="docs:summary">
  <name>Task 3: write 05-04-SUMMARY.md and hand off to Plan 05-05</name>
  <read_first>
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-03-SUMMARY.md (gold-standard SUMMARY shape — frontmatter + key decisions + metrics + hand-off)
  </read_first>
  <action>
    Write `.planning/phases/05-resilience-error-handling/05-04-SUMMARY.md` mirroring Plan 04-03's SUMMARY shape:
    - Frontmatter: phase, plan: "04", subsystem: auth + resilience, tags, dependency_graph requires=[05-02] provides=[ERR-04-token-revoked-path] affects=[05-05], tech_stack added=[], key_files, decisions (ERR-04 collapse rationale; ERR-01 regression-verify pattern), metrics (busted final count, build SHA, coverage delta)
    - Body: summary of the 4 call-site changes; the justified D-43 exception; the regression-test pattern for ERR-01; the ERR-04+retry idempotency case that gates the user-re-enters-API-key UX
    - Hand-off notes for Plan 05-05: ERR-04 gating in place — Plan 05-05's fail-whole-refresh spec can compose the ERR-04 path with the 5xx fail-whole path; the German error string format is LOCKED at Plan 05-02's i18n value

    Commit: `docs(05-04): Plan 05-04 summary — ERR-04 token-revoked collapsed, ERR-01 regression gated`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/05-resilience-error-handling/05-04-SUMMARY.md` exists with valid YAML frontmatter
    - Body documents the D-64 collapse rationale
    - Commit GPG-signed
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
New / modified files:
- `src/purchases.lua` — EXTENDED with 1-line 401-direct-check (single call site)
- `src/finance.lua` — EXTENDED with 2-4 line 401-direct-check (3 call sites: fetch + fetch_account_state liquid + fetch_account_state preliminary)
- `spec/auth_spec.lua` — EXTENDED with ERR-01 LoginFailed regression test + ERR-04 token-revoked happy-path test
- `spec/refresh_idempotency_spec.lua` — EXTENDED with ERR-04 + retry idempotency case (refresh N fails, refresh N+1 succeeds, no orphans)
- `.planning/phases/05-resilience-error-handling/05-04-SUMMARY.md` — NEW

New public symbols: NONE. The 401-direct-check is internal call-site logic; no new function added.

Contract changes:
- Resource-endpoint 401 → `error.token_revoked` German string (was previously routed through M_errors.from_http_status → generic auth error per Phase 2)
- Documented justified exception to D-43 with inline citation

Yves-blockers SURFACED by this plan: NONE. The D-64 collapse is a CONTEXT correction from RESEARCH §Pattern-2 (already authorized via Yves' full-backlog mandate); no new Yves checkpoint.

Files NOT modified: src/http.lua (Plan 05-03 owns); src/auth.lua (no change — exchange_assertion already returns LoginFailed on invalid_grant per Phase 2); src/errors.lua (no change — 401 direct-check is exception, not extension); src/i18n.lua (Plan 05-02 already added `error.token_revoked`); src/entry.lua (no change — the 401-check happens at resource-call sites, not in the orchestrator).

</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-resilience-error-handling/05-04-SUMMARY.md` summarising:
- The D-64 collapse: silent re-mint infeasible under assertion-grant + SEC-03; collapsed to direct error.token_revoked
- The 4 call-site additions (purchases × 1 + finance × 3)
- The ERR-01 regression result (PASS expected; if FAIL, escalate as Phase-2 regression — do NOT silently fix)
- Metrics: busted final count, build SHA, luacheck status
- Hand-off to Plan 05-05: ERR-04 path locked; Plan 05-05's fail-whole gating spec can compose the ERR-04 path with the 5xx and network fail-whole paths
</output>
