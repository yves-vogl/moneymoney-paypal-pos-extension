---
phase: 04-enrichment-refunds-fees-payouts
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - spec/meta_no_tax_classification_spec.lua
  - spec/meta_purpose_lines_spec.lua
  - spec/refresh_idempotency_spec.lua
  - spec/refresh_log_redaction_spec.lua
autonomous: true
requirements: [META-02, META-03, FEE-01, FEE-03, PAYOUT-01, REF-02, SALE-07, TEST-02]
tags: [wave-4, invariant-gates, idempotency, log-redaction, meta-03, mvp]

must_haves:
  truths:
    - "spec/meta_no_tax_classification_spec.lua exists and is GREEN — asserts that NONE of the 13 forbidden phrases from CONTEXT D-55 appear in ANY src/*.lua file OR in the built dist/paypal-pos.lua artifact; the spec walks the source tree via io.popen('ls src/*.lua') and reads each file via io.open + read('*a') per RESEARCH §7.2"
    - "The 13 forbidden phrases match CONTEXT D-55 verbatim: 'USt-frei', 'USt frei', 'steuerfrei', 'steuerlich', 'GoBD-konform', 'GoBD konform', 'DATEV-fähig', 'DATEV fähig', 'VAT-exempt', 'VAT exempt', 'tax-free', 'tax exempt', 'non-taxable'"
    - "The META-03 spec uses plain-text find (4th arg = true) per RESEARCH §7.2 to avoid Lua-pattern escape complications with the hyphen and accent characters"
    - "The META-03 spec ALSO scans dist/paypal-pos.lua (built first via `lua tools/build.lua` inside the spec OR by relying on the spec preamble pattern that already builds) — the build artifact is the final authoritative surface"
    - "spec/meta_purpose_lines_spec.lua exists and is GREEN — promotes Phase-3's zero-suppression invariants for META-02 (Trinkgeld line absent when sum of gratuityAmount == 0) into its own dedicated spec file per CONTEXT D-54; also covers META-01 zero-VAT-rate edge case"
    - "spec/refresh_idempotency_spec.lua extended with the FOUR D-58 cases per CONTEXT D-58 + RESEARCH §10.3: (1) simple_sale + payout_arrives_next_refresh -> first refresh emits booked=false; second refresh promotes SAME transactionCode to booked=true + valueDate; (2) payout-only refresh -> single zettle:payout:<uuid> on first refresh, ZERO new transactionCodes on second refresh; (3) per-sale fee linked -> zettle:fee:<originatingTransactionUuid> stable across refreshes; (4) aggregate fee -> zettle:fee:aggregate:<date_iso> stable across refreshes when the same finance fixture is re-queued"
    - "spec/refresh_log_redaction_spec.lua extended per D-38 prefix-gate update: the allowed transactionCode prefix set goes from Phase-3's {zettle:sale:, zettle:refund:} to Phase-4's {zettle:sale:, zettle:refund:, zettle:fee:, zettle:fee:aggregate:, zettle:payout:}; the spec walks the returned transactions array after a RefreshAccount that exercises ALL prefixes and asserts every transactionCode starts with one of the 5 allowed prefixes; ALSO asserts no Bearer/JWT-shape substring appears in any captured M_log call or LocalStorage write after a full Phase-4 RefreshAccount cycle (SEC-03 extended to cover Finance API responses per D-45 + RESEARCH §1.6)"
    - "spec/refresh_log_redaction_spec.lua extended with a new it() block walking Finance API response fixtures (finance_single_page, finance_payment_with_fee_linkage, finance_payout, balance fixtures) — for each fixture, queue + RefreshAccount, then walk all captured print/log output via the existing walk_storage helper, assert NO JWT shape (eyJ-prefixed strings) and NO literal 'Bearer eyJ' substring"
    - "lua tools/build.lua --verify reports OK: reproducible across two consecutive builds; luacheck clean on all new+touched spec files; commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642 with Conventional Commits prefix `test(04-05):` and no AI attribution"
    - "If any META-03 forbidden phrase is found in src/* by this plan's spec, the executor MUST fix the src/* file in a separate `fix(04-05):` commit BEFORE the META-03 spec can pass"
    - "Yves-blocker D-55 surfaced: the 13-phrase list is the locked META-03 invariant per CONTEXT recommendation — Yves' Pay/Compliance review either confirms (no change) or amends (extends/shortens) the list; if amended, replan via /gsd-plan-phase 4 --gaps to update the forbidden list and re-run the META-03 spec"
  artifacts:
    - path: "spec/meta_no_tax_classification_spec.lua"
      provides: "NEW spec — META-03 invariant: 13 forbidden phrases never appear in src/*.lua or dist/paypal-pos.lua"
      contains: "FORBIDDEN"
    - path: "spec/meta_purpose_lines_spec.lua"
      provides: "NEW spec — META-02 zero-suppression promoted to its own file per CONTEXT D-54; also covers META-01 zero-rate edge"
      contains: "Trinkgeld"
    - path: "spec/refresh_idempotency_spec.lua"
      provides: "EXTENDED with 4 D-58 cases: sale+payout_promote stable; payout-only stable; per-sale fee stable; aggregate fee stable across refreshes"
      contains: "D-58"
    - path: "spec/refresh_log_redaction_spec.lua"
      provides: "EXTENDED with D-38 prefix-gate update (5 allowed prefixes); extended SEC-03 walk covering Finance API responses"
      contains: "zettle:payout:"
  key_links:
    - from: "spec/meta_no_tax_classification_spec.lua FORBIDDEN list"
      to: "every src/*.lua + dist/paypal-pos.lua"
      via: "io.popen + io.open:read('*a') + content:find(phrase, 1, true)"
      pattern: "FORBIDDEN"
    - from: "spec/refresh_idempotency_spec.lua D-58 case 1 (sale+payout_promote)"
      to: "src/entry.lua RefreshAccount + M_mapping.promote_to_booked + finance_payment_and_payout_for_promotion.json fixture"
      via: "TWO RefreshAccount calls; first with finance_empty, second with the promotion fixture; assert transactionCode byte-identical, booked flipped"
      pattern: "promote_to_booked"
    - from: "spec/refresh_log_redaction_spec.lua D-38 extended prefix gate"
      to: "src/entry.lua RefreshAccount returned transactions array + Plan-04-02/03 transactionCode emissions"
      via: "string.match each transactionCode against ^(zettle:sale:|zettle:refund:|zettle:fee:|zettle:fee:aggregate:|zettle:payout:)"
      pattern: "zettle:fee:aggregate:"
---

<objective>
Wave 4 lands the three Phase-4 invariant gates that turn the implementation green-light into a release-grade contract: (a) META-03 forbidden-strings spec asserting the extension never claims tax/VAT/GoBD/DATEV conformance anywhere in src/ or the built artifact; (b) extended idempotency spec covering the four new D-58 cases (sale+payout promotion, payout-only, per-sale fee, aggregate fee); (c) extended log-redaction spec covering the new D-38 transactionCode prefix set (5 allowed prefixes) AND extending SEC-03 redaction to cover Finance API response payloads.

⚠ Yves-blocker surfaced: D-55 META-03 forbidden-strings list completeness

Per CONTEXT D-55, the 13 phrases listed below are the *recommended* forbidden list. The autonomous window cannot confirm completeness — Yves' Pay/Compliance review owns the final list. This plan implements the CONTEXT-recommended 13-phrase list verbatim. If Yves confirms unchanged, no replan. If Yves amends (adds/removes phrases), replan via `/gsd-plan-phase 4 --gaps` and re-run the META-03 spec.

The 13 phrases: `USt-frei`, `USt frei`, `steuerfrei`, `steuerlich`, `GoBD-konform`, `GoBD konform`, `DATEV-fähig`, `DATEV fähig`, `VAT-exempt`, `VAT exempt`, `tax-free`, `tax exempt`, `non-taxable`.

Purpose: META-03 is the permanent never-rewrite invariant — once locked, any future feature request that wants to add tax-classification wording requires the CONTEXT D-55 conversation to be REOPENED explicitly, not silently overridden in a plan. The spec is the structural enforcement. The extended idempotency + log-redaction specs are the load-bearing acceptance criteria for Phase 4 — if either gate fails, MoneyMoney will either double-book transactions or leak credentials, both showstoppers.

Output: spec/meta_no_tax_classification_spec.lua (NEW; META-03 invariant); spec/meta_purpose_lines_spec.lua (NEW; META-02 + META-01 zero-rate edge); spec/refresh_idempotency_spec.lua (EXTENDED with 4 D-58 cases); spec/refresh_log_redaction_spec.lua (EXTENDED with D-38 prefix gate + SEC-03 Finance API redaction); all 4 specs GREEN; reproducible build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-PATTERNS.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-03-PLAN.md
@.planning/phases/04-enrichment-refunds-fees-payouts/04-04-PLAN.md
@src/mapping.lua
@src/entry.lua
@src/finance.lua
@src/i18n.lua
@spec/refresh_idempotency_spec.lua
@spec/refresh_log_redaction_spec.lua
@spec/mapping_spec.lua
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create spec/meta_no_tax_classification_spec.lua (META-03 invariant) and pre-flight scan src/* for accidental forbidden-phrase violations</name>
  <read_first>
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md D-55 (the 13-phrase verbatim list and the once-locked-permanent-invariant rule)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §7.2 (spec implementation pattern: io.popen + scan_file + content:find with plain-flag), §7.3 (boundary considerations), §7.4 (what we ARE allowed to say)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-PATTERNS.md `spec/meta_no_tax_classification_spec.lua — META-03 invariant (NEW)` section (walk-pattern from spec/refresh_log_redaction_spec.lua adapted for file-text scanning)
    - spec/refresh_log_redaction_spec.lua (the existing log-redaction spec — close analog for the walker pattern; the spec preamble already builds dist/paypal-pos.lua so this new spec can rely on the same build-on-demand idiom)
    - All src/*.lua files (NEED a pre-flight grep before writing the spec — Plan 04-04 added new German strings; if any contain a forbidden phrase, the META-03 spec will fail on the first run and the executor must fix src/* before the spec can be GREEN)
    - src/i18n.lua (especially STRINGS.de which contains German translations — check that none of the existing Phase-3 + Plan-04-02 additions contain a forbidden phrase)
    - dist/paypal-pos.lua (the built artifact — same scan applied)
  </read_first>
  <files>spec/meta_no_tax_classification_spec.lua</files>
  <action>
    Step 1: Pre-flight scan src/*.lua + dist/paypal-pos.lua for ANY of the 13 forbidden phrases. Run a bash loop iterating over each phrase from D-55 (USt-frei, USt frei, steuerfrei, steuerlich, GoBD-konform, GoBD konform, DATEV-fähig, DATEV fähig, VAT-exempt, VAT exempt, tax-free, tax exempt, non-taxable) and grep -rnE each phrase against src/ + dist/. Expected outcome: NO HITS. The Phase-3 + Plan-04-02 i18n additions only contain German bookkeeping terms (Brutto, Netto, MwSt, Trinkgeld, Beleg, Gebühr, Auszahlung, Rückerstattung, Kartenzahlung, etc.) — none of which appear on the forbidden list per RESEARCH §7.4.

    IF any phrase IS found (e.g., a Phase-3 i18n key was inadvertently named error.gobd_conform or a code comment mentioned "DATEV-fähig"): fix the offending file in a separate `fix(04-05):` commit BEFORE creating the spec. The fix commit message documents which file + which phrase was removed and references D-55. Then proceed to Step 2.

    IF all 13 phrases are absent (expected case): proceed directly to Step 2.

    Step 2: Create spec/meta_no_tax_classification_spec.lua. Use the standard Phase-3 spec preamble pattern from spec/refresh_log_redaction_spec.lua (require mm_mocks, require fixtures helper, build dist if needed). For this spec the spec does not run RefreshAccount so the Mocks/fixtures requires are unnecessary, but the build dist invocation is needed for the artifact scan.

    Define a module-local FORBIDDEN array containing the 13 phrases verbatim from D-55 (UTF-8 byte-escape umlauts only if necessary; modern Lua + busted handle raw UTF-8 source files; preserve the literal "DATEV-fähig" form so the spec's find() matches the raw UTF-8 bytes if any future src file accidentally smuggles them in).

    Define a `scan_file(path)` local function that opens the file via io.open, reads the full content via :read("*a"), then iterates the FORBIDDEN list calling `content:find(phrase, 1, true)` (the 4th arg = true is the plain-text flag per RESEARCH §7.2; this avoids Lua-pattern escape complications with the hyphen `-` and accent characters in DATEV-fähig). Each find result must be nil; assert.is_nil with an error message citing the path + phrase + byte offset.

    Wrap two it() blocks in a describe block titled "META-03: forbidden tax-classification phrases (D-55)":
    - it("none of src/*.lua contains a forbidden phrase"): opens an io.popen("ls src/*.lua") handle, iterates :lines(), calls scan_file(line) per file, closes the handle.
    - it("dist/paypal-pos.lua contains no forbidden phrase (built artifact gate)"): calls os.execute("lua tools/build.lua >/dev/null 2>&1") to ensure the artifact exists; tolerates already-built state (exit code true/0); then calls scan_file("dist/paypal-pos.lua").

    Step 3: Run the spec and confirm GREEN. busted spec/meta_no_tax_classification_spec.lua MUST pass both it() blocks. If either fails, Step 1's pre-flight didn't catch a phrase that's actually in src/ or dist/; locate it (the spec error message includes path + phrase + byte offset), fix in a `fix(04-05):` commit, then re-run.

    Step 4: Build + verify. lua tools/build.lua --verify reports OK: reproducible. luacheck spec/meta_no_tax_classification_spec.lua clean.

    Step 5: Commit. `test(04-05): add META-03 forbidden-strings invariant spec (D-55, 13 phrases)`. GPG-signed, no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; ls spec/meta_no_tax_classification_spec.lua &amp;&amp; grep -c '"steuerfrei"' spec/meta_no_tax_classification_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c '"GoBD-konform"' spec/meta_no_tax_classification_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c '"non-taxable"' spec/meta_no_tax_classification_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua 2&gt;&amp;1 | grep -qE '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/meta_no_tax_classification_spec.lua` succeeds
    - `grep -c '"steuerfrei"' spec/meta_no_tax_classification_spec.lua` reports >= 1
    - `grep -c '"GoBD-konform"' spec/meta_no_tax_classification_spec.lua` reports >= 1
    - `grep -c '"DATEV' spec/meta_no_tax_classification_spec.lua` reports >= 2 (the two DATEV phrases — DATEV-fähig and DATEV fähig)
    - `grep -c '"non-taxable"' spec/meta_no_tax_classification_spec.lua` reports >= 1
    - All 13 D-55 phrases appear in the spec's FORBIDDEN array
    - `busted spec/meta_no_tax_classification_spec.lua` is GREEN
    - `! grep -rE 'USt-frei|GoBD-konform|VAT-exempt|tax-free|non-taxable|steuerfrei|steuerlich' src/ dist/` succeeds (no phrase appears anywhere in src/ or dist/)
    - `luacheck spec/meta_no_tax_classification_spec.lua` is clean
    - `lua tools/build.lua --verify` reports OK: reproducible
    - Commit GPG-signed; no AI attribution
  </acceptance_criteria>
  <done>
    spec/meta_no_tax_classification_spec.lua exists with the 13-phrase FORBIDDEN list; both it() blocks GREEN; no forbidden phrase anywhere in src/ or dist/; reproducible build green. Commit: `test(04-05): add META-03 forbidden-strings invariant spec (D-55, 13 phrases)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create spec/meta_purpose_lines_spec.lua (META-02 zero-suppression + META-01 zero-rate edge)</name>
  <read_first>
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md D-54 (META-02 promotion to own spec — keep Phase-3's Trinkgeld zero-suppression invariant gated separately)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §5.3 ("0% rate present" edge case — recommendation: show it because the merchant's invoice itemized it; suppress only when value is 0 AND no other VAT lines exist)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-PATTERNS.md `spec/meta_purpose_lines_spec.lua — META-02 (NEW; promote zero-suppression from mapping_spec)` section (pure-logic pattern; inline fixture-as-table inputs)
    - spec/mapping_spec.lua (the existing tests covering zero-Trinkgeld and zero-VAT suppression that Phase-3 implemented in Plan 03-03; this task PROMOTES the contracts into a dedicated spec file but does NOT remove the duplicate tests in mapping_spec.lua — the META-02 spec exists for regression-gating clarity per CONTEXT D-54)
    - src/mapping.lua (the existing _format_purpose implementation that this spec exercises)
  </read_first>
  <files>spec/meta_purpose_lines_spec.lua</files>
  <action>
    Step 1: Create spec/meta_purpose_lines_spec.lua. Use the standard Phase-3 pure-logic spec preamble (mirror spec/mapping_spec.lua's opening; require dist/paypal-pos.lua for the loaded M_mapping symbol; no Mocks needed because _format_purpose is pure-logic).

    Step 2: Inside a describe block titled "META-02: Trinkgeld zero-suppression (D-54 promotion)", add three it() blocks using inline-table inputs (cf. spec/auth_spec.lua table-driven style):

    1. it("META-02: Trinkgeld line absent when sum of gratuityAmount is 0"): inline a purchase table with currency="EUR", amount=500, vatAmount=0, payments=[{gratuityAmount=0}, {gratuityAmount=0}], purchaseUUID1="...", purchaseNumber=1001, timestamp valid; call M_mapping.purchase_to_transaction(p); assert.is_nil(txn.purpose:find("Trinkgeld", 1, true)).

    2. it("META-02: Trinkgeld line absent when payments table is empty"): inline a purchase with payments={}; same assertion.

    3. it("META-02: Trinkgeld line present and renders gratuityAmount sum when > 0"): inline a purchase with payments=[{gratuityAmount=100}, {gratuityAmount=50}]; assert.truthy(txn.purpose:find("Trinkgeld: 1,50 EUR", 1, true)) (1,50 EUR = 150 minor units / 100).

    Step 3: Inside a second describe block titled "META-01: zero-rate edge cases (RESEARCH §5.3)", add two it() blocks:

    1. it("META-01: single 0% rate in groupedVatAmounts with value 0 — suppress (no MwSt line)"): inline a purchase with vatAmount=0 and groupedVatAmounts={["0.0"]=0}; call mapping; assert.is_nil(txn.purpose:find("MwSt", 1, true)). RESEARCH §5.3 recommendation: suppress when sole entry and value 0; this gates that choice.

    2. it("META-01: 0% rate alongside 19% — show both per-rate lines including zero"): inline a purchase with vatAmount=318 and groupedVatAmounts={["0.0"]=0, ["19.0"]=318}; call mapping; assert.truthy(txn.purpose:find("0% MwSt: 0,00 EUR", 1, true)); assert.truthy(txn.purpose:find("19% MwSt: 3,18 EUR", 1, true)); assert.is_true(txn.purpose:find("19%% MwSt", 1, true) < txn.purpose:find("0%% MwSt", 1, true)) (19 sorted before 0).

    Step 4: Run spec; both describes GREEN. The META-02 tests pass against the existing Phase-3 implementation untouched. The META-01 zero-rate edge tests pass against Plan-04-04's _format_purpose extension — IF the suppression rule in test 1 of describe-2 is not yet handled, that's an OPTIONAL bonus contract to add to src/mapping.lua _format_purpose; if RESEARCH §5.3 recommendation was implemented in Plan 04-04, this test is GREEN already; if not, Task 2 of this plan may need a tiny patch to mapping.lua. RECOMMENDED: implement the suppression rule here if Plan 04-04 omitted it — the patch is one if-statement: when rate_entries has exactly 1 entry AND that entry's amount is 0, skip emitting the line entirely.

    Step 5: Build + verify. busted, luacheck clean, reproducible.

    Step 6: Commit. `test(04-05): add META-02 zero-suppression + META-01 zero-rate edge spec (D-54, RESEARCH §5.3)`. GPG-signed, no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; ls spec/meta_purpose_lines_spec.lua &amp;&amp; grep -c 'Trinkgeld' spec/meta_purpose_lines_spec.lua | awk '{ if ($1 &gt;= 3) exit 0; else exit 1 }' &amp;&amp; grep -c 'META-01' spec/meta_purpose_lines_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ./.luarocks/bin/busted spec/meta_purpose_lines_spec.lua 2&gt;&amp;1 | grep -qE '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/meta_purpose_lines_spec.lua` succeeds
    - `grep -c 'Trinkgeld' spec/meta_purpose_lines_spec.lua` reports >= 3 (3 META-02 it blocks reference Trinkgeld)
    - `grep -c 'META-01' spec/meta_purpose_lines_spec.lua` reports >= 1
    - `busted spec/meta_purpose_lines_spec.lua` is GREEN
    - `busted spec/mapping_spec.lua` still GREEN (no regression from any tiny mapping.lua patch in Step 4)
    - `lua tools/build.lua --verify` reports OK: reproducible
    - Commit GPG-signed; no AI attribution
  </acceptance_criteria>
  <done>
    spec/meta_purpose_lines_spec.lua exists with META-02 zero-suppression + META-01 zero-rate edge tests all GREEN. Commit: `test(04-05): add META-02 zero-suppression + META-01 zero-rate edge spec`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend spec/refresh_idempotency_spec.lua (4 D-58 cases) and spec/refresh_log_redaction_spec.lua (D-38 prefix gate + SEC-03 Finance API)</name>
  <read_first>
    - spec/refresh_idempotency_spec.lua (the Phase-3 baseline + the Plan-04-03 queueing-infrastructure extension; THIS task lands the 4 D-58 idempotency assertions on top)
    - spec/refresh_log_redaction_spec.lua (Phase-3 baseline; Gate C around L108-127 contains the prefix gate this task extends; the walk_storage helper around L54-62 is reused for the SEC-03 Finance API redaction extension)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-CONTEXT.md D-58 (the 4 new idempotency cases verbatim — covering simple_sale+payout_promote / payout-only / per-sale fee linked / aggregate fee dedup), D-38 (the 5-entry allowed-prefix set), D-45 (SEC-03 Bearer redaction extends to Finance API responses)
    - .planning/phases/04-enrichment-refunds-fees-payouts/04-RESEARCH.md §10.3 (idempotency gating extension scenario table), §10.4 (extended transactionCode prefix gate)
    - spec/fixtures/finance/finance_payment_and_payout_for_promotion.json + finance_payout.json + finance_payment_with_fee_linkage.json + finance_payment_fee_unlinked.json (the Plan-04-02 fixtures driving each D-58 case)
    - spec/fixtures/finance/finance_balance_liquid.json + finance_balance_preliminary.json + finance_empty.json (queueing infrastructure background — every RefreshAccount call queues these 3 alongside the test-specific finance fixture)
    - src/entry.lua RefreshAccount (the Plan-04-03 implementation — verify the 14-step sequence emits the transactionCodes asserted by the D-58 tests)
    - src/mapping.lua promote_to_booked (the mutator gating the SALE-03 D-56 promotion path)
  </read_first>
  <files>spec/refresh_idempotency_spec.lua, spec/refresh_log_redaction_spec.lua</files>
  <action>
    Step 1: Extend spec/refresh_idempotency_spec.lua. Locate the existing Phase-3 describe block (typically titled "refresh idempotency (TEST-03 / SALE-05 / D-39)"); add a new describe block titled "Phase-4 D-58 idempotency extensions (CONTEXT D-58 / RESEARCH §10.3)" below it.

    Define a helper `refresh_with_full_queue(purchase_fixture_name, finance_fixture_name)` inside this new describe that wraps the Plan-04-03 queueing pattern: queue 4 Mocks.push_response in order — (1) the purchase fixture's raw JSON, (2) finance_balance_liquid raw, (3) finance_balance_preliminary raw, (4) the finance fixture's raw JSON; then call RefreshAccount(test_account, 0) and return the result.

    Add four it() blocks per D-58:

    1. it("D-58: sale+payout_promote — first refresh booked=false; second refresh promotes SAME transactionCode to booked=true + valueDate"): First call: refresh_with_full_queue("purchases/purchase_page_with_payments_for_fee_join", "finance/finance_empty"); find the sale txn by transactionCode prefix "zettle:sale:20202020"; capture its code, assert booked==false, assert valueDate is nil. Second call: refresh_with_full_queue("purchases/purchase_page_with_payments_for_fee_join", "finance/finance_payment_and_payout_for_promotion") — NOTE this requires the finance_payment_and_payout_for_promotion fixture's PAYMENT.originatingTransactionUuid to match payment.uuid `cccccccc-cccc-cccc-cccc-cccccccccccc` from purchase_page_with_payments_for_fee_join; if Plan 04-02's fixture used `eeeeeeee-...` instead, either regenerate the promotion fixture or create an additional fixture variant `finance_payment_and_payout_for_promotion_via_cccc.json` matching the cccc UUID; the planner recommends regenerating in this task to use `cccccccc-` so the promotion fixture reuses the fee-join purchase. Find the sale txn again by the SAME transactionCode prefix; assert its transactionCode is BYTE-IDENTICAL to the first call; assert booked==true; assert valueDate is a non-nil number > 0.

    2. it("D-58: payout-only refresh produces zettle:payout:<uuid> on first refresh; ZERO new transactionCodes on second refresh"): First call: refresh_with_full_queue("purchases/purchases_empty", "finance/finance_payout"). Capture all transactionCodes from result.transactions. Assert at least one starts with "zettle:payout:". Second call: re-queue identical fixtures, refresh again. Assert the captured transactionCodes from refresh-2 are a SUBSET of refresh-1's set (in practice, identical set — MoneyMoney would dedup any duplicates by transactionCode regardless of what the extension returns; the gating contract is that the EXTENSION returns the same transactionCodes both times so MoneyMoney's dedup actually has something to dedup on).

    3. it("D-58: per-sale fee linked emits zettle:fee:<originatingTransactionUuid> stable across refreshes"): refresh_with_full_queue("purchases/purchase_page_with_payments_for_fee_join", "finance/finance_payment_with_fee_linkage") twice; assert the fee transactionCode is byte-identical across both refreshes; assert it starts with "zettle:fee:cccccccc" (the originatingTransactionUuid from the linkage fixture).

    4. it("D-58: aggregate fee emits zettle:fee:aggregate:<date_iso> stable across refreshes"): refresh_with_full_queue("purchases/purchase_page_with_payments_for_fee_join", "finance/finance_payment_fee_unlinked") twice; assert the aggregate transactionCode is byte-identical across both refreshes; assert it starts with "zettle:fee:aggregate:2026-06-15" (the Berlin-local date of the unlinked fee's timestamp `2026-06-15T14:00:00.000+0000` — Plan 04-02 chose that timestamp specifically so the Berlin-local date is 2026-06-15).

    Step 2: Extend spec/refresh_log_redaction_spec.lua. Two extensions per CONTEXT D-38 / D-45:

    Extension A: extend the existing "Gate C" (the transactionCode prefix gate, around L108-127 per PATTERNS.md). Change the allowed-prefix set from the Phase-3 {"zettle:sale:", "zettle:refund:"} to the Phase-4 5-entry set: {"zettle:sale:", "zettle:refund:", "zettle:fee:", "zettle:fee:aggregate:", "zettle:payout:"}. Implementation: change the local list-of-allowed-prefixes table; the iteration + assertion shape stays identical. Add an explicit it() block: it("D-38 extended: every returned transactionCode starts with one of the 5 Phase-4 allowed prefixes"): refresh_with_full_queue using a finance fixture that produces all 5 prefixes (use finance_single_page + the Plan-04-02 cross-fixture trick to produce sale + refund + fee + payout in one cycle; or run multiple RefreshAccount calls and union their transactionCodes); walk result.transactions; for each txn, assert.truthy(matches_one_of_allowed_prefixes(txn.transactionCode)).

    Extension B: add a new describe block titled "SEC-03 / D-45 extended: Bearer redaction covers Finance API responses (RESEARCH §1.6)". Inside, add it() blocks for each Finance fixture (finance_single_page, finance_payment_with_fee_linkage, finance_payout, finance_balance_liquid, finance_balance_preliminary): for each, queue the fixture + run RefreshAccount + walk all captured print/log output via the existing walk_storage helper + assert NO eyJ-prefixed string (JWT shape) and NO literal "Bearer eyJ" substring. The Phase-3 walk_storage iterates strings in nested tables; reuse byte-identically.

    Step 3: Build + verify. lua tools/build.lua --verify OK: reproducible. busted spec/refresh_idempotency_spec.lua spec/refresh_log_redaction_spec.lua all GREEN. Full spec suite GREEN end-to-end. luacheck on both touched spec files clean.

    Step 4: Commit. `test(04-05): extend idempotency spec (D-58 4 cases) + log-redaction spec (D-38 prefix gate + SEC-03 Finance API)`. GPG-signed, no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'D-58' spec/refresh_idempotency_spec.lua | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }' &amp;&amp; grep -c 'zettle:fee:aggregate:' spec/refresh_log_redaction_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'zettle:payout:' spec/refresh_log_redaction_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'SEC-03' spec/refresh_log_redaction_spec.lua | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; ./.luarocks/bin/busted spec/refresh_idempotency_spec.lua spec/refresh_log_redaction_spec.lua spec/meta_no_tax_classification_spec.lua spec/meta_purpose_lines_spec.lua 2&gt;&amp;1 | grep -qE '0 failures' &amp;&amp; ./.luarocks/bin/busted spec/ 2&gt;&amp;1 | grep -qE '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-58' spec/refresh_idempotency_spec.lua` reports >= 4 (the 4 new D-58 cases all reference the decision ID)
    - `grep -c 'promote_to_booked' spec/refresh_idempotency_spec.lua` reports >= 1 (case 1 references the mutator behavior indirectly through booked-flag assertions)
    - `grep -c 'zettle:fee:aggregate:' spec/refresh_log_redaction_spec.lua` reports >= 1 (the new prefix in the allowed set)
    - `grep -c 'zettle:payout:' spec/refresh_log_redaction_spec.lua` reports >= 1
    - `grep -c 'zettle:fee:[^a]' spec/refresh_log_redaction_spec.lua` reports >= 1 (the per-sale fee prefix distinct from aggregate)
    - `grep -c 'SEC-03' spec/refresh_log_redaction_spec.lua` reports >= 1 (the new Finance API extension describe block)
    - `grep -c 'D-58' spec/refresh_idempotency_spec.lua` reports >= 4
    - `busted spec/refresh_idempotency_spec.lua` GREEN (Phase-3 + Plan-04-03 infrastructure + 4 new D-58 cases all pass)
    - `busted spec/refresh_log_redaction_spec.lua` GREEN (Phase-3 + Plan-04-03 + extended prefix gate + extended SEC-03 Finance API walks all pass)
    - `busted spec/` exits with 0 failures (FULL suite GREEN end-to-end; this is the load-bearing Phase-4 milestone)
    - `luacheck spec/refresh_idempotency_spec.lua spec/refresh_log_redaction_spec.lua` clean
    - `lua tools/build.lua --verify` OK: reproducible
    - Commit GPG-signed; no AI attribution; prefix `test(04-05):`
  </acceptance_criteria>
  <done>
    spec/refresh_idempotency_spec.lua + spec/refresh_log_redaction_spec.lua extended with the D-58 / D-38 / SEC-03 Phase-4 contracts; full spec suite GREEN end-to-end. Commit: `test(04-05): extend idempotency spec (D-58 4 cases) + log-redaction spec (D-38 prefix gate + SEC-03 Finance API)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The 13 forbidden phrases → spec assertion logic | The spec uses plain-text find (4th arg true) so Lua-pattern injection from phrases containing `-` or accent is impossible |
| RefreshAccount returned transactions → transactionCode prefix gate | Each transactionCode is matched against a fixed allowed-prefix set; any unknown prefix fails the gate immediately |
| Finance API response payloads → log walk | All captured print/log output walked via Phase-3 walk_storage helper; eyJ + Bearer eyJ literal checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W4-01 | Tampering | META-03 invariant spec | mitigate | Plain-text find with 4th arg true; allowed-list is read-only inside the spec; future regression caught at every CI run |
| T-04-W4-02 | Information Disclosure | extended SEC-03 Finance API redaction | mitigate | Walk every captured log + LocalStorage write after Phase-4 RefreshAccount cycle; eyJ + Bearer eyJ literal checks; gate against ANY new log site Plan 04-03 introduced inadvertently |
| T-04-W4-03 | Tampering | D-38 extended prefix gate | mitigate | Closed set of 5 allowed prefixes; any new transaction kind in future phases must extend this set explicitly; structural enforcement against silent prefix-set growth |
| T-04-W4-04 | Tampering | D-58 idempotency aggregate-stability assertion | accept (Option B per Plan 04-03) | The spec asserts SAME-fixture re-fetch produces SAME aggregate transactionCode; the linkage-upgrade flip-to-per-sale failure mode is NOT in this spec but is documented in the README per Plan 04-06; Yves accepts the trade-off per the D-49 blocker resolution |
| T-04-W4-05 | Denial of Service | META-03 spec scans the built artifact | accept | dist/paypal-pos.lua is a single ~30kB file; full scan of 13 phrases via plain-text find is millisecond-scale |
| T-04-W4-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs |
</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Reproducible build:
lua tools/build.lua && lua tools/build.lua --verify

# All Phase-4 invariant gates GREEN:
./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua
./.luarocks/bin/busted spec/meta_purpose_lines_spec.lua
./.luarocks/bin/busted spec/refresh_idempotency_spec.lua
./.luarocks/bin/busted spec/refresh_log_redaction_spec.lua

# Full suite GREEN end-to-end:
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(failures|0 failures)'

# luacheck clean on all touched specs:
./.luarocks/bin/luacheck spec/meta_no_tax_classification_spec.lua spec/meta_purpose_lines_spec.lua spec/refresh_idempotency_spec.lua spec/refresh_log_redaction_spec.lua

# Forbidden phrase scan as a sanity backstop:
! grep -rE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' src/ dist/
```
</verification>

<success_criteria>
- spec/meta_no_tax_classification_spec.lua exists, FORBIDDEN array contains all 13 D-55 phrases, both it() blocks GREEN
- spec/meta_purpose_lines_spec.lua exists, META-02 zero-suppression + META-01 zero-rate edge tests all GREEN
- spec/refresh_idempotency_spec.lua extended with the 4 D-58 cases all GREEN
- spec/refresh_log_redaction_spec.lua extended with D-38 prefix gate (5 allowed prefixes) + extended SEC-03 Finance API redaction; GREEN
- Full `busted spec/` GREEN end-to-end (this is the load-bearing Phase-4 milestone — all gating specs from Wave 0 through Wave 4 turn fully green)
- `lua tools/build.lua --verify` OK: reproducible
- luacheck clean on all touched files
- All commits GPG-signed; no AI attribution; Conventional Commits prefixes `test(04-05):` (and `fix(04-05):` only if any META-03 violation surfaced in src/* during pre-flight)
- Yves-blocker D-55 explicitly surfaced in the plan preamble; the implemented forbidden list matches CONTEXT recommendation; replan path via /gsd-plan-phase 4 --gaps documented for any post-confirmation amendment
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `spec/meta_no_tax_classification_spec.lua` — NEW (2 it() blocks scanning src/*.lua and dist/paypal-pos.lua for the 13 D-55 phrases)
- `spec/meta_purpose_lines_spec.lua` — NEW (META-02 zero-suppression 3 tests + META-01 zero-rate edge 2 tests)
- `spec/refresh_idempotency_spec.lua` — EXTENDED with 4 D-58 cases
- `spec/refresh_log_redaction_spec.lua` — EXTENDED with D-38 prefix gate (5 allowed prefixes) + extended SEC-03 walks for Finance API responses

New public symbols: NONE (this plan is all spec-side enforcement; no src/ surface added)

New i18n keys: NONE

New ADR numbers: NONE

Locked invariants after this plan:
- META-03 forbidden phrases: 13 phrases per D-55, locked permanent (Yves can amend via blocker confirmation + replan)
- D-38 allowed transactionCode prefix set: {zettle:sale:, zettle:refund:, zettle:fee:, zettle:fee:aggregate:, zettle:payout:} — closed set; any future addition requires explicit prefix-gate update
- D-58 idempotency: 4 D-58 scenarios all gated; double-refresh produces same transactionCodes; SALE-03 promotion preserves transactionCode byte-identically
- SEC-03 extended: Bearer redaction covers Finance API responses across all 5 fixture types

Yves-blockers RESOLVED by surfacing in this plan: D-55 (the META-03 forbidden list is implemented per CONTEXT recommendation; Yves may confirm or amend during the blocker review pass)

Yves-blockers REMAINING after this plan: NONE — by the end of Plan 04-05 all three CONTEXT-queued blockers (Q3 via Plan 04-01, D-49 Option A vs B via Plan 04-03, D-55 via Plan 04-05) have been surfaced with default recommendations and replan paths documented

Files NOT modified by this plan: ANY src/*.lua file under normal circumstances (only the META-03 pre-flight may surface a need to fix src/i18n.lua or a code comment, in which case that fix is a separate `fix(04-05):` commit); no fixture changes; no manifest changes; no CHANGELOG.md / README.md changes; no docs/adr/ changes (Plan 04-06 drafts ADR-0004 if Plan 04-01's verdict was `reject scope`)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-enrichment-refunds-fees-payouts/04-05-SUMMARY.md` summarising:
- META-03 spec status (both it() blocks GREEN; pre-flight scan outcome: any phrases found in src/ or dist/, count + paths)
- META-02 + META-01 zero-edge spec status (all 5 tests GREEN; whether the META-01 zero-rate suppression required a tiny mapping.lua patch in Task 2 Step 4)
- D-58 idempotency cases (all 4 GREEN); cite the specific transactionCodes asserted byte-identical across refreshes
- D-38 extended prefix gate: confirm the 5 allowed prefixes; any prefix discovered in the Plan-04-03 entry-layer output that's NOT in the allowed set (would surface as a Plan-04-03 bug)
- SEC-03 extended Finance API walk: confirm no eyJ + no Bearer eyJ in any captured log/LocalStorage from any of the 5 fixture cycles
- Full spec suite pass count (Phase 1 + 2 + 3 + 4 cumulative)
- Coverage on src/ (Phase-3 landed 99.23%; Phase 4 target ≥95% on new code)
- Reproducible-build SHA after this plan
- Yves-blocker status: D-55 implemented per CONTEXT recommendation; awaiting confirm/amend during blocker review
- Confirmation that ALL three CONTEXT-queued blockers (Q3 / D-49 / D-55) have now been surfaced with default recommendations and documented replan paths
</output>
</content>
</invoke>