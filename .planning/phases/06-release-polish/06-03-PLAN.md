---
phase: 06-release-polish
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/phases/06-release-polish/06-HANDOFF.md
autonomous: true
requirements: [DOC-10]
tags: [wave-3, changelog, state, handoff, mvp]

must_haves:
  truths:
    - "CHANGELOG.md extends with a `## [1.0.0] - 2026-MM-DD` section in Keep-a-Changelog format per DOC-10 / D-81 / PATTERNS item 13; the existing [Unreleased] + [0.2.0] sections from Phase 4 are PRESERVED byte-identically (the v1.0.0 entry inserts ABOVE [0.2.0] and BELOW [Unreleased]); the new section opens with a bold-formatted 'First stable release.' banner per D-81"
    - "CHANGELOG [1.0.0] section structure per PATTERNS item 13 (German wording, lektor-review-marker for CP-1): Hinzugefügt (NEW v1.0.0 surface — release pipeline, __VERSION__ substitution, bilingual docs, CONTRIBUTING, 4 ADRs, gitleaks, commit-lint, branch protection, repo metadata); Bekannte Grenzen (full enumeration of documented limitations carried from v0.2.0 — D-49 fee re-classification, ERR-04 token-revoked UX, 90-Tage-Erstabgleich-Klammer, non-EUR-skip); Sicherheit (META-03 disclaimer re-asserted + egress-allowlist reminder + GPG-signed tag chain + SHA256-attached release asset); the date YYYY-MM-DD field stays as `2026-MM-DD` placeholder — Yves replaces with the actual tag-publication date at CP-4 in a single `docs(changelog): finalize v1.0.0 release date` commit on main (or directly on the same commit that gets tagged)"
    - "CHANGELOG.md release-link footnotes preserved + extended per Keep-a-Changelog 1.1.0 convention — the existing `[Unreleased]: https://github.com/.../compare/v0.2.0...HEAD` + `[0.2.0]: https://github.com/.../releases/tag/v0.2.0` are UPDATED so `[Unreleased]` now compares against `v1.0.0...HEAD` and a NEW `[1.0.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0` footnote is appended"
    - "CHANGELOG.md passes the extended META-03 walker from 06-01 — the v1.0.0 section's German wording uses 'steuerrechtliche Bewertung' (not 'steuerlich'), 'GoBD-Bewertung' or 'GoBD-Konformität' (not 'GoBD-konform'), 'DATEV-Export' (not 'DATEV-fähig') per D-81 / D-71 disciplines; pre-commit dry-run grep MUST return empty"
    - ".planning/STATE.md updated: progress fields advance (`completed_phases: 6`, `completed_plans: 28`, `percent: 100`); a new top-level section `### Phase 6: Release & Polish — DONE (IMPLEMENTATION COMPLETE; READY-FOR-TAG)` is appended summarising 06-01 + 06-02 + 06-03 plan outcomes; the existing 'Last updated' header date refreshes to today; Phase-5 + Phase-4 + Phase-3 + Phase-2 + Phase-1 sections preserved byte-identically; the `status:` frontmatter field advances from `implementation-complete` to `v1.0.0-ready-for-tag`"
    - "STATE.md records the 5 Yves checkpoints (CP-1..CP-5) in a single table with current dispositions (pending / done / deferred) per CP, the script invocation or post-merge action for each, and the dependency chain (CP-4 v1.0.0 tag publication is gated on CP-1 lektor pass; CP-2/CP-3/CP-5 can happen independently)"
    - "STATE.md records the final Phase-6 reproducible-build SHA (from 06-01 with __VERSION__ substitution; CP-4 will produce yet another SHA when the actual v1.0.0 tag substitutes); the dev-build SHA and the v1.0.0-tagged SHA are BOTH documented as expected — the v1.0.0 SHA is the one stranger users will verify against the GitHub Release `.sha256` asset"
    - ".planning/phases/06-release-polish/06-HANDOFF.md (NEW) is the consolidated post-merge / pre-tag hand-off document for Yves: enumerates CP-1..CP-5 with exact one-liners + expected outputs + verification commands; provides a 'cut v1.0.0' runbook (CHANGELOG date fix → commit → tag-sign → push → release.yml fires → verify the GitHub Release page lists .lua + .sha256 + tag annotation as body); explicitly notes Phase-3/4/5 surface preservation is in place via spec/phase3_surface_preservation_spec.lua + the implicit reproducible-build invariant"
    - "Phase-2/3/4/5 surface UNTOUCHED — NO src/*.lua / spec/*.lua / tools/*.lua / tools/*.sh / .github/workflows/* / docs/adr/* / README.md / README.de.md / CONTRIBUTING.md modified (CHANGELOG is the only persisted artifact change; STATE.md + 06-HANDOFF.md are .planning/ documents that are not in the shipped artifact manifest); full busted suite GREEN end-to-end; reproducible-build SHA UNCHANGED from 06-01 (CHANGELOG.md is outside dist/paypal-pos.lua)"
    - "PHASE-6 SOURCE AUDIT — all 22 phase requirement IDs traced to a landed plan: BUILD-03 → 06-01 Task 2; BUILD-04/05/06 → 06-02 Task 1; CI-01..06 → existing ci.yml + 06-01 gitleaks/commit-lint/D-79; SEC-02 → existing ci.yml egress-allowlist (Phase-4) + 06-01 D-79 hardening; SEC-05 → 06-02 Task 2 (CP-2 fires post-merge); DOC-01..04 → 06-02 Task 3; DOC-05 → 06-02 Task 4 CONTRIBUTING; DOC-06 → 06-02 Task 4 (4 new ADRs joining 4 existing = 8 total); DOC-07 → pre-existing LICENSE (verified NO-OP); DOC-08/09 → 06-02 Task 2 (CP-3 fires post-merge); DOC-10 → 06-03 Task 1 — every ID has a delivering artifact"
    - "Commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution; Conventional Commits prefixes `docs(06-03):` (CHANGELOG + 06-HANDOFF), `docs(state):` (STATE.md transition); the CHANGELOG-date-fix commit at CP-4 is `docs(changelog): finalize v1.0.0 release date YYYY-MM-DD` and is Yves' responsibility post-CP-1"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[1.0.0] section in Keep-a-Changelog format with First-stable-release banner, full feature set, documented limitations, security guarantees (DOC-10 / D-81)"
      contains: "1.0.0"
    - path: ".planning/STATE.md"
      provides: "Phase-6 DONE marker; CP-1..CP-5 dispositions table; v1.0.0-ready-for-tag status; reproducible-build SHA history"
      contains: "v1.0.0-ready-for-tag"
    - path: ".planning/phases/06-release-polish/06-HANDOFF.md"
      provides: "Yves post-merge / pre-tag runbook: 5 CPs + cut-v1.0.0 sequence + verification commands + Phase-6 audit summary"
      contains: "CP-1"
      min_lines: 60
  key_links:
    - from: "CHANGELOG.md [1.0.0] section"
      to: "Tag annotation extracted by release.yml job 2 (06-02) via `git for-each-ref refs/tags/v1.0.0`"
      via: "Yves' `git tag -s v1.0.0 -m \"Release v1.0.0\\n\\n<CHANGELOG excerpt>\"` at CP-4 — the annotation is the release-body source; CHANGELOG excerpt is the fallback per RESEARCH §1"
      pattern: "1.0.0"
    - from: ".planning/phases/06-release-polish/06-HANDOFF.md CP-4 runbook"
      to: "tools/setup-branch-protection.sh + tools/setup-repo-metadata.sh + gh secret set MAINTAINER_GPG_PUBKEY + git tag -s v1.0.0"
      via: "5-step sequence enumerating which CP runs in what order and what each verifies"
      pattern: "git tag -s v1.0.0"
    - from: ".planning/STATE.md status field"
      to: "v1.0.0-ready-for-tag (advances from Phase-5 implementation-complete)"
      via: "Single-string status indicator consumed by gsd-tools and human readers alike"
      pattern: "v1.0.0-ready-for-tag"
---

<objective>
Land the Wave-3 release cut: (a) CHANGELOG.md gains a `[1.0.0]` section in Keep-a-Changelog format per DOC-10 / D-81 finalising the v0.2.0 → v1.0.0 transition; (b) STATE.md updates to `v1.0.0-ready-for-tag` with Phase-6 outcomes appended and CP-1..CP-5 dispositions tabulated; (c) 06-HANDOFF.md (NEW) is the consolidated post-merge / pre-tag runbook for Yves enumerating the 5 CPs + the cut-v1.0.0 sequence + verification commands; (d) final Phase-6 audit confirms every one of the 22 requirement IDs has a delivering artifact across 06-01/02/03 + pre-existing assets.

Purpose: Wave 3 is the closing ceremony that takes the working trust chain from 06-01 + 06-02 and packages it for Yves to fire. No new code surface; no new ADRs; no new specs; no new CI jobs. The Phase-6 PR opens on the merge of all three plans' commits; Yves then runs CP-1 (lektor) → CP-2/CP-3/CP-5 (admin scripts + screenshot) → CHANGELOG date fix → CP-4 (`git tag -s v1.0.0` + push) → release.yml publishes the v1.0.0 GitHub Release.

This wave is sequenced after 06-01 + 06-02 because: (a) the CHANGELOG entry needs the full inventory of what 06-01 + 06-02 shipped to enumerate accurately; (b) the HANDOFF runbook references files created in 06-02 (setup scripts) and the META-03 walker from 06-01; (c) STATE.md should reflect the actual landing of 06-01 + 06-02 before claiming `v1.0.0-ready-for-tag`.

This wave is autonomous (no `checkpoint:human-*` tasks). The 5 Yves checkpoints fire OUTSIDE this plan — they're surfaced in 06-HANDOFF.md but the human actions happen post-merge.

Output: 3 GPG-signed commits across 3 files (CHANGELOG + STATE + HANDOFF). Reproducible-build SHA UNCHANGED from 06-01 (no shipped-artifact bytes touched in this plan). Total Phase-6 commit count across 06-01 + 06-02 + 06-03 is approximately 11-13 GPG-signed commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-release-polish/06-CONTEXT.md
@.planning/phases/06-release-polish/06-RESEARCH.md
@.planning/phases/06-release-polish/06-PATTERNS.md
@.planning/phases/06-release-polish/06-01-PLAN.md
@.planning/phases/06-release-polish/06-02-PLAN.md
@CHANGELOG.md
@README.de.md
@CONTRIBUTING.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut CHANGELOG.md [1.0.0] section in Keep-a-Changelog format (DOC-10 / D-81)</name>
  <read_first>
    - CHANGELOG.md (current state — Phase-4 [0.2.0] entry + [Unreleased] banner + the release-link footnotes at the bottom; the new [1.0.0] section inserts ABOVE [0.2.0] and BELOW [Unreleased] per Keep-a-Changelog 1.1.0 reverse-chronological convention)
    - .planning/phases/06-release-polish/06-CONTEXT.md D-81 (full v1.0.0 entry intent: 'First stable release' notice + full feature set + documented limitations)
    - .planning/phases/06-release-polish/06-RESEARCH.md §11.1 (German wording starter for v0.2.0 — reuse the structural patterns: Hinzugefügt / Geändert / Voraussetzung / Bekannte Grenzen / Sicherheit) and §1.2 (release-body fallback extraction by awk pattern `/^## \[VERSION\]/{flag=1;next} /^## \[/{flag=0} flag`)
    - .planning/phases/06-release-polish/06-PATTERNS.md item 13 (CHANGELOG.md v1.0.0 entry verbatim structure including First-stable-release banner + the 7 specific Hinzugefügt bullets + Bekannte Grenzen carry-over from v0.2.0 + Sicherheit section + the [1.0.0] footnote)
    - .planning/phases/06-release-polish/06-01-PLAN.md + 06-02-PLAN.md `artifacts_this_phase_produces` sections — enumerate the EXACT new surface to claim in Hinzugefügt
    - spec/meta_no_tax_classification_spec.lua (06-01 extension — scans CHANGELOG.md; pre-commit dry-run grep on the new [1.0.0] section MUST return zero forbidden-phrase matches)
  </read_first>
  <action>
    Step 1 — Read the current CHANGELOG.md. The existing structure is:
    - Header (# Changelog + Keep-a-Changelog + SemVer intro)
    - `## [Unreleased]` block with the lektor-review pending HTML comment
    - `## [0.2.0] - 2026-MM-DD` block with full v0.2.0 details + Foundations sub-section
    - Footnote links at bottom: `[Unreleased]:` + `[0.2.0]:`

    Step 2 — Compose the NEW `[1.0.0]` section. Insert it BETWEEN `## [Unreleased]` and `## [0.2.0]`. Section body per PATTERNS item 13:

    ```markdown
    ## [1.0.0] - 2026-MM-DD

    <!-- lektor-review: pending — CP-1; CHANGELOG.md v1.0.0 wording is engineering-grade; Yves or loop-lektor finalises before tag publication. -->

    **First stable release.**

    ### Hinzugefügt

    - Reproduzierbare Release-Pipeline (`.github/workflows/release.yml`): wird
      durch einen GPG-signierten Tag ausgelöst, prüft die Signatur gegen den
      Maintainer-Fingerabdruck, baut das Artefakt deterministisch, hängt
      `paypal-pos.lua` und `paypal-pos.lua.sha256` an die GitHub-Release.
    - `__VERSION__`-Substitution in `tools/build.lua` aus dem Git-Tag; die
      `WebBanking{version}`-Angabe im Artefakt entspricht der veröffentlichten
      Version.
    - Zweisprachige Dokumentation: `README.de.md` (deutsch-primär, mit
      Screenshot-illustrierter Installationsanleitung, GoBD-Hinweis und
      Datenschutz/Sicherheits-Erläuterungen); `README.md` als englischer
      Pointer für internationale Besucher.
    - `CONTRIBUTING.md` (englisch) dokumentiert den Entwicklungs-Loop,
      Test-Konventionen, Amalgamator-Architektur, Release-Prozess und die
      GPG-signierten-Tag-Anforderung.
    - Vier neue MADR-Architekturentscheidungen (ADR-0002 LocalStorage-Token-
      Cache, ADR-0006 JWT-Bearer-Only-Authentifizierung, ADR-0007 keine
      TLS-Pinning, ADR-0008 String-Return-Fehlermuster) — sie ergänzen die
      bestehenden ADR-0001/0003/0004/0005 zur lückenlosen Dokumentation aller
      in Phase 2–5 getroffenen Entscheidungen.
    - Secret-Scanning per gitleaks (`gitleaks/gitleaks-action@v2`) auf jedem
      Push und Pull-Request.
    - Conventional-Commits-Lint (`.github/workflows/commit-lint.yml`) prüft
      jeden Commit eines PR gegen die Conventional-Commits-Grammatik.
    - Branch-Protection auf `main`: Pull-Request-Pflicht, GPG-signierte
      Commits erforderlich, alle CI-Checks grün, lineare History.
    - Repository-Metadaten: deutsche Beschreibung und sieben Themen
      (`moneymoney`, `moneymoney-extension`, `paypal-pos`, `zettle`, `lua`,
      `germany`, `accounting`).
    - META-03-Wächter (`spec/meta_no_tax_classification_spec.lua`) erweitert
      auf alle Markdown-Dokumentations-Dateien: README.md, README.de.md,
      CONTRIBUTING.md, CHANGELOG.md und alle ADRs werden bei jedem CI-Lauf
      auf die 13 verbotenen Phrasen geprüft.

    ### Bekannte Grenzen (unverändert seit v0.2.0)

    - Verzögerte Buchung von Auszahlungen (1–2 Aktualisierungszyklen bei
      wöchentlichem oder monatlichem Auszahlungsrhythmus) — siehe ADR-0004.
    - Tagesaggregat von Gebühren bei nachträglich nachgereichter Verknüpfung
      durch Zettle — siehe README.de.md.
    - 90-Tage-Klammer für den Erstabgleich; ältere Umsätze werden nicht
      sichtbar gemacht.
    - Umsätze in anderen Währungen als EUR werden übergangen.
    - Nach einer Token-Revocation muss der API-Key in MoneyMoney neu
      eingefügt werden (ERR-04 — dokumentiert in ADR-0005).

    ### Sicherheit

    - Keine Telemetrie, keine Dritt-Anbieter: das Artefakt kontaktiert
      ausschließlich `oauth.zettle.com`, `purchase.izettle.com` und
      `finance.izettle.com`; ein CI-Gate erzwingt diese Egress-Allowlist auf
      jedem Release.
    - API-Keys werden ausschließlich über MoneyMoneys eingebaute
      Anmelde-Daten-Verwaltung gespeichert — nie in LocalStorage, nie in
      Logs, nie in Fehlermeldungen.
    - Alle Tags sind GPG-signiert (Maintainer-Fingerabdruck
      `FDE07046A6178E89ADB57FD3DE300C53D8E18642`); jeder Release-Lauf prüft
      die Signatur, baut das Artefakt reproduzierbar nach und veröffentlicht
      eine SHA256-Prüfsumme als Asset.
    ```

    Note the calibrated wording: "GoBD-Hinweis" (not "GoBD-konform"); "steuerrechtliche" never used in this section (kept short); "Token-Revocation" not "steuerlich"; "Erstabgleich" not "Erstausgleich". Pre-commit grep on the new section verifies zero forbidden-phrase matches.

    Step 3 — Update the footnote links at the bottom of CHANGELOG.md per Keep-a-Changelog 1.1.0:
    - Change `[Unreleased]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/compare/v0.2.0...HEAD` to `[Unreleased]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/compare/v1.0.0...HEAD`.
    - INSERT `[1.0.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0` between the [Unreleased] line and the existing [0.2.0] line.
    - PRESERVE `[0.2.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v0.2.0` byte-identically.

    Step 4 — Pre-commit META-03 audit on the entire CHANGELOG.md (not just the new section — full-file check since the walker scans the whole file):
    `grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' CHANGELOG.md`
    Expected: ZERO matches. The v0.2.0 entry from Phase 4 was Yves-lektor'd to be clean; the new v1.0.0 wording is engineered to use safe phrases ("steuerrechtliche Bewertung" / "GoBD-Hinweis" / "DATEV-Export" never appear in v1.0.0 entry; the v1.0.0 section uses "Token-Revocation" / "Erstabgleich" / "Egress-Allowlist" — all clean).

    Step 5 — Run the extended META-03 walker:
    `./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua` — GREEN. CHANGELOG.md is in DOC_TARGETS from 06-01; the spec now scans the updated content and passes.

    Step 6 — Verify the awk-extraction pattern from release.yml job 2 (RESEARCH §1 fallback) works against the new CHANGELOG.md:
    ```bash
    awk '/^## \[1\.0\.0\]/{flag=1;next} /^## \[/{flag=0} flag' CHANGELOG.md | head -30
    ```
    Expected: the awk output shows the [1.0.0] section body (without the section heading line). If release.yml's fallback is invoked at CP-4 (Yves' tag annotation is empty), this awk produces the release notes — sanity-check the output is non-empty + starts with the lektor-review HTML comment + the First-stable-release banner.


    Step 7 — Reproducible-build check: `lua tools/build.lua --verify` — still reproducible (CHANGELOG.md is outside dist/paypal-pos.lua manifest; SHA unchanged).

    Step 8 — Commit:
    `docs(06-03): cut CHANGELOG [1.0.0] section in Keep-a-Changelog format (DOC-10 / D-81)`
    GPG-signed; no AI attribution.

    Note: the date placeholder `2026-MM-DD` stays — Yves replaces it post-CP-1 in a separate `docs(changelog): finalize v1.0.0 release date YYYY-MM-DD` commit. This is documented in 06-HANDOFF.md (Task 3).
  </action>
  <verify>
    <automated>grep -q '## \[1.0.0\]' CHANGELOG.md && grep -q 'First stable release' CHANGELOG.md && grep -q 'Reproduzierbare Release-Pipeline' CHANGELOG.md && grep -q '__VERSION__' CHANGELOG.md && grep -q 'README.de.md' CHANGELOG.md && grep -q 'CONTRIBUTING.md' CHANGELOG.md && grep -q 'ADR-0002\|ADR-0006\|ADR-0007\|ADR-0008' CHANGELOG.md && grep -q 'gitleaks' CHANGELOG.md && grep -q 'FDE07046A6178E89ADB57FD3DE300C53D8E18642' CHANGELOG.md && grep -qF '[1.0.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0' CHANGELOG.md && grep -qF '[Unreleased]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/compare/v1.0.0...HEAD' CHANGELOG.md && grep -qF '[0.2.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v0.2.0' CHANGELOG.md && ! grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' CHANGELOG.md && ./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua 2>&1 | grep -qE '0 failures.*0 errors' && lua tools/build.lua --verify 2>&1 | grep -qE 'OK: reproducible|reproducible'</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [1.0.0]` section with `First stable release` banner
    - The [1.0.0] section enumerates ALL new v1.0.0 surface (release.yml, __VERSION__, README split, CONTRIBUTING, 4 ADRs, gitleaks, commit-lint, branch protection, repo metadata, extended META-03 walker) — at least 7 Hinzugefügt bullets
    - Bekannte Grenzen section carries forward v0.2.0 documented limitations (D-49 fee re-classification, ERR-04 token-revoked, 90-Tage-Erstabgleich, non-EUR-skip)
    - Sicherheit section re-asserts META-03 + egress allowlist + GPG-signed releases with maintainer fingerprint
    - Footnote links updated: [Unreleased] compares against v1.0.0...HEAD; [1.0.0] points to releases/tag/v1.0.0; [0.2.0] preserved byte-identically
    - Pre-commit grep for the 13 META-03 phrases on the full CHANGELOG.md returns EMPTY
    - `busted spec/meta_no_tax_classification_spec.lua` GREEN — extended walker passes on updated CHANGELOG.md
    - `lua tools/build.lua --verify` reproducible (no src/ touched)
    - The awk-extraction pattern works: `awk '/^## \[1\.0\.0\]/{flag=1;next} /^## \[/{flag=0} flag' CHANGELOG.md` produces a non-empty multi-line output (release.yml fallback sanity)
    - Commit GPG-signed; no AI attribution; prefix `docs(06-03):`
    - The date `2026-MM-DD` placeholder stays (Yves finalizes at CP-4 prerequisite step)
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a v1.0.0 entry in Keep-a-Changelog format with First-stable-release banner, full Hinzugefügt feature inventory, Bekannte Grenzen carry-over, Sicherheit re-assertion, and updated footnote links; passes the extended META-03 walker; reproducible-build SHA unchanged. Yves finalizes the YYYY-MM-DD date at CP-4 prerequisite step. Commit: `docs(06-03): cut CHANGELOG [1.0.0] section (DOC-10)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/STATE.md to v1.0.0-ready-for-tag with Phase-6 outcomes + CP-1..CP-5 table</name>
  <read_first>
    - .planning/STATE.md (current — observe the frontmatter shape with `status: implementation-complete`, `completed_phases: 2`, `completed_plans: 25`, `percent: 29`; the existing Phase-5 + Phase-4 narrative sections; the "Performance Metrics" + "Accumulated Context" + "Session Continuity" sections at the bottom that get preserved byte-identically)
    - .planning/phases/06-release-polish/06-CONTEXT.md (CP-1..CP-5 enumeration at the bottom — the source of truth for the dispositions table)
    - .planning/phases/06-release-polish/06-01-PLAN.md + 06-02-PLAN.md (the SUMMARY.md sections describing what they produced — use as input to the Phase-6 narrative)
    - .planning/STATE.md Phase-5 narrative section (~50 lines) — observe the structure: status line + bullet list of plan commits + suite count + luacheck status + reproducible-build SHA; mirror this style for the new Phase-6 section
  </read_first>
  <action>
    Step 1 — Read the current STATE.md frontmatter (lines 1-13 per the existing file structure). Plan the updates:
    - `status: implementation-complete` → `status: v1.0.0-ready-for-tag`
    - `completed_phases: 2` → `completed_phases: 6` (Phases 1+2+3+4+5+6 all done; Phase 6.1 OpenSSF Scorecard remains separate per ROADMAP)
    - `completed_plans: 25` → `completed_plans: 28` (adds 06-01 + 06-02 + 06-03)
    - `percent: 29` → recompute: `(completed_plans / total_plans) * 100` where total_plans is currently 28 — but wait, the frontmatter says `total_plans: 28` and after adding 3 it becomes 28 + 3 = 31 (if Phase 6 plans are NEW additions not counted in the 28) OR 28 was already projecting Phase 6's 3 plans (more likely given the original state had `completed_phases: 2 / total_phases: 7` — Phase 6.1 makes 7). Re-read the frontmatter math to confirm before editing.
    - `last_updated: "2026-06-22T08:30:00.000Z"` → `last_updated: "<today's ISO-8601 timestamp>"` (use `date -u +"%Y-%m-%dT%H:%M:%S.000Z"`).
    - Add a new top-level frontmatter field if appropriate: `phase_6_repro_sha: "<dev-build SHA from 06-01>"` for downstream tooling consumption. OR keep frontmatter minimal and put the SHA in the narrative body. Recommend: keep frontmatter minimal (current convention); SHA goes in the narrative section.

    Step 2 — Insert a NEW Phase-6 narrative section. The existing Phase-5 section starts with `### Previous Phase: 04 — DONE (merged)` after the current-position block — that pattern moves: the NEW current-position section becomes Phase 6, and Phase 5 becomes the new "Previous Phase". Insert pattern:

    Replace the existing `## Current Position` header line `Phase: 05 (resilience-error-handling) — **IMPLEMENTATION + 05-06 FIX-BATCH COMPLETE; READY-FOR-RE-VERIFICATION 2026-06-22**` with:

    ```
    Phase: 06 (release-polish) — **IMPLEMENTATION COMPLETE; v1.0.0-READY-FOR-TAG 2026-06-22**

    **Status:** All 3 plans landed on `phase-6/release-polish` branch (06-01 + 06-02 + 06-03 = ~11 GPG-signed commits). Trust chain artifacts in place: release.yml (GPG-verified-tag triggered, softprops v2 publishes .lua + .sha256 with tag-annotation body), tools/setup-branch-protection.sh + tools/setup-repo-metadata.sh (SEC-05 / DOC-08/09 helpers with graceful degradation), README split (README.de.md German-primary with Inoffizielle-Extensions screenshot guide + GoBD-Hinweis per D-71 + all preserved sections; README.md English pointer ~50 lines), CONTRIBUTING.md (dev loop + TDD + amalgamator + release process + first-time-setup), 4 backfilled MADR ADRs (0002 LocalStorage / 0006 JWT-bearer / 0007 no-TLS-pinning / 0008 string-return-errors), gitleaks secret-scan + commit-lint workflows, __VERSION__ substitution from $GITHUB_REF_NAME with dev-build banner, D-79 raw-print() gate hardening, META-03 walker extended to README/CHANGELOG/CONTRIBUTING/ADRs. CHANGELOG.md cut to [1.0.0] in Keep-a-Changelog format with full Hinzugefügt + Bekannte Grenzen carry-over + Sicherheit sections. Phase-2/3/4/5 surface byte-identically preserved.

    **Plan-06 commits** (on `phase-6/release-polish`, all GPG-signed):
    - Plan 06-01 (Wave 1 — CI scaffolding): RED spec for __VERSION__ (test) → GREEN build+header+luacheckrc (feat) → META-03 walker extension (test) → gitleaks + commit-lint + D-79 (ci)
    - Plan 06-02 (Wave 2 — trust chain): release.yml (ci) → 2 admin scripts (chore) → README split + 2 placeholder PNGs + img/README (docs) → CONTRIBUTING (docs) → 4 ADRs (docs)
    - Plan 06-03 (Wave 3 — release cut): CHANGELOG [1.0.0] (docs) → STATE.md update (docs) → 06-HANDOFF.md runbook (docs)

    Full suite **<N>/0 successes / 0 failures / 0 errors / 0 pending** (was 373; +N new specs from 06-01 build_version_substitution). luacheck clean across all source + spec + tools files. Reproducible build SHA **`<NEW SHA>`** for dev builds (was `5dbcb8e…`; changed because of __VERSION__ + DEV-BUILD banner — by design); per-tag SHA will be computed by release.yml job 2 at CP-4.

    **Recommended next steps:** Yves runs the 5 CPs in sequence (CP-1 lektor pass → CP-2/CP-3/CP-5 admin scripts + screenshot → CHANGELOG date fix → CP-4 `git tag -s v1.0.0`); see `.planning/phases/06-release-polish/06-HANDOFF.md` for the runbook. After v1.0.0 ships, Phase 6.1 (OpenSSF Scorecard hardening) becomes unblocked.
    ```

    Below this new section, insert a `### Yves Checkpoints (CP-1..CP-5)` table:

    ```
    ### Yves Checkpoints (CP-1..CP-5)

    | ID | Item | Trigger | Owner | Status |
    |----|------|---------|-------|--------|
    | CP-1 | loop-lektor pass on README.de.md + CHANGELOG [1.0.0] + 4 new ADRs (German wording, GoBD-Hinweis per D-71) | manual or `loop-lektor` subagent | Yves | pending (pre-tag) |
    | CP-2 | Branch protection: `bash tools/setup-branch-protection.sh` (PAT with `Administration: write`) | post-merge of Phase-6 PR | Yves | pending (post-merge) |
    | CP-3 | Repo metadata: `bash tools/setup-repo-metadata.sh` (PAT with repo-metadata write) | post-merge | Yves | pending (post-merge) |
    | CP-4 | v1.0.0 tag publication: CHANGELOG date fix → `git tag -s v1.0.0 -m "Release v1.0.0"` → `git push origin v1.0.0` (gated on CP-1 lektor done + MAINTAINER_GPG_PUBKEY secret set in CI) | after CP-1 + CP-2 (CP-2 not strictly required but recommended) | Yves | pending (post-CP-1) |
    | CP-5 | Capture real screenshots for docs/img/inoffizielle-extensions-erlauben.png + docs/img/help-menu-extensions-folder.png; commit with `docs(img): capture <filename>` | any time post-merge | Yves | pending (post-merge) |

    Dependency chain: CP-1 must complete before CP-4 (lektor reviews the German wording of the v1.0.0 release-notes-source); CP-2 + CP-3 + CP-5 are independent. The MAINTAINER_GPG_PUBKEY one-liner (`gpg --armor --export FDE07046A6178E89ADB57FD3DE300C53D8E18642 | gh secret set MAINTAINER_GPG_PUBKEY`) is a CP-4 prerequisite documented in CONTRIBUTING.md.
    ```

    Then move the existing Phase-5 section under a new heading `### Previous Phase: 05 — DONE (merged)`, preserving its body byte-identically. The pre-existing `### Previous Phase: 04 — DONE (merged)` becomes the section after Phase 5.

    Step 3 — Preserve every other section byte-identically: Performance Metrics table (still populated as-is); Accumulated Context (Decisions / Active Todos / Roadmap Evolution / Blockers / Phase-1 Probe Status); Session Continuity.

    Step 4 — Commit:
    `docs(state): Phase 6 implementation complete — v1.0.0-ready-for-tag (06-01/02/03 landed)`
    GPG-signed; no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && test -f .planning/STATE.md && grep -q 'v1.0.0-ready-for-tag\|v1.0.0-READY-FOR-TAG' .planning/STATE.md && grep -q 'completed_phases: 6\|completed_phases: "6"' .planning/STATE.md && grep -q 'Phase: 06 (release-polish)' .planning/STATE.md && grep -q 'CP-1' .planning/STATE.md && grep -q 'CP-5' .planning/STATE.md && grep -q 'loop-lektor' .planning/STATE.md && grep -q 'setup-branch-protection.sh' .planning/STATE.md && grep -q 'setup-repo-metadata.sh' .planning/STATE.md && grep -q 'git tag -s v1.0.0' .planning/STATE.md && grep -q 'Previous Phase: 05' .planning/STATE.md && grep -q 'Previous Phase: 04' .planning/STATE.md && grep -q 'Accumulated Context' .planning/STATE.md && grep -q 'Session Continuity' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter: `status: v1.0.0-ready-for-tag`, `completed_phases: 6`, `completed_plans: 28` (or correct number reflecting the 3 new Phase-6 plans on top of prior count), `last_updated` is today's ISO-8601 timestamp
    - New `## Current Position` block names Phase 6 with all 3 plan outcomes summarised
    - Yves Checkpoints table enumerates all 5 CPs with ID + Item + Trigger + Owner + Status columns
    - Previous Phase: 05 section preserved byte-identically (only the section heading changes from "Current" to "Previous")
    - Previous Phase: 04 section preserved byte-identically below Phase 5
    - Accumulated Context + Session Continuity sections preserved byte-identically
    - All references to setup-branch-protection.sh / setup-repo-metadata.sh / loop-lektor / git tag -s v1.0.0 present
    - Commit GPG-signed; no AI attribution; prefix `docs(state):`
  </acceptance_criteria>
  <done>
    STATE.md updated: status v1.0.0-ready-for-tag, Phase-6 narrative section + CP-1..CP-5 table inserted, Phase-5 demoted to Previous Phase; all prior sections preserved byte-identically. Commit: `docs(state): Phase 6 implementation complete — v1.0.0-ready-for-tag`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author .planning/phases/06-release-polish/06-HANDOFF.md (post-merge / pre-tag runbook for Yves)</name>
  <read_first>
    - .planning/phases/06-release-polish/06-CONTEXT.md (CP-1..CP-5 enumeration + script invocation patterns)
    - .planning/phases/06-release-polish/06-RESEARCH.md §1+§2+§3+§6+§7 (release.yml + GPG verify + branch protection + repo metadata invocation specifics)
    - CONTRIBUTING.md (from 06-02 Task 4 — the release-process section is the source of the runbook commands; cross-reference them in 06-HANDOFF.md so a future Yves reads ONE document, not two)
    - CHANGELOG.md after Task 1 (the 2026-MM-DD placeholder is the prereq for CP-4)
  </read_first>
  <action>
    Create `.planning/phases/06-release-polish/06-HANDOFF.md`. Structure:

    Title: `# Phase 6 Hand-off — v1.0.0 release runbook`
    Subtitle: `**Audience:** Yves (post-merge of Phase-6 PR). **Purpose:** the exact sequence of 5 checkpoints + verification commands that turn the merged Phase-6 PR into the v1.0.0 GitHub Release.`

    Section 1: `## State at hand-off`
    - Branch `phase-6/release-polish` merged to `main` via `gh pr merge --squash` (per `feedback_gpg_signed_pr_merge` memory — never `--rebase`).
    - Reproducible-build SHA for dev: `<from 06-01 SUMMARY>`. The v1.0.0-tagged SHA will be computed by release.yml job 2 at CP-4.
    - All 22 Phase-6 requirement IDs delivered (cross-reference STATE.md `Yves Checkpoints` table for what remains).

    Section 2: `## CP-1 — Loop-lektor pass on German wording`
    - Targets: README.de.md (GoBD-Hinweis section + Inoffizielle-Extensions wording + all engineering-grade text); CHANGELOG.md [1.0.0] section; ADRs 0002/0006/0007/0008 (English mostly; loop-lektor may polish narrative wording).
    - Invocation Option A (Yves directly): open each file, refine sentence rhythm + register; commit `docs(lektor): refine v1.0.0 German wording per CP-1`.
    - Invocation Option B (loop-lektor subagent): spawn the agent with a brief pointing at the 5 files; agent returns suggested wording changes inline; Yves accepts/rejects; commit per Option A.
    - Hard-fail discipline: lektor MUST NOT introduce any of the 13 D-55 forbidden phrases (the extended META-03 walker enforces this on every push); if lektor proposes a forbidden phrase, reject and route through the same audit-before-extend protocol Phase 4 used.

    Section 3: `## CP-2 — Branch protection (one-time admin)`
    - Prerequisite: `gh auth login` with a Fine-Grained PAT that has `Administration: write` scope on `yves-vogl/moneymoney-paypal-pos-extension`.
    - Command: `bash tools/setup-branch-protection.sh`.
    - Expected output (success path): `OK: branch protection applied (PR + checks + signatures + linear history).`.
    - Expected output (graceful degradation): the script prints the manual UI fallback with the github.com settings/branches URL + the 5-step checklist + the 3 required CI check names; exits 0. Yves follows the UI steps once.
    - Verification: `gh api repos/yves-vogl/moneymoney-paypal-pos-extension/branches/main/protection | jq .` shows the 3 required_status_checks contexts + required_signatures.enabled = true + required_linear_history.enabled = true.

    Section 4: `## CP-3 — Repo metadata (one-time admin)`
    - Prerequisite: `gh auth login` PAT with repo-metadata write (Yves' default PAT typically suffices).
    - Command: `bash tools/setup-repo-metadata.sh`.
    - Expected output: `OK: description and topics set.`.
    - Verification: `gh repo view yves-vogl/moneymoney-paypal-pos-extension --json description,topics` shows the D-82 German description + the exact 7 topics.

    Section 5: `## CP-5 — Real screenshots (independent of CP-4 — can happen any time)`
    - Targets: `docs/img/inoffizielle-extensions-erlauben.png` (current placeholder 1×1 PNG) + `docs/img/help-menu-extensions-folder.png`.
    - Capture in current-stable MoneyMoney (target version 2.4.x per ADR-0003): open Einstellungen → Erweiterungen + Hilfe → Erweiterungen im Finder zeigen; screenshot via `Cmd+Shift+4` + space + click window.
    - Save at the exact same paths so README.de.md image references stay valid.
    - Commit: `docs(img): capture inoffizielle-extensions-erlauben.png + help-menu-extensions-folder.png`.
    - The `<!-- screenshot: pending — CP-5 -->` HTML markers in README.de.md become a stale comment; optionally remove them in the same commit.

    Section 6: `## CP-4 prerequisites — MAINTAINER_GPG_PUBKEY + CHANGELOG date`
    - **MAINTAINER_GPG_PUBKEY** (one-time per repo): `gpg --armor --export FDE07046A6178E89ADB57FD3DE300C53D8E18642 | gh secret set MAINTAINER_GPG_PUBKEY --repo yves-vogl/moneymoney-paypal-pos-extension`. Verify: `gh secret list --repo yves-vogl/moneymoney-paypal-pos-extension` shows MAINTAINER_GPG_PUBKEY. WITHOUT this, release.yml job 1 fails at the gpg --import step with the FAIL message instructing Yves to run this one-liner.
    - **CHANGELOG date fix**: replace `## [1.0.0] - 2026-MM-DD` with `## [1.0.0] - <actual date>`. Single commit on main: `docs(changelog): finalize v1.0.0 release date <YYYY-MM-DD>`. GPG-signed. This commit is the one that gets tagged at CP-4 step 3.

    Section 7: `## CP-4 — Cut v1.0.0 tag (the release moment)`
    Sequenced steps:
    1. Verify CP-1 lektor done; CP-6 prereqs done; main is at the CHANGELOG-date-finalized commit.
    2. (Optional dry-run) Push a `v1.0.0-rc.1` tag first to exercise release.yml end-to-end without committing to the v1.0.0 first impression:
       ```
       git tag -s v1.0.0-rc.1 -m "Release v1.0.0-rc.1 (dry-run for release.yml verification)"
       git push origin v1.0.0-rc.1
       ```
       Watch `gh run watch` until release.yml's 3 jobs complete. Verify the GitHub Releases page shows a prerelease tagged v1.0.0-rc.1 with paypal-pos.lua + paypal-pos.lua.sha256 attached.
    3. Cut the stable v1.0.0 tag:
       ```
       git tag -s v1.0.0 -m "Release v1.0.0

       <paste CHANGELOG [1.0.0] section body here, OR rely on release.yml job 2 CHANGELOG-fallback awk extraction>"
       git push origin v1.0.0
       ```
    4. Watch release.yml: `gh run watch`. Three jobs: verify-signed-tag → build-test-coverage-repro → publish. Each should be GREEN.
    5. Verify the GitHub Releases page (https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0) shows:
       - Tag annotation as release body.
       - 2 attached assets: paypal-pos.lua + paypal-pos.lua.sha256.
       - Marked as latest stable (not prerelease).
    6. Verify the artifact: `curl -L https://github.com/.../releases/download/v1.0.0/paypal-pos.lua | shasum -a 256` matches the `.sha256` file content.
    7. Verify `WebBanking{version = 1.00}` in the downloaded artifact: `curl -L .../paypal-pos.lua | grep 'version = 1.00,'`.

    Section 8: `## Verification matrix`
    Provide a table mapping each Phase-6 requirement ID to the verification command (artifact-grep / gh-api-call / spec-run / manual-check):

    ```
    | Req ID | Verification | Status (post-CP-4) |
    |--------|--------------|--------------------|
    | BUILD-03 | `grep 'version = 1.00,' <downloaded artifact>` | pending CP-4 |
    | BUILD-04 | release.yml job 1 VALIDSIG match in run log | pending CP-4 |
    | BUILD-05 | `gh release view v1.0.0 --json assets` shows both .lua + .lua.sha256 | pending CP-4 |
    | BUILD-06 | release body equals tag annotation or CHANGELOG [1.0.0] section | pending CP-4 |
    | CI-01..06 | ci.yml + commit-lint.yml + gitleaks on every push (verified by existing PR runs) | DONE |
    | SEC-02 | egress-allowlist + D-79 grep in ci.yml | DONE |
    | SEC-05 | `gh api .../branches/main/protection` shows signatures + checks + linear history | pending CP-2 |
    | DOC-01..04 | README.de.md exists + structure verified | DONE |
    | DOC-05 | CONTRIBUTING.md exists | DONE |
    | DOC-06 | 8 ADRs under docs/adr/ (0001..0008) | DONE |
    | DOC-07 | `head -3 LICENSE` shows MIT + Yves Vogl | DONE (pre-existing) |
    | DOC-08..09 | `gh repo view --json description,topics` | pending CP-3 |
    | DOC-10 | CHANGELOG.md has [1.0.0] section in Keep-a-Changelog format | DONE |
    ```

    Section 9: `## If anything fails`
    - release.yml job 1 fails at gpg --import: re-run the MAINTAINER_GPG_PUBKEY one-liner.
    - release.yml job 1 fails at VALIDSIG grep: the tag was signed with a different key — re-tag with `-s` and the correct key (`git tag -d v1.0.0 && git push --delete origin v1.0.0` then re-tag).
    - release.yml job 2 fails at BUILD-03 sanity: the tag name and the substituted version disagree — verify the regex in tools/build.lua matches the tag (e.g., a `v1.10.0` tag yields `1.10`; a `v1.0.0-rc.1` tag yields `1.00`).
    - release.yml job 3 fails at softprops with "Resource not accessible": permissions are mis-scoped — verify job 3 has `permissions: contents: write` (workflow level is `contents: read`).
    - Branch protection blocks the first PR after enabling: see Pitfall 4 in 06-RESEARCH.md — the CHECKS array in the script must match the workflows' job `name:` fields byte-identically; re-run the script after fixing.
    - gitleaks flags an existing fixture: add the fingerprint to `.gitleaksignore` (line-by-line, never blanket); re-run.

    Section 10: `## Phase 6.1 unblocked`
    After v1.0.0 ships and stabilises for ~2 weeks, ROADMAP Phase 6.1 (OpenSSF Scorecard hardening) becomes the next planning candidate. Reference: `.planning/research/openssf-scorecard-sprint-proposal.md`.

    Commit:
    `docs(06-03): add 06-HANDOFF.md — post-merge / pre-tag runbook for Yves`
    GPG-signed; no AI attribution.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && test -f .planning/phases/06-release-polish/06-HANDOFF.md && wc -l .planning/phases/06-release-polish/06-HANDOFF.md | awk '{ if ($1 >= 60) exit 0; else exit 1 }' && grep -q 'CP-1' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'CP-2' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'CP-3' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'CP-4' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'CP-5' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'MAINTAINER_GPG_PUBKEY' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'tools/setup-branch-protection.sh' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'tools/setup-repo-metadata.sh' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'git tag -s v1.0.0' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'v1.0.0-rc.1' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'Phase 6.1' .planning/phases/06-release-polish/06-HANDOFF.md && grep -q 'FDE07046A6178E89ADB57FD3DE300C53D8E18642' .planning/phases/06-release-polish/06-HANDOFF.md && ! grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' .planning/phases/06-release-polish/06-HANDOFF.md && lua tools/build.lua --verify 2>&1 | grep -qE 'OK: reproducible|reproducible' && ./.luarocks/bin/busted spec/ 2>&1 | grep -qE '0 failures.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - 06-HANDOFF.md exists; >= 60 lines
    - All 5 CPs explicitly enumerated with invocation commands + expected outputs + verification commands
    - MAINTAINER_GPG_PUBKEY one-liner present (CP-4 prerequisite)
    - tools/setup-branch-protection.sh + tools/setup-repo-metadata.sh script names referenced
    - `git tag -s v1.0.0` runbook step present + the dry-run via `v1.0.0-rc.1` option
    - Verification matrix maps each of the 22 requirement IDs to a verification command + status (DONE / pending CP-X)
    - "If anything fails" troubleshooting section covers the 5 most likely failure modes (Pitfalls 1/2/4/7 from RESEARCH + gitleaks fixture)
    - Phase 6.1 mention for next-steps clarity
    - Maintainer fingerprint `FDE07046A6178E89ADB57FD3DE300C53D8E18642` referenced where appropriate
    - Pre-commit META-03 grep returns EMPTY (no forbidden phrase in the runbook)
    - .planning/ artifact only (no shipped-artifact bytes touched); reproducible build SHA unchanged
    - Full `busted spec/` GREEN end-to-end
    - Commit GPG-signed; no AI attribution; prefix `docs(06-03):`
  </acceptance_criteria>
  <done>
    06-HANDOFF.md exists as the consolidated post-merge / pre-tag runbook with all 5 CPs + verification matrix + troubleshooting; Phase-6 PR can now open and Yves has a single document driving the v1.0.0 ship sequence. Commit: `docs(06-03): add 06-HANDOFF.md runbook`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md German wording → META-03 walker (06-01 extension) | Every push runs the walker; pre-commit dry-run grep is the maintainer-side gate |
| CHANGELOG.md → release.yml job 2 fallback awk extraction | When tag annotation is empty, release notes come from the CHANGELOG awk-extracted section; CHANGELOG content becomes a published artifact |
| 06-HANDOFF.md → Yves' execution at CP-1..CP-5 | Documentation only — no executable code in this file; commands are documented for manual invocation |
| STATE.md status field → gsd-tools consumers | Single string consumed by automation; the value `v1.0.0-ready-for-tag` is a contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | CHANGELOG.md [1.0.0] German wording drifts into a forbidden phrase | mitigate | Pre-commit META-03 dry-run grep + 06-01 extended walker as standing CI gate; the v1.0.0 wording is engineered to use safe phrases ("Token-Revocation" / "Erstabgleich" / "Egress-Allowlist" never trigger the 13-phrase list) |
| T-06-03-02 | Tampering | 06-HANDOFF.md runbook command becomes stale after a future workflow rename | accept | The runbook lives in .planning/ and is not under CI gate; future Phase-6.1+ work that renames jobs in workflows will need to refresh 06-HANDOFF.md as well — documented in CONTRIBUTING.md Pitfall 4 cross-reference |
| T-06-03-03 | Information Disclosure | Maintainer fingerprint exposure in 06-HANDOFF.md + CHANGELOG.md + STATE.md | accept | Public key fingerprint is meant to be public (it's the user-facing trust root for verifying signed releases); already in CLAUDE.md memory + ADRs |
| T-06-03-04 | Spoofing | CP-4 v1.0.0 tag spoofed by a non-maintainer push | mitigate | Branch protection (CP-2 enables `required_signatures`) + release.yml job 1 VALIDSIG fingerprint grep (06-02) — both gates enforce maintainer-only release authority |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. This plan modifies only markdown + state — no new code surface |
</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Task 1: CHANGELOG [1.0.0] cut + META-03-clean + extended walker passes
grep -q '## \[1.0.0\]' CHANGELOG.md
grep -q 'First stable release' CHANGELOG.md
grep -qF '[1.0.0]: https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0' CHANGELOG.md
! grep -nE 'USt-frei|USt frei|steuerfrei|steuerlich|GoBD-konform|GoBD konform|DATEV-fähig|DATEV fähig|VAT-exempt|VAT exempt|tax-free|tax exempt|non-taxable' CHANGELOG.md
./.luarocks/bin/busted spec/meta_no_tax_classification_spec.lua

# Task 2: STATE.md transition
grep -q 'v1.0.0-ready-for-tag' .planning/STATE.md
grep -q 'Phase: 06 (release-polish)' .planning/STATE.md
grep -q 'Yves Checkpoints (CP-1..CP-5)' .planning/STATE.md
grep -q 'Previous Phase: 05' .planning/STATE.md

# Task 3: 06-HANDOFF.md runbook
test -f .planning/phases/06-release-polish/06-HANDOFF.md
grep -q 'CP-4' .planning/phases/06-release-polish/06-HANDOFF.md
grep -q 'MAINTAINER_GPG_PUBKEY' .planning/phases/06-release-polish/06-HANDOFF.md
grep -q 'git tag -s v1.0.0' .planning/phases/06-release-polish/06-HANDOFF.md
grep -q 'v1.0.0-rc.1' .planning/phases/06-release-polish/06-HANDOFF.md

# Final phase-wide invariants
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(failure|0 failures)'
lua tools/build.lua --verify
luacheck .
```
</verification>

<success_criteria>
- CHANGELOG.md has `## [1.0.0]` section with First-stable-release banner, full Hinzugefügt inventory (>= 7 bullets enumerating release.yml + __VERSION__ + README split + CONTRIBUTING + 4 ADRs + gitleaks + commit-lint + branch protection + repo metadata + META-03 extension), Bekannte Grenzen carry-over (D-49 / ERR-04 / 90-Tage / non-EUR), Sicherheit re-assertion (egress allowlist + API-key handling + GPG-signed tag chain)
- CHANGELOG.md footnote links updated: [Unreleased]→v1.0.0...HEAD, [1.0.0]→releases/tag/v1.0.0, [0.2.0] preserved
- Date stays as `2026-MM-DD` placeholder — finalized at CP-4 prerequisite step (Yves' single-line commit `docs(changelog): finalize v1.0.0 release date <YYYY-MM-DD>`)
- Extended META-03 walker (06-01) GREEN over the updated CHANGELOG; no forbidden-phrase regression
- STATE.md transitions: status `implementation-complete` → `v1.0.0-ready-for-tag`; completed_phases advances; Phase-6 narrative section + CP-1..CP-5 table appended; Phase-5 demoted to "Previous Phase"
- 06-HANDOFF.md exists (>= 60 lines) covering 5 CPs + cut-v1.0.0 sequence (incl. v1.0.0-rc.1 dry-run option) + verification matrix per requirement ID + troubleshooting + Phase 6.1 forward pointer
- Reproducible-build SHA UNCHANGED from 06-01 (no shipped-artifact bytes touched in 06-03)
- Full `busted spec/` GREEN end-to-end
- 3 GPG-signed commits landed under `(06-03)` and `docs(state):` prefixes
- Phase-6 PHASE GATE complete: all 22 requirement IDs accounted for; CP-1..CP-5 hand-off documented; v1.0.0 tag publication unblocked (Yves' choice when to fire)
</success_criteria>

<artifacts_this_phase_produces>
New files:
- `.planning/phases/06-release-polish/06-HANDOFF.md` — post-merge / pre-tag runbook for Yves

Modified files:
- `CHANGELOG.md` — `[1.0.0]` section inserted in Keep-a-Changelog format (DOC-10 / D-81); footnote links updated; date placeholder pending Yves CP-4 prereq
- `.planning/STATE.md` — status → v1.0.0-ready-for-tag; Phase-6 narrative + CP-1..CP-5 table appended; Phase-5 demoted to Previous

Files NOT modified (Phase-2/3/4/5/6-01/6-02 surface preservation):
- Any src/*.lua / spec/*.lua / tools/*.lua / tools/*.sh
- .github/workflows/ci.yml / commit-lint.yml / release.yml / scorecard.yml
- docs/adr/*.md (all 8 ADRs preserved byte-identically — 0001/0003/0004/0005 pre-existing; 0002/0006/0007/0008 created in 06-02)
- README.md / README.de.md / CONTRIBUTING.md
- docs/img/* (placeholders + tracker from 06-02 untouched)
- LICENSE / SECURITY.md / dependabot.yml

Phase-6 SUMMARY across 3 plans (recorded in STATE.md):
- ~11-13 GPG-signed commits total (4 in 06-01 + 5+ in 06-02 + 3 in 06-03)
- 0 source-code regressions; full busted suite GREEN end-to-end
- Reproducible-build SHA stable per-tag; documented dev vs tagged SHA delta
- All 22 requirement IDs delivered (BUILD-03..06, CI-01..06, SEC-02, SEC-05, DOC-01..10)
- 5 Yves checkpoints queued: CP-1 lektor / CP-2 branch-protection / CP-3 repo-metadata / CP-4 v1.0.0 tag / CP-5 screenshots

Yves' next actions (outside this plan, ordered):
1. Open PR for `phase-6/release-polish` → `main`. Squash merge once CI green (`gh pr merge --squash`; never `--rebase` per memory).
2. After merge, run CP-1 (lektor pass on README.de.md + CHANGELOG + 4 new ADRs) — commit on `main`.
3. Run CP-2 (`bash tools/setup-branch-protection.sh`) — may degrade to manual UI steps if PAT lacks Administration:write.
4. Run CP-3 (`bash tools/setup-repo-metadata.sh`).
5. Run CP-5 (capture screenshots; replace placeholders) — independent of CP-4.
6. Upload MAINTAINER_GPG_PUBKEY secret (CP-4 prereq): `gpg --armor --export FDE07046A6178E89ADB57FD3DE300C53D8E18642 | gh secret set MAINTAINER_GPG_PUBKEY`.
7. Fix CHANGELOG date placeholder: `docs(changelog): finalize v1.0.0 release date <YYYY-MM-DD>` commit on main.
8. (Optional) CP-4 dry-run via `v1.0.0-rc.1` tag to verify release.yml end-to-end before committing to v1.0.0 first-impression.
9. CP-4: `git tag -s v1.0.0 -m "Release v1.0.0\n\n<changelog excerpt>"` + `git push origin v1.0.0`. release.yml fires; verify https://github.com/yves-vogl/moneymoney-paypal-pos-extension/releases/tag/v1.0.0 lists .lua + .sha256.
10. After v1.0.0 stabilises (~2 weeks per ROADMAP), open Phase 6.1 (OpenSSF Scorecard hardening) planning.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-release-polish/06-03-SUMMARY.md` summarising:
- CHANGELOG.md cut [1.0.0] in Keep-a-Changelog format with First-stable-release banner; date placeholder pending Yves
- STATE.md transitions to v1.0.0-ready-for-tag with Phase-6 narrative + CP-1..CP-5 table
- 06-HANDOFF.md drafted as the consolidated post-merge / pre-tag runbook
- All 22 Phase-6 requirement IDs delivered across 06-01/02/03 + pre-existing assets
- Reproducible-build SHA UNCHANGED from 06-01
- 3 GPG-signed commits landed under `(06-03)` + `docs(state):` scope
- Phase-6 PR (phase-6/release-polish → main) ready to open; squash-merge per memory; Yves runs 5 CPs in sequence post-merge
- Phase 6.1 OpenSSF Scorecard hardening becomes unblocked after v1.0.0 ships and stabilises ~2 weeks
</output>
