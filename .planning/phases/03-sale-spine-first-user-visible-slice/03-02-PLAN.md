---
phase: 03-sale-spine-first-user-visible-slice
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - spec/refresh_idempotency_spec.lua
  - spec/mapping_schema_spec.lua
autonomous: true
requirements: [SALE-02, SALE-03, SALE-05, SALE-08, TEST-03, TEST-04, I18N-01]
requirements_addressed: [SALE-02, SALE-03, SALE-05, SALE-08, TEST-03, TEST-04, I18N-01]
tags: [wave-1, red-spec, idempotency, schema-gate, mvp]

must_haves:
  truths:
    - "spec/refresh_idempotency_spec.lua exists and FAILS (RED) against the empty src/purchases.lua + src/mapping.lua + entry.lua RefreshAccount fixture state — this proves the spec exercises the path that would miss idempotency if mapping were wrong (D-39 / TEST-03)"
    - "spec/mapping_schema_spec.lua exists and FAILS (RED) against the empty src/mapping.lua stub — proves the seven mandatory transaction fields are not yet produced by Phase-3 mapping code (TEST-04)"
    - "Idempotency spec queues the same purchase fixture twice via Mocks.push_response, seeds LocalStorage flat cache so M_auth.cached_token returns non-nil (Phase-2 D-23c double-write), runs RefreshAccount twice, asserts every transactionCode in result2 was already in result1 (D-39)"
    - "Schema spec asserts seven required fields {name, amount, currency, bookingDate, purpose, transactionCode, booked} on every mapped transaction across fixture types: simple sale, vat+tip, refund, dst summer, dst winter, card metadata (TEST-04)"
    - "Schema spec asserts D-31 Phase-3 invariants: booked == false on every transaction; valueDate is nil on every transaction"
    - "Schema spec asserts D-32 refund invariants: refund_to_transaction returns negative amount; transactionCode matches ^zettle:refund: prefix (D-38)"
    - "Schema spec asserts D-37 silent-skip: M_mapping.purchase_to_transaction(non_eur_purchase) returns nil so RefreshAccount can filter it out"
    - "Both specs follow the standard Phase-2 before_each(Mocks.setup + dofile) preamble identical to spec/http_spec.lua L11-44"
    - "Bearer token used in the idempotency spec is AT-VALID (NOT JWT-shaped) — same convention as 02-07-SUMMARY.md Test 3 to keep future SEC-03 walk-pattern noise-free"
    - "Card metadata test references payments[0].attributes.cardType + maskedPan per RESEARCH §1 correction over CONTEXT D-35 wording (cardBrand/cardLastFour)"
    - "Both gating specs committed in RED state; a file-header comment cites which Wave will turn them GREEN (Wave 2 partial for schema, Wave 4 full for idempotency)"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in commit message, code, or comments"
  artifacts:
    - path: "spec/refresh_idempotency_spec.lua"
      provides: "TEST-03 gating spec — double-refresh produces no new transactionCodes (D-39)"
      contains: "transactionCode"
      min_lines: 70
    - path: "spec/mapping_schema_spec.lua"
      provides: "TEST-04 gating spec — seven required MoneyMoney transaction fields + D-31/D-32/D-37 invariants"
      contains: "REQUIRED_FIELDS"
      min_lines: 80
  key_links:
    - from: "spec/refresh_idempotency_spec.lua"
      to: "src/entry.lua RefreshAccount (Wave 4 rewires)"
      via: "double-call comparison of result1.transactions vs result2.transactions on transactionCode"
      pattern: "RefreshAccount"
    - from: "spec/refresh_idempotency_spec.lua"
      to: "src/auth.lua M_auth.cached_token"
      via: "LocalStorage flat-key seed so cached_token returns AT-VALID without re-auth"
      pattern: "LocalStorage"
    - from: "spec/mapping_schema_spec.lua"
      to: "src/mapping.lua M_mapping.purchase_to_transaction (Wave 2)"
      via: "assert_schema walk asserts seven required output fields"
      pattern: "purchase_to_transaction"
    - from: "spec/mapping_schema_spec.lua"
      to: "src/mapping.lua M_mapping.refund_to_transaction (Wave 2)"
      via: "asserts amount < 0 and transactionCode prefix"
      pattern: "refund_to_transaction"
---

<objective>
Author the two gating specs that the rest of Phase 3 must turn green: `spec/refresh_idempotency_spec.lua` for TEST-03 / SALE-02 / SALE-05 / D-39 (the load-bearing acceptance criterion of the entire phase), and `spec/mapping_schema_spec.lua` for TEST-04 / SALE-03 / SALE-08 (the structural schema gate).

Purpose: Per CONTEXT.md specifics and ROADMAP Flagged Risk #2, the idempotency gate must exist BEFORE the implementation that satisfies it, and it must be RED first — otherwise we have no proof that the spec actually exercises the path it claims to gate. The schema gate is the same: failing structurally against empty stubs proves it discriminates against incomplete mappings.

Output: Two spec files committed in RED state. Both follow the standard Phase-2 build-once + before_each(Mocks.setup + dofile) preamble. The idempotency spec asserts every second-call transactionCode is in the first-call set (D-39). The schema spec asserts seven required transaction fields across all Phase-3 fixtures, plus Phase-3 invariants `booked=false` (D-31), `valueDate=nil` (D-31), `amount<0` on refunds (D-32), `transactionCode` prefix `"zettle:refund:"` on refunds (D-38), and `nil` return on non-EUR purchases (D-37).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@spec/entry_spec.lua
@spec/log_redaction_spec.lua
@spec/http_spec.lua
@src/entry.lua
@src/auth.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author spec/refresh_idempotency_spec.lua (TEST-03 RED gating spec)</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2c "Idempotency Proof" (verbatim spec skeleton L399-441)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/refresh_idempotency_spec.lua" (LocalStorage flat-cache seed pattern from spec/entry_spec.lua L270-288)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-38 / D-39 / D-41
    - .planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md Test 3 (Bearer = "AT-VALID" non-JWT-shaped convention)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-23c (flat-fallback path: LocalStorage["zettle:"..orgUuid] = JSON-encoded entry)
    - spec/entry_spec.lua L270-410 (multi-stage integration: queueing, two sequential callback calls)
    - spec/http_spec.lua L11-44 (build-once + dofile preamble — verbatim copy template)
    - src/entry.lua (current RefreshAccount fixture body — what Wave 4 replaces)
    - src/auth.lua M_auth.cached_token (flat fallback path)
  </read_first>
  <files>spec/refresh_idempotency_spec.lua</files>
  <behavior>
    - Test 1: "double-refresh of purchase_simple_sale produces no new transactionCodes on second call" — queues fixture twice, seeds flat LocalStorage so cached_token returns AT-VALID, runs RefreshAccount twice, asserts every code in r2 is in r1's seen-set
    - Test 2: same shape with purchase_with_vat_and_tip fixture, orgUuid "org-2"
    - Test 3: same shape with purchase_refund fixture, orgUuid "org-3"; additionally asserts at least one transactionCode in r1 matches ^zettle:refund: (D-32 / D-38)
    - Test 4: RefreshAccount returns M_i18n.t("error.network", "—") when cached_token is nil (D-41 nil-token guard) — does NOT seed LocalStorage
    - All four tests EXPECTED TO FAIL at this commit; Wave 2 partially greens 1-3 (mapping returns codes); Wave 4 fully greens 1-3 (entry.lua drives pipeline) and 4 (D-41 guard wired)
  </behavior>
  <action>
    Create spec/refresh_idempotency_spec.lua with the standard Phase-2 preamble copied verbatim from spec/http_spec.lua L11-44 (error message substituted to "refresh_idempotency_spec: failed to build dist/paypal-pos.lua before suite"). File top: `local Mocks = require("spec.helpers.mm_mocks")`, `local Fixtures = require("spec.helpers.fixtures")`, build-once `do … end`, `local function load_artifact() dofile("dist/paypal-pos.lua") end`. File-header comment (5-10 lines) cites TEST-03 / SALE-02 / SALE-05 / D-38 / D-39 / D-41 and states RED-in-Wave-1 / partial-GREEN-in-Wave-2 / full-GREEN-in-Wave-4.

    Describe block: `describe("RefreshAccount idempotency (TEST-03 / SALE-02 / SALE-05 / D-39)", function() ... end)` with `before_each(function() Mocks.setup(); load_artifact() end)` and `after_each(function() Mocks.teardown() end)`.

    Inside describe, declare two locals BEFORE it blocks: (a) `seed_token(orgUuid)` writes JSON-serialized entry to `LocalStorage["zettle:" .. orgUuid]` with `access_token="AT-VALID"`, `expires_at=os.time()+7200`, `obtained_at=os.time()`, `client_id="client-x"`, `uuid="u-1"`, `publicName="Beispiel Café"`; (b) `refresh_with_fixture(fixture_name, orgUuid)` calls seed_token, loads the raw fixture via Fixtures.load, pushes the raw content twice via Mocks.push_response, builds `account = {accountNumber=orgUuid, currency="EUR", balance=0}`, calls `RefreshAccount(account, 0)` twice, returns the two results.

    Tests (one it() per behavior, names verbatim):
    1. `it("double-refresh of purchase_simple_sale produces no new transactionCodes on second call", function() ...)` — uses refresh_with_fixture("purchase_simple_sale", "org-1"); asserts both results are tables, both .transactions are tables, builds seen-set from r1, asserts every r2 code is in seen.
    2. `it("double-refresh of purchase_with_vat_and_tip preserves transactionCode stability", function() ...)` — same shape, fixture "purchase_with_vat_and_tip", orgUuid "org-2".
    3. `it("double-refresh of purchase_refund preserves transactionCode stability (D-32 / D-38)", function() ...)` — same shape, fixture "purchase_refund", orgUuid "org-3"; ALSO asserts at least one transactionCode in r1.transactions matches `^zettle:refund:`.
    4. `it("RefreshAccount returns German error.network string when cached_token is nil (D-41)", function() ...)` — does NOT call seed_token; pushes purchase_simple_sale once (defensively, in case implementation tries fetch anyway); calls RefreshAccount on accountNumber "org-no-token"; asserts result equals `M_i18n.t("error.network", "—")`.

    Diagnostic messages on failures: "NEW transactionCode on second refresh: " .. tostring(t.transactionCode). Apply `-- luacheck: ignore 431` at file top if describe callback shadowing is needed. Do NOT monkey-patch RefreshAccount or M_auth.cached_token — RED is the gating proof. Do NOT add pcall wrappers.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && ./.luarocks/bin/busted spec/refresh_idempotency_spec.lua 2>&1 | tee /tmp/wave1_idem.log ; grep -q 'failure\|Failure' /tmp/wave1_idem.log &amp;&amp; grep -c '^  it(' spec/refresh_idempotency_spec.lua | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `spec/refresh_idempotency_spec.lua` exists with build-once + before_each + dofile preamble
    - `grep -c '^  it(' spec/refresh_idempotency_spec.lua` reports >= 4
    - `grep -n 'AT-VALID' spec/refresh_idempotency_spec.lua` finds the non-JWT Bearer placeholder
    - `grep -n 'zettle:' spec/refresh_idempotency_spec.lua` finds the flat-cache seed key prefix
    - `grep -n 'transactionCode' spec/refresh_idempotency_spec.lua` finds the dedup-set assertion
    - `grep -n 'NEW transactionCode on second refresh' spec/refresh_idempotency_spec.lua` finds the diagnostic message
    - `grep -n 'error.network' spec/refresh_idempotency_spec.lua` finds the D-41 nil-token assertion
    - `busted spec/refresh_idempotency_spec.lua` exits NON-ZERO (RED state)
    - Failure output cites assertion failures (not Lua errors) — proves the spec loads the artifact and exercises the path
    - Full suite `busted spec/` exits NON-ZERO because of this spec + Task-2 schema spec only (no Phase-1/2 regression)
  </acceptance_criteria>
  <done>
    spec/refresh_idempotency_spec.lua exists in RED state with four it() tests covering D-39 across three fixtures and D-41 nil-token guard. Commit: `test(03-02): add RED idempotency gating spec (TEST-03)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author spec/mapping_schema_spec.lua (TEST-04 RED gating spec)</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §2d "Schema Gate (TEST-04)" (verbatim spec skeleton L450-488)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/mapping_schema_spec.lua" (invariant walk pattern from spec/log_redaction_spec.lua + table-driven assertions from spec/errors_spec.lua)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-31 (booked=false, no valueDate), D-32 (refund), D-37 (non-EUR skip), D-38 (transactionCode prefixes)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md (fixture inventory and field shapes)
    - spec/log_redaction_spec.lua L327-344 (walk helper pattern)
    - spec/errors_spec.lua (table-driven one-it-per-case)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §1 Payment Sub-Object correction (card metadata under payments[].attributes.cardType / maskedPan, NOT cardBrand / cardLastFour)
  </read_first>
  <files>spec/mapping_schema_spec.lua</files>
  <behavior>
    - Test 1: purchase_simple_sale → purchase_to_transaction returns table with all 7 required fields; booked==false; valueDate==nil; currency=="EUR"
    - Test 2: purchase_with_vat_and_tip → schema; amount == p.amount/100 (SALE-01 gross-amount mapping)
    - Test 3: purchase_with_card_metadata → schema; name starts with "Visa " and contains "1111" (D-35 corrected via attributes.cardType + maskedPan path)
    - Test 4: purchase_refund → refund_to_transaction returns table with all 7 required fields; amount < 0 (D-32); transactionCode matches ^zettle:refund: (D-38); booked==false
    - Test 5: purchase_dst_boundary_summer → schema; bookingDate decomposed via os.date("!*t") yields y=2026 m=6 d=20 (CEST conversion, D-36)
    - Test 6: purchase_dst_boundary_winter → schema; bookingDate decomposed yields y=2026 m=2 d=1 (CET conversion, D-36)
    - Test 7: purchase_non_eur → purchase_to_transaction returns nil (D-37 silent skip)
    - Test 8: every emitted transaction across sale fixtures has currency=="EUR" (Phase 2 D-23a invariant)
  </behavior>
  <action>
    Create spec/mapping_schema_spec.lua with the standard Phase-2 preamble (error message substituted to "mapping_schema_spec: failed to build dist/paypal-pos.lua before suite"). File top requires Mocks and Fixtures. File-header comment (5-10 lines) cites TEST-04 / SALE-01 / SALE-03 / SALE-08 / D-31 / D-32 / D-37 / D-38 and states "RED in Wave 1; Wave 2 (src/mapping.lua) greens Tests 1-4 and 7-8; Tests 5 + 6 require the Wave-2 DST table inside _to_berlin_local_time".

    Describe block: `describe("transaction schema gate (TEST-04)", function() ... end)` with same before_each / after_each as Task 1.

    Inside describe, declare three locals BEFORE it blocks:
    - `REQUIRED_FIELDS = {"name","amount","currency","bookingDate","purpose","transactionCode","booked"}`
    - `assert_schema(txn, label)` — asserts txn is a table; iterates REQUIRED_FIELDS; uses assert.is_not_nil with diagnostic "missing required field '<field>'"
    - `load_record(name)` — calls Fixtures.load("purchases/"..name); asserts decoded is table and decoded.purchases is table; returns decoded.purchases[1]

    Tests (one it() per behavior, names verbatim from the behavior list):
    1. `it("purchase_simple_sale maps to a valid transaction schema (D-31)", function() ...)` — loads, calls M_mapping.purchase_to_transaction, asserts schema, asserts is_false(txn.booked), is_nil(txn.valueDate), equals("EUR", txn.currency).
    2. `it("purchase_with_vat_and_tip maps gross amount = purchase.amount / 100 (SALE-01)", function() ...)` — schema + amount equality assertion.
    3. `it("purchase_with_card_metadata upgrades name to <Brand> dot-dot-dot <last4> (SALE-08 / D-35 via attributes path)", function() ...)` — schema + assertions that name is_string, name:find("Visa", 1, true) is truthy, name:find("1111", 1, true) is truthy.
    4. `it("purchase_refund maps via refund_to_transaction with negative amount and zettle:refund: prefix (D-32 / D-38)", function() ...)` — schema + amount<0 + transactionCode:find("^zettle:refund:") truthy + is_false(txn.booked).
    5. `it("purchase_dst_boundary_summer maps bookingDate to Berlin local day 2026-06-20 (SALE-04 / D-36 CEST)", function() ...)` — schema + `local t = os.date("!*t", txn.bookingDate); assert.equals(2026, t.year); assert.equals(6, t.month); assert.equals(20, t.day)`.
    6. `it("purchase_dst_boundary_winter maps bookingDate to Berlin local day 2026-02-01 (SALE-04 / D-36 CET)", function() ...)` — same shape, expects year=2026 month=2 day=1.
    7. `it("purchase_non_eur returns nil (D-37 silent skip)", function() ...)` — asserts is_nil(txn).
    8. `it("every mapped transaction across fixtures sets currency = EUR (Phase 2 D-23a invariant)", function() ...)` — loops over the five EUR fixtures, asserts each mapped txn is a table with currency=="EUR".

    Apply `-- luacheck: ignore 431` at file top if describe shadowing is needed. Do NOT add pcall wrappers. Do NOT monkey-patch M_mapping — RED is the gating proof.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin && ./.luarocks/bin/busted spec/mapping_schema_spec.lua 2>&1 | tee /tmp/wave1_schema.log ; grep -q 'failure\|Failure' /tmp/wave1_schema.log &amp;&amp; grep -c '^  it(' spec/mapping_schema_spec.lua | awk '{ if ($1 &gt;= 8) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `spec/mapping_schema_spec.lua` exists with build-once + before_each + dofile preamble
    - `grep -c '^  it(' spec/mapping_schema_spec.lua` reports >= 8
    - `grep -n 'REQUIRED_FIELDS' spec/mapping_schema_spec.lua` finds the seven-field array
    - `grep -n 'M_mapping.purchase_to_transaction' spec/mapping_schema_spec.lua` finds at least one call
    - `grep -n 'M_mapping.refund_to_transaction' spec/mapping_schema_spec.lua` finds at least one call
    - `grep -n 'zettle:refund:' spec/mapping_schema_spec.lua` finds the D-38 prefix assertion
    - `grep -n 'is_false(txn.booked' spec/mapping_schema_spec.lua` finds D-31 booked invariant
    - `grep -n 'is_nil(txn.valueDate' spec/mapping_schema_spec.lua` finds D-31 valueDate invariant
    - `grep -n 'purchase_non_eur' spec/mapping_schema_spec.lua` finds D-37 skip assertion
    - `grep -n 't.year' spec/mapping_schema_spec.lua` finds DST boundary assertions (Tests 5 + 6)
    - `busted spec/mapping_schema_spec.lua` exits NON-ZERO (RED state)
    - Failure output cites assertion failures, not Lua errors — proves the spec loads the artifact
    - Full suite `busted spec/` exits NON-ZERO due to this + Task-1 only; no Phase-1/2 regression
  </acceptance_criteria>
  <done>
    spec/mapping_schema_spec.lua exists in RED state with eight it() tests covering the seven-field schema gate across simple sale, vat+tip, card metadata, refund, two DST boundary fixtures, the D-37 non-EUR skip, and a cross-fixture EUR-currency invariant. Commit: `test(03-02): add RED schema gating spec (TEST-04)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec test code → mock surface | Spec files load the artifact via `dofile`; mocks substitute for MoneyMoney globals. No external network. |
| LocalStorage seed → src/auth.lua M_auth.cached_token | Spec uses flat-key path per Phase-2 D-23c contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W1-01 | Information Disclosure | spec/refresh_idempotency_spec.lua Bearer seed | mitigate | Bearer placeholder `AT-VALID` is NOT JWT-shaped (no two dots) per 02-07 Test-3 convention; prevents future SEC-03 walk-pattern noise. No real merchant credentials. |
| T-03-W1-02 | Tampering | spec/mapping_schema_spec.lua REQUIRED_FIELDS array | mitigate | Single source of truth for the seven required fields; every test calls assert_schema, so silent renames break every test simultaneously. |
| T-03-W1-03 | Tampering | spec/refresh_idempotency_spec.lua double-call comparison | mitigate | If either side drifts (mapper non-deterministic, entry.lua reorders), the spec breaks. Both directions covered. |
| T-03-W1-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Pure-Lua spec authoring only. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Wave 1 RED gates — MUST fail (proof the specs exercise real paths):
./.luarocks/bin/busted spec/refresh_idempotency_spec.lua 2>&1 | grep -E '(Failure|failures)'
./.luarocks/bin/busted spec/mapping_schema_spec.lua 2>&1 | grep -E '(Failure|failures)'

# Pre-existing specs MUST still pass (no regressions):
./.luarocks/bin/busted spec/auth_spec.lua spec/entry_spec.lua spec/http_spec.lua spec/log_redaction_spec.lua spec/i18n_spec.lua spec/errors_spec.lua spec/mm_mocks_spec.lua spec/build_spec.lua

# Wave 0 pending scaffolds still pending (not regressed):
./.luarocks/bin/busted spec/dst_table_spec.lua spec/mapping_spec.lua spec/pagination_spec.lua spec/purchases_spec.lua 2>&1 | grep Pending

# Reproducible build still green (no src/ change):
lua tools/build.lua --verify
```
</verification>

<success_criteria>
- `spec/refresh_idempotency_spec.lua` exists with standard preamble, four it() tests (D-39 / D-41), RED against current entry.lua fixture body
- `spec/mapping_schema_spec.lua` exists with standard preamble, eight it() tests (TEST-04 + D-31 / D-32 / D-37 / D-38), RED against empty src/mapping.lua
- Both specs use the corrected RESEARCH §1 path (`payments[].attributes.cardType` / `maskedPan`) — NOT the deprecated `cardBrand` / `cardLastFour` wording
- Both specs use `AT-VALID` (non-JWT-shaped) when seeding the LocalStorage Bearer
- Neither spec monkey-patches `RefreshAccount` or `M_mapping` to artificially turn green
- Full suite `busted spec/` exits non-zero because of these two specs only; pre-existing specs and Wave-0 pending scaffolds unchanged
- `lua tools/build.lua --verify` SHA is unchanged from Phase 2 / Wave 0
</success_criteria>

<artifacts_this_phase_produces>
New files:
- `spec/refresh_idempotency_spec.lua` (RED gating spec for TEST-03 / SALE-02 / SALE-05 / D-39 / D-41)
- `spec/mapping_schema_spec.lua` (RED gating spec for TEST-04 / SALE-01 / SALE-03 / SALE-08 / D-31 / D-32 / D-37 / D-38)

New test names referenced by Wave 2 and Wave 4:
- "double-refresh of purchase_simple_sale produces no new transactionCodes on second call"
- "double-refresh of purchase_with_vat_and_tip preserves transactionCode stability"
- "double-refresh of purchase_refund preserves transactionCode stability (D-32 / D-38)"
- "RefreshAccount returns German error.network string when cached_token is nil (D-41)"
- "purchase_simple_sale maps to a valid transaction schema (D-31)"
- "purchase_with_vat_and_tip maps gross amount = purchase.amount / 100 (SALE-01)"
- "purchase_with_card_metadata upgrades name to <Brand> dot-dot-dot <last4> (SALE-08 / D-35 via attributes path)"
- "purchase_refund maps via refund_to_transaction with negative amount and zettle:refund: prefix (D-32 / D-38)"
- "purchase_dst_boundary_summer maps bookingDate to Berlin local day 2026-06-20 (SALE-04 / D-36 CEST)"
- "purchase_dst_boundary_winter maps bookingDate to Berlin local day 2026-02-01 (SALE-04 / D-36 CET)"
- "purchase_non_eur returns nil (D-37 silent skip)"
- "every mapped transaction across fixtures sets currency = EUR (Phase 2 D-23a invariant)"

Modified files: none in `src/`, none in `tools/`, none in `spec/helpers/`, none in pre-existing spec/*.lua. Pure RED-spec wave.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-02-SUMMARY.md` when done, summarising:
- Both gating specs exist; both are RED (assertion failures, not Lua errors)
- Idempotency spec test count + which fixtures each test uses
- Schema spec test count + which fields and invariants each test asserts
- Confirmation that no pre-existing Phase-1/2 spec regressed
- Confirmation that `lua tools/build.lua --verify` SHA is unchanged
- Notes on any RESEARCH.md / CONTEXT.md discrepancies surfaced while authoring
</output>
