---
phase: 03-sale-spine-first-user-visible-slice
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/purchases.lua
  - spec/purchases_spec.lua
autonomous: true
requirements: [SALE-06]
requirements_addressed: [SALE-06]
tags: [wave-3, purchases, http-fetch, since-clamp, mvp]

must_haves:
  truths:
    - "src/purchases.lua fills the Phase-1 stub with two public functions per CONTEXT D-40: M_purchases.fetch(clamped_since, bearer, cursor) for single page and M_purchases.fetch_all(clamped_since, bearer) which delegates to M_pagination.iterate"
    - "M_purchases.fetch URL = \"https://purchase.izettle.com/purchases/v2?\" + alphabetical sorted url-encoded query params: descending=false, limit=200, lastPurchaseHash=<cursor> (when present), startDate=<utc-iso-8601-of-clamped_since> (per RESEARCH §1 — single host on egress allowlist Phase-2 D-26)"
    - "startDate is ISO-8601 UTC formatted as os.date(\"!%Y-%m-%dT%H:%M:%SZ\", clamped_since) — per RESEARCH §1 query-param table"
    - "Authorization header = \"Bearer \" .. bearer; M_http.get_json logs only the URL (not the header) per Phase-2 SEC-03 contract — no new redaction call sites in purchases.lua (D-45)"
    - "M_purchases.fetch returns the 3-tuple from M_http.get_json verbatim (parsed_page, status, raw) so M_pagination.iterate can route via M_errors.from_http_status (D-43)"
    - "M_purchases.fetch_all(clamped_since, bearer) calls M_pagination.iterate(fetch_page_fn, {limit=200, descending=false, startDate=<iso>}) — initial params do NOT include lastPurchaseHash (the iterator updates that between pages)"
    - "fetch_page_fn is built as a closure that captures clamped_since and bearer and calls M_purchases.fetch(clamped_since, bearer, params.lastPurchaseHash) on each invocation"
    - "_url_encode_query(params) is a private helper that sorts keys, urlencodes via MM.urlencode (Phase-1 mock), joins with & — borrowed shape from src/http.lua L29-38 (_form_encode); private to the do…end block"
    - "_iso8601_utc(posix) is a private helper using os.date(\"!%Y-%m-%dT%H:%M:%SZ\", posix) — POSIX in, UTC ISO-8601 string out"
    - "No new egress host introduced — purchase.izettle.com is already in the Phase-2 D-26 allowlist; egress-grep CI gate in Plan 03-07 confirms this"
    - "spec/purchases_spec.lua fills every Wave-0 pending with a passing it() asserting URL shape (startDate, limit, descending, lastPurchaseHash), Bearer header, and error routing via Mocks.push_response + Mocks._last_request introspection"
    - "src/purchases.lua follows the Phase-2 module preamble: ownership citing SALE-06 / D-42 / D-43; M_purchases predeclared in webbanking_header.lua; no require() of siblings (D-02)"
    - "tools/manifest.txt is NOT modified (purchases already in the manifest after auth and before payouts)"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts:
    - path: "src/purchases.lua"
      provides: "M_purchases.fetch(clamped_since, bearer, cursor) single-page GET; M_purchases.fetch_all(clamped_since, bearer) delegates to M_pagination.iterate"
      contains: "M_purchases.fetch"
      min_lines: 60
    - path: "spec/purchases_spec.lua"
      provides: "Passing URL-shape + bearer + startDate tests (SALE-06)"
      contains: "M_purchases.fetch"
  key_links:
    - from: "src/purchases.lua M_purchases.fetch"
      to: "M_http.get_json (Phase-2 src/http.lua)"
      via: "Bearer header + 3-tuple destructure; identical shape to src/auth.lua fetch_profile (L86-92)"
      pattern: "M_http.get_json"
    - from: "src/purchases.lua M_purchases.fetch_all"
      to: "M_pagination.iterate (Plan 03-04)"
      via: "fetch_page_fn closure carries clamped_since and bearer; iterator updates params.lastPurchaseHash between calls"
      pattern: "M_pagination.iterate"
    - from: "spec/purchases_spec.lua"
      to: "Mocks._last_request.url"
      via: "URL capture pattern from spec/http_spec.lua L58-84 — assert startDate substring and Bearer in headers"
      pattern: "Mocks._last_request"
---

<objective>
Fill src/purchases.lua with M_purchases.fetch (single-page GET to purchase.izettle.com/purchases/v2 with Bearer header + URL-encoded query params) and M_purchases.fetch_all (drives the M_pagination.iterate loop from Plan 03-04). Then fill spec/purchases_spec.lua so every Wave-0 pending test passes. This plan runs in parallel with Plan 03-04 (different files); both must complete before Wave 4 wires entry.lua.

Purpose: Per CONTEXT D-40 + RESEARCH §1, this module owns the URL construction and query parameter encoding for the Purchase API. The HTTP transport itself is fully delegated to Phase-2's M_http.get_json (D-42), and pagination is delegated to Plan 03-04's M_pagination.iterate. This keeps each module single-purpose and lets specs test URL shape independently of cursor behavior.

Output: src/purchases.lua with two public functions plus two private helpers (_url_encode_query, _iso8601_utc); spec/purchases_spec.lua with passing tests covering startDate (SALE-06 incremental refresh), limit=200 (RESEARCH §1), descending=false, Bearer header, cursor handoff, and error routing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@src/purchases.lua
@src/http.lua
@src/auth.lua
@src/errors.lua
@src/i18n.lua
@spec/purchases_spec.lua
@spec/http_spec.lua
@spec/auth_spec.lua
@spec/helpers/mm_mocks.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill src/purchases.lua with M_purchases.fetch + fetch_all</name>
  <read_first>
    - src/purchases.lua (Phase-1 stub)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md §1 (endpoint URL, query params, response shape, ASSUMED limit=200, descending=false)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"src/purchases.lua" — single GET call pattern from src/auth.lua L86-92 (fetch_profile); module preamble pattern from src/http.lua L1-12; URL construction inspired by src/http.lua L29-38 (_form_encode)
    - src/http.lua (M_http.get_json signature: (url, headers) -> (decoded, status, raw); M_http._infer_status; the existing _form_encode is sorted-key urlencode which we mirror for query params)
    - src/auth.lua L86-92 (fetch_profile shape — exact analog: M_http.get_json(url, {Authorization="Bearer "..token}))
    - src/pagination.lua (Plan 03-04 — M_pagination.iterate signature: iterate(fetch_page_fn, initial_params))
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md D-42 (transport reuse), D-43 (error reuse), D-44 (fixture names), D-45 (no new redaction call sites)
  </read_first>
  <files>src/purchases.lua</files>
  <behavior>
    - M_purchases.fetch(clamped_since, bearer, cursor) -> (parsed_table|nil, status|nil, raw_body): GETs purchase.izettle.com/purchases/v2 with the sorted url-encoded query (startDate, limit, descending, optional lastPurchaseHash), passes Authorization: Bearer header. Returns the 3-tuple from M_http.get_json verbatim.
    - The URL query param order (alphabetical for byte-stable + reproducible-build invariance per Phase-1 D-31 spirit): descending, lastPurchaseHash (if cursor non-nil/non-empty), limit, startDate. All values url-encoded via MM.urlencode.
    - startDate value = os.date("!%Y-%m-%dT%H:%M:%SZ", clamped_since); urlencoded (colons become %3A).
    - limit value = "200" (string).
    - descending value = "false" (string).
    - M_purchases.fetch_all(clamped_since, bearer) -> (all_purchases|nil, error_string|nil): builds initial_params = {} (NOT passed as URL params; M_pagination.iterate manages cursor in params.lastPurchaseHash); builds fetch_page_fn closure capturing clamped_since and bearer that calls M_purchases.fetch(clamped_since, bearer, params.lastPurchaseHash); calls M_pagination.iterate(fetch_page_fn, initial_params); returns its result.
    - When cursor (nil or empty string) is passed to fetch, the lastPurchaseHash query param is OMITTED from the URL (not present as empty value).
  </behavior>
  <action>
    Rewrite src/purchases.lua in full.

    Module-level comment block (lines 1-9): cite ownership of SALE-06 / D-33 / D-42 / D-43; "Provides: M_purchases.fetch(clamped_since, bearer, cursor) single-page; M_purchases.fetch_all(clamped_since, bearer) drives M_pagination.iterate"; "The M_purchases table is predeclared in src/webbanking_header.lua."; "NO require() of sibling modules (D-02)."

    Private helper `local function _iso8601_utc(posix)`: returns `os.date("!%Y-%m-%dT%H:%M:%SZ", posix)`. Guard: if `type(posix) ~= "number"` then return `os.date("!%Y-%m-%dT%H:%M:%SZ", 0)` (epoch fallback — caller error would be visible upstream).

    Private helper `local function _url_encode_query(params)`: collects keys into an array, sorts alphabetically (table.sort), iterates and skips nil/empty-string values, returns `table.concat(parts, "&")` where each part is `MM.urlencode(k) .. "=" .. MM.urlencode(tostring(v))`. Borrowed shape from src/http.lua L29-38 (_form_encode).

    Public function `M_purchases.fetch(clamped_since, bearer, cursor)`:
    1. Build local q = {descending="false", limit="200", startDate=_iso8601_utc(clamped_since)}
    2. If type(cursor) == "string" and #cursor > 0 then q.lastPurchaseHash = cursor end
    3. local url = "https://purchase.izettle.com/purchases/v2?" .. _url_encode_query(q)
    4. local headers = {Authorization = "Bearer " .. tostring(bearer)}
    5. return M_http.get_json(url, headers)

    Public function `M_purchases.fetch_all(clamped_since, bearer)`:
    1. local fetch_page_fn = function(params) return M_purchases.fetch(clamped_since, bearer, params.lastPurchaseHash) end
    2. return M_pagination.iterate(fetch_page_fn, {})

    Apply luacheck annotations if shadowing. Do NOT add pcall around M_http.get_json (the get_json already pcalls JSON parse internally). Do NOT log the Bearer (M_http.get_json logs only the URL per Phase-2 contract — no new log call sites in this module per D-45). Do NOT add any direct Connection() call.

    Run `lua tools/build.lua` after the edit; confirm reproducible build still green.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; grep -c 'M_purchases.fetch' src/purchases.lua | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; grep -c 'purchase.izettle.com' src/purchases.lua | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; ! grep -E 'finance.izettle.com' src/purchases.lua</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/purchases.lua` reports >= 60
    - `grep -n 'M_purchases.fetch' src/purchases.lua` finds both the fetch and fetch_all definitions
    - `grep -c 'purchase.izettle.com' src/purchases.lua` reports exactly 1 (single egress host literal)
    - `grep -L 'finance.izettle.com' src/purchases.lua` succeeds (Phase 4 territory, MUST NOT appear)
    - `grep -L 'oauth.zettle.com' src/purchases.lua` succeeds (Phase 2 territory; purchases.lua does not touch auth host directly)
    - `grep -n 'M_http.get_json' src/purchases.lua` finds the transport delegation
    - `grep -n 'M_pagination.iterate' src/purchases.lua` finds the cursor delegation
    - `grep -n 'Bearer' src/purchases.lua` finds the Authorization header assembly (only place)
    - `grep -n '!%Y-%m-%dT%H:%M:%SZ' src/purchases.lua` finds the ISO-8601 format string
    - `grep -L 'M_log.debug\|M_log.info\|M_log.warn' src/purchases.lua` succeeds (no new log call sites per D-45)
    - `grep -L 'require(' src/purchases.lua` succeeds (D-02)
    - `grep -L 'conn:request\|Connection(' src/purchases.lua` succeeds (no direct transport)
    - `grep -L 'pcall' src/purchases.lua` succeeds (input parsed by M_http)
    - `lua tools/build.lua --verify` reports OK: reproducible
    - Pre-existing Phase-1/2/Wave-2/Plan-04 specs unchanged
  </acceptance_criteria>
  <done>
    src/purchases.lua filled with M_purchases.fetch + fetch_all; URL hits purchase.izettle.com only; Bearer flows through M_http.get_json; reproducible build green. Commit: `feat(03-05): implement M_purchases.fetch + fetch_all (SALE-06 / D-33 / D-42)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill spec/purchases_spec.lua (Wave-0 pending → passing)</name>
  <read_first>
    - spec/purchases_spec.lua (Wave-0 pending scaffold — 8+ pending tests)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-PATTERNS.md §"spec/purchases_spec.lua" (URL + param capture pattern from spec/http_spec.lua L58-84; fixture loading pattern from spec/auth_spec.lua L57-62)
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-01-PLAN.md (fixture inventory — purchase_simple_sale, purchase_page1, purchase_page2)
    - src/purchases.lua (after Task 1 — the implementation under test)
    - spec/http_spec.lua L58-90 (URL/body capture via Mocks._last_request)
    - spec/auth_spec.lua L57-62 (Fixtures.load + Mocks.push_response pattern)
    - spec/helpers/mm_mocks.lua (Mocks._last_request fields: url, headers, body)
  </read_first>
  <files>spec/purchases_spec.lua</files>
  <behavior>
    - All 8+ Wave-0 pending tests become passing it() with verbatim names
    - Sanity test "M_purchases module table is exposed" preserved
    - Tests use Mocks.push_response with raw fixture content (or empty payload) and assert on Mocks._last_request fields (url, headers)
  </behavior>
  <action>
    Replace each pending block in spec/purchases_spec.lua with a passing it() preserving the exact name from Wave-0 scaffold. Use Mocks.push_response with `Fixtures.load("purchases/purchase_simple_sale")` raw content for happy-path tests; use `{content = "{}"}` for tests that only inspect the request URL/headers.

    Test 1 ("fetch GETs https://purchase.izettle.com/purchases/v2 (host allowlist)"): push response `{content="{}"}`; call `M_purchases.fetch(1700000000, "AT-VALID", nil)`; assert.truthy(Mocks._last_request.url:find("^https://purchase.izettle.com/purchases/v2", 1, false)) (use pattern; substring find with `^` at start).

    Test 2 ("fetch includes Authorization: Bearer <token> header (D-42)"): push `{content="{}"}`; call fetch; assert.equals("Bearer AT-VALID", Mocks._last_request.headers["Authorization"]).

    Test 3 ("fetch includes startDate query param formatted as UTC ISO-8601 (SALE-06 / D-33)"): push `{content="{}"}`; call M_purchases.fetch(1700000000, "AT-VALID", nil) (1700000000 = 2023-11-14T22:13:20Z); assert.truthy(Mocks._last_request.url:find("startDate=", 1, true)); also assert the URL contains "2023" and ":" url-encoded as "%3A" (since startDate=2023-11-14T22%3A13%3A20Z).

    Test 4 ("fetch includes limit=200 query param (RESEARCH §1 / A1)"): push `{content="{}"}`; call fetch; assert.truthy(Mocks._last_request.url:find("limit=200", 1, true)).

    Test 5 ("fetch includes descending=false query param (RESEARCH §1)"): push `{content="{}"}`; call fetch; assert.truthy(Mocks._last_request.url:find("descending=false", 1, true)).

    Test 6 ("fetch includes lastPurchaseHash query param when continuing pagination (RESEARCH §2a)"): push `{content="{}"}`; call M_purchases.fetch(1700000000, "AT-VALID", "hash-from-prev-page"); assert.truthy(Mocks._last_request.url:find("lastPurchaseHash=hash%-from%-prev%-page", 1, false)). Also do a second sub-case with cursor=nil and assert.is_nil(Mocks._last_request.url:find("lastPurchaseHash", 1, true)) (omitted when nil).

    Test 7 ("fetch routes error via M_errors.from_http_status (D-43)"): push `{content = '{"error":"invalid_grant"}'}`; call fetch; the 3-tuple should be (parsed_table, 400, raw_body) per Phase-2's M_http._infer_status mapping invalid_grant → 400. assert.equals(400, status) (deconstruct the 3-tuple in the test).

    Test 8 ("fetch_all drives M_pagination.iterate with fetch as the fetch_page_fn"): set up a two-fixture queue — load page1 + page2 raw content, push both responses; call M_purchases.fetch_all(1700000000, "AT-VALID"); assert.is_nil(err); assert #all == 1; assert all[1].purchaseUUID1 == "44444444-4444-4444-4444-444444444444". Also assert that two HTTP requests were captured (e.g. by counting how many times Mocks._last_request was overwritten — or by examining Mocks._response_queue is now empty).

    Bonus integration smoke (optional 9th test): "fetch_all returns empty array on purchases_empty fixture (SALE-06 incremental empty-refresh)" — push purchases_empty.json; call fetch_all; assert #all == 0; assert err is nil.

    Use Mocks._last_request for assertions. Add diagnostic strings on every assert.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; ./.luarocks/bin/busted spec/purchases_spec.lua 2>&amp;1 | tee /tmp/wave3_pur.log &amp;&amp; grep -qE '0 failures' /tmp/wave3_pur.log &amp;&amp; ! grep -E 'Pending' /tmp/wave3_pur.log &amp;&amp; grep -c '^  it(' spec/purchases_spec.lua | awk '{ if ($1 &gt;= 8) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  it(' spec/purchases_spec.lua` reports >= 8
    - `grep -c '^  pending(' spec/purchases_spec.lua` reports 0
    - `busted spec/purchases_spec.lua` is GREEN
    - `busted spec/purchases_spec.lua -t "fetch includes startDate query param formatted as UTC ISO-8601 (SALE-06 / D-33)"` passes
    - `busted spec/purchases_spec.lua -t "fetch includes Authorization: Bearer <token> header (D-42)"` passes
    - `busted spec/purchases_spec.lua -t "fetch_all drives M_pagination.iterate with fetch as the fetch_page_fn"` passes
    - Full suite `busted spec/` baseline now reduces RED to: only spec/refresh_idempotency_spec.lua (Wave 4 fixes); all other specs pass
    - `lua tools/build.lua --verify` reports OK: reproducible
  </acceptance_criteria>
  <done>
    spec/purchases_spec.lua filled with 8+ passing it() tests covering URL host, Bearer header, query params (startDate, limit, descending, lastPurchaseHash), error routing, and fetch_all → iterate delegation. Commit: `test(03-05): fill purchases unit specs (SALE-06 / D-33 / D-42 / D-43)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bearer token (in-memory) → Authorization header → M_http.get_json | Bearer is concatenated only inside the Authorization header table; M_http.get_json structurally omits headers from logs (Phase-2 T-02-04-02 guarantee). No new log call sites added by purchases.lua. |
| clamped_since parameter → URL startDate | Numeric POSIX integer; os.date format string is hard-coded; no injection possible. |
| Zettle JSON response → M_http.get_json pcall | Same upstream handling as Phase 2; purchases.lua never directly parses JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W3b-01 | Information Disclosure | M_purchases.fetch Authorization header | mitigate | Bearer concatenation lives only inside the headers table; M_http.get_json L125-130 explicitly logs only the URL, never the header table. Phase-2 SEC-03 gating spec (log_redaction_spec.lua) already covers M_http; Phase 3 adds no new log call sites in purchases.lua per D-45. |
| T-03-W3b-02 | Tampering | M_purchases.fetch URL query encoding | mitigate | _url_encode_query sorts keys alphabetically and urlencodes values via MM.urlencode (Phase-1 mock = identity on test, real-encode on production). Reproducible-build invariant: same params produce byte-identical URL. |
| T-03-W3b-03 | Spoofing | Egress to non-allowlisted host | mitigate | Single URL literal "https://purchase.izettle.com/purchases/v2" — grep gate in Plan 03-07 confirms only oauth.zettle.com (Phase 2) + purchase.izettle.com (Phase 3) appear in dist/paypal-pos.lua. finance.izettle.com MUST NOT appear (Phase 4). |
| T-03-W3b-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Pure-Lua source edits + spec authoring only. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin
lua tools/build.lua && lua tools/build.lua --verify

# Wave-3b purchases spec GREEN:
./.luarocks/bin/busted spec/purchases_spec.lua 2>&1 | grep -E '0 failures'

# Pagination spec (Plan 03-04) still GREEN if run in parallel:
./.luarocks/bin/busted spec/pagination_spec.lua 2>&1 | grep -E '0 failures'

# Pre-existing specs still pass; only refresh_idempotency_spec (Wave 4) remains RED:
./.luarocks/bin/busted spec/ 2>&1 | grep -E '(Success|Failure)'

# Egress allowlist sanity (this plan adds purchase.izettle.com to artifact):
grep -E 'https?://[a-z.]+' dist/paypal-pos.lua | sort -u
```
</verification>

<success_criteria>
- src/purchases.lua has substantial implementation (>= 60 lines) with M_purchases.fetch + M_purchases.fetch_all + two private helpers
- src/purchases.lua references purchase.izettle.com exactly once (egress allowlist clean)
- src/purchases.lua does NOT reference finance.izettle.com (Phase 4 territory)
- src/purchases.lua does NOT add new log call sites (D-45 / SEC-03 invariant preserved)
- spec/purchases_spec.lua has 8+ passing it() tests; zero pending
- Pre-existing Phase-1/2/Wave-2 specs unchanged
- `lua tools/build.lua --verify` is reproducible
- After this plan + Plan 03-04, the only RED spec is spec/refresh_idempotency_spec.lua (Wave 4 closer)
</success_criteria>

<artifacts_this_phase_produces>
New / modified files:
- `src/purchases.lua` — filled with M_purchases.fetch, M_purchases.fetch_all, _url_encode_query, _iso8601_utc
- `spec/purchases_spec.lua` — filled (Wave-0 pending → passing)

New public symbols:
- `M_purchases.fetch(clamped_since, bearer, cursor)` — single-page GET to purchase.izettle.com/purchases/v2; returns (parsed, status, raw) 3-tuple from M_http.get_json
- `M_purchases.fetch_all(clamped_since, bearer)` — drives M_pagination.iterate; returns (all_purchases, error_or_nil) 2-tuple

Module-internal:
- `_iso8601_utc(posix)` — format POSIX as UTC ISO-8601
- `_url_encode_query(params)` — sorted-key url-encoded query string

New egress host in dist/paypal-pos.lua: `purchase.izettle.com` (already in Phase-2 D-26 allowlist; the host literal appears in source for the first time in this plan)

Files NOT modified: `tools/manifest.txt`, `src/webbanking_header.lua`, `src/pagination.lua` (Plan 03-04), `src/mapping.lua` (Plan 03-03), `src/entry.lua` (Plan 03-06).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-05-SUMMARY.md` summarising:
- M_purchases.fetch URL shape (host, query params, encoding)
- M_purchases.fetch_all delegation to M_pagination.iterate
- Bearer header construction location (single concatenation site)
- Egress sanity: grep on dist/paypal-pos.lua should now show oauth.zettle.com + purchase.izettle.com (no finance host)
- Purchases spec test count + pass count
- Reproducible build SHA after this plan
- Confirmation that no new log call sites added in src/purchases.lua (D-45)
</output>
