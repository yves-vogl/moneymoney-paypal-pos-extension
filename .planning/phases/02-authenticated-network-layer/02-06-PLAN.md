---
phase: 02-authenticated-network-layer
plan: 06
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/entry.lua
  - spec/entry_spec.lua
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-06, ACCT-01, ACCT-02, ACCT-04]
requirements_addressed: [AUTH-01, AUTH-03, AUTH-06, ACCT-01, ACCT-02, ACCT-04]
tags: [entry, integration, initializesession2, listaccounts, endsession, lua]

must_haves:
  truths:
    - "InitializeSession2 with valid credentials runs the D-21 two-call probe (token exchange → /users/self → cache write) synchronously inside the add-account dialog (per D-21 / AUTH-03)"
    - "InitializeSession2 first call (credentials==nil) preserves the Phase-1 challenge object verbatim (D-10 surface contract preserved per CONTEXT §code_context)"
    - "InitializeSession2 credential-extraction block (Phase-1 L22-L41) is preserved verbatim; new code is INSERTED AFTER it (per CONTEXT §code_context Integration Points)"
    - "A malformed assertion JWT (cannot yield client_id) returns the German error.invalid_grant string synchronously without making any network call (per D-22 / Pattern 4)"
    - "A 400/401/403 response on /token returns the LoginFailed literal synchronously via M_errors.from_http_status (per D-21 leg 1 / AUTH-03)"
    - "A 401/403 response on /users/self returns the LoginFailed literal synchronously (per D-21 leg 2 / AUTH-03)"
    - "On success, persist_session is called and the function returns nil — MoneyMoney then calls ListAccounts (per D-21)"
    - "ListAccounts iterates LocalStorage.zettle (or flat-key fallback) and emits one record per cached orgUuid with accountNumber=orgUuid, name='PayPal POS — '+publicName (or '+orgUuid:sub(1,8) on empty publicName), type=AccountTypeGiro, currency='EUR' (per D-23a / D-23b / ACCT-01 / ACCT-02 / ACCT-04)"
    - "EndSession calls M_http.shutdown() and emits one INFO log line; LocalStorage is NOT cleared (AUTH-06 cache must survive across sessions per RESEARCH §Pattern 4 anti-pattern note)"
    - "RefreshAccount is preserved verbatim — Phase 3/4 owns real mapping (per CONTEXT §domain Out of scope)"
    - "API key never leaks: entry.lua emits at most one INFO line per session that includes only length() of api_key, never the value (per Phase-1 L46 idiom)"
    - "Egress allowlist {oauth.zettle.com, purchase.izettle.com, finance.izettle.com} respected — entry.lua adds no host strings (delegates to auth.lua)"
    - "All user-facing strings via M_i18n.t (German primary) — credential.api_key.label, error.invalid_grant, account.name template"
    - "Commits are GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution in commit message, code, or comments"
  artifacts:
    - path: "src/entry.lua"
      provides: "Rewritten InitializeSession2 (with D-22 client_id extraction + D-21 two-call probe + persist_session), rewritten ListAccounts (cache-read multi-merchant), rewritten EndSession (M_http.shutdown), preserved RefreshAccount + SupportsBank"
      contains: "M_auth._extract_client_id"
      min_lines: 130
    - path: "spec/entry_spec.lua"
      provides: "Greened tests for AUTH-01, AUTH-03, ACCT-01 (extended), ACCT-02, ACCT-04, AUTH-06 (cache-survives-reload simulation)"
  key_links:
    - from: "src/entry.lua InitializeSession2"
      to: "M_auth._extract_client_id → M_auth.exchange_assertion → M_errors.from_http_status → M_auth.fetch_profile → M_auth.persist_session"
      via: "D-21 two-call probe + D-22 client_id extraction (Pattern 4)"
      pattern: "M_auth%."
    - from: "src/entry.lua ListAccounts"
      to: "LocalStorage.zettle (nested) + LocalStorage[\"zettle:...\"] (flat fallback)"
      via: "iterates cached entries; one MoneyMoney account record per orgUuid (D-23a/b/c)"
      pattern: "LocalStorage"
    - from: "src/entry.lua EndSession"
      to: "M_http.shutdown()"
      via: "closes module-local Connection per D-25"
      pattern: "M_http%.shutdown"
    - from: "src/entry.lua InitializeSession2"
      to: "M_errors.from_http_status"
      via: "called twice — once after exchange_assertion, once after fetch_profile (per Pattern 4 L617–L626)"
      pattern: "M_errors%.from_http_status"
---

<objective>
Rewrite the Phase-1 walking-skeleton callbacks in src/entry.lua so InitializeSession2 runs the D-21 two-call probe, ListAccounts reads from the LocalStorage cache (multi-merchant), and EndSession releases the Connection — while preserving Phase-1's credential-extraction block verbatim and leaving RefreshAccount untouched. This is the integration plan that turns Phase 2 into a user-visible slice: paste API key → account appears in sidebar, or wrong key → synchronous LoginFailed in the add-account dialog.

Purpose: AUTH-01 + AUTH-03 + ACCT-01 + ACCT-02 + ACCT-04 collectively demand a complete add-account UX. Wave 1 + Wave 2 delivered the building blocks (errors, JWT decoder, HTTP transport, auth orchestration); Wave 3 composes them inside the MoneyMoney callback surface. Without this plan, Phase 2 has no user-observable behavior. AUTH-06 (cache survives restart) is also gated here through the entry-integration test that simulates a restart by tearing down the in-memory Connection while keeping LocalStorage flat keys.

Output: A ~130-LoC src/entry.lua (up from 87) with rewritten InitializeSession2 / ListAccounts / EndSession; greened spec/entry_spec.lua tests for the seven new behaviors documented in 02-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authenticated-network-layer/02-CONTEXT.md
@.planning/phases/02-authenticated-network-layer/02-RESEARCH.md
@.planning/phases/02-authenticated-network-layer/02-PATTERNS.md
@.planning/phases/02-authenticated-network-layer/02-03-SUMMARY.md
@.planning/phases/02-authenticated-network-layer/02-04-SUMMARY.md
@.planning/phases/02-authenticated-network-layer/02-05-SUMMARY.md
@.planning/phases/01-foundations-sandbox-probes/CONTEXT.md
@src/entry.lua
@src/auth.lua
@src/http.lua
@src/errors.lua
@src/i18n.lua
@src/log.lua
@spec/helpers/mm_mocks.lua
@spec/helpers/fixtures.lua
@spec/entry_spec.lua
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite InitializeSession2 with D-22 client_id extraction + D-21 two-call probe + persist_session</name>
  <read_first>
    - src/entry.lua (current Phase-1 walking-skeleton — preserve L5-L7 SupportsBank, L9-L20 challenge block, L22-L41 credential extraction, L43-L45 empty-key guard, L62-L82 RefreshAccount; rewrite everything else)
    - .planning/phases/01-foundations-sandbox-probes/CONTEXT.md D-10 (InitializeSession2 surface contract — credential extraction block is locked verbatim)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-21 (two-call probe), D-22 (JWT client_id), D-24 (errors map)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Pattern 4" L565–L633 (the full InitializeSession2 sketch)
    - .planning/phases/02-authenticated-network-layer/02-PATTERNS.md §"src/entry.lua" L118–L153 (the surgery localization map: preserve verbatim L5-L45, insert after L45, before L46-L47)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Anti-Patterns to Avoid" L636–L643 (do not store api_key anywhere; no pcall around Connection)
    - spec/entry_spec.lua (Phase-1 walking-skeleton tests still in place — preserve passing tests; ADD integration tests)
    - spec/fixtures/auth/token_ok.json, token_invalid_grant.json, users_self_ok.json, users_self_unauthorized.json (Wave 0)
    - spec/helpers/mm_mocks.lua (push_response queue + LocalStorage reset per Setup)
  </read_first>
  <files>src/entry.lua, spec/entry_spec.lua</files>
  <behavior>
    InitializeSession2 first call (credentials==nil) returns the unchanged Phase-1 challenge object {title, challenge, label} all set to M_i18n.t("credential.api_key.label"). NO behavior change.

    InitializeSession2 with credentials:
    - Extracts api_key via the existing Phase-1 block — preserved verbatim
    - NEW: client_id = M_auth._extract_client_id(api_key); if nil emit one M_log.info "InitializeSession2: assertion JWT could not yield client_id" then return M_i18n.t("error.invalid_grant") synchronously (no network)
    - NEW: token_table, status, raw_body = M_auth.exchange_assertion(api_key, client_id); err = M_errors.from_http_status(status, raw_body); if err then return err end
    - NEW: profile, p_status, p_raw = M_auth.fetch_profile(token_table.access_token); p_err = M_errors.from_http_status(p_status, p_raw); if p_err then return p_err end
    - NEW: M_auth.persist_session(token_table, profile, client_id); return nil

    Test cases:
    - "InitializeSession2 with valid credentials populates LocalStorage" — push token_ok then users_self_ok; call InitializeSession2 with {value="hdr.eyJhdWQiOiJjbGllbnQteCJ9.sig"}; assert return is nil; assert LocalStorage.zettle is a non-empty table; assert LocalStorage["zettle:<orgUuid>"] is a non-empty string
    - "InitializeSession2 with invalid_grant returns LoginFailed" — push token_invalid_grant; call InitializeSession2 with {value="hdr.eyJhdWQiOiJjbGllbnQteCJ9.sig"}; assert return is LoginFailed; assert LocalStorage.zettle is nil (not populated)
    - "InitializeSession2 with scope-failure on /users/self returns LoginFailed" — push token_ok then users_self_unauthorized; call InitializeSession2; assert return is LoginFailed; assert LocalStorage.zettle is nil (rolled back — no persist_session call)
    - "InitializeSession2 with malformed JWT returns error.invalid_grant without any network call" — call InitializeSession2 with {value="not-a-jwt"}; assert return equals M_i18n.t("error.invalid_grant"); assert Mocks._response_queue length unchanged (no consumption); assert Mocks._last_request is nil (no HTTP call made)
    - "InitializeSession2 first call (credentials==nil) returns the German credential challenge object" — pre-existing Phase-1 test; must still pass after the rewrite
  </behavior>
  <action>
    Edit src/entry.lua. Rewrite InitializeSession2 per PATTERNS §"src/entry.lua" L118–L153 surgery map:

    1. PRESERVE verbatim L5-L7 SupportsBank
    2. PRESERVE verbatim L9-L20 the first-call challenge block (early return when credentials==nil)
    3. PRESERVE verbatim L22-L41 the credential-extraction block (string / positional array / challenge-style table / {username,password})
    4. PRESERVE verbatim L43-L45 the empty-key guard (returns M_i18n.t("error.invalid_grant"))
    5. REPLACE L46-L47 (the M_log.info + return nil) with the new probe sequence:

       Step 3 (D-22): client_id extraction
       - local client_id = M_auth._extract_client_id(api_key)
       - if not client_id then M_log.info("InitializeSession2: assertion JWT could not yield client_id"); return M_i18n.t("error.invalid_grant") end

       Step 4 (D-21 leg 1): POST /token
       - local token_table, status, raw_body = M_auth.exchange_assertion(api_key, client_id)
       - local err = M_errors.from_http_status(status, raw_body)
       - if err then return err end

       Step 5 (D-21 leg 2): GET /users/self
       - local profile, p_status, p_raw = M_auth.fetch_profile(token_table.access_token)
       - local p_err = M_errors.from_http_status(p_status, p_raw)
       - if p_err then return p_err end

       Step 6 (D-23c): persist
       - M_auth.persist_session(token_table, profile, client_id)
       - return nil

    Keep the existing "-- luacheck: ignore 431" annotation on the signature line.

    Preserve RefreshAccount (L62-L82) verbatim — Phase 3/4 owns real mapping.

    NO pcall around the M_auth calls. The M_auth.* functions return (table|nil, status, raw); M_errors.from_http_status handles every status case including nil. M_auth._extract_client_id returns string|nil and never raises.

    No new host strings in entry.lua — all URLs are inside src/auth.lua (Plan 02-05).

    Then GREEN spec/entry_spec.lua with the test cases listed in <behavior>. Use the existing spec/entry_spec.lua setup-once pattern (L17–L24: build, dofile once, before_each resets capture buffers). For each test, push the appropriate fixture(s) via Mocks.push_response({content=Fixtures.load("auth/..."), …}) before calling InitializeSession2.

    For the "InitializeSession2 with malformed JWT" test: do NOT push any response. The test asserts no HTTP call is made (Mocks._last_request stays nil).

    For tests that need a synthetic JWT, reuse the precomputed base64url middle-segment strings from Plan 02-03 (e.g. {"aud":"client-x"} → "eyJhdWQiOiJjbGllbnQteCJ9").

    Preserve the existing entry_spec tests: SupportsBank cases, the first-call challenge test, ListAccounts shape test (unchanged in this task — will extend in Task 2), RefreshAccount fixture test, EndSession returns nil (will extend in Task 2 / Task 3).

    No fenced code blocks in <action>.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/entry_spec.lua --filter "InitializeSession2" 2>&amp;1 | tee /tmp/entry_t1.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/entry_t1.log</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/entry_spec.lua --filter "InitializeSession2" passes all InitializeSession2-related tests (Phase-1 challenge test + four new integration tests)
    - grep -c 'M_auth._extract_client_id' src/entry.lua is exactly 1
    - grep -c 'M_auth.exchange_assertion' src/entry.lua is exactly 1
    - grep -c 'M_auth.fetch_profile' src/entry.lua is exactly 1
    - grep -c 'M_auth.persist_session' src/entry.lua is exactly 1
    - grep -c 'M_errors.from_http_status' src/entry.lua is exactly 2 (once after token exchange, once after profile fetch)
    - grep -cE '(oauth\.zettle\.com|purchase\.izettle\.com|finance\.izettle\.com)' src/entry.lua is exactly 0 (no host strings in entry.lua; delegate to auth.lua)
    - grep -c 'pcall' src/entry.lua is exactly 0 (no pcall needed; all callees return nil-on-failure rather than raising)
    - The Phase-1 credential extraction block (L22-L41 in the original) is bytewise preserved — diff the new file against the old to confirm L22-L41 region is unchanged
    - The RefreshAccount function is bytewise preserved
    - luacheck src/entry.lua spec/entry_spec.lua exits 0
    - lua tools/build.lua --verify reports reproducible build
  </acceptance_criteria>
  <done>
    InitializeSession2 implements the D-22 client_id extraction + D-21 two-call probe + persist_session, surfaces synchronous LoginFailed via M_errors.from_http_status on 400/401/403 from either /token or /users/self, and surfaces M_i18n.t("error.invalid_grant") on malformed JWT without any network call. Phase-1 credential extraction block preserved verbatim; RefreshAccount preserved verbatim; spec tests for the four new InitializeSession2 behaviors are green; reproducible build holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite ListAccounts to read from LocalStorage cache (ACCT-01 / ACCT-02 / ACCT-04)</name>
  <read_first>
    - src/entry.lua L50-L60 (current Phase-1 fixture — to be replaced)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-23a, D-23b
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Code Examples / ListAccounts reading from cache" L1239–L1282 (full sketch including fallback for empty cache)
    - .planning/phases/02-authenticated-network-layer/02-PATTERNS.md §"src/entry.lua" L154–L173 (target shape: iterate orgUuid → publicName fallback to orgUuid:sub(1,8))
    - spec/entry_spec.lua existing ListAccounts test (Phase-1 walking-skeleton — must still pass when cache is empty per RESEARCH §"ListAccounts reading from cache" defensive fallback)
  </read_first>
  <files>src/entry.lua, spec/entry_spec.lua</files>
  <behavior>
    - ListAccounts iterates LocalStorage.zettle and emits one record per orgUuid: {accountNumber=orgUuid, type=AccountTypeGiro, name="PayPal POS — "+publicName, currency="EUR", portfolio=false}
    - When publicName is nil OR empty, label uses orgUuid:sub(1, 8) — never produces an empty label
    - When two orgs exist in cache, ListAccounts returns two records with distinct accountNumbers and labels
    - When LocalStorage.zettle is nil/empty, ListAccounts returns the Phase-1 placeholder {accountNumber="paypal-pos-fixture-001", name=M_i18n.t("account.name", "Test-Händler"), currency="EUR", type=AccountTypeGiro, portfolio=false} so Phase-1 walking-skeleton tests still pass
    - knownAccounts parameter is accepted but not used (Phase 2 always returns the full cache state)

    Test cases:
    - "ListAccounts label uses publicName when cache populated" — persist_session with publicName="Beispiel Café"; call ListAccounts({}); assert result[1].name == "PayPal POS — Beispiel Café" AND result[1].accountNumber == "<orgUuid>"
    - "ListAccounts label falls back to orgUuid prefix when publicName empty" — persist_session with publicName=nil; call ListAccounts({}); assert result[1].name == "PayPal POS — " + orgUuid:sub(1,8)
    - "ListAccounts returns two records for two merchants" (ACCT-04) — persist_session twice with org-1 and org-2; call ListAccounts({}); assert #result == 2; assert one record has accountNumber=org-1 and the other accountNumber=org-2; both have type=AccountTypeGiro and currency="EUR"
    - "ListAccounts returns Phase-1 fixture when cache empty" — pre-existing Phase-1 test; must still pass when LocalStorage.zettle is nil
    - "ListAccounts returns AccountTypeGiro" — pre-existing; covered by all four cases above
  </behavior>
  <action>
    Edit src/entry.lua. Replace the L50-L60 fixture ListAccounts with the cache-reading implementation per RESEARCH §"ListAccounts reading from cache" L1239–L1282:

    1. Iterate `LocalStorage.zettle or {}`
    2. For each orgUuid → entry: build label = "PayPal POS — " + entry.publicName if type(entry.publicName)=="string" and #entry.publicName>0, else "PayPal POS — " + orgUuid:sub(1,8)
    3. Insert {accountNumber=orgUuid, type=AccountTypeGiro, name=label, currency="EUR", portfolio=false} into the output array
    4. If the output array is still empty after iteration, return the Phase-1 fallback record (so existing walking-skeleton tests still pass) per RESEARCH §"ListAccounts" L1266–L1280

    Keep the "-- luacheck: ignore 431" annotation on the signature.

    Then GREEN the ListAccounts tests in spec/entry_spec.lua per <behavior>. Reuse the persist_session calls from Plan 02-05 spec to populate LocalStorage. The before_each in entry_spec.lua resets LocalStorage via Mocks.setup() so each test starts from a clean cache; populate cache explicitly inside each it().

    Add a note in spec/entry_spec.lua's pre-existing "ListAccounts returns Phase-1 fixture" test (if needed) explicitly asserting LocalStorage.zettle is nil before the call, so the fallback path is exercised.

    No fenced code blocks in <action>.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/entry_spec.lua --filter "ListAccounts" 2>&amp;1 | tee /tmp/entry_t2.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/entry_t2.log</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/entry_spec.lua --filter "ListAccounts" passes all ListAccounts tests (Phase-1 fallback + three new cache-reading tests)
    - grep -c 'function ListAccounts' src/entry.lua is exactly 1
    - grep -c 'PayPal POS — ' src/entry.lua is at least 2 (publicName branch + orgUuid-prefix fallback branch)
    - grep -c 'AccountTypeGiro' src/entry.lua is at least 2 (cache record + fallback record)
    - grep -c 'paypal-pos-fixture-001' src/entry.lua is exactly 1 (the empty-cache fallback retains the Phase-1 placeholder per RESEARCH §"ListAccounts" L1266–L1280)
    - luacheck src/entry.lua spec/entry_spec.lua exits 0
    - lua tools/build.lua --verify reports reproducible build
    - busted spec/ (full suite) shows zero new failures
  </acceptance_criteria>
  <done>
    ListAccounts reads LocalStorage.zettle and emits one record per cached merchant with stable accountNumber=orgUuid (D-23a) and label="PayPal POS — "+publicName (D-23b) with orgUuid-prefix fallback when publicName is empty; two-merchant coexistence verified (ACCT-04); Phase-1 fallback retained for empty cache; reproducible build holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire EndSession to M_http.shutdown + AUTH-06 cache-survives-reload test</name>
  <read_first>
    - src/entry.lua L84-L87 (current Phase-1 no-op EndSession)
    - .planning/phases/02-authenticated-network-layer/02-CONTEXT.md D-25 (M_http.shutdown contract), Code Insights / Integration Points (EndSession hooks shutdown; LocalStorage NOT cleared)
    - .planning/phases/02-authenticated-network-layer/02-RESEARCH.md §"Anti-Patterns to Avoid" L636–L643 (specifically: "Clearing LocalStorage.zettle in EndSession" — that would defeat AUTH-06)
    - .planning/phases/02-authenticated-network-layer/02-VALIDATION.md AUTH-06 row (cache populated after auth survives a Mocks.teardown() + re-dofile cycle when written to flat-key fallback path)
    - src/http.lua (M_http.shutdown contract: closes module-local Connection; idempotent)
  </read_first>
  <files>src/entry.lua, spec/entry_spec.lua</files>
  <behavior>
    - EndSession calls M_http.shutdown() then returns nil
    - LocalStorage is NOT cleared by EndSession (cache survives across sessions per AUTH-06)
    - EndSession emits one INFO log line "EndSession called" (preserved Phase-1 behavior + Connection cleanup)
    - EndSession is idempotent — a second call without an intervening exchange_assertion does not error

    AUTH-06 simulation test:
    - persist_session populates LocalStorage with org-1
    - EndSession is called (shutdown closes the connection; LocalStorage untouched)
    - Simulate restart by setting LocalStorage.zettle = nil (Q5 nested-table-lost worst case)
    - cached_token("org-1") returns the access_token via the flat-fallback path (proves AUTH-06 holds defensively per D-23c)
  </behavior>
  <action>
    Edit src/entry.lua. Replace L84-L87 (the current no-op EndSession) with:

    function EndSession()
      M_log.info("EndSession called")
      M_http.shutdown()
      return nil
    end

    Do NOT clear LocalStorage.zettle (anti-pattern per RESEARCH L640–L643). Do NOT clear LocalStorage["zettle:..."] flat keys. The persistent cache is the explicit guarantee of AUTH-06.

    Then add two tests to spec/entry_spec.lua:

    1. "EndSession calls M_http.shutdown" — after a successful InitializeSession2 (which created the module-local _conn inside M_http via exchange_assertion), call EndSession; then push a new response and call exchange_assertion AGAIN; assert that Connection() global was invoked TWICE total (count via a wrapping closure on _G.Connection installed in the test). This proves shutdown nilled the cache and the next call created a fresh Connection.

    2. "EndSession does NOT clear LocalStorage cache (AUTH-06)" — persist_session for org-1; call EndSession; assert LocalStorage.zettle["org-1"] is still the original entry; assert LocalStorage["zettle:org-1"] is still the JSON-encoded string.

    Also add the AUTH-06 simulation test:
    3. "cache survives EndSession + simulated restart via flat fallback (AUTH-06)" — persist_session for org-1; call EndSession; set LocalStorage.zettle = nil (simulates restart losing nested tables); call M_auth.cached_token("org-1"); assert it returns the access_token via the flat-string fallback path

    These three tests bring the entry_spec.lua suite to full Phase-2 coverage for AUTH-06.

    No fenced code blocks in <action>.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; busted spec/entry_spec.lua 2>&amp;1 | tee /tmp/entry_t3.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/entry_t3.log &amp;&amp; ! grep -q 'Pending' /tmp/entry_t3.log &amp;&amp; busted spec/ 2>&amp;1 | tee /tmp/all_t3.log &amp;&amp; ! grep -E 'Failure|Error' /tmp/all_t3.log</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/entry_spec.lua reports zero pending, zero failures across all integration tests (Phase-1 + Plan 02-06 Tasks 1/2/3)
    - busted spec/ (full Phase-2 suite) reports zero failures across all spec files
    - grep -c 'M_http.shutdown' src/entry.lua is exactly 1
    - grep -c 'LocalStorage.zettle = nil' src/entry.lua is exactly 0 (NEVER cleared in EndSession)
    - grep -c 'LocalStorage' src/entry.lua matches what ListAccounts reads — no clearing operation present
    - The 3 new entry_spec tests are visible by name: "EndSession calls M_http.shutdown", "EndSession does NOT clear LocalStorage cache (AUTH-06)", "cache survives EndSession + simulated restart via flat fallback (AUTH-06)"
    - luacheck src/entry.lua spec/entry_spec.lua exits 0
    - lua tools/build.lua --verify reports reproducible build
  </acceptance_criteria>
  <done>
    EndSession hooks M_http.shutdown() while explicitly preserving LocalStorage per the AUTH-06 anti-pattern note; tests confirm Connection cleanup AND cache persistence; AUTH-06 simulation test proves the flat-string fallback handles the worst-case Q5 outcome; full Phase-2 spec suite green; reproducible build holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MoneyMoney → InitializeSession2(credentials) | credentials is untrusted user paste; extraction block handles every shape MM passes |
| InitializeSession2 → M_auth chain | api_key flows through extraction → JWT decode → exchange_assertion; never persisted; never logged |
| LocalStorage.zettle ↔ ListAccounts | Read-only consumption of cached entries; never the api_key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | api_key in InitializeSession2 return string | mitigate | M_errors.from_http_status (Plan 02-02) builds strings from M_i18n.t templates only; body parameter is structurally never echoed. SEC-03 gating test in Plan 02-07 asserts the full integration path. |
| T-02-06-02 | Tampering | EndSession accidentally clearing LocalStorage | mitigate | Explicit anti-pattern note in RESEARCH §"Anti-Patterns" L640–L643; spec test "EndSession does NOT clear LocalStorage cache (AUTH-06)" gates structurally. |
| T-02-06-03 | Spoofing | Malformed JWT crashing the chunk via unguarded indexing | mitigate | _extract_client_id returns nil for malformed input (Plan 02-03); entry.lua checks nil-return and returns error.invalid_grant before any further processing. Test "InitializeSession2 with malformed JWT" gates. |
| T-02-06-04 | Repudiation | Add-account success not logged | accept | One M_log.info on successful InitializeSession2 entry would be helpful but not required. Wave-3 plan defers any extra log line to Phase 5/6. |
| T-02-06-SC | Tampering | npm/luarocks installs | accept | Zero installs in this plan — pure Lua source edits only. |
</threat_model>

<verification>
- busted spec/entry_spec.lua reports zero pending, zero failures
- busted spec/ (full Phase-2 suite) reports zero failures across all spec files
- luacheck src/ spec/ exits 0
- lua tools/build.lua --verify reports reproducible build
- Egress allowlist: grep -cE '(oauth\.zettle\.com|purchase\.izettle\.com|finance\.izettle\.com)' src/entry.lua is exactly 0 (all hosts hard-coded in src/auth.lua only)
- grep -c 'LocalStorage' src/entry.lua matches ListAccounts only — no clearing operation
- The Phase-1 credential-extraction block (now somewhere mid-file) is bytewise preserved
</verification>

<success_criteria>
- All new InitializeSession2 tests green (success, invalid_grant on /token, scope-failure on /users/self, malformed JWT no-network)
- All new ListAccounts tests green (publicName label, fallback label, two merchants coexist, empty-cache fallback)
- All new EndSession tests green (M_http.shutdown invoked, LocalStorage untouched, flat-fallback proves AUTH-06)
- Phase-1 spec tests still pass (SupportsBank, first-call challenge, RefreshAccount fixture)
- Full Phase-2 spec suite green; reproducible build holds
- Egress allowlist preserved; no host strings in entry.lua
</success_criteria>

<output>
Create `.planning/phases/02-authenticated-network-layer/02-06-SUMMARY.md` recording:
- Final LoC of src/entry.lua (target ~130)
- The exact sequence of InitializeSession2 returns under each fixture scenario (so Plan 02-07's SEC-03 gating spec can target the integration path explicitly)
- Confirmation that the Phase-1 credential-extraction block is bytewise preserved (cite git diff or specific line range comparison)
- Confirmation that EndSession does not clear LocalStorage
- The first-8-chars idiom used in the publicName-empty fallback label (so Plan 02-07 can replicate)
</output>
