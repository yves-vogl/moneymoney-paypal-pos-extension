---
phase: 03-sale-spine-first-user-visible-slice
plan: 07
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
files_modified: []
autonomous: true
requirements: [TEST-03, TEST-04, SALE-01, SALE-02, SALE-03, SALE-04, SALE-05, SALE-06, SALE-08, I18N-01]
requirements_addressed: [TEST-03, TEST-04, SALE-01, SALE-02, SALE-03, SALE-04, SALE-05, SALE-06, SALE-08, I18N-01]
tags: [wave-5, coverage, reproducible-build, egress, phase-closure, mvp]

must_haves:
  truths:
    - "Full busted spec/ run reports 0 failures, 0 errors, 0 pending across the entire Phase-3 suite (covers TEST-03 + TEST-04 contract closure)"
    - "Line coverage on src/ excluding src/webbanking_header.lua is >= 85% (Phase-1 D-06 gate carryover)"
    - "Phase-3-introduced modules (src/mapping.lua, src/pagination.lua, src/purchases.lua) each achieve >= 95% coverage (Phase-3 stretch per VALIDATION.md)"
    - "Reproducible build verified by running lua tools/build.lua --verify twice in succession — both runs produce IDENTICAL SHA256 (BUILD-02 / D-31 carryover from Phase 1)"
    - "Egress allowlist grep on dist/paypal-pos.lua finds exactly three host literals: https://oauth.zettle.com/token, https://oauth.zettle.com/users/self, https://purchase.izettle.com/purchases/v2 — finance.izettle.com is ABSENT (Phase 4 territory)"
    - "Egress grep produces NO third-party hosts (no analytics, no telemetry, no auto-update pings) per PROJECT.md no-telemetry constraint"
    - "Manifest order in tools/manifest.txt is unchanged from Phase 2: webbanking_header → log → errors → i18n → model → http → auth → pagination → purchases → payouts → balance → mapping → entry (confirmed via tools/build.lua manifest read; Phase-3 RESEARCH §6 Risk R-1 confirms this is the only correct order — Phase 3 added zero new manifest entries because the DST table is inlined into src/mapping.lua per planner discretion under D-40)"
    - "DEBUG = false in src/webbanking_header.lua (Phase-1 SEC-04 carryover — Phase 3 did not touch this; gate runs anyway)"
    - "No new log call sites in src/purchases.lua, src/pagination.lua (D-45) — confirmed by greppable absence of M_log.* in those files (M_log.warn / M_log.info inside src/pagination.lua MAX_PAGES guard is the SOLE exception, which is intentional and does not surface the Bearer)"
    - "luacheck on src/ + spec/ is clean on CI Lua 5.4 (local Lua 5.5 environment is broken per Phase-2 02-07 deviation; CI is the authority)"
    - "No regression in pre-existing Phase-1/2 specs — auth_spec, http_spec, log_redaction_spec, errors_spec, i18n_spec, mm_mocks_spec, build_spec all GREEN"
    - "No source code changes in this plan — pure verification + gate enforcement; if any check fails, the executor opens a follow-up issue and DOES NOT silently fix (transparency over speed)"
    - "Commits in this plan are commit-message-only updates (e.g. STATE.md tick, ROADMAP.md plan-list checkboxes) — no Lua source touched; all commits GPG-signed by FDE07046A6178E89ADB57FD3DE300C53D8E18642; no Claude/AI attribution"
  artifacts: []
  key_links:
    - from: "Phase-3 closure gate"
      to: "dist/paypal-pos.lua"
      via: "reproducible-build SHA256 + egress allowlist grep + DEBUG=false grep"
      pattern: "paypal-pos.lua"
    - from: "Phase-3 closure gate"
      to: "luacov.report.out"
      via: "busted --coverage spec/ && luacov; cat luacov.report.out | awk for src/ exclusion + module-by-module % parse"
      pattern: "luacov"
---

<objective>
Verify Phase 3 closes cleanly: full spec suite green, coverage ≥85% on src/ (and ≥95% on Phase-3-introduced modules), reproducible build SHA stable across two consecutive runs, egress allowlist grep clean, DEBUG=false, manifest order unchanged. This plan introduces ZERO source code changes — it is purely verification + commit-message-only updates to STATE.md and ROADMAP.md to tick off the phase.

Purpose: Per VALIDATION.md "Validation Sign-Off" checklist + ROADMAP "Flagged Risks 2", the Phase-3 idempotency gate (SALE-05 / TEST-03) is the single most expensive bug-class to recover from in production; it MUST be confirmed green before declaring the phase complete. The reproducible build gate is the supply-chain anchor for the eventual Phase-6 release. Egress allowlist is the SEC-02 invariant — no third-party hosts.

Output: A clean verification report capturing six pass/fail signals (full-suite test, coverage threshold, reproducible build, egress allowlist, manifest order, DEBUG=false); no source edits; STATE.md updated to reflect Phase 3 complete and Phase 4 ready to plan; ROADMAP.md plan list ticked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-CONTEXT.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md
@.planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md
@.planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md
@.planning/phases/01-foundations-sandbox-probes/CONTEXT.md
@docs/adr/0001-amalgamator-design.md
@tools/build.lua
@tools/manifest.txt
@src/webbanking_header.lua
@.luacheckrc
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full verification gate (build, suite, coverage, reproducibility, egress, DEBUG, manifest, luacheck)</name>
  <read_first>
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-VALIDATION.md §"Validation Sign-Off" (eight-item checklist)
    - .planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md (the analogous Phase-2 verification report shape — emulate this structure for the Phase-3 SUMMARY)
    - .planning/phases/01-foundations-sandbox-probes/CONTEXT.md D-06 (≥85% coverage gate excluding webbanking_header.lua), D-31 (reproducible build LF + manifest)
    - tools/build.lua (read to understand the --verify flag behavior — it builds twice and asserts byte-identical SHA256)
    - tools/manifest.txt (the order is the manifest order; do NOT modify)
    - src/webbanking_header.lua (verify DEBUG = false on line 22; Phase 3 did not touch this file)
  </read_first>
  <files></files>
  <action>
    Run the eight verification commands in order. Capture each command's exit code and a one-line summary; assemble the report content into the SUMMARY.md in Task 2.

    1. **Build**: `cd /Users/yves/Development/paypal-pos-plugin && lua tools/build.lua` — exit 0 expected; capture the printed SHA256.

    2. **Reproducible build (double-run + SHA match)**: `lua tools/build.lua --verify` — exit 0 expected; output should match the pattern `OK: reproducible (sha256: <hex>)`. Then run a SECOND independent build: `cp dist/paypal-pos.lua /tmp/phase3_build1.lua && lua tools/build.lua && diff /tmp/phase3_build1.lua dist/paypal-pos.lua` — exit 0 expected (no diff). If the diff produces ANY output, the reproducible-build gate FAILS — STOP and surface the failure (do not paper over).

    3. **Full spec suite**: `./.luarocks/bin/busted spec/ 2>&1` — must report `0 failures` and `0 errors`. Acceptable: 0 pending (Wave-0 pendings have all been filled by Waves 1-4). If any failure or error appears, STOP and report.

    4. **Coverage gate on src/ excluding webbanking_header.lua**: `./.luarocks/bin/busted --coverage spec/ && luacov` then `cat luacov.report.out | awk '/^[^- ]/ { print }' | grep -v webbanking_header.lua | awk -F'\t' '/^[Ff]ile/ { next } { hits=$2; missed=$3; pct=$4; print pct " " $1 }'` (the exact awk pipeline may need tuning depending on luacov 0.16's output format — read luacov.report.out and adapt). Compute the line-coverage percentage on src/ excluding webbanking_header.lua. Must be >= 85% (Phase-1 D-06). Also compute per-module coverage for src/mapping.lua, src/pagination.lua, src/purchases.lua — target >= 95% each (Phase-3 stretch per VALIDATION.md). Capture the exact numbers for the SUMMARY.

    5. **Egress allowlist grep**: `grep -E -o 'https?://[a-zA-Z0-9.-]+/[a-zA-Z0-9/-]*' dist/paypal-pos.lua | sort -u`. Expected output (exactly three URL literals):
       - `https://oauth.zettle.com/token`
       - `https://oauth.zettle.com/users/self`
       - `https://purchase.izettle.com/purchases/v2`
       Any other host (especially `finance.izettle.com` — Phase 4 — or any third-party / analytics / telemetry host) is a FAIL — STOP.

    6. **DEBUG = false**: `grep -n 'DEBUG = ' src/webbanking_header.lua` — must report exactly one match: `22:DEBUG = false`. If `DEBUG = true` appears anywhere in src/ (including comments check: `grep -Rn 'DEBUG = true' src/` should report nothing — comments are allowed but a top-level `DEBUG = true` assignment is forbidden by SEC-04).

    7. **Manifest order**: `cat tools/manifest.txt | grep -v '^#' | grep -v '^$' | tr '\n' ' '` — output must be exactly: `webbanking_header log errors i18n model http auth pagination purchases payouts balance mapping entry `. Any other order is a FAIL.

    8. **Luacheck**: `luacheck src/ spec/ 2>&1 || true`. The local Lua 5.5 environment is documented broken in 02-07-SUMMARY.md — accept that the local run may surface env errors. The TRUE gate is on CI (Lua 5.4); record the local output for human review but do not block on local failures (per Phase-2 02-07 deviation).

    For each command, record:
    - command (exact bash invocation)
    - exit code
    - one-line summary of the result (e.g. "reproducible build SHA256: <hex>", or "coverage 92.3% on src/ excluding header")

    If ANY of 1-7 fails, STOP execution and surface the failure prominently in the SUMMARY. Do NOT attempt a fix in this plan — Plan 03-07 is verification only.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; lua tools/build.lua &amp;&amp; lua tools/build.lua --verify | grep -q 'OK: reproducible' &amp;&amp; ./.luarocks/bin/busted spec/ 2>&amp;1 | tee /tmp/wave5_suite.log &amp;&amp; grep -qE '0 failures' /tmp/wave5_suite.log &amp;&amp; ! grep -E 'Error\|Pending' /tmp/wave5_suite.log &amp;&amp; grep -n 'DEBUG = ' src/webbanking_header.lua | grep -q 'DEBUG = false' &amp;&amp; grep -E -o 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u | grep -q 'oauth.zettle.com' &amp;&amp; grep -E -o 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u | grep -q 'purchase.izettle.com' &amp;&amp; ! grep -E -o 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u | grep -q 'finance.izettle.com'</automated>
  </verify>
  <acceptance_criteria>
    - Command 1 (build) exits 0; prints a SHA256
    - Command 2 (reproducible) prints `OK: reproducible` and the second-run diff is empty
    - Command 3 (full suite) prints `0 failures, 0 errors` for the first time in Phase 3
    - Command 4 (coverage) reports >= 85% on src/ excluding webbanking_header.lua; per-module: mapping.lua >= 95%, pagination.lua >= 95%, purchases.lua >= 95% (or close to it, with any gap documented)
    - Command 5 (egress) shows exactly three URLs: oauth.zettle.com/token, oauth.zettle.com/users/self, purchase.izettle.com/purchases/v2
    - Command 5 confirms NO finance.izettle.com, NO third-party host
    - Command 6 (DEBUG): `DEBUG = false` present at L22 of webbanking_header.lua; no `DEBUG = true` assignment in src/
    - Command 7 (manifest) shows the exact 13-module order (webbanking_header → log → errors → i18n → model → http → auth → pagination → purchases → payouts → balance → mapping → entry)
    - Command 8 (luacheck) on CI Lua 5.4 is the authority; local env failures are noted but not blocking (02-07 deviation)
  </acceptance_criteria>
  <done>
    All eight verification commands run; each result captured for the SUMMARY; any failure surfaces a STOP signal (no silent fix in this plan). No commits in this task — Task 2 commits the SUMMARY + STATE.md tick.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write Phase-3 closure SUMMARY + update STATE.md + tick ROADMAP plan list</name>
  <read_first>
    - .planning/phases/02-authenticated-network-layer/02-07-SUMMARY.md (the structural template — emulate the dependency_graph YAML, the "Performance" section, the "Verification Gate Results" tables)
    - .planning/STATE.md (current state: position pre-Phase-3-execution; this task updates the position to phase 3 COMPLETE)
    - .planning/ROADMAP.md §"Phase 3" plan list (currently TBD; this task fills in the 7 plan entries with [x] checkboxes)
    - The captured outputs from Task 1 — all 8 verification results
    - .planning/phases/03-sale-spine-first-user-visible-slice/03-RESEARCH.md "Manual-Only Verifications" (the SUMMARY should remind the maintainer to run the live end-to-end smoke against a real PayPal POS API key before declaring v0.1.0 — that is the human gate that no CI can run)
  </read_first>
  <files>
    .planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    Three steps:

    **Step 1 — Write 03-07-SUMMARY.md** at `.planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md`. Emulate the 02-07-SUMMARY.md frontmatter+body shape. Sections (mirroring 02-07-SUMMARY.md):
    - Frontmatter: phase, plan "07", subsystem "verification", tags [verification, coverage, reproducible-build, egress, phase-closure], dependency_graph.requires citing 03-01..06, dependency_graph.provides citing the eight gate results, dependency_graph.affects [phase-4-finance-api, /gsd-verify-work].
    - Title: "Phase 3 Plan 07: Phase-3 Closure Verification Summary"
    - Subtitle: a one-line description of what verified.
    - Performance: duration estimate (~15 min for verification commands + SUMMARY write).
    - Accomplishments: bullet list — full suite 0/0/0, coverage X% on src/ + per-module breakdown, reproducible SHA, egress clean, DEBUG=false, manifest order unchanged.
    - Verification Gate Results: a section per gate (Manifest Order, Reproducible Build, Full Spec Suite, Coverage Gate, Egress Allowlist, DEBUG flag, Luacheck) — each with command, output, verdict (CORRECT / IDENTICAL / GREEN / PASSES / CLEAN / verbatim from Task 1).
    - Files Created/Modified: none (verification only).
    - Decisions Made: note the planner-discretion exercise from D-40 — DST table inlined in src/mapping.lua, no src/timezone.lua hoisted; manifest unchanged.
    - Deviations from Plan: any local luacheck env issue from Phase-2 02-07 deviation; surface only if applicable.
    - Issues Encountered: any non-blocking surprises.
    - Next Phase Readiness: declare Phase 3 ready for `/gsd-verify-work`; cite the maintainer-only manual smoke from VALIDATION.md "Manual-Only Verifications" as the ONE thing CI cannot do; flag Phase 4 (Finance API + ACCT-03 + REF / FEE / PAYOUT enrichment) as the next planning target with the Q3 (finance.izettle.com host) probe-resolution being the first item.

    **Step 2 — Update STATE.md**:
    - frontmatter `last_updated`: today's date
    - frontmatter `progress`: completed_phases += 1 (was 1 from STATE pre-execution; or use whatever the current value is + 1), total_plans += 7, completed_plans += 7, percent recomputed
    - body §"Current Position" Phase: change "Phase: 03 ... PLANNING" → "Phase: 04 (enrichment) — READY TO PLAN"
    - body §"Current Position" Status: change "Discuss-phase 3 complete" → "Phase 3 EXECUTED — 7 plans complete, full suite 0/0/0, ready for /gsd-verify-work"
    - body §"Current Position" Progress: tick another phase complete (`[██████████████░░░░░] 3/7 phases complete`)
    - body Phase status diagram: Phase 3 → DONE; Phase 4 → READY
    - body §"Active Todos": replace Phase-3 execution todos with Phase-4 planning todos (research Q3 / finance.izettle.com / Finance API; CONTEXT for REF/FEE/PAYOUT)
    - body §"Session Continuity" Last action: "Phase 3 executed across 7 plans. Full suite 0/0/0. Reproducible build SHA <hex>. Egress clean (oauth + purchase only)."
    - body §"Session Continuity" Next action: "/gsd-verify-work for Phase 3, then /gsd-discuss-phase 4."

    **Step 3 — Update ROADMAP.md §"Phase 3" plan list**: locate the "**Plans:** TBD" line in Phase 3 section; replace with:
    ```
    **Plans:** 7/7 plans complete
    **Wave 0**

    - [x] 03-01-PLAN.md — Wave 0 fixtures + RED scaffolds (10 fixtures, 4 pending spec scaffolds)

    **Wave 1** *(blocked on Wave 0 completion)*

    - [x] 03-02-PLAN.md — Wave 1 RED gating specs (TEST-03 idempotency + TEST-04 schema)

    **Wave 2** *(blocked on Wave 1 completion)*

    - [x] 03-03-PLAN.md — Wave 2 mapping pure-logic + inline DST table (SALE-01/02/04/08, I18N-01, D-31/32/34/35/36/37/38)

    **Wave 3** *(blocked on Wave 2 completion; 03-04 + 03-05 run in parallel — different files)*

    - [x] 03-04-PLAN.md — Wave 3 pagination cursor loop (SALE-06, D-43)
    - [x] 03-05-PLAN.md — Wave 3 purchases fetch + fetch_all (SALE-06, D-33, D-42)

    **Wave 4** *(blocked on Wave 3 completion)*

    - [x] 03-06-PLAN.md — Wave 4 entry.lua RefreshAccount rewire (TEST-03 + TEST-04 GREEN, D-31/33/37/41)

    **Wave 5** *(blocked on Wave 4 completion)*

    - [x] 03-07-PLAN.md — Wave 5 closure verification (coverage / reproducible build / egress / DEBUG / manifest)
    ```
    Also update the "Progress Table" row for Phase 3: `3. Sale Spine | 7/7 | Complete | <today's date>` and the cross-cutting constraints block (mirror Phase 2's).

    Commit message: `docs(03-07): close Phase 3 — 7 plans complete, full suite 0/0/0, reproducible build verified`.
  </action>
  <verify>
    <automated>cd /Users/yves/Development/paypal-pos-plugin &amp;&amp; test -f .planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md &amp;&amp; grep -q '7/7 plans complete' .planning/ROADMAP.md &amp;&amp; grep -q '03-01-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q '03-07-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q 'Phase 3 EXECUTED\|3/7 phases complete' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md` exists and follows the 02-07-SUMMARY.md shape (frontmatter + Performance + Accomplishments + Verification Gate Results + Files + Decisions + Next Phase Readiness)
    - SUMMARY captures: full-suite result, coverage % (overall + per-module), reproducible SHA256, egress URL list (three entries), DEBUG=false confirmation, manifest order verbatim
    - SUMMARY explicitly references the VALIDATION.md "Manual-Only Verifications" maintainer end-to-end smoke as the human-gate Phase 3 cannot CI-verify
    - .planning/STATE.md frontmatter has updated completed_phases / completed_plans / last_updated; body Position shows "Phase 3 EXECUTED" or "Phase 4 — READY TO PLAN"
    - .planning/STATE.md progress diagram updated to 3/7 phases complete
    - .planning/ROADMAP.md §"Phase 3" Plans block shows all 7 plans with [x]; "Plans: TBD" replaced with "Plans: 7/7 plans complete"
    - .planning/ROADMAP.md Progress Table Phase-3 row shows 7/7 Complete with today's date
    - All three files committed with the conventional commit message above; commit is GPG-signed
    - No source code (src/, tools/, spec/) modified in this plan
  </acceptance_criteria>
  <done>
    03-07-SUMMARY.md written; STATE.md updated to reflect Phase 3 complete; ROADMAP.md Plan list ticked with the 7 plans; single commit `docs(03-07): close Phase 3 ...`. Phase 3 is now ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification commands → repository state | Read-only inspection of build artifact, spec output, source files; no source mutation (Step 2 modifies only .planning/STATE.md + ROADMAP.md + writes 03-07-SUMMARY.md) |
| Reproducible-build SHA256 → maintainer trust | The recorded SHA in SUMMARY.md is the supply-chain anchor for the eventual Phase-6 release; it MUST match the actual artifact byte-for-byte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W5-01 | Tampering | Reproducible-build verification | mitigate | Two consecutive builds + diff is the gate; if SHA drifts the executor MUST stop and surface the failure (no silent fix). Recorded SHA in SUMMARY becomes the audit trail for Phase 6. |
| T-03-W5-02 | Spoofing | Egress allowlist grep | mitigate | grep on dist/paypal-pos.lua structurally rejects any host outside the Phase-2 D-26 allowlist; finance.izettle.com appearing here would mean Phase-3 leaked Phase-4 work into the artifact — verification catches it. |
| T-03-W5-03 | Information Disclosure | SEC-04 DEBUG = false | mitigate | grep on src/webbanking_header.lua confirms DEBUG = false is shipped; if any developer accidentally toggled DEBUG = true mid-Phase-3, this gate catches it before the closure commit. |
| T-03-W5-SC | Tampering | npm/luarocks installs in this plan | accept | Zero installs. Verification commands only. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/yves/Development/paypal-pos-plugin

# Re-run the eight gates (independent re-verification):
lua tools/build.lua --verify
./.luarocks/bin/busted spec/ 2>&1 | grep -E '0 failures'
grep -E -o 'https?://[a-zA-Z0-9.-]+' dist/paypal-pos.lua | sort -u
grep -n 'DEBUG = ' src/webbanking_header.lua
cat tools/manifest.txt | grep -v '^#' | grep -v '^$' | tr '\n' ' '

# Closure check:
test -f .planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md
grep '3/7 phases complete' .planning/STATE.md
grep '7/7 plans complete' .planning/ROADMAP.md
git log --oneline -5  # latest commit should be the docs(03-07) closure
```
</verification>

<success_criteria>
- Full spec suite passes with 0 failures, 0 errors
- Coverage on src/ (excluding webbanking_header.lua) is >= 85%; mapping.lua / pagination.lua / purchases.lua each >= 95%
- Reproducible build SHA256 matches across two consecutive runs
- Egress allowlist grep shows exactly three URL literals (oauth.zettle.com/token + /users/self + purchase.izettle.com/purchases/v2); no finance.izettle.com, no third-party hosts
- DEBUG = false confirmed in src/webbanking_header.lua
- Manifest order is the verified 13-module sequence; no Phase-3 additions
- .planning/STATE.md reflects Phase 3 complete; Phase 4 ready to plan
- .planning/ROADMAP.md §"Phase 3" plan list shows all 7 plans with [x]
- .planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md exists and follows the 02-07-SUMMARY.md template shape
- Single closure commit landed; GPG-signed; no AI attribution
- No source code changes in src/, tools/, or spec/
</success_criteria>

<artifacts_this_phase_produces>
New files:
- `.planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md` — closure summary in the 02-07-SUMMARY.md style

Modified files:
- `.planning/STATE.md` — frontmatter progress + body position + diagram + active-todos + session-continuity
- `.planning/ROADMAP.md` — §"Phase 3" plan list filled with 7 [x] entries; Progress Table Phase-3 row Complete

No source code touched (src/, tools/, spec/, docs/, .github/, .luacheckrc all untouched).

Phase status at completion:
- Phase 1 ✅ merged (Phase-2 carryover)
- Phase 2 ✅ merged (Phase-2 carryover)
- Phase 3 ✅ EXECUTED — ready for /gsd-verify-work + (manual) maintainer end-to-end smoke
- Phase 4 — ready to plan (Finance API + ACCT-03 + REF / FEE / PAYOUT)
- Phases 5-6 / 6.1 — unchanged
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-sale-spine-first-user-visible-slice/03-07-SUMMARY.md` per Task 2's action. Additionally, the running summary string the executor returns to /gsd-execute-phase should include:
- The recorded reproducible SHA256
- Coverage % overall + per-module
- The exact egress URL list (3 entries)
- Confirmation Phase 3 is ready for /gsd-verify-work + the maintainer manual smoke
</output>
